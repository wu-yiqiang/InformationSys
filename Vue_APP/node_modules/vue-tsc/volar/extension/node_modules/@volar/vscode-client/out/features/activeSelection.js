"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.activate = void 0;
const vscode = require("vscode");
const shared_1 = require("@volar/shared");
async function activate(context, languageClient) {
    await languageClient.onReady();
    context.subscriptions.push(languageClient.onRequest(shared_1.ActiveSelectionRequest.type, () => {
        const editor = vscode.window.activeTextEditor;
        if (editor) {
            return {
                uri: languageClient.code2ProtocolConverter.asUri(editor.document.uri),
                offset: editor.document.offsetAt(editor.selection.end),
            };
        }
    }));
}
exports.activate = activate;
//# sourceMappingURL=activeSelection.js.map