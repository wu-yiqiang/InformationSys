"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.register = void 0;
const transforms_1 = require("../utils/transforms");
const shared_1 = require("@volar/shared");
function register(languageService, getTextDocument) {
    return (uri, position) => {
        const document = getTextDocument(uri);
        if (!document)
            return [];
        const fileName = shared_1.uriToFsPath(document.uri);
        const offset = document.offsetAt(position);
        const entries = languageService.getReferencesAtPosition(fileName, offset);
        if (!entries)
            return [];
        return transforms_1.entriesToLocations([...entries], getTextDocument);
    };
}
exports.register = register;
//# sourceMappingURL=references.js.map