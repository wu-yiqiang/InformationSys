import {
  Fragment,
  Teleport,
  Transition,
  computed,
  createApp,
  createTextVNode,
  createVNode,
  defineComponent,
  getCurrentInstance,
  h,
  init_vue_runtime_esm_bundler,
  inject,
  isVNode,
  mergeProps,
  nextTick,
  onActivated,
  onBeforeUnmount,
  onBeforeUpdate,
  onDeactivated,
  onMounted,
  onUnmounted,
  onUpdated,
  provide,
  reactive,
  ref,
  resolveDirective,
  unref,
  vShow,
  watch,
  withDirectives
} from "./chunk-DUPLCFWK.js";
import "./chunk-XNAE4CLY.js";

// node_modules/vant/es/utils/base.js
function noop() {
}
var extend = Object.assign;
var inBrowser = typeof window !== "undefined";
var unknownProp = null;
var truthProp = {
  type: Boolean,
  default: true
};
function get(object, path) {
  var keys = path.split(".");
  var result = object;
  keys.forEach((key) => {
    var _result$key;
    result = (_result$key = result[key]) != null ? _result$key : "";
  });
  return result;
}
function pick(obj, keys, ignoreUndefined) {
  return keys.reduce((ret, key) => {
    if (!ignoreUndefined || obj[key] !== void 0) {
      ret[key] = obj[key];
    }
    return ret;
  }, {});
}

// node_modules/vant/es/utils/create/bem.js
function gen(name96, mods) {
  if (!mods) {
    return "";
  }
  if (typeof mods === "string") {
    return " " + name96 + "--" + mods;
  }
  if (Array.isArray(mods)) {
    return mods.reduce((ret, item) => ret + gen(name96, item), "");
  }
  return Object.keys(mods).reduce((ret, key) => ret + (mods[key] ? gen(name96, key) : ""), "");
}
function createBEM(name96) {
  return function(el, mods) {
    if (el && typeof el !== "string") {
      mods = el;
      el = "";
    }
    el = el ? name96 + "__" + el : name96;
    return "" + el + gen(el, mods);
  };
}

// node_modules/vant/es/utils/format/string.js
var camelizeRE = /-(\w)/g;
function camelize(str) {
  return str.replace(camelizeRE, (_2, c) => c.toUpperCase());
}
function kebabCase(str) {
  return str.replace(/([A-Z])/g, "-$1").toLowerCase().replace(/^-/, "");
}
function padZero(num, targetLength = 2) {
  var str = num + "";
  while (str.length < targetLength) {
    str = "0" + str;
  }
  return str;
}

// node_modules/vant/es/utils/validate.js
function isDef(val) {
  return val !== void 0 && val !== null;
}
function isFunction(val) {
  return typeof val === "function";
}
function isObject(val) {
  return val !== null && typeof val === "object";
}
function isPromise(val) {
  return isObject(val) && isFunction(val.then) && isFunction(val.catch);
}
function isDate(val) {
  return Object.prototype.toString.call(val) === "[object Date]" && !Number.isNaN(val.getTime());
}
function isMobile(value) {
  value = value.replace(/[^-|\d]/g, "");
  return /^((\+86)|(86))?(1)\d{10}$/.test(value) || /^0[0-9-]{10,13}$/.test(value);
}
function isNumeric(val) {
  return typeof val === "number" || /^\d+(\.\d+)?$/.test(val);
}
function isAndroid() {
  return inBrowser ? /android/.test(navigator.userAgent.toLowerCase()) : false;
}
function isIOS() {
  return inBrowser ? /ios|iphone|ipad|ipod/.test(navigator.userAgent.toLowerCase()) : false;
}

// node_modules/vant/es/locale/index.js
init_vue_runtime_esm_bundler();

// node_modules/vant/es/utils/deep-assign.js
var {
  hasOwnProperty
} = Object.prototype;
function assignKey(to, from, key) {
  var val = from[key];
  if (!isDef(val)) {
    return;
  }
  if (!hasOwnProperty.call(to, key) || !isObject(val)) {
    to[key] = val;
  } else {
    to[key] = deepAssign(Object(to[key]), from[key]);
  }
}
function deepAssign(to, from) {
  Object.keys(from).forEach((key) => {
    assignKey(to, from, key);
  });
  return to;
}

// node_modules/vant/es/locale/lang/zh-CN.js
var zh_CN_default = {
  name: "\u59D3\u540D",
  tel: "\u7535\u8BDD",
  save: "\u4FDD\u5B58",
  confirm: "\u786E\u8BA4",
  cancel: "\u53D6\u6D88",
  delete: "\u5220\u9664",
  complete: "\u5B8C\u6210",
  loading: "\u52A0\u8F7D\u4E2D...",
  telEmpty: "\u8BF7\u586B\u5199\u7535\u8BDD",
  nameEmpty: "\u8BF7\u586B\u5199\u59D3\u540D",
  nameInvalid: "\u8BF7\u8F93\u5165\u6B63\u786E\u7684\u59D3\u540D",
  confirmDelete: "\u786E\u5B9A\u8981\u5220\u9664\u5417",
  telInvalid: "\u8BF7\u8F93\u5165\u6B63\u786E\u7684\u624B\u673A\u53F7",
  vanCalendar: {
    end: "\u7ED3\u675F",
    start: "\u5F00\u59CB",
    title: "\u65E5\u671F\u9009\u62E9",
    confirm: "\u786E\u5B9A",
    startEnd: "\u5F00\u59CB/\u7ED3\u675F",
    weekdays: ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"],
    monthTitle: (year, month) => year + "\u5E74" + month + "\u6708",
    rangePrompt: (maxRange) => "\u9009\u62E9\u5929\u6570\u4E0D\u80FD\u8D85\u8FC7 " + maxRange + " \u5929"
  },
  vanCascader: {
    select: "\u8BF7\u9009\u62E9"
  },
  vanContactCard: {
    addText: "\u6DFB\u52A0\u8054\u7CFB\u4EBA"
  },
  vanContactList: {
    addText: "\u65B0\u5EFA\u8054\u7CFB\u4EBA"
  },
  vanPagination: {
    prev: "\u4E0A\u4E00\u9875",
    next: "\u4E0B\u4E00\u9875"
  },
  vanPullRefresh: {
    pulling: "\u4E0B\u62C9\u5373\u53EF\u5237\u65B0...",
    loosing: "\u91CA\u653E\u5373\u53EF\u5237\u65B0..."
  },
  vanSubmitBar: {
    label: "\u5408\u8BA1\uFF1A"
  },
  vanCoupon: {
    unlimited: "\u65E0\u4F7F\u7528\u95E8\u69DB",
    discount: (discount) => discount + "\u6298",
    condition: (condition) => "\u6EE1" + condition + "\u5143\u53EF\u7528"
  },
  vanCouponCell: {
    title: "\u4F18\u60E0\u5238",
    tips: "\u6682\u65E0\u53EF\u7528",
    count: (count) => count + "\u5F20\u53EF\u7528"
  },
  vanCouponList: {
    empty: "\u6682\u65E0\u4F18\u60E0\u5238",
    exchange: "\u5151\u6362",
    close: "\u4E0D\u4F7F\u7528\u4F18\u60E0\u5238",
    enable: "\u53EF\u7528",
    disabled: "\u4E0D\u53EF\u7528",
    placeholder: "\u8BF7\u8F93\u5165\u4F18\u60E0\u7801"
  },
  vanAddressEdit: {
    area: "\u5730\u533A",
    postal: "\u90AE\u653F\u7F16\u7801",
    areaEmpty: "\u8BF7\u9009\u62E9\u5730\u533A",
    addressEmpty: "\u8BF7\u586B\u5199\u8BE6\u7EC6\u5730\u5740",
    postalEmpty: "\u90AE\u653F\u7F16\u7801\u683C\u5F0F\u4E0D\u6B63\u786E",
    defaultAddress: "\u8BBE\u4E3A\u9ED8\u8BA4\u6536\u8D27\u5730\u5740",
    telPlaceholder: "\u6536\u8D27\u4EBA\u624B\u673A\u53F7",
    namePlaceholder: "\u6536\u8D27\u4EBA\u59D3\u540D",
    areaPlaceholder: "\u9009\u62E9\u7701 / \u5E02 / \u533A"
  },
  vanAddressEditDetail: {
    label: "\u8BE6\u7EC6\u5730\u5740",
    placeholder: "\u8857\u9053\u95E8\u724C\u3001\u697C\u5C42\u623F\u95F4\u53F7\u7B49\u4FE1\u606F"
  },
  vanAddressList: {
    add: "\u65B0\u589E\u5730\u5740"
  }
};

// node_modules/vant/es/locale/index.js
var lang = ref("zh-CN");
var messages = reactive({
  "zh-CN": zh_CN_default
});
var Locale = {
  messages() {
    return messages[lang.value];
  },
  use(newLang, newMessages) {
    lang.value = newLang;
    this.add({
      [newLang]: newMessages
    });
  },
  add(newMessages = {}) {
    deepAssign(messages, newMessages);
  }
};
var locale_default = Locale;

// node_modules/vant/es/utils/create/translate.js
function createTranslate(name96) {
  var prefix2 = camelize(name96) + ".";
  return function(path, ...args) {
    var messages2 = locale_default.messages();
    var message = get(messages2, prefix2 + path) || get(messages2, path);
    return isFunction(message) ? message(...args) : message;
  };
}

// node_modules/vant/es/utils/create/index.js
function createNamespace(name96) {
  var prefixedName = "van-" + name96;
  return [prefixedName, createBEM(prefixedName), createTranslate(prefixedName)];
}

// node_modules/vant/es/utils/with-install.js
function withInstall(options) {
  options.install = (app) => {
    var {
      name: name96
    } = options;
    app.component(name96, options);
    app.component(camelize("-" + name96), options);
  };
  return options;
}

// node_modules/vant/es/utils/format/unit.js
function addUnit(value) {
  if (!isDef(value)) {
    return void 0;
  }
  return isNumeric(value) ? value + "px" : String(value);
}
function getSizeStyle(originSize) {
  if (isDef(originSize)) {
    var size = addUnit(originSize);
    return {
      width: size,
      height: size
    };
  }
}
function getZIndexStyle(zIndex) {
  var style2 = {};
  if (zIndex !== void 0) {
    style2.zIndex = +zIndex;
  }
  return style2;
}
var rootFontSize;
function getRootFontSize() {
  if (!rootFontSize) {
    var doc = document.documentElement;
    var fontSize = doc.style.fontSize || window.getComputedStyle(doc).fontSize;
    rootFontSize = parseFloat(fontSize);
  }
  return rootFontSize;
}
function convertRem(value) {
  value = value.replace(/rem/g, "");
  return +value * getRootFontSize();
}
function convertVw(value) {
  value = value.replace(/vw/g, "");
  return +value * window.innerWidth / 100;
}
function convertVh(value) {
  value = value.replace(/vh/g, "");
  return +value * window.innerHeight / 100;
}
function unitToPx(value) {
  if (typeof value === "number") {
    return value;
  }
  if (inBrowser) {
    if (value.includes("rem")) {
      return convertRem(value);
    }
    if (value.includes("vw")) {
      return convertVw(value);
    }
    if (value.includes("vh")) {
      return convertVh(value);
    }
  }
  return parseFloat(value);
}

// node_modules/vant/es/utils/format/number.js
function clamp(num, min, max) {
  return Math.min(Math.max(num, min), max);
}
function trimExtraChar(value, char, regExp) {
  var index = value.indexOf(char);
  if (index === -1) {
    return value;
  }
  if (char === "-" && index !== 0) {
    return value.slice(0, index);
  }
  return value.slice(0, index + 1) + value.slice(index).replace(regExp, "");
}
function formatNumber(value, allowDot = true, allowMinus = true) {
  if (allowDot) {
    value = trimExtraChar(value, ".", /\./g);
  } else {
    value = value.split(".")[0];
  }
  if (allowMinus) {
    value = trimExtraChar(value, "-", /-/g);
  } else {
    value = value.replace(/-/, "");
  }
  var regExp = allowDot ? /[^-0-9.]/g : /[^-0-9]/g;
  return value.replace(regExp, "");
}
function addNumber(num1, num2) {
  var cardinal = Math.pow(10, 10);
  return Math.round((num1 + num2) * cardinal) / cardinal;
}

// node_modules/vant/es/utils/dom/style.js
init_vue_runtime_esm_bundler();
function isHidden(elementRef) {
  var el = unref(elementRef);
  if (!el) {
    return false;
  }
  var style2 = window.getComputedStyle(el);
  var hidden = style2.display === "none";
  var parentHidden = el.offsetParent === null && style2.position !== "fixed";
  return hidden || parentHidden;
}

// node_modules/vant/es/utils/dom/event.js
function stopPropagation(event) {
  event.stopPropagation();
}
function preventDefault(event, isStopPropagation) {
  if (typeof event.cancelable !== "boolean" || event.cancelable) {
    event.preventDefault();
  }
  if (isStopPropagation) {
    stopPropagation(event);
  }
}
function trigger(target, type) {
  var inputEvent = document.createEvent("HTMLEvents");
  inputEvent.initEvent(type, true, true);
  target.dispatchEvent(inputEvent);
}

// node_modules/vant/es/utils/dom/scroll.js
function isWindow(val) {
  return val === window;
}
function getScrollTop(el) {
  var top2 = "scrollTop" in el ? el.scrollTop : el.pageYOffset;
  return Math.max(top2, 0);
}
function setScrollTop(el, value) {
  if ("scrollTop" in el) {
    el.scrollTop = value;
  } else {
    el.scrollTo(el.scrollX, value);
  }
}
function getRootScrollTop() {
  return window.pageYOffset || document.documentElement.scrollTop || document.body.scrollTop || 0;
}
function setRootScrollTop(value) {
  setScrollTop(window, value);
  setScrollTop(document.body, value);
}
function getElementTop(el, scroller) {
  if (isWindow(el)) {
    return 0;
  }
  var scrollTop = scroller ? getScrollTop(scroller) : getRootScrollTop();
  return el.getBoundingClientRect().top + scrollTop;
}
function getVisibleHeight(el) {
  if (isWindow(el)) {
    return el.innerHeight;
  }
  return el.getBoundingClientRect().height;
}
function getVisibleTop(el) {
  if (isWindow(el)) {
    return 0;
  }
  return el.getBoundingClientRect().top;
}
var isIOS2 = isIOS();
function resetScroll() {
  if (isIOS2) {
    setRootScrollTop(getRootScrollTop());
  }
}

// node_modules/vant/es/action-bar/ActionBar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/@vant/use/dist/esm/utils.js
var inBrowser2 = typeof window !== "undefined";
var root = inBrowser2 ? window : global;
var prev = Date.now();
function rafPolyfill(fn2) {
  var curr = Date.now();
  var ms = Math.max(0, 16 - (curr - prev));
  var id = setTimeout(fn2, ms);
  prev = curr + ms;
  return id;
}
function raf(fn2) {
  var requestAnimationFrame = root.requestAnimationFrame || rafPolyfill;
  return requestAnimationFrame.call(root, fn2);
}
function cancelRaf(id) {
  var cancelAnimationFrame = root.cancelAnimationFrame || root.clearTimeout;
  cancelAnimationFrame.call(root, id);
}
function doubleRaf(fn2) {
  raf(() => raf(fn2));
}

// node_modules/@vant/use/dist/esm/useRect/index.js
init_vue_runtime_esm_bundler();
function isWindow2(val) {
  return val === window;
}
function makeDOMRect(width, height) {
  return {
    top: 0,
    left: 0,
    right: width,
    bottom: height,
    width,
    height
  };
}
var useRect = (elementOrRef) => {
  var element = unref(elementOrRef);
  if (isWindow2(element)) {
    var width = element.innerWidth;
    var height = element.innerHeight;
    return makeDOMRect(width, height);
  }
  if (element && element.getBoundingClientRect) {
    return element.getBoundingClientRect();
  }
  return makeDOMRect(0, 0);
};

// node_modules/@vant/use/dist/esm/useToggle/index.js
init_vue_runtime_esm_bundler();
function useToggle(defaultValue = false) {
  var state = ref(defaultValue);
  var toggle = (value = !state.value) => {
    state.value = value;
  };
  return [state, toggle];
}

// node_modules/@vant/use/dist/esm/useRelation/useParent.js
init_vue_runtime_esm_bundler();
function useParent(key) {
  var parent = inject(key, null);
  if (parent) {
    var instance4 = getCurrentInstance();
    var {
      link: _link,
      unlink: _unlink,
      internalChildren
    } = parent;
    _link(instance4);
    onUnmounted(() => _unlink(instance4));
    var index = computed(() => internalChildren.indexOf(instance4));
    return {
      parent,
      index
    };
  }
  return {
    parent: null,
    index: ref(-1)
  };
}

// node_modules/@vant/use/dist/esm/useRelation/useChildren.js
init_vue_runtime_esm_bundler();
function flattenVNodes(children) {
  var result = [];
  var traverse = (children2) => {
    if (Array.isArray(children2)) {
      children2.forEach((child) => {
        if (isVNode(child)) {
          var _child$component;
          result.push(child);
          if ((_child$component = child.component) != null && _child$component.subTree) {
            traverse(child.component.subTree.children);
          }
          if (child.children) {
            traverse(child.children);
          }
        }
      });
    }
  };
  traverse(children);
  return result;
}
function sortChildren(parent, publicChildren, internalChildren) {
  var vnodes = flattenVNodes(parent.subTree.children);
  internalChildren.sort((a, b) => vnodes.indexOf(a.vnode) - vnodes.indexOf(b.vnode));
  var orderedPublicChildren = internalChildren.map((item) => item.proxy);
  publicChildren.sort((a, b) => {
    var indexA = orderedPublicChildren.indexOf(a);
    var indexB = orderedPublicChildren.indexOf(b);
    return indexA - indexB;
  });
}
function useChildren(key) {
  var publicChildren = reactive([]);
  var internalChildren = reactive([]);
  var parent = getCurrentInstance();
  var linkChildren = (value) => {
    var link = (child) => {
      if (child.proxy) {
        internalChildren.push(child);
        publicChildren.push(child.proxy);
        sortChildren(parent, publicChildren, internalChildren);
      }
    };
    var unlink = (child) => {
      var index = internalChildren.indexOf(child);
      publicChildren.splice(index, 1);
      internalChildren.splice(index, 1);
    };
    provide(key, Object.assign({
      link,
      unlink,
      children: publicChildren,
      internalChildren
    }, value));
  };
  return {
    children: publicChildren,
    linkChildren
  };
}

// node_modules/@vant/use/dist/esm/useCountDown/index.js
init_vue_runtime_esm_bundler();
var SECOND = 1e3;
var MINUTE = 60 * SECOND;
var HOUR = 60 * MINUTE;
var DAY = 24 * HOUR;
function parseTime(time) {
  var days = Math.floor(time / DAY);
  var hours = Math.floor(time % DAY / HOUR);
  var minutes = Math.floor(time % HOUR / MINUTE);
  var seconds = Math.floor(time % MINUTE / SECOND);
  var milliseconds = Math.floor(time % SECOND);
  return {
    total: time,
    days,
    hours,
    minutes,
    seconds,
    milliseconds
  };
}
function isSameSecond(time1, time2) {
  return Math.floor(time1 / 1e3) === Math.floor(time2 / 1e3);
}
function useCountDown(options) {
  var rafId2;
  var endTime;
  var counting;
  var deactivated;
  var remain = ref(options.time);
  var current = computed(() => parseTime(remain.value));
  var pause = () => {
    counting = false;
    cancelRaf(rafId2);
  };
  var getCurrentRemain = () => Math.max(endTime - Date.now(), 0);
  var setRemain = (value) => {
    remain.value = value;
    options.onChange == null ? void 0 : options.onChange(current.value);
    if (value === 0) {
      pause();
      options.onFinish == null ? void 0 : options.onFinish();
    }
  };
  var microTick = () => {
    rafId2 = raf(() => {
      if (counting) {
        setRemain(getCurrentRemain());
        if (remain.value > 0) {
          microTick();
        }
      }
    });
  };
  var macroTick = () => {
    rafId2 = raf(() => {
      if (counting) {
        var remainRemain = getCurrentRemain();
        if (!isSameSecond(remainRemain, remain.value) || remainRemain === 0) {
          setRemain(remainRemain);
        }
        if (remain.value > 0) {
          macroTick();
        }
      }
    });
  };
  var tick = () => {
    if (!inBrowser2) {
      return;
    }
    if (options.millisecond) {
      microTick();
    } else {
      macroTick();
    }
  };
  var start2 = () => {
    if (!counting) {
      endTime = Date.now() + remain.value;
      counting = true;
      tick();
    }
  };
  var reset = (totalTime = options.time) => {
    pause();
    remain.value = totalTime;
  };
  onBeforeUnmount(pause);
  onActivated(() => {
    if (deactivated) {
      counting = true;
      deactivated = false;
      tick();
    }
  });
  onDeactivated(() => {
    if (counting) {
      pause();
      deactivated = true;
    }
  });
  return {
    start: start2,
    pause,
    reset,
    current
  };
}

// node_modules/@vant/use/dist/esm/useClickAway/index.js
init_vue_runtime_esm_bundler();

// node_modules/@vant/use/dist/esm/useEventListener/index.js
init_vue_runtime_esm_bundler();

// node_modules/@vant/use/dist/esm/onMountedOrActivated/index.js
init_vue_runtime_esm_bundler();
function onMountedOrActivated(hook) {
  var mounted;
  onMounted(() => {
    hook();
    nextTick(() => {
      mounted = true;
    });
  });
  onActivated(() => {
    if (mounted) {
      hook();
    }
  });
}

// node_modules/@vant/use/dist/esm/useEventListener/index.js
var supportsPassive = false;
if (inBrowser2) {
  try {
    opts = {};
    Object.defineProperty(opts, "passive", {
      get() {
        supportsPassive = true;
      }
    });
    window.addEventListener("test-passive", null, opts);
  } catch (e) {
  }
}
var opts;
function useEventListener(type, listener, options = {}) {
  if (!inBrowser2) {
    return;
  }
  var {
    target = window,
    passive: passive2 = false,
    capture = false
  } = options;
  var attached;
  var add = () => {
    var element = unref(target);
    if (element && !attached) {
      element.addEventListener(type, listener, supportsPassive ? {
        capture,
        passive: passive2
      } : capture);
      attached = true;
    }
  };
  var remove2 = () => {
    var element = unref(target);
    if (element && attached) {
      element.removeEventListener(type, listener, capture);
      attached = false;
    }
  };
  onUnmounted(remove2);
  onDeactivated(remove2);
  onMountedOrActivated(add);
}

// node_modules/@vant/use/dist/esm/useClickAway/index.js
function useClickAway(target, listener, options = {}) {
  if (!inBrowser2) {
    return;
  }
  var {
    eventName = "click"
  } = options;
  var onClick = (event) => {
    var element = unref(target);
    if (element && !element.contains(event.target)) {
      listener(event);
    }
  };
  useEventListener(eventName, onClick, {
    target: document
  });
}

// node_modules/@vant/use/dist/esm/useWindowSize/index.js
init_vue_runtime_esm_bundler();
function useWindowSize() {
  var width = ref(inBrowser2 ? window.innerWidth : 0);
  var height = ref(inBrowser2 ? window.innerHeight : 0);
  var onResize = () => {
    width.value = window.innerWidth;
    height.value = window.innerHeight;
  };
  useEventListener("resize", onResize);
  useEventListener("orientationchange", onResize);
  return {
    width,
    height
  };
}

// node_modules/@vant/use/dist/esm/useScrollParent/index.js
init_vue_runtime_esm_bundler();
var overflowScrollReg = /scroll|auto/i;
var defaultRoot = inBrowser2 ? window : void 0;
function isElement(node) {
  var ELEMENT_NODE_TYPE = 1;
  return node.tagName !== "HTML" && node.tagName !== "BODY" && node.nodeType === ELEMENT_NODE_TYPE;
}
function getScrollParent(el, root2 = defaultRoot) {
  var node = el;
  while (node && node !== root2 && isElement(node)) {
    var {
      overflowY
    } = window.getComputedStyle(node);
    if (overflowScrollReg.test(overflowY)) {
      return node;
    }
    node = node.parentNode;
  }
  return root2;
}
function useScrollParent(el, root2 = defaultRoot) {
  var scrollParent2 = ref();
  onMounted(() => {
    if (el.value) {
      scrollParent2.value = getScrollParent(el.value, root2);
    }
  });
  return scrollParent2;
}

// node_modules/@vant/use/dist/esm/usePageVisibility/index.js
init_vue_runtime_esm_bundler();
function usePageVisibility() {
  var visibility = ref("visible");
  var setVisibility = () => {
    if (inBrowser2) {
      visibility.value = document.hidden ? "hidden" : "visible";
    }
  };
  setVisibility();
  useEventListener("visibilitychange", setVisibility);
  return visibility;
}

// node_modules/vant/es/action-bar/ActionBar.js
var [name, bem] = createNamespace("action-bar");
var ACTION_BAR_KEY = Symbol(name);
var ActionBar_default = defineComponent({
  name,
  props: {
    safeAreaInsetBottom: truthProp
  },
  setup(props10, {
    slots
  }) {
    var {
      linkChildren
    } = useChildren(ACTION_BAR_KEY);
    linkChildren();
    return () => createVNode("div", {
      "class": bem({
        unfit: !props10.safeAreaInsetBottom
      })
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/action-bar/index.js
var ActionBar = withInstall(ActionBar_default);

// node_modules/vant/es/action-bar-button/ActionBarButton.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/composables/use-expose.js
init_vue_runtime_esm_bundler();
function useExpose(apis) {
  var instance4 = getCurrentInstance();
  if (instance4) {
    extend(instance4.proxy, apis);
  }
}

// node_modules/vant/es/composables/use-route.js
init_vue_runtime_esm_bundler();
var routeProps = {
  to: [String, Object],
  url: String,
  replace: Boolean
};
function route(vm) {
  var router = vm.$router;
  var {
    to,
    url,
    replace
  } = vm;
  if (to && router) {
    router[replace ? "replace" : "push"](to);
  } else if (url) {
    replace ? location.replace(url) : location.href = url;
  }
}
function useRoute() {
  var vm = getCurrentInstance().proxy;
  return () => route(vm);
}

// node_modules/vant/es/button/Button.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/utils/constant.js
var BORDER = "van-hairline";
var BORDER_TOP = BORDER + "--top";
var BORDER_LEFT = BORDER + "--left";
var BORDER_BOTTOM = BORDER + "--bottom";
var BORDER_SURROUND = BORDER + "--surround";
var BORDER_TOP_BOTTOM = BORDER + "--top-bottom";
var BORDER_UNSET_TOP_BOTTOM = BORDER + "-unset--top-bottom";

// node_modules/vant/es/icon/Icon.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/badge/Badge.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name2, bem2] = createNamespace("badge");
var Badge_default = defineComponent({
  name: name2,
  props: {
    dot: Boolean,
    max: [Number, String],
    color: String,
    offset: Array,
    content: [Number, String],
    showZero: truthProp,
    tag: {
      type: String,
      default: "div"
    }
  },
  setup(props10, {
    slots
  }) {
    var hasContent = () => {
      if (slots.content) {
        return true;
      }
      var {
        content,
        showZero
      } = props10;
      return isDef(content) && content !== "" && (showZero || content !== 0);
    };
    var renderContent = () => {
      var {
        dot,
        max,
        content
      } = props10;
      if (!dot && hasContent()) {
        if (slots.content) {
          return slots.content();
        }
        if (isDef(max) && isNumeric(content) && +content > max) {
          return max + "+";
        }
        return content;
      }
    };
    var renderBadge = () => {
      if (hasContent() || props10.dot) {
        var style2 = {
          background: props10.color
        };
        if (props10.offset) {
          var [x, y] = props10.offset;
          if (slots.default) {
            style2.top = addUnit(y);
            style2.right = "-" + addUnit(x);
          } else {
            style2.marginTop = addUnit(y);
            style2.marginLeft = addUnit(x);
          }
        }
        return createVNode("div", {
          "class": bem2({
            dot: props10.dot,
            fixed: !!slots.default
          }),
          "style": style2
        }, [renderContent()]);
      }
    };
    return () => {
      if (slots.default) {
        var {
          tag
        } = props10;
        return createVNode(tag, {
          "class": bem2("wrapper")
        }, {
          default: () => [slots.default(), renderBadge()]
        });
      }
      return renderBadge();
    };
  }
});

// node_modules/vant/es/badge/index.js
var Badge = withInstall(Badge_default);

// node_modules/vant/es/config-provider/ConfigProvider.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name3, bem3] = createNamespace("config-provider");
var CONFIG_PROVIDER_KEY = Symbol(name3);
function mapThemeVarsToCSSVars(themeVars) {
  var cssVars = {};
  Object.keys(themeVars).forEach((key) => {
    cssVars["--van-" + kebabCase(key)] = themeVars[key];
  });
  return cssVars;
}
var ConfigProvider_default = defineComponent({
  name: name3,
  props: {
    themeVars: Object,
    iconPrefix: String,
    tag: {
      type: String,
      default: "div"
    }
  },
  setup(props10, {
    slots
  }) {
    var style2 = computed(() => {
      if (props10.themeVars) {
        return mapThemeVarsToCSSVars(props10.themeVars);
      }
    });
    provide(CONFIG_PROVIDER_KEY, props10);
    return () => createVNode(props10.tag, {
      "class": bem3(),
      "style": style2.value
    }, {
      default: () => [slots.default == null ? void 0 : slots.default()]
    });
  }
});

// node_modules/vant/es/icon/Icon.js
var [name4, bem4] = createNamespace("icon");
function isImage(name96) {
  return name96 == null ? void 0 : name96.includes("/");
}
var Icon_default = defineComponent({
  name: name4,
  props: {
    dot: Boolean,
    name: String,
    size: [Number, String],
    badge: [Number, String],
    color: String,
    classPrefix: String,
    tag: {
      type: String,
      default: "i"
    }
  },
  setup(props10, {
    slots
  }) {
    var config = inject(CONFIG_PROVIDER_KEY, null);
    var classPrefix = computed(() => props10.classPrefix || (config == null ? void 0 : config.iconPrefix) || bem4());
    return () => {
      var {
        tag,
        dot,
        name: name96,
        size,
        badge,
        color
      } = props10;
      var isImageIcon = isImage(name96);
      return createVNode(Badge, {
        "dot": dot,
        "tag": tag,
        "content": badge,
        "class": [classPrefix.value, isImageIcon ? "" : classPrefix.value + "-" + name96],
        "style": {
          color,
          fontSize: addUnit(size)
        }
      }, {
        default: () => [slots.default == null ? void 0 : slots.default(), isImageIcon && createVNode("img", {
          "class": bem4("image"),
          "src": name96
        }, null)]
      });
    };
  }
});

// node_modules/vant/es/icon/index.js
var Icon = withInstall(Icon_default);

// node_modules/vant/es/loading/Loading.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name5, bem5] = createNamespace("loading");
var SpinIcon = Array(12).fill(createVNode("i", null, null));
var CircularIcon = createVNode("svg", {
  "class": bem5("circular"),
  "viewBox": "25 25 50 50"
}, [createVNode("circle", {
  "cx": "50",
  "cy": "50",
  "r": "20",
  "fill": "none"
}, null)]);
var Loading_default = defineComponent({
  name: name5,
  props: {
    size: [Number, String],
    color: String,
    vertical: Boolean,
    textSize: [Number, String],
    textColor: String,
    type: {
      type: String,
      default: "circular"
    }
  },
  setup(props10, {
    slots
  }) {
    var spinnerStyle = computed(() => extend({
      color: props10.color
    }, getSizeStyle(props10.size)));
    var renderText = () => {
      if (slots.default) {
        var _props$textColor;
        return createVNode("span", {
          "class": bem5("text"),
          "style": {
            fontSize: addUnit(props10.textSize),
            color: (_props$textColor = props10.textColor) != null ? _props$textColor : props10.color
          }
        }, [slots.default()]);
      }
    };
    return () => {
      var {
        type,
        vertical
      } = props10;
      return createVNode("div", {
        "class": bem5([type, {
          vertical
        }])
      }, [createVNode("span", {
        "class": bem5("spinner", type),
        "style": spinnerStyle.value
      }, [type === "spinner" ? SpinIcon : CircularIcon]), renderText()]);
    };
  }
});

// node_modules/vant/es/loading/index.js
var Loading = withInstall(Loading_default);

// node_modules/vant/es/button/Button.js
var [name6, bem6] = createNamespace("button");
var Button_default = defineComponent({
  name: name6,
  props: extend({}, routeProps, {
    text: String,
    icon: String,
    color: String,
    block: Boolean,
    plain: Boolean,
    round: Boolean,
    square: Boolean,
    loading: Boolean,
    hairline: Boolean,
    disabled: Boolean,
    iconPrefix: String,
    loadingSize: String,
    loadingText: String,
    loadingType: String,
    tag: {
      type: String,
      default: "button"
    },
    type: {
      type: String,
      default: "default"
    },
    size: {
      type: String,
      default: "normal"
    },
    nativeType: {
      type: String,
      default: "button"
    },
    iconPosition: {
      type: String,
      default: "left"
    }
  }),
  emits: ["click"],
  setup(props10, {
    emit,
    slots
  }) {
    var route2 = useRoute();
    var renderLoadingIcon = () => {
      if (slots.loading) {
        return slots.loading();
      }
      return createVNode(Loading, {
        "size": props10.loadingSize,
        "type": props10.loadingType,
        "class": bem6("loading")
      }, null);
    };
    var renderIcon = () => {
      if (props10.loading) {
        return renderLoadingIcon();
      }
      if (slots.icon) {
        return createVNode("div", {
          "class": bem6("icon")
        }, [slots.icon()]);
      }
      if (props10.icon) {
        return createVNode(Icon, {
          "name": props10.icon,
          "class": bem6("icon"),
          "classPrefix": props10.iconPrefix
        }, null);
      }
    };
    var renderText = () => {
      var text;
      if (props10.loading) {
        text = props10.loadingText;
      } else {
        text = slots.default ? slots.default() : props10.text;
      }
      if (text) {
        return createVNode("span", {
          "class": bem6("text")
        }, [text]);
      }
    };
    var getStyle = () => {
      var {
        color,
        plain
      } = props10;
      if (color) {
        var style2 = {
          color: plain ? color : "white"
        };
        if (!plain) {
          style2.background = color;
        }
        if (color.includes("gradient")) {
          style2.border = 0;
        } else {
          style2.borderColor = color;
        }
        return style2;
      }
    };
    var onClick = (event) => {
      if (props10.loading) {
        event.preventDefault();
      } else if (!props10.disabled) {
        emit("click", event);
        route2();
      }
    };
    return () => {
      var {
        tag,
        type,
        size,
        block,
        round: round2,
        plain,
        square,
        loading,
        disabled,
        hairline,
        nativeType,
        iconPosition
      } = props10;
      var classes = [bem6([type, size, {
        plain,
        block,
        round: round2,
        square,
        loading,
        disabled,
        hairline
      }]), {
        [BORDER_SURROUND]: hairline
      }];
      return createVNode(tag, {
        "type": nativeType,
        "class": classes,
        "style": getStyle(),
        "disabled": disabled,
        "onClick": onClick
      }, {
        default: () => [createVNode("div", {
          "class": bem6("content")
        }, [iconPosition === "left" && renderIcon(), renderText(), iconPosition === "right" && renderIcon()])]
      });
    };
  }
});

// node_modules/vant/es/button/index.js
var Button = withInstall(Button_default);

// node_modules/vant/es/action-bar-button/ActionBarButton.js
var [name7, bem7] = createNamespace("action-bar-button");
var ActionBarButton_default = defineComponent({
  name: name7,
  props: extend({}, routeProps, {
    type: String,
    text: String,
    icon: String,
    color: String,
    loading: Boolean,
    disabled: Boolean
  }),
  setup(props10, {
    slots
  }) {
    var route2 = useRoute();
    var {
      parent,
      index
    } = useParent(ACTION_BAR_KEY);
    var isFirst = computed(() => {
      if (parent) {
        var prev2 = parent.children[index.value - 1];
        return !(prev2 && "isButton" in prev2);
      }
    });
    var isLast = computed(() => {
      if (parent) {
        var next = parent.children[index.value + 1];
        return !(next && "isButton" in next);
      }
    });
    useExpose({
      isButton: true
    });
    return () => {
      var {
        type,
        icon,
        text,
        color,
        loading,
        disabled
      } = props10;
      return createVNode(Button, {
        "class": bem7([type, {
          last: isLast.value,
          first: isFirst.value
        }]),
        "size": "large",
        "type": type,
        "icon": icon,
        "color": color,
        "loading": loading,
        "disabled": disabled,
        "onClick": route2
      }, {
        default: () => [slots.default ? slots.default() : text]
      });
    };
  }
});

// node_modules/vant/es/action-bar-button/index.js
var ActionBarButton = withInstall(ActionBarButton_default);

// node_modules/vant/es/action-bar-icon/ActionBarIcon.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name8, bem8] = createNamespace("action-bar-icon");
var ActionBarIcon_default = defineComponent({
  name: name8,
  props: extend({}, routeProps, {
    dot: Boolean,
    text: String,
    icon: String,
    color: String,
    badge: [Number, String],
    iconClass: unknownProp,
    iconPrefix: String
  }),
  setup(props10, {
    slots
  }) {
    var route2 = useRoute();
    useParent(ACTION_BAR_KEY);
    var renderIcon = () => {
      var {
        dot,
        badge,
        icon,
        color,
        iconClass,
        iconPrefix
      } = props10;
      if (slots.icon) {
        return createVNode(Badge, {
          "dot": dot,
          "content": badge,
          "class": bem8("icon")
        }, {
          default: () => [slots.icon()]
        });
      }
      return createVNode(Icon, {
        "tag": "div",
        "dot": dot,
        "name": icon,
        "badge": badge,
        "color": color,
        "class": [bem8("icon"), iconClass],
        "classPrefix": iconPrefix
      }, null);
    };
    return () => createVNode("div", {
      "role": "button",
      "class": bem8(),
      "tabindex": 0,
      "onClick": route2
    }, [renderIcon(), slots.default ? slots.default() : props10.text]);
  }
});

// node_modules/vant/es/action-bar-icon/index.js
var ActionBarIcon = withInstall(ActionBarIcon_default);

// node_modules/vant/es/action-sheet/ActionSheet.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/popup/Popup.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/popup/shared.js
var popupSharedProps = {
  show: Boolean,
  zIndex: [Number, String],
  overlay: truthProp,
  duration: [Number, String],
  teleport: [String, Object],
  lockScroll: truthProp,
  lazyRender: truthProp,
  overlayStyle: Object,
  overlayClass: unknownProp,
  transitionAppear: Boolean,
  closeOnClickOverlay: truthProp
};
var popupSharedPropKeys = Object.keys(popupSharedProps);

// node_modules/vant/es/composables/use-lock-scroll.js
init_vue_runtime_esm_bundler();

// node_modules/vant/es/composables/use-touch.js
init_vue_runtime_esm_bundler();
var MIN_DISTANCE = 10;
function getDirection(x, y) {
  if (x > y && x > MIN_DISTANCE) {
    return "horizontal";
  }
  if (y > x && y > MIN_DISTANCE) {
    return "vertical";
  }
  return "";
}
function useTouch() {
  var startX = ref(0);
  var startY = ref(0);
  var deltaX = ref(0);
  var deltaY = ref(0);
  var offsetX = ref(0);
  var offsetY = ref(0);
  var direction = ref("");
  var isVertical = () => direction.value === "vertical";
  var isHorizontal = () => direction.value === "horizontal";
  var reset = () => {
    deltaX.value = 0;
    deltaY.value = 0;
    offsetX.value = 0;
    offsetY.value = 0;
    direction.value = "";
  };
  var start2 = (event) => {
    reset();
    startX.value = event.touches[0].clientX;
    startY.value = event.touches[0].clientY;
  };
  var move = (event) => {
    var touch = event.touches[0];
    deltaX.value = touch.clientX < 0 ? 0 : touch.clientX - startX.value;
    deltaY.value = touch.clientY - startY.value;
    offsetX.value = Math.abs(deltaX.value);
    offsetY.value = Math.abs(deltaY.value);
    if (!direction.value) {
      direction.value = getDirection(offsetX.value, offsetY.value);
    }
  };
  return {
    move,
    start: start2,
    reset,
    startX,
    startY,
    deltaX,
    deltaY,
    offsetX,
    offsetY,
    direction,
    isVertical,
    isHorizontal
  };
}

// node_modules/vant/es/composables/use-lock-scroll.js
var totalLockCount = 0;
var BODY_LOCK_CLASS = "van-overflow-hidden";
function useLockScroll(rootRef, shouldLock) {
  var touch = useTouch();
  var onTouchMove = (event) => {
    touch.move(event);
    var direction = touch.deltaY.value > 0 ? "10" : "01";
    var el = getScrollParent(event.target, rootRef.value);
    var {
      scrollHeight,
      offsetHeight,
      scrollTop
    } = el;
    var status = "11";
    if (scrollTop === 0) {
      status = offsetHeight >= scrollHeight ? "00" : "01";
    } else if (scrollTop + offsetHeight >= scrollHeight) {
      status = "10";
    }
    if (status !== "11" && touch.isVertical() && !(parseInt(status, 2) & parseInt(direction, 2))) {
      preventDefault(event, true);
    }
  };
  var lock = () => {
    document.addEventListener("touchstart", touch.start);
    document.addEventListener("touchmove", onTouchMove, supportsPassive ? {
      passive: false
    } : false);
    if (!totalLockCount) {
      document.body.classList.add(BODY_LOCK_CLASS);
    }
    totalLockCount++;
  };
  var unlock = () => {
    if (totalLockCount) {
      document.removeEventListener("touchstart", touch.start);
      document.removeEventListener("touchmove", onTouchMove);
      totalLockCount--;
      if (!totalLockCount) {
        document.body.classList.remove(BODY_LOCK_CLASS);
      }
    }
  };
  var init = () => {
    if (shouldLock()) {
      lock();
    }
  };
  var destroy = () => {
    if (shouldLock()) {
      unlock();
    }
  };
  onMountedOrActivated(init);
  onDeactivated(destroy);
  onBeforeUnmount(destroy);
  watch(shouldLock, (value) => {
    value ? lock() : unlock();
  });
}

// node_modules/vant/es/composables/use-lazy-render.js
init_vue_runtime_esm_bundler();
function useLazyRender(show) {
  var inited = ref(false);
  watch(show, (value) => {
    if (value) {
      inited.value = value;
    }
  }, {
    immediate: true
  });
  return (render) => () => inited.value ? render() : null;
}

// node_modules/vant/es/composables/on-popup-reopen.js
init_vue_runtime_esm_bundler();
var POPUP_TOGGLE_KEY = Symbol();
function onPopupReopen(callback) {
  var popupToggleStatus = inject(POPUP_TOGGLE_KEY, null);
  if (popupToggleStatus) {
    watch(popupToggleStatus, (show) => {
      if (show) {
        callback();
      }
    });
  }
}

// node_modules/vant/es/overlay/Overlay.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name9, bem9] = createNamespace("overlay");
var Overlay_default = defineComponent({
  name: name9,
  props: {
    show: Boolean,
    zIndex: [Number, String],
    duration: [Number, String],
    className: unknownProp,
    lockScroll: truthProp,
    customStyle: Object
  },
  setup(props10, {
    slots
  }) {
    var lazyRender = useLazyRender(() => props10.show);
    var preventTouchMove = (event) => {
      preventDefault(event, true);
    };
    var renderOverlay = lazyRender(() => {
      var style2 = extend(getZIndexStyle(props10.zIndex), props10.customStyle);
      if (isDef(props10.duration)) {
        style2.animationDuration = props10.duration + "s";
      }
      return withDirectives(createVNode("div", {
        "style": style2,
        "class": [bem9(), props10.className],
        "onTouchmove": props10.lockScroll ? preventTouchMove : noop
      }, [slots.default == null ? void 0 : slots.default()]), [[vShow, props10.show]]);
    });
    return () => createVNode(Transition, {
      "name": "van-fade"
    }, {
      default: () => [renderOverlay()]
    });
  }
});

// node_modules/vant/es/overlay/index.js
var Overlay = withInstall(Overlay_default);

// node_modules/vant/es/popup/Popup.js
var [name10, bem10] = createNamespace("popup");
var globalZIndex = 2e3;
var Popup_default = defineComponent({
  name: name10,
  inheritAttrs: false,
  props: extend({}, popupSharedProps, {
    round: Boolean,
    closeable: Boolean,
    transition: String,
    iconPrefix: String,
    closeOnPopstate: Boolean,
    safeAreaInsetBottom: Boolean,
    position: {
      type: String,
      default: "center"
    },
    closeIcon: {
      type: String,
      default: "cross"
    },
    closeIconPosition: {
      type: String,
      default: "top-right"
    }
  }),
  emits: ["open", "close", "click", "opened", "closed", "update:show", "click-overlay", "click-close-icon"],
  setup(props10, {
    emit,
    attrs,
    slots
  }) {
    var opened;
    var shouldReopen;
    var zIndex = ref();
    var popupRef = ref();
    var lazyRender = useLazyRender(() => props10.show || !props10.lazyRender);
    var style2 = computed(() => {
      var style3 = {
        zIndex: zIndex.value
      };
      if (isDef(props10.duration)) {
        var key = props10.position === "center" ? "animationDuration" : "transitionDuration";
        style3[key] = props10.duration + "s";
      }
      return style3;
    });
    var open = () => {
      if (!opened) {
        if (props10.zIndex !== void 0) {
          globalZIndex = +props10.zIndex;
        }
        opened = true;
        zIndex.value = ++globalZIndex;
      }
    };
    var close = () => {
      if (opened) {
        opened = false;
        emit("update:show", false);
      }
    };
    var onClickOverlay = (event) => {
      emit("click-overlay", event);
      if (props10.closeOnClickOverlay) {
        close();
      }
    };
    var renderOverlay = () => {
      if (props10.overlay) {
        return createVNode(Overlay, {
          "show": props10.show,
          "class": props10.overlayClass,
          "zIndex": zIndex.value,
          "duration": props10.duration,
          "customStyle": props10.overlayStyle,
          "onClick": onClickOverlay
        }, {
          default: slots["overlay-content"]
        });
      }
    };
    var onClickCloseIcon = (event) => {
      emit("click-close-icon", event);
      close();
    };
    var renderCloseIcon = () => {
      if (props10.closeable) {
        return createVNode(Icon, {
          "role": "button",
          "tabindex": 0,
          "name": props10.closeIcon,
          "class": bem10("close-icon", props10.closeIconPosition),
          "classPrefix": props10.iconPrefix,
          "onClick": onClickCloseIcon
        }, null);
      }
    };
    var onClick = (event) => emit("click", event);
    var onOpened = () => emit("opened");
    var onClosed = () => emit("closed");
    var renderPopup = lazyRender(() => {
      var {
        round: round2,
        position,
        safeAreaInsetBottom
      } = props10;
      return withDirectives(createVNode("div", mergeProps({
        "ref": popupRef,
        "style": style2.value,
        "class": bem10({
          round: round2,
          [position]: position,
          "safe-area-inset-bottom": safeAreaInsetBottom
        }),
        "onClick": onClick
      }, attrs), [slots.default == null ? void 0 : slots.default(), renderCloseIcon()]), [[vShow, props10.show]]);
    });
    var renderTransition = () => {
      var {
        position,
        transition,
        transitionAppear
      } = props10;
      var name96 = position === "center" ? "van-fade" : "van-popup-slide-" + position;
      return createVNode(Transition, {
        "name": transition || name96,
        "appear": transitionAppear,
        "onAfterEnter": onOpened,
        "onAfterLeave": onClosed
      }, {
        default: () => [renderPopup()]
      });
    };
    watch(() => props10.show, (value) => {
      if (value) {
        open();
        emit("open");
      } else {
        close();
        emit("close");
      }
    });
    useExpose({
      popupRef
    });
    useLockScroll(popupRef, () => props10.show && props10.lockScroll);
    useEventListener("popstate", () => {
      if (props10.closeOnPopstate) {
        close();
        shouldReopen = false;
      }
    });
    onMounted(() => {
      if (props10.show) {
        open();
      }
    });
    onActivated(() => {
      if (shouldReopen) {
        emit("update:show", true);
        shouldReopen = false;
      }
    });
    onDeactivated(() => {
      if (props10.show) {
        close();
        shouldReopen = true;
      }
    });
    provide(POPUP_TOGGLE_KEY, () => props10.show);
    return () => {
      if (props10.teleport) {
        return createVNode(Teleport, {
          "to": props10.teleport
        }, {
          default: () => [renderOverlay(), renderTransition()]
        });
      }
      return createVNode(Fragment, null, [renderOverlay(), renderTransition()]);
    };
  }
});

// node_modules/vant/es/popup/index.js
var Popup = withInstall(Popup_default);

// node_modules/vant/es/action-sheet/ActionSheet.js
var [name11, bem11] = createNamespace("action-sheet");
var ActionSheet_default = defineComponent({
  name: name11,
  props: extend({}, popupSharedProps, {
    title: String,
    round: truthProp,
    actions: Array,
    closeable: truthProp,
    cancelText: String,
    description: String,
    closeOnPopstate: Boolean,
    closeOnClickAction: Boolean,
    safeAreaInsetBottom: truthProp,
    closeIcon: {
      type: String,
      default: "cross"
    }
  }),
  emits: ["select", "cancel", "update:show"],
  setup(props10, {
    slots,
    emit
  }) {
    var updateShow = (show) => emit("update:show", show);
    var onCancel = () => {
      updateShow(false);
      emit("cancel");
    };
    var renderHeader = () => {
      if (props10.title) {
        return createVNode("div", {
          "class": bem11("header")
        }, [props10.title, props10.closeable && createVNode(Icon, {
          "name": props10.closeIcon,
          "class": bem11("close"),
          "onClick": onCancel
        }, null)]);
      }
    };
    var renderCancel = () => {
      if (slots.cancel || props10.cancelText) {
        return [createVNode("div", {
          "class": bem11("gap")
        }, null), createVNode("button", {
          "type": "button",
          "class": bem11("cancel"),
          "onClick": onCancel
        }, [slots.cancel ? slots.cancel() : props10.cancelText])];
      }
    };
    var renderOption = (item, index) => {
      var {
        name: name96,
        color,
        subname,
        loading,
        callback,
        disabled,
        className
      } = item;
      var Content = loading ? createVNode(Loading, {
        "class": bem11("loading-icon")
      }, null) : [createVNode("span", {
        "class": bem11("name")
      }, [name96]), subname && createVNode("div", {
        "class": bem11("subname")
      }, [subname])];
      var onClick = () => {
        if (disabled || loading) {
          return;
        }
        if (callback) {
          callback(item);
        }
        if (props10.closeOnClickAction) {
          updateShow(false);
        }
        nextTick(() => emit("select", item, index));
      };
      return createVNode("button", {
        "type": "button",
        "style": {
          color
        },
        "class": [bem11("item", {
          loading,
          disabled
        }), className],
        "onClick": onClick
      }, [Content]);
    };
    var renderDescription = () => {
      if (props10.description || slots.description) {
        var content = slots.description ? slots.description() : props10.description;
        return createVNode("div", {
          "class": bem11("description")
        }, [content]);
      }
    };
    var renderOptions = () => {
      if (props10.actions) {
        return props10.actions.map(renderOption);
      }
    };
    return () => createVNode(Popup, mergeProps({
      "class": bem11(),
      "round": props10.round,
      "position": "bottom",
      "safeAreaInsetBottom": props10.safeAreaInsetBottom
    }, pick(props10, popupSharedPropKeys), {
      "onUpdate:show": updateShow
    }), {
      default: () => [renderHeader(), renderDescription(), createVNode("div", {
        "class": bem11("content")
      }, [renderOptions(), slots.default == null ? void 0 : slots.default()]), renderCancel()]
    });
  }
});

// node_modules/vant/es/action-sheet/index.js
var ActionSheet = withInstall(ActionSheet_default);

// node_modules/vant/es/address-edit/AddressEdit.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/area/Area.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/utils/deep-clone.js
function deepClone(obj) {
  if (!isDef(obj)) {
    return obj;
  }
  if (Array.isArray(obj)) {
    return obj.map((item) => deepClone(item));
  }
  if (typeof obj === "object") {
    var to = {};
    Object.keys(obj).forEach((key) => {
      to[key] = deepClone(obj[key]);
    });
    return to;
  }
  return obj;
}

// node_modules/vant/es/picker/Picker.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/picker/PickerColumn.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var DEFAULT_DURATION = 200;
var MOMENTUM_LIMIT_TIME = 300;
var MOMENTUM_LIMIT_DISTANCE = 15;
var [name12, bem12] = createNamespace("picker-column");
function getElementTranslateY(element) {
  var style2 = window.getComputedStyle(element);
  var transform = style2.transform || style2.webkitTransform;
  var translateY = transform.slice(7, transform.length - 1).split(", ")[5];
  return Number(translateY);
}
var PICKER_KEY = Symbol(name12);
function isOptionDisabled(option) {
  return isObject(option) && option.disabled;
}
var PickerColumn_default = defineComponent({
  name: name12,
  props: {
    readonly: Boolean,
    allowHtml: Boolean,
    className: unknownProp,
    textKey: {
      type: String,
      required: true
    },
    itemHeight: {
      type: Number,
      required: true
    },
    swipeDuration: {
      type: [Number, String],
      required: true
    },
    visibleItemCount: {
      type: [Number, String],
      required: true
    },
    defaultIndex: {
      type: Number,
      default: 0
    },
    initialOptions: {
      type: Array,
      default: () => []
    }
  },
  emits: ["change"],
  setup(props10, {
    emit,
    slots
  }) {
    var moving;
    var startOffset;
    var touchStartTime;
    var momentumOffset;
    var transitionEndTrigger;
    var wrapper = ref();
    var state = reactive({
      index: props10.defaultIndex,
      offset: 0,
      duration: 0,
      options: deepClone(props10.initialOptions)
    });
    var touch = useTouch();
    var count = () => state.options.length;
    var baseOffset = () => props10.itemHeight * (+props10.visibleItemCount - 1) / 2;
    var adjustIndex = (index) => {
      index = clamp(index, 0, count());
      for (var i = index; i < count(); i++) {
        if (!isOptionDisabled(state.options[i]))
          return i;
      }
      for (var _i = index - 1; _i >= 0; _i--) {
        if (!isOptionDisabled(state.options[_i]))
          return _i;
      }
    };
    var setIndex = (index, emitChange) => {
      index = adjustIndex(index) || 0;
      var offset2 = -index * props10.itemHeight;
      var trigger2 = () => {
        if (index !== state.index) {
          state.index = index;
          if (emitChange) {
            emit("change", index);
          }
        }
      };
      if (moving && offset2 !== state.offset) {
        transitionEndTrigger = trigger2;
      } else {
        trigger2();
      }
      state.offset = offset2;
    };
    var setOptions = (options) => {
      if (JSON.stringify(options) !== JSON.stringify(state.options)) {
        state.options = deepClone(options);
        setIndex(props10.defaultIndex);
      }
    };
    var onClickItem = (index) => {
      if (moving || props10.readonly) {
        return;
      }
      transitionEndTrigger = null;
      state.duration = DEFAULT_DURATION;
      setIndex(index, true);
    };
    var getOptionText = (option) => {
      if (isObject(option) && props10.textKey in option) {
        return option[props10.textKey];
      }
      return option;
    };
    var getIndexByOffset = (offset2) => clamp(Math.round(-offset2 / props10.itemHeight), 0, count() - 1);
    var momentum = (distance, duration) => {
      var speed = Math.abs(distance / duration);
      distance = state.offset + speed / 3e-3 * (distance < 0 ? -1 : 1);
      var index = getIndexByOffset(distance);
      state.duration = +props10.swipeDuration;
      setIndex(index, true);
    };
    var stopMomentum = () => {
      moving = false;
      state.duration = 0;
      if (transitionEndTrigger) {
        transitionEndTrigger();
        transitionEndTrigger = null;
      }
    };
    var onTouchStart = (event) => {
      if (props10.readonly) {
        return;
      }
      touch.start(event);
      if (moving) {
        var translateY = getElementTranslateY(wrapper.value);
        state.offset = Math.min(0, translateY - baseOffset());
        startOffset = state.offset;
      } else {
        startOffset = state.offset;
      }
      state.duration = 0;
      touchStartTime = Date.now();
      momentumOffset = startOffset;
      transitionEndTrigger = null;
    };
    var onTouchMove = (event) => {
      if (props10.readonly) {
        return;
      }
      touch.move(event);
      if (touch.isVertical()) {
        moving = true;
        preventDefault(event, true);
      }
      state.offset = clamp(startOffset + touch.deltaY.value, -(count() * props10.itemHeight), props10.itemHeight);
      var now = Date.now();
      if (now - touchStartTime > MOMENTUM_LIMIT_TIME) {
        touchStartTime = now;
        momentumOffset = state.offset;
      }
    };
    var onTouchEnd = () => {
      if (props10.readonly) {
        return;
      }
      var distance = state.offset - momentumOffset;
      var duration = Date.now() - touchStartTime;
      var allowMomentum = duration < MOMENTUM_LIMIT_TIME && Math.abs(distance) > MOMENTUM_LIMIT_DISTANCE;
      if (allowMomentum) {
        momentum(distance, duration);
        return;
      }
      var index = getIndexByOffset(state.offset);
      state.duration = DEFAULT_DURATION;
      setIndex(index, true);
      setTimeout(() => {
        moving = false;
      }, 0);
    };
    var renderOptions = () => {
      var optionStyle = {
        height: props10.itemHeight + "px"
      };
      return state.options.map((option, index) => {
        var text = getOptionText(option);
        var disabled = isOptionDisabled(option);
        var data = {
          role: "button",
          style: optionStyle,
          tabindex: disabled ? -1 : 0,
          class: bem12("item", {
            disabled,
            selected: index === state.index
          }),
          onClick: () => onClickItem(index)
        };
        var childData = {
          class: "van-ellipsis",
          [props10.allowHtml ? "innerHTML" : "textContent"]: text
        };
        return createVNode("li", data, [slots.option ? slots.option(option) : createVNode("div", childData, null)]);
      });
    };
    var setValue = (value) => {
      var {
        options
      } = state;
      for (var i = 0; i < options.length; i++) {
        if (getOptionText(options[i]) === value) {
          return setIndex(i);
        }
      }
    };
    var getValue = () => state.options[state.index];
    setIndex(state.index);
    useParent(PICKER_KEY);
    useExpose({
      state,
      setIndex,
      getValue,
      setValue,
      setOptions,
      stopMomentum
    });
    watch(() => props10.initialOptions, setOptions);
    watch(() => props10.defaultIndex, (value) => {
      setIndex(value);
    });
    return () => {
      var wrapperStyle = {
        transform: "translate3d(0, " + (state.offset + baseOffset()) + "px, 0)",
        transitionDuration: state.duration + "ms",
        transitionProperty: state.duration ? "all" : "none"
      };
      return createVNode("div", {
        "class": [bem12(), props10.className],
        "onTouchstart": onTouchStart,
        "onTouchmove": onTouchMove,
        "onTouchend": onTouchEnd,
        "onTouchcancel": onTouchEnd
      }, [createVNode("ul", {
        "ref": wrapper,
        "style": wrapperStyle,
        "class": bem12("wrapper"),
        "onTransitionend": stopMomentum
      }, [renderOptions()])]);
    };
  }
});

// node_modules/vant/es/picker/Picker.js
var [name13, bem13, t] = createNamespace("picker");
var pickerProps = {
  title: String,
  loading: Boolean,
  readonly: Boolean,
  allowHtml: Boolean,
  showToolbar: truthProp,
  cancelButtonText: String,
  confirmButtonText: String,
  itemHeight: {
    type: [Number, String],
    default: 44
  },
  visibleItemCount: {
    type: [Number, String],
    default: 6
  },
  swipeDuration: {
    type: [Number, String],
    default: 1e3
  }
};
var Picker_default = defineComponent({
  name: name13,
  props: extend({}, pickerProps, {
    valueKey: String,
    columnsFieldNames: Object,
    columns: {
      type: Array,
      default: () => []
    },
    defaultIndex: {
      type: [Number, String],
      default: 0
    },
    toolbarPosition: {
      type: String,
      default: "top"
    }
  }),
  emits: ["confirm", "cancel", "change"],
  setup(props10, {
    emit,
    slots
  }) {
    if (true) {
      if (slots.default) {
        console.warn('[Vant] Picker: "default" slot is deprecated, please use "toolbar" slot instead.');
      }
      if (props10.valueKey) {
        console.warn('[Vant] Picker: "valueKey" prop is deprecated, please use "columnsFieldNames" prop instead.');
      }
    }
    var formattedColumns = ref([]);
    var {
      text: textKey,
      values: valuesKey,
      children: childrenKey
    } = extend({
      text: props10.valueKey || "text",
      values: "values",
      children: "children"
    }, props10.columnsFieldNames);
    var {
      children,
      linkChildren
    } = useChildren(PICKER_KEY);
    linkChildren();
    var itemHeight = computed(() => unitToPx(props10.itemHeight));
    var dataType = computed(() => {
      var firstColumn = props10.columns[0];
      if (typeof firstColumn === "object") {
        if (childrenKey in firstColumn) {
          return "cascade";
        }
        if (valuesKey in firstColumn) {
          return "object";
        }
      }
      return "plain";
    });
    var formatCascade = () => {
      var formatted = [];
      var cursor = {
        [childrenKey]: props10.columns
      };
      while (cursor && cursor[childrenKey]) {
        var _cursor$defaultIndex;
        var _children = cursor[childrenKey];
        var defaultIndex = (_cursor$defaultIndex = cursor.defaultIndex) != null ? _cursor$defaultIndex : +props10.defaultIndex;
        while (_children[defaultIndex] && _children[defaultIndex].disabled) {
          if (defaultIndex < _children.length - 1) {
            defaultIndex++;
          } else {
            defaultIndex = 0;
            break;
          }
        }
        formatted.push({
          [valuesKey]: cursor[childrenKey],
          className: cursor.className,
          defaultIndex
        });
        cursor = _children[defaultIndex];
      }
      formattedColumns.value = formatted;
    };
    var format3 = () => {
      var {
        columns
      } = props10;
      if (dataType.value === "plain") {
        formattedColumns.value = [{
          [valuesKey]: columns
        }];
      } else if (dataType.value === "cascade") {
        formatCascade();
      } else {
        formattedColumns.value = columns;
      }
    };
    var getIndexes = () => children.map((child) => child.state.index);
    var setColumnValues = (index, options) => {
      var column = children[index];
      if (column) {
        column.setOptions(options);
      }
    };
    var onCascadeChange = (columnIndex) => {
      var cursor = {
        [childrenKey]: props10.columns
      };
      var indexes = getIndexes();
      for (var i = 0; i <= columnIndex; i++) {
        cursor = cursor[childrenKey][indexes[i]];
      }
      while (cursor && cursor[childrenKey]) {
        columnIndex++;
        setColumnValues(columnIndex, cursor[childrenKey]);
        cursor = cursor[childrenKey][cursor.defaultIndex || 0];
      }
    };
    var getChild = (index) => children[index];
    var getColumnValue = (index) => {
      var column = getChild(index);
      if (column) {
        return column.getValue();
      }
    };
    var setColumnValue = (index, value) => {
      var column = getChild(index);
      if (column) {
        column.setValue(value);
        if (dataType.value === "cascade") {
          onCascadeChange(index);
        }
      }
    };
    var getColumnIndex = (index) => {
      var column = getChild(index);
      if (column) {
        return column.state.index;
      }
    };
    var setColumnIndex = (columnIndex, optionIndex) => {
      var column = getChild(columnIndex);
      if (column) {
        column.setIndex(optionIndex);
        if (dataType.value === "cascade") {
          onCascadeChange(columnIndex);
        }
      }
    };
    var getColumnValues = (index) => {
      var column = getChild(index);
      if (column) {
        return column.state.options;
      }
    };
    var getValues = () => children.map((child) => child.getValue());
    var setValues = (values) => {
      values.forEach((value, index) => {
        setColumnValue(index, value);
      });
    };
    var setIndexes = (indexes) => {
      indexes.forEach((optionIndex, columnIndex) => {
        setColumnIndex(columnIndex, optionIndex);
      });
    };
    var emitAction = (event) => {
      if (dataType.value === "plain") {
        emit(event, getColumnValue(0), getColumnIndex(0));
      } else {
        emit(event, getValues(), getIndexes());
      }
    };
    var onChange = (columnIndex) => {
      if (dataType.value === "cascade") {
        onCascadeChange(columnIndex);
      }
      if (dataType.value === "plain") {
        emit("change", getColumnValue(0), getColumnIndex(0));
      } else {
        emit("change", getValues(), columnIndex);
      }
    };
    var confirm = () => {
      children.forEach((child) => child.stopMomentum());
      emitAction("confirm");
    };
    var cancel = () => emitAction("cancel");
    var renderTitle = () => {
      if (slots.title) {
        return slots.title();
      }
      if (props10.title) {
        return createVNode("div", {
          "class": [bem13("title"), "van-ellipsis"]
        }, [props10.title]);
      }
    };
    var renderCancel = () => {
      var text = props10.cancelButtonText || t("cancel");
      return createVNode("button", {
        "type": "button",
        "class": bem13("cancel"),
        "onClick": cancel
      }, [slots.cancel ? slots.cancel() : text]);
    };
    var renderConfirm = () => {
      var text = props10.confirmButtonText || t("confirm");
      return createVNode("button", {
        "type": "button",
        "class": bem13("confirm"),
        "onClick": confirm
      }, [slots.confirm ? slots.confirm() : text]);
    };
    var renderToolbar = () => {
      if (props10.showToolbar) {
        var slot = slots.toolbar || slots.default;
        return createVNode("div", {
          "class": bem13("toolbar")
        }, [slot ? slot() : [renderCancel(), renderTitle(), renderConfirm()]]);
      }
    };
    var renderColumnItems = () => formattedColumns.value.map((item, columnIndex) => {
      var _item$defaultIndex;
      return createVNode(PickerColumn_default, {
        "textKey": textKey,
        "readonly": props10.readonly,
        "allowHtml": props10.allowHtml,
        "className": item.className,
        "itemHeight": itemHeight.value,
        "defaultIndex": (_item$defaultIndex = item.defaultIndex) != null ? _item$defaultIndex : +props10.defaultIndex,
        "swipeDuration": props10.swipeDuration,
        "initialOptions": item[valuesKey],
        "visibleItemCount": props10.visibleItemCount,
        "onChange": () => onChange(columnIndex)
      }, {
        option: slots.option
      });
    });
    var renderColumns = () => {
      var wrapHeight = itemHeight.value * +props10.visibleItemCount;
      var frameStyle = {
        height: itemHeight.value + "px"
      };
      var columnsStyle = {
        height: wrapHeight + "px"
      };
      var maskStyle = {
        backgroundSize: "100% " + (wrapHeight - itemHeight.value) / 2 + "px"
      };
      return createVNode("div", {
        "class": bem13("columns"),
        "style": columnsStyle,
        "onTouchmove": preventDefault
      }, [renderColumnItems(), createVNode("div", {
        "class": bem13("mask"),
        "style": maskStyle
      }, null), createVNode("div", {
        "class": [BORDER_UNSET_TOP_BOTTOM, bem13("frame")],
        "style": frameStyle
      }, null)]);
    };
    watch(() => props10.columns, format3, {
      immediate: true
    });
    useExpose({
      confirm,
      getValues,
      setValues,
      getIndexes,
      setIndexes,
      getColumnIndex,
      setColumnIndex,
      getColumnValue,
      setColumnValue,
      getColumnValues,
      setColumnValues
    });
    return () => {
      var _slots$columnsTop, _slots$columnsBottom;
      return createVNode("div", {
        "class": bem13()
      }, [props10.toolbarPosition === "top" ? renderToolbar() : null, props10.loading ? createVNode(Loading, {
        "class": bem13("loading")
      }, null) : null, (_slots$columnsTop = slots["columns-top"]) == null ? void 0 : _slots$columnsTop.call(slots), renderColumns(), (_slots$columnsBottom = slots["columns-bottom"]) == null ? void 0 : _slots$columnsBottom.call(slots), props10.toolbarPosition === "bottom" ? renderToolbar() : null]);
    };
  }
});

// node_modules/vant/es/picker/index.js
var Picker = withInstall(Picker_default);

// node_modules/vant/es/area/Area.js
var [name14, bem14] = createNamespace("area");
var EMPTY_CODE = "000000";
var INHERIT_SLOTS = ["title", "cancel", "confirm", "toolbar", "columns-top", "columns-bottom"];
var INHERIT_PROPS = ["title", "loading", "readonly", "itemHeight", "swipeDuration", "visibleItemCount", "cancelButtonText", "confirmButtonText"];
function isOverseaCode(code) {
  return code[0] === "9";
}
var Area_default = defineComponent({
  name: name14,
  props: extend({}, pickerProps, {
    value: String,
    areaList: {
      type: Object,
      default: () => ({})
    },
    columnsNum: {
      type: [Number, String],
      default: 3
    },
    isOverseaCode: {
      type: Function,
      default: isOverseaCode
    },
    columnsPlaceholder: {
      type: Array,
      default: () => []
    }
  }),
  emits: ["change", "confirm"],
  setup(props10, {
    emit,
    slots
  }) {
    var pickerRef = ref();
    var state = reactive({
      code: props10.value,
      columns: [{
        values: []
      }, {
        values: []
      }, {
        values: []
      }]
    });
    var areaList = computed(() => {
      var {
        areaList: areaList2
      } = props10;
      return {
        province: areaList2.province_list || {},
        city: areaList2.city_list || {},
        county: areaList2.county_list || {}
      };
    });
    var placeholderMap = computed(() => {
      var {
        columnsPlaceholder
      } = props10;
      return {
        province: columnsPlaceholder[0] || "",
        city: columnsPlaceholder[1] || "",
        county: columnsPlaceholder[2] || ""
      };
    });
    var getDefaultCode = () => {
      if (props10.columnsPlaceholder.length) {
        return EMPTY_CODE;
      }
      var {
        county,
        city
      } = areaList.value;
      var countyCodes = Object.keys(county);
      if (countyCodes[0]) {
        return countyCodes[0];
      }
      var cityCodes = Object.keys(city);
      if (cityCodes[0]) {
        return cityCodes[0];
      }
      return "";
    };
    var getColumnValues = (type, code) => {
      var column = [];
      if (type !== "province" && !code) {
        return column;
      }
      var list = areaList.value[type];
      column = Object.keys(list).map((listCode) => ({
        code: listCode,
        name: list[listCode]
      }));
      if (code) {
        if (type === "city" && props10.isOverseaCode(code)) {
          code = "9";
        }
        column = column.filter((item) => item.code.indexOf(code) === 0);
      }
      if (placeholderMap.value[type] && column.length) {
        var codeFill = "";
        if (type === "city") {
          codeFill = EMPTY_CODE.slice(2, 4);
        } else if (type === "county") {
          codeFill = EMPTY_CODE.slice(4, 6);
        }
        column.unshift({
          code: code + codeFill,
          name: placeholderMap.value[type]
        });
      }
      return column;
    };
    var getIndex = (type, code) => {
      var compareNum = code.length;
      if (type === "province") {
        compareNum = props10.isOverseaCode(code) ? 1 : 2;
      }
      if (type === "city") {
        compareNum = 4;
      }
      code = code.slice(0, compareNum);
      var list = getColumnValues(type, compareNum > 2 ? code.slice(0, compareNum - 2) : "");
      for (var i = 0; i < list.length; i++) {
        if (list[i].code.slice(0, compareNum) === code) {
          return i;
        }
      }
      return 0;
    };
    var setValues = () => {
      var code = state.code || getDefaultCode();
      var picker = pickerRef.value;
      var province = getColumnValues("province");
      var city = getColumnValues("city", code.slice(0, 2));
      if (!picker) {
        return;
      }
      picker.setColumnValues(0, province);
      picker.setColumnValues(1, city);
      if (city.length && code.slice(2, 4) === "00" && !props10.isOverseaCode(code)) {
        [{
          code
        }] = city;
      }
      picker.setColumnValues(2, getColumnValues("county", code.slice(0, 4)));
      picker.setIndexes([getIndex("province", code), getIndex("city", code), getIndex("county", code)]);
    };
    var parseValues = (values) => {
      return values.map((value, index) => {
        if (value) {
          value = deepClone(value);
          if (!value.code || value.name === props10.columnsPlaceholder[index]) {
            value.code = "";
            value.name = "";
          }
        }
        return value;
      });
    };
    var getValues = () => {
      if (pickerRef.value) {
        var values = pickerRef.value.getValues().filter(Boolean);
        return parseValues(values);
      }
      return [];
    };
    var getArea = () => {
      var values = getValues();
      var area = {
        code: "",
        country: "",
        province: "",
        city: "",
        county: ""
      };
      if (!values.length) {
        return area;
      }
      var names = values.map((item) => item.name);
      var validValues = values.filter((value) => value.code);
      area.code = validValues.length ? validValues[validValues.length - 1].code : "";
      if (props10.isOverseaCode(area.code)) {
        area.country = names[1] || "";
        area.province = names[2] || "";
      } else {
        area.province = names[0] || "";
        area.city = names[1] || "";
        area.county = names[2] || "";
      }
      return area;
    };
    var reset = (newCode = "") => {
      state.code = newCode;
      setValues();
    };
    var onChange = (values, index) => {
      state.code = values[index].code;
      setValues();
      var parsedValues = parseValues(pickerRef.value.getValues());
      emit("change", parsedValues, index);
    };
    var onConfirm = (values, index) => {
      setValues();
      emit("confirm", parseValues(values), index);
    };
    onMounted(setValues);
    watch(() => props10.value, (value) => {
      state.code = value;
      setValues();
    });
    watch(() => props10.areaList, setValues, {
      deep: true
    });
    watch(() => props10.columnsNum, () => nextTick(setValues));
    useExpose({
      reset,
      getArea,
      getValues
    });
    return () => {
      var columns = state.columns.slice(0, +props10.columnsNum);
      return createVNode(Picker, mergeProps({
        "ref": pickerRef,
        "class": bem14(),
        "columns": columns,
        "columnsFieldNames": {
          text: "name"
        },
        "onChange": onChange,
        "onConfirm": onConfirm
      }, pick(props10, INHERIT_PROPS)), pick(slots, INHERIT_SLOTS));
    };
  }
});

// node_modules/vant/es/area/index.js
var Area = withInstall(Area_default);

// node_modules/vant/es/cell/Cell.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name15, bem15] = createNamespace("cell");
var cellProps = {
  icon: String,
  size: String,
  title: [Number, String],
  value: [Number, String],
  label: [Number, String],
  center: Boolean,
  isLink: Boolean,
  border: truthProp,
  required: Boolean,
  iconPrefix: String,
  valueClass: unknownProp,
  labelClass: unknownProp,
  titleClass: unknownProp,
  titleStyle: null,
  arrowDirection: String,
  clickable: {
    type: Boolean,
    default: null
  }
};
var Cell_default = defineComponent({
  name: name15,
  props: extend({}, cellProps, routeProps),
  setup(props10, {
    slots
  }) {
    if (true) {
      if (slots.default) {
        console.warn('[Vant] Cell: "default" slot is deprecated, please use "value" slot instead.');
      }
    }
    var route2 = useRoute();
    var renderLabel = () => {
      var showLabel = slots.label || isDef(props10.label);
      if (showLabel) {
        return createVNode("div", {
          "class": [bem15("label"), props10.labelClass]
        }, [slots.label ? slots.label() : props10.label]);
      }
    };
    var renderTitle = () => {
      if (slots.title || isDef(props10.title)) {
        return createVNode("div", {
          "class": [bem15("title"), props10.titleClass],
          "style": props10.titleStyle
        }, [slots.title ? slots.title() : createVNode("span", null, [props10.title]), renderLabel()]);
      }
    };
    var renderValue = () => {
      var slot = slots.value || slots.default;
      var hasValue = slot || isDef(props10.value);
      if (hasValue) {
        var hasTitle = slots.title || isDef(props10.title);
        return createVNode("div", {
          "class": [bem15("value", {
            alone: !hasTitle
          }), props10.valueClass]
        }, [slot ? slot() : createVNode("span", null, [props10.value])]);
      }
    };
    var renderLeftIcon = () => {
      if (slots.icon) {
        return slots.icon();
      }
      if (props10.icon) {
        return createVNode(Icon, {
          "name": props10.icon,
          "class": bem15("left-icon"),
          "classPrefix": props10.iconPrefix
        }, null);
      }
    };
    var renderRightIcon = () => {
      if (slots["right-icon"]) {
        return slots["right-icon"]();
      }
      if (props10.isLink) {
        var _name = props10.arrowDirection ? "arrow-" + props10.arrowDirection : "arrow";
        return createVNode(Icon, {
          "name": _name,
          "class": bem15("right-icon")
        }, null);
      }
    };
    return () => {
      var _props$clickable;
      var {
        size,
        center,
        border,
        isLink,
        required
      } = props10;
      var clickable = (_props$clickable = props10.clickable) != null ? _props$clickable : isLink;
      var classes = {
        center,
        required,
        clickable,
        borderless: !border
      };
      if (size) {
        classes[size] = !!size;
      }
      return createVNode("div", {
        "class": bem15(classes),
        "role": clickable ? "button" : void 0,
        "tabindex": clickable ? 0 : void 0,
        "onClick": route2
      }, [renderLeftIcon(), renderTitle(), renderValue(), renderRightIcon(), slots.extra == null ? void 0 : slots.extra()]);
    };
  }
});

// node_modules/vant/es/cell/index.js
var Cell = withInstall(Cell_default);

// node_modules/vant/es/field/Field.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/field/utils.js
function isEmptyValue(value) {
  if (Array.isArray(value)) {
    return !value.length;
  }
  if (value === 0) {
    return false;
  }
  return !value;
}
function runSyncRule(value, rule) {
  if (rule.required && isEmptyValue(value)) {
    return false;
  }
  if (rule.pattern && !rule.pattern.test(String(value))) {
    return false;
  }
  return true;
}
function runRuleValidator(value, rule) {
  return new Promise((resolve) => {
    var returnVal = rule.validator(value, rule);
    if (isPromise(returnVal)) {
      return returnVal.then(resolve);
    }
    resolve(returnVal);
  });
}
function getRuleMessage(value, rule) {
  var {
    message
  } = rule;
  if (isFunction(message)) {
    return message(value, rule);
  }
  return message || "";
}
function startComposing(event) {
  event.target.composing = true;
}
function endComposing(event) {
  var {
    target
  } = event;
  if (target.composing) {
    target.composing = false;
    trigger(target, "input");
  }
}
function resizeTextarea(input, autosize) {
  input.style.height = "auto";
  var height = input.scrollHeight;
  if (isObject(autosize)) {
    var {
      maxHeight,
      minHeight
    } = autosize;
    if (maxHeight !== void 0) {
      height = Math.min(height, maxHeight);
    }
    if (minHeight !== void 0) {
      height = Math.max(height, minHeight);
    }
  }
  if (height) {
    input.style.height = height + "px";
  }
}
function mapInputType(type) {
  if (type === "number") {
    return {
      type: "text",
      inputmode: "decimal"
    };
  }
  if (type === "digit") {
    return {
      type: "tel",
      inputmode: "numeric"
    };
  }
  return {
    type
  };
}

// node_modules/vant/es/composables/use-link-field.js
init_vue_runtime_esm_bundler();
var FORM_KEY = Symbol("van-form");
var FIELD_KEY = Symbol("van-field");
function useLinkField(getValue) {
  var field = inject(FIELD_KEY, null);
  if (field && !field.childFieldValue.value) {
    field.childFieldValue.value = getValue;
    watch(getValue, () => {
      field.resetValidation();
      field.validateWithTrigger("onChange");
    });
  }
}

// node_modules/vant/es/field/Field.js
var [name16, bem16] = createNamespace("field");
var fieldProps = {
  formatter: Function,
  leftIcon: String,
  rightIcon: String,
  autofocus: Boolean,
  clearable: Boolean,
  maxlength: [Number, String],
  inputAlign: String,
  placeholder: String,
  errorMessage: String,
  error: {
    type: Boolean,
    default: null
  },
  disabled: {
    type: Boolean,
    default: null
  },
  readonly: {
    type: Boolean,
    default: null
  },
  clearIcon: {
    type: String,
    default: "clear"
  },
  modelValue: {
    type: [Number, String],
    default: ""
  },
  clearTrigger: {
    type: String,
    default: "focus"
  },
  formatTrigger: {
    type: String,
    default: "onChange"
  }
};
var Field_default = defineComponent({
  name: name16,
  props: extend({}, cellProps, fieldProps, {
    rows: [Number, String],
    name: String,
    rules: Array,
    autosize: [Boolean, Object],
    labelWidth: [Number, String],
    labelClass: unknownProp,
    labelAlign: String,
    autocomplete: String,
    showWordLimit: Boolean,
    errorMessageAlign: String,
    type: {
      type: String,
      default: "text"
    },
    colon: {
      type: Boolean,
      default: null
    }
  }),
  emits: ["blur", "focus", "clear", "keypress", "click-input", "click-left-icon", "click-right-icon", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var state = reactive({
      focused: false,
      validateFailed: false,
      validateMessage: ""
    });
    var inputRef = ref();
    var childFieldValue = ref();
    var {
      parent: form
    } = useParent(FORM_KEY);
    var getModelValue = () => {
      var _props$modelValue;
      return String((_props$modelValue = props10.modelValue) != null ? _props$modelValue : "");
    };
    var getProp = (key) => {
      if (isDef(props10[key])) {
        return props10[key];
      }
      if (form && isDef(form.props[key])) {
        return form.props[key];
      }
    };
    var showClear = computed(() => {
      var readonly = getProp("readonly");
      if (props10.clearable && !readonly) {
        var hasValue = getModelValue() !== "";
        var trigger2 = props10.clearTrigger === "always" || props10.clearTrigger === "focus" && state.focused;
        return hasValue && trigger2;
      }
      return false;
    });
    var formValue = computed(() => {
      if (childFieldValue.value && slots.input) {
        return childFieldValue.value();
      }
      return props10.modelValue;
    });
    var runRules = (rules) => rules.reduce((promise, rule) => promise.then(() => {
      if (state.validateFailed) {
        return;
      }
      var {
        value
      } = formValue;
      if (rule.formatter) {
        value = rule.formatter(value, rule);
      }
      if (!runSyncRule(value, rule)) {
        state.validateFailed = true;
        state.validateMessage = getRuleMessage(value, rule);
        return;
      }
      if (rule.validator) {
        return runRuleValidator(value, rule).then((result) => {
          if (result && typeof result === "string") {
            state.validateFailed = true;
            state.validateMessage = result;
          } else if (result === false) {
            state.validateFailed = true;
            state.validateMessage = getRuleMessage(value, rule);
          }
        });
      }
    }), Promise.resolve());
    var resetValidation = () => {
      if (state.validateFailed) {
        state.validateFailed = false;
        state.validateMessage = "";
      }
    };
    var validate = (rules = props10.rules) => new Promise((resolve) => {
      resetValidation();
      if (rules) {
        runRules(rules).then(() => {
          if (state.validateFailed) {
            resolve({
              name: props10.name,
              message: state.validateMessage
            });
          } else {
            resolve();
          }
        });
      } else {
        resolve();
      }
    });
    var validateWithTrigger = (trigger2) => {
      if (form && props10.rules) {
        var defaultTrigger = form.props.validateTrigger === trigger2;
        var rules = props10.rules.filter((rule) => {
          if (rule.trigger) {
            return rule.trigger === trigger2;
          }
          return defaultTrigger;
        });
        if (rules.length) {
          validate(rules);
        }
      }
    };
    var limitValueLength = (value) => {
      var {
        maxlength
      } = props10;
      if (isDef(maxlength) && value.length > maxlength) {
        var modelValue = getModelValue();
        if (modelValue && modelValue.length === +maxlength) {
          return modelValue;
        }
        return value.slice(0, +maxlength);
      }
      return value;
    };
    var updateValue = (value, trigger2 = "onChange") => {
      value = limitValueLength(value);
      if (props10.type === "number" || props10.type === "digit") {
        var isNumber = props10.type === "number";
        value = formatNumber(value, isNumber, isNumber);
      }
      if (props10.formatter && trigger2 === props10.formatTrigger) {
        value = props10.formatter(value);
      }
      if (inputRef.value && inputRef.value.value !== value) {
        inputRef.value.value = value;
      }
      if (value !== props10.modelValue) {
        emit("update:modelValue", value);
      }
    };
    var onInput = (event) => {
      if (!event.target.composing) {
        updateValue(event.target.value);
      }
    };
    var blur = () => {
      var _inputRef$value;
      return (_inputRef$value = inputRef.value) == null ? void 0 : _inputRef$value.blur();
    };
    var focus = () => {
      var _inputRef$value2;
      return (_inputRef$value2 = inputRef.value) == null ? void 0 : _inputRef$value2.focus();
    };
    var onFocus = (event) => {
      state.focused = true;
      emit("focus", event);
      var readonly = getProp("readonly");
      if (readonly) {
        blur();
      }
    };
    var onBlur = (event) => {
      state.focused = false;
      updateValue(getModelValue(), "onBlur");
      emit("blur", event);
      validateWithTrigger("onBlur");
      resetScroll();
    };
    var onClickInput = (event) => emit("click-input", event);
    var onClickLeftIcon = (event) => emit("click-left-icon", event);
    var onClickRightIcon = (event) => emit("click-right-icon", event);
    var onClear = (event) => {
      preventDefault(event);
      emit("update:modelValue", "");
      emit("clear", event);
    };
    var showError = computed(() => {
      if (typeof props10.error === "boolean") {
        return props10.error;
      }
      if (form && form.props.showError && state.validateFailed) {
        return true;
      }
    });
    var labelStyle = computed(() => {
      var labelWidth = getProp("labelWidth");
      if (labelWidth) {
        return {
          width: addUnit(labelWidth)
        };
      }
    });
    var onKeypress = (event) => {
      var ENTER_CODE = 13;
      if (event.keyCode === ENTER_CODE) {
        var submitOnEnter = form && form.props.submitOnEnter;
        if (!submitOnEnter && props10.type !== "textarea") {
          preventDefault(event);
        }
        if (props10.type === "search") {
          blur();
        }
      }
      emit("keypress", event);
    };
    var adjustTextareaSize = () => {
      var input = inputRef.value;
      if (props10.type === "textarea" && props10.autosize && input) {
        resizeTextarea(input, props10.autosize);
      }
    };
    var renderInput = () => {
      var inputAlign = getProp("inputAlign");
      if (slots.input) {
        return createVNode("div", {
          "class": bem16("control", [inputAlign, "custom"]),
          "onClick": onClickInput
        }, [slots.input()]);
      }
      var inputAttrs = {
        ref: inputRef,
        name: props10.name,
        rows: props10.rows !== void 0 ? +props10.rows : void 0,
        class: bem16("control", inputAlign),
        value: props10.modelValue,
        disabled: getProp("disabled"),
        readonly: getProp("readonly"),
        autofocus: props10.autofocus,
        placeholder: props10.placeholder,
        autocomplete: props10.autocomplete,
        onBlur,
        onFocus,
        onInput,
        onClick: onClickInput,
        onChange: endComposing,
        onKeypress,
        onCompositionend: endComposing,
        onCompositionstart: startComposing
      };
      if (props10.type === "textarea") {
        return createVNode("textarea", inputAttrs, null);
      }
      return createVNode("input", mergeProps(mapInputType(props10.type), inputAttrs), null);
    };
    var renderLeftIcon = () => {
      var leftIconSlot = slots["left-icon"];
      if (props10.leftIcon || leftIconSlot) {
        return createVNode("div", {
          "class": bem16("left-icon"),
          "onClick": onClickLeftIcon
        }, [leftIconSlot ? leftIconSlot() : createVNode(Icon, {
          "name": props10.leftIcon,
          "classPrefix": props10.iconPrefix
        }, null)]);
      }
    };
    var renderRightIcon = () => {
      var rightIconSlot = slots["right-icon"];
      if (props10.rightIcon || rightIconSlot) {
        return createVNode("div", {
          "class": bem16("right-icon"),
          "onClick": onClickRightIcon
        }, [rightIconSlot ? rightIconSlot() : createVNode(Icon, {
          "name": props10.rightIcon,
          "classPrefix": props10.iconPrefix
        }, null)]);
      }
    };
    var renderWordLimit = () => {
      if (props10.showWordLimit && props10.maxlength) {
        var count = getModelValue().length;
        return createVNode("div", {
          "class": bem16("word-limit")
        }, [createVNode("span", {
          "class": bem16("word-num")
        }, [count]), createTextVNode("/"), props10.maxlength]);
      }
    };
    var renderMessage = () => {
      if (form && form.props.showErrorMessage === false) {
        return;
      }
      var message = props10.errorMessage || state.validateMessage;
      if (message) {
        var errorMessageAlign = getProp("errorMessageAlign");
        return createVNode("div", {
          "class": bem16("error-message", errorMessageAlign)
        }, [message]);
      }
    };
    var renderLabel = () => {
      var colon = getProp("colon") ? ":" : "";
      if (slots.label) {
        return [slots.label(), colon];
      }
      if (props10.label) {
        return createVNode("span", null, [props10.label + colon]);
      }
    };
    var renderFieldBody = () => [createVNode("div", {
      "class": bem16("body")
    }, [renderInput(), showClear.value && createVNode(Icon, {
      "name": props10.clearIcon,
      "class": bem16("clear"),
      "onTouchstart": onClear
    }, null), renderRightIcon(), slots.button && createVNode("div", {
      "class": bem16("button")
    }, [slots.button()])]), renderWordLimit(), renderMessage()];
    useExpose({
      blur,
      focus,
      validate,
      formValue,
      resetValidation
    });
    provide(FIELD_KEY, {
      childFieldValue,
      resetValidation,
      validateWithTrigger
    });
    watch(() => props10.modelValue, () => {
      updateValue(getModelValue());
      resetValidation();
      validateWithTrigger("onChange");
      nextTick(adjustTextareaSize);
    });
    onMounted(() => {
      updateValue(getModelValue(), props10.formatTrigger);
      nextTick(adjustTextareaSize);
    });
    return () => {
      var disabled = getProp("disabled");
      var labelAlign = getProp("labelAlign");
      var Label = renderLabel();
      var LeftIcon = renderLeftIcon();
      return createVNode(Cell, {
        "size": props10.size,
        "icon": props10.leftIcon,
        "class": bem16({
          error: showError.value,
          disabled,
          ["label-" + labelAlign]: labelAlign,
          "min-height": props10.type === "textarea" && !props10.autosize
        }),
        "center": props10.center,
        "border": props10.border,
        "isLink": props10.isLink,
        "required": props10.required,
        "clickable": props10.clickable,
        "titleStyle": labelStyle.value,
        "valueClass": bem16("value"),
        "titleClass": [bem16("label", labelAlign), props10.labelClass],
        "arrowDirection": props10.arrowDirection
      }, {
        icon: LeftIcon ? () => LeftIcon : null,
        title: Label ? () => Label : null,
        value: renderFieldBody,
        extra: slots.extra
      });
    };
  }
});

// node_modules/vant/es/field/index.js
var Field = withInstall(Field_default);

// node_modules/vant/es/toast/function-call.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/utils/mount-component.js
init_vue_runtime_esm_bundler();
function usePopupState() {
  var state = reactive({
    show: false
  });
  var toggle = (show) => {
    state.show = show;
  };
  var open = (props10) => {
    extend(state, props10);
    nextTick(() => toggle(true));
  };
  var close = () => toggle(false);
  useExpose({
    open,
    close,
    toggle
  });
  return {
    open,
    close,
    state,
    toggle
  };
}
function mountComponent(RootComponent) {
  var app = createApp(RootComponent);
  var root2 = document.createElement("div");
  document.body.appendChild(root2);
  return {
    instance: app.mount(root2),
    unmount() {
      app.unmount();
      document.body.removeChild(root2);
    }
  };
}

// node_modules/vant/es/toast/Toast.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/toast/lock-click.js
var lockCount = 0;
function lockClick(lock) {
  if (lock) {
    if (!lockCount) {
      document.body.classList.add("van-toast--unclickable");
    }
    lockCount++;
  } else if (lockCount) {
    lockCount--;
    if (!lockCount) {
      document.body.classList.remove("van-toast--unclickable");
    }
  }
}

// node_modules/vant/es/toast/Toast.js
var [name17, bem17] = createNamespace("toast");
var Toast_default = defineComponent({
  name: name17,
  props: {
    icon: String,
    show: Boolean,
    overlay: Boolean,
    message: [Number, String],
    iconSize: [Number, String],
    className: unknownProp,
    iconPrefix: String,
    loadingType: String,
    forbidClick: Boolean,
    overlayClass: unknownProp,
    overlayStyle: Object,
    closeOnClick: Boolean,
    closeOnClickOverlay: Boolean,
    type: {
      type: String,
      default: "text"
    },
    duration: {
      type: Number,
      default: 2e3
    },
    position: {
      type: String,
      default: "middle"
    },
    transition: {
      type: String,
      default: "van-fade"
    }
  },
  emits: ["update:show"],
  setup(props10, {
    emit
  }) {
    var timer2;
    var clickable = false;
    var toggleClickable = () => {
      var newValue = props10.show && props10.forbidClick;
      if (clickable !== newValue) {
        clickable = newValue;
        lockClick(clickable);
      }
    };
    var updateShow = (show) => emit("update:show", show);
    var onClick = () => {
      if (props10.closeOnClick) {
        updateShow(false);
      }
    };
    var clearTimer = () => {
      clearTimeout(timer2);
    };
    var renderIcon = () => {
      var {
        icon,
        type,
        iconSize,
        iconPrefix,
        loadingType
      } = props10;
      var hasIcon = icon || type === "success" || type === "fail";
      if (hasIcon) {
        return createVNode(Icon, {
          "name": icon || type,
          "size": iconSize,
          "class": bem17("icon"),
          "classPrefix": iconPrefix
        }, null);
      }
      if (type === "loading") {
        return createVNode(Loading, {
          "class": bem17("loading"),
          "size": iconSize,
          "type": loadingType
        }, null);
      }
    };
    var renderMessage = () => {
      var {
        type,
        message
      } = props10;
      if (isDef(message) && message !== "") {
        return type === "html" ? createVNode("div", {
          "class": bem17("text"),
          "innerHTML": String(message)
        }, null) : createVNode("div", {
          "class": bem17("text")
        }, [message]);
      }
    };
    watch(() => [props10.show, props10.forbidClick], toggleClickable);
    watch(() => [props10.show, props10.type, props10.message, props10.duration], () => {
      clearTimer();
      if (props10.show && props10.duration > 0) {
        timer2 = setTimeout(() => {
          updateShow(false);
        }, props10.duration);
      }
    });
    onMounted(toggleClickable);
    onUnmounted(toggleClickable);
    return () => createVNode(Popup, mergeProps({
      "show": props10.show,
      "class": [bem17([props10.position, {
        [props10.type]: !props10.icon
      }]), props10.className],
      "overlay": props10.overlay,
      "lockScroll": false,
      "transition": props10.transition,
      "overlayClass": props10.overlayClass,
      "overlayStyle": props10.overlayStyle,
      "closeOnClickOverlay": props10.closeOnClickOverlay,
      "onClick": onClick,
      "onClosed": clearTimer
    }, {
      "onUpdate:show": updateShow
    }), {
      default: () => [renderIcon(), renderMessage()]
    });
  }
});

// node_modules/vant/es/toast/function-call.js
var defaultOptions = {
  icon: "",
  type: "text",
  message: "",
  className: "",
  overlay: false,
  onClose: void 0,
  onOpened: void 0,
  duration: 2e3,
  teleport: "body",
  iconSize: void 0,
  iconPrefix: void 0,
  position: "middle",
  transition: "van-fade",
  forbidClick: false,
  loadingType: void 0,
  overlayClass: "",
  overlayStyle: void 0,
  closeOnClick: false,
  closeOnClickOverlay: false
};
var queue = [];
var allowMultiple = false;
var currentOptions = extend({}, defaultOptions);
var defaultOptionsMap = {};
function parseOptions(message) {
  if (isObject(message)) {
    return message;
  }
  return {
    message
  };
}
function createInstance() {
  var {
    instance: instance4,
    unmount
  } = mountComponent({
    setup() {
      var message = ref("");
      var {
        open,
        state,
        close,
        toggle
      } = usePopupState();
      var onClosed = () => {
        if (allowMultiple) {
          queue = queue.filter((item) => item !== instance4);
          unmount();
        }
      };
      var render = () => {
        var attrs = {
          onClosed,
          "onUpdate:show": toggle
        };
        if (message.value) {
          attrs.message = message.value;
        }
        return createVNode(Toast_default, mergeProps(state, attrs), null);
      };
      getCurrentInstance().render = render;
      return {
        open,
        clear: close,
        message
      };
    }
  });
  return instance4;
}
function getInstance() {
  if (!queue.length || allowMultiple) {
    var instance4 = createInstance();
    queue.push(instance4);
  }
  return queue[queue.length - 1];
}
function Toast(options = {}) {
  if (!inBrowser) {
    return {};
  }
  var toast = getInstance();
  var parsedOptions = parseOptions(options);
  toast.open(extend({}, currentOptions, defaultOptionsMap[parsedOptions.type || currentOptions.type], parsedOptions));
  return toast;
}
var createMethod = (type) => (options) => Toast(extend({
  type
}, parseOptions(options)));
Toast.loading = createMethod("loading");
Toast.success = createMethod("success");
Toast.fail = createMethod("fail");
Toast.clear = (all) => {
  if (queue.length) {
    if (all) {
      queue.forEach((toast) => {
        toast.clear();
      });
      queue = [];
    } else if (!allowMultiple) {
      queue[0].clear();
    } else {
      queue.shift().clear();
    }
  }
};
function setDefaultOptions(type, options) {
  if (typeof type === "string") {
    defaultOptionsMap[type] = options;
  } else {
    extend(currentOptions, type);
  }
}
Toast.setDefaultOptions = setDefaultOptions;
Toast.resetDefaultOptions = (type) => {
  if (typeof type === "string") {
    defaultOptionsMap[type] = null;
  } else {
    currentOptions = extend({}, defaultOptions);
    defaultOptionsMap = {};
  }
};
Toast.allowMultiple = (value = true) => {
  allowMultiple = value;
};
Toast.install = (app) => {
  app.use(withInstall(Toast_default));
  app.config.globalProperties.$toast = Toast;
};

// node_modules/vant/es/dialog/function-call.js
init_vue_runtime_esm_bundler();

// node_modules/vant/es/dialog/Dialog.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/utils/interceptor.js
function callInterceptor(options) {
  var {
    interceptor,
    args,
    done,
    canceled
  } = options;
  if (interceptor) {
    var returnVal = interceptor.apply(null, args || []);
    if (isPromise(returnVal)) {
      returnVal.then((value) => {
        if (value) {
          done();
        } else if (canceled) {
          canceled();
        }
      }).catch(noop);
    } else if (returnVal) {
      done();
    } else if (canceled) {
      canceled();
    }
  } else {
    done();
  }
}

// node_modules/vant/es/dialog/Dialog.js
var [name18, bem18, t2] = createNamespace("dialog");
var popupKeys = [...popupSharedPropKeys, "transition", "closeOnPopstate"];
var Dialog_default = defineComponent({
  name: name18,
  props: extend({}, popupSharedProps, {
    title: String,
    theme: String,
    width: [Number, String],
    message: [String, Function],
    callback: Function,
    allowHtml: Boolean,
    className: unknownProp,
    beforeClose: Function,
    messageAlign: String,
    closeOnPopstate: truthProp,
    showCancelButton: Boolean,
    cancelButtonText: String,
    cancelButtonColor: String,
    confirmButtonText: String,
    confirmButtonColor: String,
    showConfirmButton: truthProp,
    closeOnClickOverlay: Boolean,
    transition: {
      type: String,
      default: "van-dialog-bounce"
    }
  }),
  emits: ["confirm", "cancel", "update:show"],
  setup(props10, {
    emit,
    slots
  }) {
    var loading = reactive({
      confirm: false,
      cancel: false
    });
    var updateShow = (value) => emit("update:show", value);
    var close = (action) => {
      updateShow(false);
      if (props10.callback) {
        props10.callback(action);
      }
    };
    var getActionHandler = (action) => () => {
      if (!props10.show) {
        return;
      }
      emit(action);
      if (props10.beforeClose) {
        loading[action] = true;
        callInterceptor({
          interceptor: props10.beforeClose,
          args: [action],
          done() {
            close(action);
            loading[action] = false;
          },
          canceled() {
            loading[action] = false;
          }
        });
      } else {
        close(action);
      }
    };
    var onCancel = getActionHandler("cancel");
    var onConfirm = getActionHandler("confirm");
    var renderTitle = () => {
      var title = slots.title ? slots.title() : props10.title;
      if (title) {
        return createVNode("div", {
          "class": bem18("header", {
            isolated: !props10.message && !slots.default
          })
        }, [title]);
      }
    };
    var renderMessage = (hasTitle) => {
      var {
        message,
        allowHtml,
        messageAlign
      } = props10;
      var classNames = bem18("message", {
        "has-title": hasTitle,
        [messageAlign]: messageAlign
      });
      var content = isFunction(message) ? message() : message;
      if (allowHtml && typeof content === "string") {
        return createVNode("div", {
          "class": classNames,
          "innerHTML": content
        }, null);
      }
      return createVNode("div", {
        "class": classNames
      }, [content]);
    };
    var renderContent = () => {
      if (slots.default) {
        return createVNode("div", {
          "class": bem18("content")
        }, [slots.default()]);
      }
      var {
        title,
        message,
        allowHtml
      } = props10;
      if (message) {
        var hasTitle = !!(title || slots.title);
        return createVNode("div", {
          "key": allowHtml ? 1 : 0,
          "class": bem18("content", {
            isolated: !hasTitle
          })
        }, [renderMessage(hasTitle)]);
      }
    };
    var renderButtons = () => createVNode("div", {
      "class": [BORDER_TOP, bem18("footer")]
    }, [props10.showCancelButton && createVNode(Button, {
      "size": "large",
      "text": props10.cancelButtonText || t2("cancel"),
      "class": bem18("cancel"),
      "style": {
        color: props10.cancelButtonColor
      },
      "loading": loading.cancel,
      "onClick": onCancel
    }, null), props10.showConfirmButton && createVNode(Button, {
      "size": "large",
      "text": props10.confirmButtonText || t2("confirm"),
      "class": [bem18("confirm"), {
        [BORDER_LEFT]: props10.showCancelButton
      }],
      "style": {
        color: props10.confirmButtonColor
      },
      "loading": loading.confirm,
      "onClick": onConfirm
    }, null)]);
    var renderRoundButtons = () => createVNode(ActionBar, {
      "class": bem18("footer")
    }, {
      default: () => [props10.showCancelButton && createVNode(ActionBarButton, {
        "type": "warning",
        "text": props10.cancelButtonText || t2("cancel"),
        "class": bem18("cancel"),
        "color": props10.cancelButtonColor,
        "loading": loading.cancel,
        "onClick": onCancel
      }, null), props10.showConfirmButton && createVNode(ActionBarButton, {
        "type": "danger",
        "text": props10.confirmButtonText || t2("confirm"),
        "class": bem18("confirm"),
        "color": props10.confirmButtonColor,
        "loading": loading.confirm,
        "onClick": onConfirm
      }, null)]
    });
    var renderFooter = () => {
      if (slots.footer) {
        return slots.footer();
      }
      return props10.theme === "round-button" ? renderRoundButtons() : renderButtons();
    };
    return () => {
      var {
        width,
        title,
        theme,
        message,
        className
      } = props10;
      return createVNode(Popup, mergeProps({
        "role": "dialog",
        "class": [bem18([theme]), className],
        "style": {
          width: addUnit(width)
        },
        "aria-labelledby": title || message
      }, pick(props10, popupKeys), {
        "onUpdate:show": updateShow
      }), {
        default: () => [renderTitle(), renderContent(), renderFooter()]
      });
    };
  }
});

// node_modules/vant/es/dialog/function-call.js
var instance;
function initInstance() {
  var Wrapper = {
    setup() {
      var {
        state,
        toggle
      } = usePopupState();
      return () => createVNode(Dialog_default, mergeProps(state, {
        "onUpdate:show": toggle
      }), null);
    }
  };
  ({
    instance
  } = mountComponent(Wrapper));
}
function Dialog(options) {
  if (!inBrowser) {
    return Promise.resolve();
  }
  return new Promise((resolve, reject) => {
    if (!instance) {
      initInstance();
    }
    instance.open(extend({}, Dialog.currentOptions, options, {
      callback: (action) => {
        (action === "confirm" ? resolve : reject)(action);
      }
    }));
  });
}
Dialog.defaultOptions = {
  title: "",
  width: "",
  theme: null,
  message: "",
  overlay: true,
  callback: null,
  teleport: "body",
  className: "",
  allowHtml: false,
  lockScroll: true,
  transition: "van-dialog-bounce",
  beforeClose: null,
  overlayClass: "",
  overlayStyle: void 0,
  messageAlign: "",
  cancelButtonText: "",
  cancelButtonColor: null,
  confirmButtonText: "",
  confirmButtonColor: null,
  showConfirmButton: true,
  showCancelButton: false,
  closeOnPopstate: true,
  closeOnClickOverlay: false
};
Dialog.currentOptions = extend({}, Dialog.defaultOptions);
Dialog.alert = Dialog;
Dialog.confirm = (options) => Dialog(extend({
  showCancelButton: true
}, options));
Dialog.close = () => {
  if (instance) {
    instance.toggle(false);
  }
};
Dialog.setDefaultOptions = (options) => {
  extend(Dialog.currentOptions, options);
};
Dialog.resetDefaultOptions = () => {
  Dialog.currentOptions = extend({}, Dialog.defaultOptions);
};
Dialog.install = (app) => {
  app.use(withInstall(Dialog_default));
  app.config.globalProperties.$dialog = Dialog;
};
Dialog.Component = withInstall(Dialog_default);

// node_modules/vant/es/switch/Switch.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name19, bem19] = createNamespace("switch");
var Switch_default = defineComponent({
  name: name19,
  props: {
    size: [Number, String],
    loading: Boolean,
    disabled: Boolean,
    modelValue: unknownProp,
    activeColor: String,
    inactiveColor: String,
    activeValue: {
      type: unknownProp,
      default: true
    },
    inactiveValue: {
      type: unknownProp,
      default: false
    }
  },
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit
  }) {
    var isChecked = () => props10.modelValue === props10.activeValue;
    var onClick = () => {
      if (!props10.disabled && !props10.loading) {
        var newValue = isChecked() ? props10.inactiveValue : props10.activeValue;
        emit("update:modelValue", newValue);
        emit("change", newValue);
      }
    };
    var renderLoading = () => {
      if (props10.loading) {
        var color = isChecked() ? props10.activeColor : props10.inactiveColor;
        return createVNode(Loading, {
          "class": bem19("loading"),
          "color": color
        }, null);
      }
    };
    useLinkField(() => props10.modelValue);
    return () => {
      var {
        size,
        loading,
        disabled,
        activeColor,
        inactiveColor
      } = props10;
      var checked = isChecked();
      var style2 = {
        fontSize: addUnit(size),
        backgroundColor: checked ? activeColor : inactiveColor
      };
      return createVNode("div", {
        "role": "switch",
        "class": bem19({
          on: checked,
          loading,
          disabled
        }),
        "style": style2,
        "aria-checked": checked,
        "onClick": onClick
      }, [createVNode("div", {
        "class": bem19("node")
      }, [renderLoading()])]);
    };
  }
});

// node_modules/vant/es/switch/index.js
var Switch = withInstall(Switch_default);

// node_modules/vant/es/address-edit/AddressEditDetail.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name20, bem20, t3] = createNamespace("address-edit-detail");
var android = isAndroid();
var AddressEditDetail_default = defineComponent({
  name: name20,
  props: {
    show: Boolean,
    value: String,
    focused: Boolean,
    detailRows: [Number, String],
    searchResult: Array,
    errorMessage: String,
    detailMaxlength: [Number, String],
    showSearchResult: Boolean
  },
  emits: ["blur", "focus", "input", "select-search"],
  setup(props10, {
    emit
  }) {
    var field = ref();
    var showSearchResult = () => props10.focused && props10.searchResult && props10.showSearchResult;
    var onSelect = (express) => {
      emit("select-search", express);
      emit("input", ((express.address || "") + " " + (express.name || "")).trim());
    };
    var onFinish = () => {
      field.value.blur();
    };
    var renderFinish = () => {
      if (props10.value && props10.focused && android) {
        return createVNode("div", {
          "class": bem20("finish"),
          "onClick": onFinish
        }, [t3("complete")]);
      }
    };
    var renderSearchTitle = (express) => {
      if (express.name) {
        var text = express.name.replace(props10.value, "<span class=" + bem20("keyword") + ">" + props10.value + "</span>");
        return createVNode("div", {
          "innerHTML": text
        }, null);
      }
    };
    var renderSearchResult = () => {
      if (!showSearchResult()) {
        return;
      }
      var {
        searchResult
      } = props10;
      return searchResult.map((express) => createVNode(Cell, {
        "clickable": true,
        "key": express.name + express.address,
        "icon": "location-o",
        "label": express.address,
        "class": bem20("search-item"),
        "border": false,
        "onClick": () => onSelect(express)
      }, {
        title: () => renderSearchTitle(express)
      }));
    };
    var onBlur = (event) => emit("blur", event);
    var onFocus = (event) => emit("focus", event);
    var onInput = (value) => emit("input", value);
    return () => {
      if (props10.show) {
        return createVNode(Fragment, null, [createVNode(Field, mergeProps({
          "autosize": true,
          "ref": field,
          "class": bem20(),
          "rows": props10.detailRows,
          "type": "textarea",
          "label": t3("label"),
          "border": !showSearchResult(),
          "clearable": !android,
          "maxlength": props10.detailMaxlength,
          "modelValue": props10.value,
          "placeholder": t3("placeholder"),
          "errorMessage": props10.errorMessage,
          "onBlur": onBlur,
          "onFocus": onFocus
        }, {
          "onUpdate:modelValue": onInput
        }), {
          icon: renderFinish
        }), renderSearchResult()]);
      }
    };
  }
});

// node_modules/vant/es/address-edit/AddressEdit.js
var [name21, bem21, t4] = createNamespace("address-edit");
var defaultData = {
  name: "",
  tel: "",
  city: "",
  county: "",
  country: "",
  province: "",
  areaCode: "",
  isDefault: false,
  postalCode: "",
  addressDetail: ""
};
function isPostal(value) {
  return /^\d{6}$/.test(value);
}
var AddressEdit_default = defineComponent({
  name: name21,
  props: {
    areaList: Object,
    isSaving: Boolean,
    isDeleting: Boolean,
    validator: Function,
    showArea: truthProp,
    showDetail: truthProp,
    showDelete: Boolean,
    showPostal: Boolean,
    disableArea: Boolean,
    searchResult: Array,
    telMaxlength: [Number, String],
    showSetDefault: Boolean,
    saveButtonText: String,
    areaPlaceholder: String,
    deleteButtonText: String,
    showSearchResult: Boolean,
    detailRows: {
      type: [Number, String],
      default: 1
    },
    detailMaxlength: {
      type: [Number, String],
      default: 200
    },
    addressInfo: {
      type: Object,
      default: () => extend({}, defaultData)
    },
    telValidator: {
      type: Function,
      default: isMobile
    },
    postalValidator: {
      type: Function,
      default: isPostal
    },
    areaColumnsPlaceholder: {
      type: Array,
      default: () => []
    }
  },
  emits: ["save", "focus", "delete", "click-area", "change-area", "change-detail", "cancel-delete", "select-search", "change-default"],
  setup(props10, {
    emit,
    slots
  }) {
    var areaRef = ref();
    var state = reactive({
      data: {},
      showAreaPopup: false,
      detailFocused: false,
      errorInfo: {
        tel: "",
        name: "",
        areaCode: "",
        postalCode: "",
        addressDetail: ""
      }
    });
    var areaListLoaded = computed(() => isObject(props10.areaList) && Object.keys(props10.areaList).length);
    var areaText = computed(() => {
      var {
        country,
        province,
        city,
        county,
        areaCode
      } = state.data;
      if (areaCode) {
        var arr = [country, province, city, county];
        if (province && province === city) {
          arr.splice(1, 1);
        }
        return arr.filter(Boolean).join("/");
      }
      return "";
    });
    var hideBottomFields = computed(() => {
      var _props$searchResult;
      return ((_props$searchResult = props10.searchResult) == null ? void 0 : _props$searchResult.length) && state.detailFocused;
    });
    var assignAreaValues = () => {
      if (areaRef.value) {
        var detail = areaRef.value.getArea();
        detail.areaCode = detail.code;
        delete detail.code;
        extend(state.data, detail);
      }
    };
    var onFocus = (key) => {
      state.errorInfo[key] = "";
      state.detailFocused = key === "addressDetail";
      emit("focus", key);
    };
    var getErrorMessage = (key) => {
      var value = String(state.data[key] || "").trim();
      if (props10.validator) {
        var message = props10.validator(key, value);
        if (message) {
          return message;
        }
      }
      switch (key) {
        case "name":
          return value ? "" : t4("nameEmpty");
        case "tel":
          return props10.telValidator(value) ? "" : t4("telInvalid");
        case "areaCode":
          return value ? "" : t4("areaEmpty");
        case "addressDetail":
          return value ? "" : t4("addressEmpty");
        case "postalCode":
          return value && !props10.postalValidator(value) ? t4("postalEmpty") : "";
      }
    };
    var onSave = () => {
      var items = ["name", "tel"];
      if (props10.showArea) {
        items.push("areaCode");
      }
      if (props10.showDetail) {
        items.push("addressDetail");
      }
      if (props10.showPostal) {
        items.push("postalCode");
      }
      var isValid = items.every((item) => {
        var msg = getErrorMessage(item);
        if (msg) {
          state.errorInfo[item] = msg;
        }
        return !msg;
      });
      if (isValid && !props10.isSaving) {
        emit("save", state.data);
      }
    };
    var onChangeDetail = (val) => {
      state.data.addressDetail = val;
      emit("change-detail", val);
    };
    var onAreaConfirm = (values) => {
      values = values.filter(Boolean);
      if (values.some((value) => !value.code)) {
        Toast(t4("areaEmpty"));
        return;
      }
      state.showAreaPopup = false;
      assignAreaValues();
      emit("change-area", values);
    };
    var onDelete = () => {
      Dialog.confirm({
        title: t4("confirmDelete")
      }).then(() => emit("delete", state.data)).catch(() => emit("cancel-delete", state.data));
    };
    var getArea = () => areaRef.value ? areaRef.value.getValues() : [];
    var setAreaCode = (code) => {
      state.data.areaCode = code || "";
      if (code) {
        nextTick(assignAreaValues);
      }
    };
    var onDetailBlur = () => {
      setTimeout(() => {
        state.detailFocused = false;
      });
    };
    var setAddressDetail = (value) => {
      state.data.addressDetail = value;
    };
    var renderSetDefaultCell = () => {
      if (props10.showSetDefault) {
        var _slots = {
          "right-icon": () => createVNode(Switch, {
            "modelValue": state.data.isDefault,
            "onUpdate:modelValue": ($event) => state.data.isDefault = $event,
            "size": "24",
            "onChange": (event) => emit("change-default", event)
          }, null)
        };
        return withDirectives(createVNode(Cell, {
          "center": true,
          "title": t4("defaultAddress"),
          "class": bem21("default")
        }, _slots), [[vShow, !hideBottomFields.value]]);
      }
      return null;
    };
    useExpose({
      getArea,
      setAreaCode,
      setAddressDetail
    });
    watch(() => props10.areaList, () => setAreaCode(state.data.areaCode));
    watch(() => props10.addressInfo, (value) => {
      state.data = extend({}, defaultData, value);
      setAreaCode(value.areaCode);
    }, {
      deep: true,
      immediate: true
    });
    return () => {
      var {
        data,
        errorInfo
      } = state;
      var {
        disableArea
      } = props10;
      return createVNode("div", {
        "class": bem21()
      }, [createVNode("div", {
        "class": bem21("fields")
      }, [createVNode(Field, {
        "modelValue": data.name,
        "onUpdate:modelValue": ($event) => data.name = $event,
        "clearable": true,
        "label": t4("name"),
        "placeholder": t4("namePlaceholder"),
        "errorMessage": errorInfo.name,
        "onFocus": () => onFocus("name")
      }, null), createVNode(Field, {
        "modelValue": data.tel,
        "onUpdate:modelValue": ($event) => data.tel = $event,
        "clearable": true,
        "type": "tel",
        "label": t4("tel"),
        "maxlength": props10.telMaxlength,
        "placeholder": t4("telPlaceholder"),
        "errorMessage": errorInfo.tel,
        "onFocus": () => onFocus("tel")
      }, null), withDirectives(createVNode(Field, {
        "readonly": true,
        "label": t4("area"),
        "clickable": !disableArea,
        "rightIcon": !disableArea ? "arrow" : void 0,
        "modelValue": areaText.value,
        "placeholder": props10.areaPlaceholder || t4("areaPlaceholder"),
        "errorMessage": errorInfo.areaCode,
        "onFocus": () => onFocus("areaCode"),
        "onClick": () => {
          emit("click-area");
          state.showAreaPopup = !disableArea;
        }
      }, null), [[vShow, props10.showArea]]), createVNode(AddressEditDetail_default, {
        "show": props10.showDetail,
        "value": data.addressDetail,
        "focused": state.detailFocused,
        "detailRows": props10.detailRows,
        "errorMessage": errorInfo.addressDetail,
        "searchResult": props10.searchResult,
        "detailMaxlength": props10.detailMaxlength,
        "showSearchResult": props10.showSearchResult,
        "onBlur": onDetailBlur,
        "onFocus": () => onFocus("addressDetail"),
        "onInput": onChangeDetail,
        "onSelectSearch": (event) => emit("select-search", event)
      }, null), props10.showPostal && withDirectives(createVNode(Field, {
        "modelValue": data.postalCode,
        "onUpdate:modelValue": ($event) => data.postalCode = $event,
        "type": "tel",
        "label": t4("postal"),
        "maxlength": "6",
        "placeholder": t4("postal"),
        "errorMessage": errorInfo.postalCode,
        "onFocus": () => onFocus("postalCode")
      }, null), [[vShow, !hideBottomFields.value]]), slots.default == null ? void 0 : slots.default()]), renderSetDefaultCell(), withDirectives(createVNode("div", {
        "class": bem21("buttons")
      }, [createVNode(Button, {
        "block": true,
        "round": true,
        "type": "danger",
        "text": props10.saveButtonText || t4("save"),
        "loading": props10.isSaving,
        "onClick": onSave
      }, null), props10.showDelete && createVNode(Button, {
        "block": true,
        "round": true,
        "loading": props10.isDeleting,
        "text": props10.deleteButtonText || t4("delete"),
        "onClick": onDelete
      }, null)]), [[vShow, !hideBottomFields.value]]), createVNode(Popup, {
        "show": state.showAreaPopup,
        "onUpdate:show": ($event) => state.showAreaPopup = $event,
        "round": true,
        "teleport": "body",
        "position": "bottom",
        "lazyRender": false
      }, {
        default: () => [createVNode(Area, {
          "ref": areaRef,
          "value": data.areaCode,
          "loading": !areaListLoaded.value,
          "areaList": props10.areaList,
          "columnsPlaceholder": props10.areaColumnsPlaceholder,
          "onConfirm": onAreaConfirm,
          "onCancel": () => {
            state.showAreaPopup = false;
          }
        }, null)]
      })]);
    };
  }
});

// node_modules/vant/es/address-edit/index.js
var AddressEdit = withInstall(AddressEdit_default);

// node_modules/vant/es/address-list/AddressList.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/radio-group/RadioGroup.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name22, bem22] = createNamespace("radio-group");
var RADIO_KEY = Symbol(name22);
var props = {
  disabled: Boolean,
  iconSize: [Number, String],
  direction: String,
  modelValue: unknownProp,
  checkedColor: String
};
var RadioGroup_default = defineComponent({
  name: name22,
  props,
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      linkChildren
    } = useChildren(RADIO_KEY);
    var updateValue = (value) => emit("update:modelValue", value);
    watch(() => props10.modelValue, (value) => emit("change", value));
    linkChildren({
      props: props10,
      updateValue
    });
    useLinkField(() => props10.modelValue);
    return () => createVNode("div", {
      "class": bem22([props10.direction]),
      "role": "radiogroup"
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/radio-group/index.js
var RadioGroup = withInstall(RadioGroup_default);

// node_modules/vant/es/address-list/AddressListItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/tag/Tag.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name23, bem23] = createNamespace("tag");
var Tag_default = defineComponent({
  name: name23,
  props: {
    size: String,
    mark: Boolean,
    show: truthProp,
    color: String,
    plain: Boolean,
    round: Boolean,
    textColor: String,
    closeable: Boolean,
    type: {
      type: String,
      default: "default"
    }
  },
  emits: ["close"],
  setup(props10, {
    slots,
    emit
  }) {
    var onClose = (event) => {
      event.stopPropagation();
      emit("close", event);
    };
    var getStyle = () => {
      if (props10.plain) {
        return {
          color: props10.textColor || props10.color,
          borderColor: props10.color
        };
      }
      return {
        color: props10.textColor,
        background: props10.color
      };
    };
    var renderTag = () => {
      var {
        type,
        mark,
        plain,
        round: round2,
        size,
        closeable
      } = props10;
      var classes = {
        mark,
        plain,
        round: round2
      };
      if (size) {
        classes[size] = size;
      }
      var CloseIcon = closeable && createVNode(Icon, {
        "name": "cross",
        "class": bem23("close"),
        "onClick": onClose
      }, null);
      return createVNode("span", {
        "style": getStyle(),
        "class": bem23([classes, type])
      }, [slots.default == null ? void 0 : slots.default(), CloseIcon]);
    };
    return () => createVNode(Transition, {
      "name": props10.closeable ? "van-fade" : void 0
    }, {
      default: () => [props10.show ? renderTag() : null]
    });
  }
});

// node_modules/vant/es/tag/index.js
var Tag = withInstall(Tag_default);

// node_modules/vant/es/radio/Radio.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/checkbox/Checker.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var checkerProps = {
  name: unknownProp,
  disabled: Boolean,
  iconSize: [Number, String],
  modelValue: unknownProp,
  checkedColor: String,
  labelPosition: String,
  labelDisabled: Boolean,
  shape: {
    type: String,
    default: "round"
  }
};
var Checker_default = defineComponent({
  props: extend({}, checkerProps, {
    role: String,
    parent: Object,
    checked: Boolean,
    bindGroup: truthProp,
    bem: {
      type: Function,
      required: true
    }
  }),
  emits: ["click", "toggle"],
  setup(props10, {
    emit,
    slots
  }) {
    var iconRef = ref();
    var getParentProp = (name96) => {
      if (props10.parent && props10.bindGroup) {
        return props10.parent.props[name96];
      }
    };
    var disabled = computed(() => getParentProp("disabled") || props10.disabled);
    var direction = computed(() => getParentProp("direction"));
    var iconStyle = computed(() => {
      var checkedColor = props10.checkedColor || getParentProp("checkedColor");
      if (checkedColor && props10.checked && !disabled.value) {
        return {
          borderColor: checkedColor,
          backgroundColor: checkedColor
        };
      }
    });
    var onClick = (event) => {
      var {
        target
      } = event;
      var icon = iconRef.value;
      var iconClicked = icon === target || icon.contains(target);
      if (!disabled.value && (iconClicked || !props10.labelDisabled)) {
        emit("toggle");
      }
      emit("click", event);
    };
    var renderIcon = () => {
      var {
        bem: bem92,
        shape,
        checked
      } = props10;
      var iconSize = props10.iconSize || getParentProp("iconSize");
      return createVNode("div", {
        "ref": iconRef,
        "class": bem92("icon", [shape, {
          disabled: disabled.value,
          checked
        }]),
        "style": {
          fontSize: addUnit(iconSize)
        }
      }, [slots.icon ? slots.icon({
        checked,
        disabled: disabled.value
      }) : createVNode(Icon, {
        "name": "success",
        "style": iconStyle.value
      }, null)]);
    };
    var renderLabel = () => {
      if (slots.default) {
        return createVNode("span", {
          "class": props10.bem("label", [props10.labelPosition, {
            disabled: disabled.value
          }])
        }, [slots.default()]);
      }
    };
    return () => {
      var nodes = [renderIcon()];
      if (props10.labelPosition === "left") {
        nodes.unshift(renderLabel());
      } else {
        nodes.push(renderLabel());
      }
      return createVNode("div", {
        "role": props10.role,
        "class": props10.bem([{
          disabled: disabled.value,
          "label-disabled": props10.labelDisabled
        }, direction.value]),
        "tabindex": disabled.value ? -1 : 0,
        "aria-checked": props10.checked,
        "onClick": onClick
      }, [nodes]);
    };
  }
});

// node_modules/vant/es/radio/Radio.js
var [name24, bem24] = createNamespace("radio");
var Radio_default = defineComponent({
  name: name24,
  props: checkerProps,
  emits: ["update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      parent
    } = useParent(RADIO_KEY);
    var checked = () => {
      var value = parent ? parent.props.modelValue : props10.modelValue;
      return value === props10.name;
    };
    var toggle = () => {
      if (parent) {
        parent.updateValue(props10.name);
      } else {
        emit("update:modelValue", props10.name);
      }
    };
    return () => createVNode(Checker_default, mergeProps({
      "bem": bem24,
      "role": "radio",
      "parent": parent,
      "checked": checked(),
      "onToggle": toggle
    }, props10), pick(slots, ["default", "icon"]));
  }
});

// node_modules/vant/es/radio/index.js
var Radio = withInstall(Radio_default);

// node_modules/vant/es/address-list/AddressListItem.js
var [name25, bem25] = createNamespace("address-item");
var AddressListItem_default = defineComponent({
  name: name25,
  props: {
    disabled: Boolean,
    switchable: Boolean,
    defaultTagText: String,
    address: {
      type: Object,
      required: true
    }
  },
  emits: ["edit", "click", "select"],
  setup(props10, {
    slots,
    emit
  }) {
    var onClick = () => {
      if (props10.switchable) {
        emit("select");
      }
      emit("click");
    };
    var renderRightIcon = () => createVNode(Icon, {
      "name": "edit",
      "class": bem25("edit"),
      "onClick": (event) => {
        event.stopPropagation();
        emit("edit");
        emit("click");
      }
    }, null);
    var renderTag = () => {
      if (slots.tag) {
        return slots.tag(props10.address);
      }
      if (props10.address.isDefault && props10.defaultTagText) {
        return createVNode(Tag, {
          "type": "danger",
          "round": true,
          "class": bem25("tag")
        }, {
          default: () => [props10.defaultTagText]
        });
      }
    };
    var renderContent = () => {
      var {
        address,
        disabled,
        switchable
      } = props10;
      var Info = [createVNode("div", {
        "class": bem25("name")
      }, [address.name + " " + address.tel, renderTag()]), createVNode("div", {
        "class": bem25("address")
      }, [address.address])];
      if (switchable && !disabled) {
        return createVNode(Radio, {
          "name": address.id,
          "iconSize": 18
        }, {
          default: () => [Info]
        });
      }
      return Info;
    };
    return () => {
      var {
        disabled
      } = props10;
      return createVNode("div", {
        "class": bem25({
          disabled
        }),
        "onClick": onClick
      }, [createVNode(Cell, {
        "border": false,
        "valueClass": bem25("value")
      }, {
        value: renderContent,
        "right-icon": renderRightIcon
      }), slots.bottom == null ? void 0 : slots.bottom(extend({}, props10.address, {
        disabled
      }))]);
    };
  }
});

// node_modules/vant/es/address-list/AddressList.js
var [name26, bem26, t5] = createNamespace("address-list");
var AddressList_default = defineComponent({
  name: name26,
  props: {
    modelValue: [Number, String],
    switchable: truthProp,
    disabledText: String,
    addButtonText: String,
    defaultTagText: String,
    list: {
      type: Array,
      default: () => []
    },
    disabledList: {
      type: Array,
      default: () => []
    }
  },
  emits: ["add", "edit", "select", "click-item", "edit-disabled", "select-disabled", "update:modelValue"],
  setup(props10, {
    slots,
    emit
  }) {
    var renderItem = (item, index, disabled) => {
      var onEdit = () => {
        var name96 = disabled ? "edit-disabled" : "edit";
        emit(name96, item, index);
      };
      var onClick = () => emit("click-item", item, index);
      var onSelect = () => {
        var name96 = disabled ? "select-disabled" : "select";
        emit(name96, item, index);
        if (!disabled) {
          emit("update:modelValue", item.id);
        }
      };
      return createVNode(AddressListItem_default, {
        "key": item.id,
        "address": item,
        "disabled": disabled,
        "switchable": props10.switchable,
        "defaultTagText": props10.defaultTagText,
        "onEdit": onEdit,
        "onClick": onClick,
        "onSelect": onSelect
      }, {
        bottom: slots["item-bottom"],
        tag: slots.tag
      });
    };
    var renderList = (list, disabled) => {
      if (list) {
        return list.map((item, index) => renderItem(item, index, disabled));
      }
    };
    var renderBottom = () => createVNode("div", {
      "class": bem26("bottom")
    }, [createVNode(Button, {
      "round": true,
      "block": true,
      "type": "danger",
      "text": props10.addButtonText || t5("add"),
      "class": bem26("add"),
      "onClick": () => emit("add")
    }, null)]);
    return () => {
      var List2 = renderList(props10.list);
      var DisabledList = renderList(props10.disabledList, true);
      var DisabledText = props10.disabledText && createVNode("div", {
        "class": bem26("disabled-text")
      }, [props10.disabledText]);
      return createVNode("div", {
        "class": bem26()
      }, [slots.top == null ? void 0 : slots.top(), createVNode(RadioGroup, {
        "modelValue": props10.modelValue
      }, {
        default: () => [List2]
      }), DisabledText, DisabledList, slots.default == null ? void 0 : slots.default(), renderBottom()]);
    };
  }
});

// node_modules/vant/es/address-list/index.js
var AddressList = withInstall(AddressList_default);

// node_modules/vant/es/calendar/Calendar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/calendar/utils.js
var [name27, bem27, t6] = createNamespace("calendar");
function formatMonthTitle(date) {
  return t6("monthTitle", date.getFullYear(), date.getMonth() + 1);
}
function compareMonth(date1, date2) {
  var year1 = date1.getFullYear();
  var year2 = date2.getFullYear();
  if (year1 === year2) {
    var month1 = date1.getMonth();
    var month2 = date2.getMonth();
    return month1 === month2 ? 0 : month1 > month2 ? 1 : -1;
  }
  return year1 > year2 ? 1 : -1;
}
function compareDay(day1, day2) {
  var compareMonthResult = compareMonth(day1, day2);
  if (compareMonthResult === 0) {
    var date1 = day1.getDate();
    var date2 = day2.getDate();
    return date1 === date2 ? 0 : date1 > date2 ? 1 : -1;
  }
  return compareMonthResult;
}
var cloneDate = (date) => new Date(date);
var cloneDates = (dates) => Array.isArray(dates) ? dates.map(cloneDate) : cloneDate(dates);
function getDayByOffset(date, offset2) {
  var cloned = cloneDate(date);
  cloned.setDate(cloned.getDate() + offset2);
  return cloned;
}
var getPrevDay = (date) => getDayByOffset(date, -1);
var getNextDay = (date) => getDayByOffset(date, 1);
var getToday = () => {
  var today = new Date();
  today.setHours(0, 0, 0, 0);
  return today;
};
function calcDateNum(date) {
  var day1 = date[0].getTime();
  var day2 = date[1].getTime();
  return (day2 - day1) / (1e3 * 60 * 60 * 24) + 1;
}

// node_modules/vant/es/composables/use-refs.js
init_vue_runtime_esm_bundler();
function useRefs() {
  var refs = ref([]);
  onBeforeUpdate(() => {
    refs.value = [];
  });
  var setRefs = (index) => (el) => {
    refs.value[index] = el;
  };
  return [refs, setRefs];
}

// node_modules/vant/es/calendar/CalendarMonth.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/datetime-picker/utils.js
var sharedProps = extend({}, pickerProps, {
  filter: Function,
  columnsOrder: Array,
  formatter: {
    type: Function,
    default: (type, value) => value
  }
});
var pickerKeys = Object.keys(pickerProps);
function times(n, iteratee) {
  var index = -1;
  var result = Array(n);
  while (++index < n) {
    result[index] = iteratee(index);
  }
  return result;
}
function getTrueValue(value) {
  if (!value) {
    return 0;
  }
  while (Number.isNaN(parseInt(value, 10))) {
    if (value.length > 1) {
      value = value.slice(1);
    } else {
      return 0;
    }
  }
  return parseInt(value, 10);
}
function getMonthEndDay(year, month) {
  return 32 - new Date(year, month - 1, 32).getDate();
}

// node_modules/vant/es/composables/use-height.js
init_vue_runtime_esm_bundler();
var useHeight = (element) => {
  var height = ref();
  onMounted(() => {
    nextTick(() => {
      height.value = useRect(element).height;
    });
  });
  return height;
};

// node_modules/vant/es/calendar/CalendarDay.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name28] = createNamespace("calendar-day");
var CalendarDay_default = defineComponent({
  name: name28,
  props: {
    color: String,
    index: Number,
    rowHeight: String,
    offset: {
      type: Number,
      default: 0
    },
    item: {
      type: Object,
      required: true
    }
  },
  emits: ["click"],
  setup(props10, {
    emit,
    slots
  }) {
    var style2 = computed(() => {
      var {
        item,
        index,
        color,
        offset: offset2,
        rowHeight
      } = props10;
      var style3 = {
        height: rowHeight
      };
      if (item.type === "placeholder") {
        style3.width = "100%";
        return style3;
      }
      if (index === 0) {
        style3.marginLeft = 100 * offset2 / 7 + "%";
      }
      if (color) {
        switch (item.type) {
          case "end":
          case "start":
          case "start-end":
          case "multiple-middle":
          case "multiple-selected":
            style3.background = color;
            break;
          case "middle":
            style3.color = color;
            break;
        }
      }
      return style3;
    });
    var onClick = () => {
      if (props10.item.type !== "disabled") {
        emit("click", props10.item);
      }
    };
    var renderTopInfo = () => {
      var {
        topInfo
      } = props10.item;
      if (topInfo || slots["top-info"]) {
        return createVNode("div", {
          "class": bem27("top-info")
        }, [slots["top-info"] ? slots["top-info"](props10.item) : topInfo]);
      }
    };
    var renderBottomInfo = () => {
      var {
        bottomInfo
      } = props10.item;
      if (bottomInfo || slots["bottom-info"]) {
        return createVNode("div", {
          "class": bem27("bottom-info")
        }, [slots["bottom-info"] ? slots["bottom-info"](props10.item) : bottomInfo]);
      }
    };
    var renderContent = () => {
      var {
        item,
        color,
        rowHeight
      } = props10;
      var {
        type,
        text
      } = item;
      var Nodes = [renderTopInfo(), text, renderBottomInfo()];
      if (type === "selected") {
        return createVNode("div", {
          "class": bem27("selected-day"),
          "style": {
            width: rowHeight,
            height: rowHeight,
            background: color
          }
        }, [Nodes]);
      }
      return Nodes;
    };
    return () => {
      var {
        type,
        className
      } = props10.item;
      if (type === "placeholder") {
        return createVNode("div", {
          "class": bem27("day"),
          "style": style2.value
        }, null);
      }
      return createVNode("div", {
        "role": "gridcell",
        "style": style2.value,
        "class": [bem27("day", type), className],
        "tabindex": type === "disabled" ? void 0 : -1,
        "onClick": onClick
      }, [renderContent()]);
    };
  }
});

// node_modules/vant/es/calendar/CalendarMonth.js
var [name29] = createNamespace("calendar-month");
var CalendarMonth_default = defineComponent({
  name: name29,
  props: {
    type: String,
    color: String,
    showMark: Boolean,
    rowHeight: [Number, String],
    formatter: Function,
    lazyRender: Boolean,
    currentDate: [Date, Array],
    allowSameDay: Boolean,
    showSubtitle: Boolean,
    showMonthTitle: Boolean,
    firstDayOfWeek: Number,
    date: {
      type: Date,
      required: true
    },
    minDate: {
      type: Date,
      required: true
    },
    maxDate: {
      type: Date,
      required: true
    }
  },
  emits: ["click", "update-height"],
  setup(props10, {
    emit,
    slots
  }) {
    var [visible, setVisible] = useToggle();
    var daysRef = ref();
    var monthRef = ref();
    var height = useHeight(monthRef);
    var title = computed(() => formatMonthTitle(props10.date));
    var rowHeight = computed(() => addUnit(props10.rowHeight));
    var offset2 = computed(() => {
      var realDay = props10.date.getDay();
      if (props10.firstDayOfWeek) {
        return (realDay + 7 - props10.firstDayOfWeek) % 7;
      }
      return realDay;
    });
    var totalDay = computed(() => getMonthEndDay(props10.date.getFullYear(), props10.date.getMonth() + 1));
    var shouldRender = computed(() => visible.value || !props10.lazyRender);
    var getTitle = () => title.value;
    var scrollIntoView = (body) => {
      var el = props10.showSubtitle ? daysRef.value : monthRef.value;
      var scrollTop = el.getBoundingClientRect().top - body.getBoundingClientRect().top + body.scrollTop;
      setScrollTop(body, scrollTop);
    };
    var getMultipleDayType = (day) => {
      var isSelected = (date) => props10.currentDate.some((item) => compareDay(item, date) === 0);
      if (isSelected(day)) {
        var prevDay = getPrevDay(day);
        var nextDay = getNextDay(day);
        var prevSelected = isSelected(prevDay);
        var nextSelected = isSelected(nextDay);
        if (prevSelected && nextSelected) {
          return "multiple-middle";
        }
        if (prevSelected) {
          return "end";
        }
        if (nextSelected) {
          return "start";
        }
        return "multiple-selected";
      }
      return "";
    };
    var getRangeDayType = (day) => {
      var [startDay, endDay] = props10.currentDate;
      if (!startDay) {
        return "";
      }
      var compareToStart = compareDay(day, startDay);
      if (!endDay) {
        return compareToStart === 0 ? "start" : "";
      }
      var compareToEnd = compareDay(day, endDay);
      if (props10.allowSameDay && compareToStart === 0 && compareToEnd === 0) {
        return "start-end";
      }
      if (compareToStart === 0) {
        return "start";
      }
      if (compareToEnd === 0) {
        return "end";
      }
      if (compareToStart > 0 && compareToEnd < 0) {
        return "middle";
      }
      return "";
    };
    var getDayType = (day) => {
      var {
        type,
        minDate,
        maxDate,
        currentDate
      } = props10;
      if (compareDay(day, minDate) < 0 || compareDay(day, maxDate) > 0) {
        return "disabled";
      }
      if (currentDate === null) {
        return "";
      }
      if (Array.isArray(currentDate)) {
        if (type === "multiple") {
          return getMultipleDayType(day);
        }
        if (type === "range") {
          return getRangeDayType(day);
        }
      } else if (type === "single") {
        return compareDay(day, currentDate) === 0 ? "selected" : "";
      }
      return "";
    };
    var getBottomInfo = (dayType) => {
      if (props10.type === "range") {
        if (dayType === "start" || dayType === "end") {
          return t6(dayType);
        }
        if (dayType === "start-end") {
          return t6("startEnd");
        }
      }
    };
    var renderTitle = () => {
      if (props10.showMonthTitle) {
        return createVNode("div", {
          "class": bem27("month-title")
        }, [title.value]);
      }
    };
    var renderMark = () => {
      if (props10.showMark && shouldRender.value) {
        return createVNode("div", {
          "class": bem27("month-mark")
        }, [props10.date.getMonth() + 1]);
      }
    };
    var placeholders = computed(() => {
      var count = Math.ceil((totalDay.value + offset2.value) / 7);
      return Array(count).fill({
        type: "placeholder"
      });
    });
    var days = computed(() => {
      var days2 = [];
      var year = props10.date.getFullYear();
      var month = props10.date.getMonth();
      for (var day = 1; day <= totalDay.value; day++) {
        var date = new Date(year, month, day);
        var type = getDayType(date);
        var config = {
          date,
          type,
          text: day,
          bottomInfo: getBottomInfo(type)
        };
        if (props10.formatter) {
          config = props10.formatter(config);
        }
        days2.push(config);
      }
      return days2;
    });
    var renderDay = (item, index) => createVNode(CalendarDay_default, {
      "item": item,
      "index": index,
      "color": props10.color,
      "offset": offset2.value,
      "rowHeight": rowHeight.value,
      "onClick": (item2) => emit("click", item2)
    }, pick(slots, ["top-info", "bottom-info"]));
    var renderDays = () => {
      return createVNode("div", {
        "ref": daysRef,
        "role": "grid",
        "class": bem27("days")
      }, [renderMark(), (shouldRender.value ? days : placeholders).value.map(renderDay)]);
    };
    useExpose({
      getTitle,
      getHeight: () => height.value,
      setVisible,
      scrollIntoView
    });
    return () => createVNode("div", {
      "class": bem27("month"),
      "ref": monthRef
    }, [renderTitle(), renderDays()]);
  }
});

// node_modules/vant/es/calendar/CalendarHeader.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name30] = createNamespace("calendar-header");
var CalendarHeader_default = defineComponent({
  name: name30,
  props: {
    title: String,
    subtitle: String,
    showTitle: Boolean,
    showSubtitle: Boolean,
    firstDayOfWeek: Number
  },
  emits: ["click-subtitle"],
  setup(props10, {
    slots,
    emit
  }) {
    var renderTitle = () => {
      if (props10.showTitle) {
        var text = props10.title || t6("title");
        var title = slots.title ? slots.title() : text;
        return createVNode("div", {
          "class": bem27("header-title")
        }, [title]);
      }
    };
    var onClickSubtitle = (event) => {
      emit("click-subtitle", event);
    };
    var renderSubtitle = () => {
      if (props10.showSubtitle) {
        var title = slots.subtitle ? slots.subtitle() : props10.subtitle;
        return createVNode("div", {
          "class": bem27("header-subtitle"),
          "onClick": onClickSubtitle
        }, [title]);
      }
    };
    var renderWeekDays = () => {
      var {
        firstDayOfWeek
      } = props10;
      var weekdays = t6("weekdays");
      var renderWeekDays2 = [...weekdays.slice(firstDayOfWeek, 7), ...weekdays.slice(0, firstDayOfWeek)];
      return createVNode("div", {
        "class": bem27("weekdays")
      }, [renderWeekDays2.map((text) => createVNode("span", {
        "class": bem27("weekday")
      }, [text]))]);
    };
    return () => createVNode("div", {
      "class": bem27("header")
    }, [renderTitle(), renderSubtitle(), renderWeekDays()]);
  }
});

// node_modules/vant/es/calendar/Calendar.js
var Calendar_default = defineComponent({
  name: name27,
  props: {
    show: Boolean,
    title: String,
    color: String,
    round: truthProp,
    readonly: Boolean,
    poppable: truthProp,
    teleport: [String, Object],
    showMark: truthProp,
    showTitle: truthProp,
    formatter: Function,
    rowHeight: [Number, String],
    confirmText: String,
    rangePrompt: String,
    lazyRender: truthProp,
    showConfirm: truthProp,
    defaultDate: [Date, Array],
    allowSameDay: Boolean,
    showSubtitle: truthProp,
    closeOnPopstate: truthProp,
    confirmDisabledText: String,
    closeOnClickOverlay: truthProp,
    safeAreaInsetBottom: truthProp,
    type: {
      type: String,
      default: "single"
    },
    position: {
      type: String,
      default: "bottom"
    },
    maxRange: {
      type: [Number, String],
      default: null
    },
    minDate: {
      type: Date,
      validator: isDate,
      default: getToday
    },
    maxDate: {
      type: Date,
      validator: isDate,
      default: () => {
        var now = getToday();
        return new Date(now.getFullYear(), now.getMonth() + 6, now.getDate());
      }
    },
    firstDayOfWeek: {
      type: [Number, String],
      default: 0,
      validator: (val) => val >= 0 && val <= 6
    },
    showRangePrompt: {
      type: Boolean,
      default: true
    }
  },
  emits: ["select", "confirm", "unselect", "month-show", "over-range", "update:show", "click-subtitle"],
  setup(props10, {
    emit,
    slots
  }) {
    var limitDateRange = (date, minDate = props10.minDate, maxDate = props10.maxDate) => {
      if (compareDay(date, minDate) === -1) {
        return minDate;
      }
      if (compareDay(date, maxDate) === 1) {
        return maxDate;
      }
      return date;
    };
    var getInitialDate = (defaultDate = props10.defaultDate) => {
      var {
        type,
        minDate,
        maxDate
      } = props10;
      if (defaultDate === null) {
        return defaultDate;
      }
      var now = getToday();
      if (type === "range") {
        if (!Array.isArray(defaultDate)) {
          defaultDate = [];
        }
        var start2 = limitDateRange(defaultDate[0] || now, minDate, getPrevDay(maxDate));
        var end2 = limitDateRange(defaultDate[1] || now, getNextDay(minDate));
        return [start2, end2];
      }
      if (type === "multiple") {
        if (Array.isArray(defaultDate)) {
          return defaultDate.map((date) => limitDateRange(date));
        }
        return [limitDateRange(now)];
      }
      if (!defaultDate || Array.isArray(defaultDate)) {
        defaultDate = now;
      }
      return limitDateRange(defaultDate);
    };
    var bodyHeight;
    var bodyRef = ref();
    var state = reactive({
      subtitle: "",
      currentDate: getInitialDate()
    });
    var [monthRefs, setMonthRefs] = useRefs();
    var dayOffset = computed(() => props10.firstDayOfWeek ? +props10.firstDayOfWeek % 7 : 0);
    var months = computed(() => {
      var months2 = [];
      var cursor = new Date(props10.minDate);
      cursor.setDate(1);
      do {
        months2.push(new Date(cursor));
        cursor.setMonth(cursor.getMonth() + 1);
      } while (compareMonth(cursor, props10.maxDate) !== 1);
      return months2;
    });
    var buttonDisabled = computed(() => {
      var {
        currentDate
      } = state;
      if (currentDate) {
        if (props10.type === "range") {
          return !currentDate[0] || !currentDate[1];
        }
        if (props10.type === "multiple") {
          return !currentDate.length;
        }
      }
      return !currentDate;
    });
    var onScroll = () => {
      var top2 = getScrollTop(bodyRef.value);
      var bottom2 = top2 + bodyHeight;
      var heights = months.value.map((item, index) => monthRefs.value[index].getHeight());
      var heightSum = heights.reduce((a, b) => a + b, 0);
      if (bottom2 > heightSum && top2 > 0) {
        return;
      }
      var height = 0;
      var currentMonth;
      var visibleRange = [-1, -1];
      for (var i = 0; i < months.value.length; i++) {
        var month = monthRefs.value[i];
        var visible = height <= bottom2 && height + heights[i] >= top2;
        if (visible) {
          visibleRange[1] = i;
          if (!currentMonth) {
            currentMonth = month;
            visibleRange[0] = i;
          }
          if (!monthRefs.value[i].showed) {
            monthRefs.value[i].showed = true;
            emit("month-show", {
              date: month.date,
              title: month.title
            });
          }
        }
        height += heights[i];
      }
      months.value.forEach((month2, index) => {
        var visible2 = index >= visibleRange[0] - 1 && index <= visibleRange[1] + 1;
        monthRefs.value[index].setVisible(visible2);
      });
      if (currentMonth) {
        state.subtitle = currentMonth.getTitle();
      }
    };
    var scrollToDate = (targetDate) => {
      raf(() => {
        months.value.some((month, index) => {
          if (compareMonth(month, targetDate) === 0) {
            monthRefs.value[index].scrollIntoView(bodyRef.value);
            return true;
          }
          return false;
        });
        onScroll();
      });
    };
    var scrollIntoView = () => {
      if (props10.poppable && !props10.show) {
        return;
      }
      var {
        currentDate
      } = state;
      if (currentDate) {
        var targetDate = props10.type === "single" ? currentDate : currentDate[0];
        scrollToDate(targetDate);
      } else {
        raf(onScroll);
      }
    };
    var init = () => {
      if (props10.poppable && !props10.show) {
        return;
      }
      raf(() => {
        bodyHeight = Math.floor(useRect(bodyRef).height);
        scrollIntoView();
      });
    };
    var reset = (date = getInitialDate()) => {
      state.currentDate = date;
      scrollIntoView();
    };
    var checkRange = (date) => {
      var {
        maxRange,
        rangePrompt,
        showRangePrompt
      } = props10;
      if (maxRange && calcDateNum(date) > maxRange) {
        if (showRangePrompt) {
          Toast(rangePrompt || t6("rangePrompt", maxRange));
        }
        emit("over-range");
        return false;
      }
      return true;
    };
    var onConfirm = () => emit("confirm", cloneDates(state.currentDate));
    var select = (date, complete) => {
      var setCurrentDate = (date2) => {
        state.currentDate = date2;
        emit("select", cloneDates(state.currentDate));
      };
      if (complete && props10.type === "range") {
        var valid = checkRange(date);
        if (!valid) {
          if (props10.showConfirm) {
            setCurrentDate([date[0], getDayByOffset(date[0], +props10.maxRange - 1)]);
          } else {
            setCurrentDate(date);
          }
          return;
        }
      }
      setCurrentDate(date);
      if (complete && !props10.showConfirm) {
        onConfirm();
      }
    };
    var onClickDay = (item) => {
      if (props10.readonly || !item.date) {
        return;
      }
      var {
        date
      } = item;
      var {
        type
      } = props10;
      var {
        currentDate
      } = state;
      if (type === "range") {
        if (!currentDate) {
          select([date]);
          return;
        }
        var [startDay, endDay] = currentDate;
        if (startDay && !endDay) {
          var compareToStart = compareDay(date, startDay);
          if (compareToStart === 1) {
            select([startDay, date], true);
          } else if (compareToStart === -1) {
            select([date]);
          } else if (props10.allowSameDay) {
            select([date, date], true);
          }
        } else {
          select([date]);
        }
      } else if (type === "multiple") {
        if (!currentDate) {
          select([date]);
          return;
        }
        var selectedIndex;
        var selected = state.currentDate.some((dateItem, index) => {
          var equal2 = compareDay(dateItem, date) === 0;
          if (equal2) {
            selectedIndex = index;
          }
          return equal2;
        });
        if (selected) {
          var [unselectedDate] = currentDate.splice(selectedIndex, 1);
          emit("unselect", cloneDate(unselectedDate));
        } else if (props10.maxRange && currentDate.length >= props10.maxRange) {
          Toast(props10.rangePrompt || t6("rangePrompt", props10.maxRange));
        } else {
          select([...currentDate, date]);
        }
      } else {
        select(date, true);
      }
    };
    var updateShow = (value) => emit("update:show", value);
    var renderMonth = (date, index) => {
      var showMonthTitle = index !== 0 || !props10.showSubtitle;
      return createVNode(CalendarMonth_default, mergeProps({
        "ref": setMonthRefs(index),
        "date": date,
        "currentDate": state.currentDate,
        "showMonthTitle": showMonthTitle,
        "firstDayOfWeek": dayOffset.value
      }, pick(props10, ["type", "color", "minDate", "maxDate", "showMark", "formatter", "rowHeight", "lazyRender", "showSubtitle", "allowSameDay"]), {
        "onClick": onClickDay
      }), pick(slots, ["top-info", "bottom-info"]));
    };
    var renderFooterButton = () => {
      if (slots.footer) {
        return slots.footer();
      }
      if (props10.showConfirm) {
        var text = buttonDisabled.value ? props10.confirmDisabledText : props10.confirmText;
        return createVNode(Button, {
          "round": true,
          "block": true,
          "type": "danger",
          "color": props10.color,
          "class": bem27("confirm"),
          "disabled": buttonDisabled.value,
          "nativeType": "button",
          "onClick": onConfirm
        }, {
          default: () => [text || t6("confirm")]
        });
      }
    };
    var renderFooter = () => createVNode("div", {
      "class": bem27("footer", {
        unfit: !props10.safeAreaInsetBottom
      })
    }, [renderFooterButton()]);
    var renderCalendar = () => createVNode("div", {
      "class": bem27()
    }, [createVNode(CalendarHeader_default, {
      "title": props10.title,
      "subtitle": state.subtitle,
      "showTitle": props10.showTitle,
      "showSubtitle": props10.showSubtitle,
      "firstDayOfWeek": dayOffset.value,
      "onClick-subtitle": (event) => {
        emit("click-subtitle", event);
      }
    }, pick(slots, ["title", "subtitle"])), createVNode("div", {
      "ref": bodyRef,
      "class": bem27("body"),
      "onScroll": onScroll
    }, [months.value.map(renderMonth)]), renderFooter()]);
    watch(() => props10.show, init);
    watch(() => [props10.type, props10.minDate, props10.maxDate], () => {
      reset(getInitialDate(state.currentDate));
    });
    watch(() => props10.defaultDate, (value) => {
      state.currentDate = value;
      scrollIntoView();
    });
    useExpose({
      reset,
      scrollToDate
    });
    onMountedOrActivated(init);
    return () => {
      if (props10.poppable) {
        return createVNode(Popup, mergeProps({
          "show": props10.show,
          "class": bem27("popup"),
          "round": props10.round,
          "position": props10.position,
          "closeable": props10.showTitle || props10.showSubtitle,
          "teleport": props10.teleport,
          "closeOnPopstate": props10.closeOnPopstate,
          "closeOnClickOverlay": props10.closeOnClickOverlay
        }, {
          "onUpdate:show": updateShow
        }), {
          default: () => [renderCalendar()]
        });
      }
      return renderCalendar();
    };
  }
});

// node_modules/vant/es/calendar/index.js
var Calendar = withInstall(Calendar_default);

// node_modules/vant/es/card/Card.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/image/Image.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name31, bem28] = createNamespace("image");
var Image_default = defineComponent({
  name: name31,
  props: {
    src: String,
    alt: String,
    fit: String,
    round: Boolean,
    width: [Number, String],
    height: [Number, String],
    radius: [Number, String],
    lazyLoad: Boolean,
    iconSize: [Number, String],
    showError: truthProp,
    iconPrefix: String,
    showLoading: truthProp,
    errorIcon: {
      type: String,
      default: "photo-fail"
    },
    loadingIcon: {
      type: String,
      default: "photo"
    }
  },
  emits: ["load", "error"],
  setup(props10, {
    emit,
    slots
  }) {
    var error = ref(false);
    var loading = ref(true);
    var imageRef = ref();
    var {
      $Lazyload
    } = getCurrentInstance().proxy;
    var style2 = computed(() => {
      var style3 = {};
      if (isDef(props10.width)) {
        style3.width = addUnit(props10.width);
      }
      if (isDef(props10.height)) {
        style3.height = addUnit(props10.height);
      }
      if (isDef(props10.radius)) {
        style3.overflow = "hidden";
        style3.borderRadius = addUnit(props10.radius);
      }
      return style3;
    });
    watch(() => props10.src, () => {
      error.value = false;
      loading.value = true;
    });
    var onLoad = (event) => {
      loading.value = false;
      emit("load", event);
    };
    var onError = (event) => {
      error.value = true;
      loading.value = false;
      emit("error", event);
    };
    var renderLoadingIcon = () => {
      if (slots.loading) {
        return slots.loading();
      }
      return createVNode(Icon, {
        "size": props10.iconSize,
        "name": props10.loadingIcon,
        "class": bem28("loading-icon"),
        "classPrefix": props10.iconPrefix
      }, null);
    };
    var renderErrorIcon = () => {
      if (slots.error) {
        return slots.error();
      }
      return createVNode(Icon, {
        "size": props10.iconSize,
        "name": props10.errorIcon,
        "class": bem28("error-icon"),
        "classPrefix": props10.iconPrefix
      }, null);
    };
    var renderPlaceholder = () => {
      if (loading.value && props10.showLoading) {
        return createVNode("div", {
          "class": bem28("loading")
        }, [renderLoadingIcon()]);
      }
      if (error.value && props10.showError) {
        return createVNode("div", {
          "class": bem28("error")
        }, [renderErrorIcon()]);
      }
    };
    var renderImage = () => {
      if (error.value || !props10.src) {
        return;
      }
      var attrs = {
        alt: props10.alt,
        class: bem28("img"),
        style: {
          objectFit: props10.fit
        }
      };
      if (props10.lazyLoad) {
        return withDirectives(createVNode("img", mergeProps({
          "ref": imageRef
        }, attrs), null), [[resolveDirective("lazy"), props10.src]]);
      }
      return createVNode("img", mergeProps({
        "src": props10.src,
        "onLoad": onLoad,
        "onError": onError
      }, attrs), null);
    };
    var onLazyLoaded = ({
      el
    }) => {
      if (el === imageRef.value && loading.value) {
        onLoad();
      }
    };
    var onLazyLoadError = ({
      el
    }) => {
      if (el === imageRef.value && !error.value) {
        onError();
      }
    };
    if ($Lazyload && inBrowser) {
      $Lazyload.$on("loaded", onLazyLoaded);
      $Lazyload.$on("error", onLazyLoadError);
      onBeforeUnmount(() => {
        $Lazyload.$off("loaded", onLazyLoaded);
        $Lazyload.$off("error", onLazyLoadError);
      });
    }
    return () => createVNode("div", {
      "class": bem28({
        round: props10.round
      }),
      "style": style2.value
    }, [renderImage(), renderPlaceholder(), slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/image/index.js
var Image2 = withInstall(Image_default);

// node_modules/vant/es/card/Card.js
var [name32, bem29] = createNamespace("card");
var Card_default = defineComponent({
  name: name32,
  props: {
    tag: String,
    num: [Number, String],
    desc: String,
    thumb: String,
    title: String,
    price: [Number, String],
    centered: Boolean,
    lazyLoad: Boolean,
    thumbLink: String,
    originPrice: [Number, String],
    currency: {
      type: String,
      default: "\xA5"
    }
  },
  emits: ["click-thumb"],
  setup(props10, {
    slots,
    emit
  }) {
    var renderTitle = () => {
      if (slots.title) {
        return slots.title();
      }
      if (props10.title) {
        return createVNode("div", {
          "class": [bem29("title"), "van-multi-ellipsis--l2"]
        }, [props10.title]);
      }
    };
    var renderThumbTag = () => {
      if (slots.tag || props10.tag) {
        return createVNode("div", {
          "class": bem29("tag")
        }, [slots.tag ? slots.tag() : createVNode(Tag, {
          "mark": true,
          "type": "danger"
        }, {
          default: () => [props10.tag]
        })]);
      }
    };
    var renderThumbImage = () => {
      if (slots.thumb) {
        return slots.thumb();
      }
      return createVNode(Image2, {
        "src": props10.thumb,
        "fit": "cover",
        "width": "100%",
        "height": "100%",
        "lazyLoad": props10.lazyLoad
      }, null);
    };
    var renderThumb = () => {
      if (slots.thumb || props10.thumb) {
        return createVNode("a", {
          "href": props10.thumbLink,
          "class": bem29("thumb"),
          "onClick": (event) => emit("click-thumb", event)
        }, [renderThumbImage(), renderThumbTag()]);
      }
    };
    var renderDesc = () => {
      if (slots.desc) {
        return slots.desc();
      }
      if (props10.desc) {
        return createVNode("div", {
          "class": [bem29("desc"), "van-ellipsis"]
        }, [props10.desc]);
      }
    };
    var renderPriceText = () => {
      var priceArr = props10.price.toString().split(".");
      return createVNode("div", null, [createVNode("span", {
        "class": bem29("price-currency")
      }, [props10.currency]), createVNode("span", {
        "class": bem29("price-integer")
      }, [priceArr[0]]), createTextVNode("."), createVNode("span", {
        "class": bem29("price-decimal")
      }, [priceArr[1]])]);
    };
    return () => {
      var _slots$priceTop;
      var showNum = slots.num || isDef(props10.num);
      var showPrice = slots.price || isDef(props10.price);
      var showOriginPrice = slots["origin-price"] || isDef(props10.originPrice);
      var showBottom = showNum || showPrice || showOriginPrice || slots.bottom;
      var Price = showPrice && createVNode("div", {
        "class": bem29("price")
      }, [slots.price ? slots.price() : renderPriceText()]);
      var OriginPrice = showOriginPrice && createVNode("div", {
        "class": bem29("origin-price")
      }, [slots["origin-price"] ? slots["origin-price"]() : props10.currency + " " + props10.originPrice]);
      var Num = showNum && createVNode("div", {
        "class": bem29("num")
      }, [slots.num ? slots.num() : "x" + props10.num]);
      var Footer = slots.footer && createVNode("div", {
        "class": bem29("footer")
      }, [slots.footer()]);
      var Bottom = showBottom && createVNode("div", {
        "class": bem29("bottom")
      }, [(_slots$priceTop = slots["price-top"]) == null ? void 0 : _slots$priceTop.call(slots), Price, OriginPrice, Num, slots.bottom == null ? void 0 : slots.bottom()]);
      return createVNode("div", {
        "class": bem29()
      }, [createVNode("div", {
        "class": bem29("header")
      }, [renderThumb(), createVNode("div", {
        "class": bem29("content", {
          centered: props10.centered
        })
      }, [createVNode("div", null, [renderTitle(), renderDesc(), slots.tags == null ? void 0 : slots.tags()]), Bottom])]), Footer]);
    };
  }
});

// node_modules/vant/es/card/index.js
var Card = withInstall(Card_default);

// node_modules/vant/es/cascader/Cascader.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/tab/Tab.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/tabs/Tabs.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/tabs/utils.js
var rafId;
function scrollLeftTo(scroller, to, duration) {
  cancelRaf(rafId);
  var count = 0;
  var from = scroller.scrollLeft;
  var frames = duration === 0 ? 1 : Math.round(duration * 1e3 / 16);
  function animate() {
    scroller.scrollLeft += (to - from) / frames;
    if (++count < frames) {
      rafId = raf(animate);
    }
  }
  animate();
}
function scrollTopTo(scroller, to, duration, callback) {
  var current = getScrollTop(scroller);
  var isDown = current < to;
  var frames = duration === 0 ? 1 : Math.round(duration * 1e3 / 16);
  var step = (to - current) / frames;
  function animate() {
    current += step;
    if (isDown && current > to || !isDown && current < to) {
      current = to;
    }
    setScrollTop(scroller, current);
    if (isDown && current < to || !isDown && current > to) {
      raf(animate);
    } else if (callback) {
      raf(callback);
    }
  }
  animate();
}

// node_modules/vant/es/sticky/Sticky.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/composables/use-visibility-change.js
init_vue_runtime_esm_bundler();
function useVisibilityChange(target, onChange) {
  if (!inBrowser || !window.IntersectionObserver) {
    return;
  }
  var observer = new IntersectionObserver((entries) => {
    onChange(entries[0].intersectionRatio > 0);
  }, {
    root: document.body
  });
  var observe = () => {
    if (target.value) {
      observer.observe(target.value);
    }
  };
  var unobserve = () => {
    if (target.value) {
      observer.unobserve(target.value);
    }
  };
  onDeactivated(unobserve);
  onBeforeUnmount(unobserve);
  onMountedOrActivated(observe);
}

// node_modules/vant/es/sticky/Sticky.js
var [name33, bem30] = createNamespace("sticky");
var Sticky_default = defineComponent({
  name: name33,
  props: {
    zIndex: [Number, String],
    container: Object,
    offsetTop: {
      type: [Number, String],
      default: 0
    },
    offsetBottom: {
      type: [Number, String],
      default: 0
    },
    position: {
      type: String,
      default: "top"
    }
  },
  emits: ["scroll", "change"],
  setup(props10, {
    emit,
    slots
  }) {
    var root2 = ref();
    var scrollParent2 = useScrollParent(root2);
    var state = reactive({
      fixed: false,
      width: 0,
      height: 0,
      transform: 0
    });
    var offset2 = computed(() => unitToPx(props10.position === "top" ? props10.offsetTop : props10.offsetBottom));
    var rootStyle = computed(() => {
      var {
        fixed,
        height,
        width
      } = state;
      if (fixed) {
        return {
          width: width + "px",
          height: height + "px"
        };
      }
    });
    var stickyStyle = computed(() => {
      if (!state.fixed) {
        return;
      }
      var style2 = extend(getZIndexStyle(props10.zIndex), {
        width: state.width + "px",
        height: state.height + "px",
        [props10.position]: offset2.value + "px"
      });
      if (state.transform) {
        style2.transform = "translate3d(0, " + state.transform + "px, 0)";
      }
      return style2;
    });
    var emitScroll = (scrollTop) => emit("scroll", {
      scrollTop,
      isFixed: state.fixed
    });
    var onScroll = () => {
      if (!root2.value || isHidden(root2)) {
        return;
      }
      var {
        container,
        position
      } = props10;
      var rootRect = useRect(root2);
      var scrollTop = getScrollTop(window);
      state.width = rootRect.width;
      state.height = rootRect.height;
      if (position === "top") {
        if (container) {
          var containerRect = useRect(container);
          var difference = containerRect.bottom - offset2.value - state.height;
          state.fixed = offset2.value > rootRect.top && containerRect.bottom > 0;
          state.transform = difference < 0 ? difference : 0;
        } else {
          state.fixed = offset2.value > rootRect.top;
        }
      } else {
        var {
          clientHeight
        } = document.documentElement;
        if (container) {
          var _containerRect = useRect(container);
          var _difference = clientHeight - _containerRect.top - offset2.value - state.height;
          state.fixed = clientHeight - offset2.value < rootRect.bottom && clientHeight > _containerRect.top;
          state.transform = _difference < 0 ? -_difference : 0;
        } else {
          state.fixed = clientHeight - offset2.value < rootRect.bottom;
        }
      }
      emitScroll(scrollTop);
    };
    watch(() => state.fixed, (value) => emit("change", value));
    useEventListener("scroll", onScroll, {
      target: scrollParent2
    });
    useVisibilityChange(root2, onScroll);
    return () => createVNode("div", {
      "ref": root2,
      "style": rootStyle.value
    }, [createVNode("div", {
      "class": bem30({
        fixed: state.fixed
      }),
      "style": stickyStyle.value
    }, [slots.default == null ? void 0 : slots.default()])]);
  }
});

// node_modules/vant/es/sticky/index.js
var Sticky = withInstall(Sticky_default);

// node_modules/vant/es/tabs/TabsTitle.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name34, bem31] = createNamespace("tab");
var TabsTitle_default = defineComponent({
  name: name34,
  props: {
    dot: Boolean,
    type: String,
    color: String,
    title: String,
    badge: [Number, String],
    isActive: Boolean,
    disabled: Boolean,
    scrollable: Boolean,
    activeColor: String,
    renderTitle: Function,
    inactiveColor: String
  },
  setup(props10) {
    var style2 = computed(() => {
      var style3 = {};
      var {
        type,
        color,
        disabled,
        isActive,
        activeColor,
        inactiveColor
      } = props10;
      var isCard = type === "card";
      if (color && isCard) {
        style3.borderColor = color;
        if (!disabled) {
          if (isActive) {
            style3.backgroundColor = color;
          } else {
            style3.color = color;
          }
        }
      }
      var titleColor = isActive ? activeColor : inactiveColor;
      if (titleColor) {
        style3.color = titleColor;
      }
      return style3;
    });
    var renderText = () => {
      var Text = createVNode("span", {
        "class": bem31("text", {
          ellipsis: !props10.scrollable
        })
      }, [props10.renderTitle ? props10.renderTitle() : props10.title]);
      if (props10.dot || isDef(props10.badge) && props10.badge !== "") {
        return createVNode(Badge, {
          "dot": props10.dot,
          "content": props10.badge
        }, {
          default: () => [Text]
        });
      }
      return Text;
    };
    return () => createVNode("div", {
      "role": "tab",
      "class": [bem31({
        active: props10.isActive,
        disabled: props10.disabled
      })],
      "style": style2.value,
      "aria-selected": props10.isActive
    }, [renderText()]);
  }
});

// node_modules/vant/es/tabs/TabsContent.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/swipe/Swipe.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name35, bem32] = createNamespace("swipe");
var SWIPE_KEY = Symbol(name35);
var props2 = {
  loop: truthProp,
  width: [Number, String],
  height: [Number, String],
  vertical: Boolean,
  touchable: truthProp,
  lazyRender: Boolean,
  indicatorColor: String,
  showIndicators: truthProp,
  stopPropagation: truthProp,
  autoplay: {
    type: [Number, String],
    default: 0
  },
  duration: {
    type: [Number, String],
    default: 500
  },
  initialSwipe: {
    type: [Number, String],
    default: 0
  }
};
var Swipe_default = defineComponent({
  name: name35,
  props: props2,
  emits: ["change"],
  setup(props10, {
    emit,
    slots
  }) {
    var root2 = ref();
    var state = reactive({
      rect: null,
      width: 0,
      height: 0,
      offset: 0,
      active: 0,
      swiping: false
    });
    var touch = useTouch();
    var windowSize = useWindowSize();
    var {
      children,
      linkChildren
    } = useChildren(SWIPE_KEY);
    var count = computed(() => children.length);
    var size = computed(() => state[props10.vertical ? "height" : "width"]);
    var delta = computed(() => props10.vertical ? touch.deltaY.value : touch.deltaX.value);
    var minOffset = computed(() => {
      if (state.rect) {
        var base = props10.vertical ? state.rect.height : state.rect.width;
        return base - size.value * count.value;
      }
      return 0;
    });
    var maxCount = computed(() => Math.ceil(Math.abs(minOffset.value) / size.value));
    var trackSize = computed(() => count.value * size.value);
    var activeIndicator = computed(() => (state.active + count.value) % count.value);
    var isCorrectDirection = computed(() => {
      var expect = props10.vertical ? "vertical" : "horizontal";
      return touch.direction.value === expect;
    });
    var trackStyle = computed(() => {
      var style2 = {
        transitionDuration: (state.swiping ? 0 : props10.duration) + "ms",
        transform: "translate" + (props10.vertical ? "Y" : "X") + "(" + state.offset + "px)"
      };
      if (size.value) {
        var mainAxis = props10.vertical ? "height" : "width";
        var crossAxis = props10.vertical ? "width" : "height";
        style2[mainAxis] = trackSize.value + "px";
        style2[crossAxis] = props10[crossAxis] ? props10[crossAxis] + "px" : "";
      }
      return style2;
    });
    var getTargetActive = (pace) => {
      var {
        active
      } = state;
      if (pace) {
        if (props10.loop) {
          return clamp(active + pace, -1, count.value);
        }
        return clamp(active + pace, 0, maxCount.value);
      }
      return active;
    };
    var getTargetOffset = (targetActive, offset2 = 0) => {
      var currentPosition = targetActive * size.value;
      if (!props10.loop) {
        currentPosition = Math.min(currentPosition, -minOffset.value);
      }
      var targetOffset = offset2 - currentPosition;
      if (!props10.loop) {
        targetOffset = clamp(targetOffset, minOffset.value, 0);
      }
      return targetOffset;
    };
    var move = ({
      pace = 0,
      offset: offset2 = 0,
      emitChange
    }) => {
      if (count.value <= 1) {
        return;
      }
      var {
        active
      } = state;
      var targetActive = getTargetActive(pace);
      var targetOffset = getTargetOffset(targetActive, offset2);
      if (props10.loop) {
        if (children[0] && targetOffset !== minOffset.value) {
          var outRightBound = targetOffset < minOffset.value;
          children[0].setOffset(outRightBound ? trackSize.value : 0);
        }
        if (children[count.value - 1] && targetOffset !== 0) {
          var outLeftBound = targetOffset > 0;
          children[count.value - 1].setOffset(outLeftBound ? -trackSize.value : 0);
        }
      }
      state.active = targetActive;
      state.offset = targetOffset;
      if (emitChange && targetActive !== active) {
        emit("change", activeIndicator.value);
      }
    };
    var correctPosition = () => {
      state.swiping = true;
      if (state.active <= -1) {
        move({
          pace: count.value
        });
      } else if (state.active >= count.value) {
        move({
          pace: -count.value
        });
      }
    };
    var prev2 = () => {
      correctPosition();
      touch.reset();
      doubleRaf(() => {
        state.swiping = false;
        move({
          pace: -1,
          emitChange: true
        });
      });
    };
    var next = () => {
      correctPosition();
      touch.reset();
      doubleRaf(() => {
        state.swiping = false;
        move({
          pace: 1,
          emitChange: true
        });
      });
    };
    var autoplayTimer;
    var stopAutoplay = () => clearTimeout(autoplayTimer);
    var autoplay = () => {
      stopAutoplay();
      if (props10.autoplay > 0 && count.value > 1) {
        autoplayTimer = setTimeout(() => {
          next();
          autoplay();
        }, +props10.autoplay);
      }
    };
    var initialize = (active = +props10.initialSwipe) => {
      if (!root2.value) {
        return;
      }
      if (!isHidden(root2)) {
        var _props$width, _props$height;
        var rect = {
          width: root2.value.offsetWidth,
          height: root2.value.offsetHeight
        };
        state.rect = rect;
        state.width = +((_props$width = props10.width) != null ? _props$width : rect.width);
        state.height = +((_props$height = props10.height) != null ? _props$height : rect.height);
      }
      if (count.value) {
        active = Math.min(count.value - 1, active);
      }
      state.active = active;
      state.swiping = true;
      state.offset = getTargetOffset(active);
      children.forEach((swipe) => {
        swipe.setOffset(0);
      });
    };
    var resize = () => initialize(state.active);
    var touchStartTime;
    var onTouchStart = (event) => {
      if (!props10.touchable)
        return;
      touch.start(event);
      touchStartTime = Date.now();
      stopAutoplay();
      correctPosition();
    };
    var onTouchMove = (event) => {
      if (props10.touchable && state.swiping) {
        touch.move(event);
        if (isCorrectDirection.value) {
          preventDefault(event, props10.stopPropagation);
          move({
            offset: delta.value
          });
        }
      }
    };
    var onTouchEnd = () => {
      if (!props10.touchable || !state.swiping) {
        return;
      }
      var duration = Date.now() - touchStartTime;
      var speed = delta.value / duration;
      var shouldSwipe = Math.abs(speed) > 0.25 || Math.abs(delta.value) > size.value / 2;
      if (shouldSwipe && isCorrectDirection.value) {
        var offset2 = props10.vertical ? touch.offsetY.value : touch.offsetX.value;
        var pace = 0;
        if (props10.loop) {
          pace = offset2 > 0 ? delta.value > 0 ? -1 : 1 : 0;
        } else {
          pace = -Math[delta.value > 0 ? "ceil" : "floor"](delta.value / size.value);
        }
        move({
          pace,
          emitChange: true
        });
      } else if (delta.value) {
        move({
          pace: 0
        });
      }
      state.swiping = false;
      autoplay();
    };
    var swipeTo = (index, options = {}) => {
      correctPosition();
      touch.reset();
      doubleRaf(() => {
        var targetIndex;
        if (props10.loop && index === count.value) {
          targetIndex = state.active === 0 ? 0 : index;
        } else {
          targetIndex = index % count.value;
        }
        if (options.immediate) {
          doubleRaf(() => {
            state.swiping = false;
          });
        } else {
          state.swiping = false;
        }
        move({
          pace: targetIndex - state.active,
          emitChange: true
        });
      });
    };
    var renderDot = (_2, index) => {
      var active = index === activeIndicator.value;
      var style2 = active ? {
        backgroundColor: props10.indicatorColor
      } : void 0;
      return createVNode("i", {
        "style": style2,
        "class": bem32("indicator", {
          active
        })
      }, null);
    };
    var renderIndicator = () => {
      if (slots.indicator) {
        return slots.indicator({
          active: activeIndicator.value
        });
      }
      if (props10.showIndicators && count.value > 1) {
        return createVNode("div", {
          "class": bem32("indicators", {
            vertical: props10.vertical
          })
        }, [Array(count.value).fill("").map(renderDot)]);
      }
    };
    useExpose({
      prev: prev2,
      next,
      state,
      resize,
      swipeTo
    });
    linkChildren({
      size,
      props: props10,
      count,
      activeIndicator
    });
    watch(() => props10.initialSwipe, (value) => initialize(+value));
    watch(count, () => initialize(state.active));
    watch([count, () => props10.autoplay], autoplay);
    watch([windowSize.width, windowSize.height], resize);
    watch(usePageVisibility(), (visible) => {
      if (visible === "visible") {
        autoplay();
      } else {
        stopAutoplay();
      }
    });
    onMounted(initialize);
    onActivated(() => initialize(state.active));
    onPopupReopen(() => initialize(state.active));
    onDeactivated(stopAutoplay);
    onBeforeUnmount(stopAutoplay);
    return () => createVNode("div", {
      "ref": root2,
      "class": bem32()
    }, [createVNode("div", {
      "style": trackStyle.value,
      "class": bem32("track", {
        vertical: props10.vertical
      }),
      "onTouchstart": onTouchStart,
      "onTouchmove": onTouchMove,
      "onTouchend": onTouchEnd,
      "onTouchcancel": onTouchEnd
    }, [slots.default == null ? void 0 : slots.default()]), renderIndicator()]);
  }
});

// node_modules/vant/es/swipe/index.js
var Swipe = withInstall(Swipe_default);

// node_modules/vant/es/tabs/TabsContent.js
var [name36, bem33] = createNamespace("tabs");
var TabsContent_default = defineComponent({
  name: name36,
  props: {
    inited: Boolean,
    animated: Boolean,
    swipeable: Boolean,
    lazyRender: Boolean,
    count: {
      type: Number,
      required: true
    },
    duration: {
      type: [Number, String],
      required: true
    },
    currentIndex: {
      type: Number,
      required: true
    }
  },
  emits: ["change"],
  setup(props10, {
    emit,
    slots
  }) {
    var swipeRef = ref();
    var onChange = (index) => emit("change", index);
    var renderChildren = () => {
      var Content = slots.default == null ? void 0 : slots.default();
      if (props10.animated || props10.swipeable) {
        return createVNode(Swipe, {
          "ref": swipeRef,
          "loop": false,
          "class": bem33("track"),
          "duration": +props10.duration * 1e3,
          "touchable": props10.swipeable,
          "lazyRender": props10.lazyRender,
          "showIndicators": false,
          "onChange": onChange
        }, {
          default: () => [Content]
        });
      }
      return Content;
    };
    var swipeToCurrentTab = (index) => {
      var swipe = swipeRef.value;
      if (swipe && swipe.state.active !== index) {
        swipe.swipeTo(index, {
          immediate: !props10.inited
        });
      }
    };
    watch(() => props10.currentIndex, swipeToCurrentTab);
    onMounted(() => {
      swipeToCurrentTab(props10.currentIndex);
    });
    return () => createVNode("div", {
      "class": bem33("content", {
        animated: props10.animated || props10.swipeable
      })
    }, [renderChildren()]);
  }
});

// node_modules/vant/es/tabs/Tabs.js
var [name37, bem34] = createNamespace("tabs");
var TABS_KEY = Symbol(name37);
var props3 = {
  color: String,
  border: Boolean,
  sticky: Boolean,
  animated: Boolean,
  ellipsis: truthProp,
  swipeable: Boolean,
  scrollspy: Boolean,
  background: String,
  lazyRender: truthProp,
  lineWidth: [Number, String],
  lineHeight: [Number, String],
  beforeChange: Function,
  titleActiveColor: String,
  titleInactiveColor: String,
  type: {
    type: String,
    default: "line"
  },
  active: {
    type: [Number, String],
    default: 0
  },
  duration: {
    type: [Number, String],
    default: 0.3
  },
  offsetTop: {
    type: [Number, String],
    default: 0
  },
  swipeThreshold: {
    type: [Number, String],
    default: 5
  }
};
var Tabs_default = defineComponent({
  name: name37,
  props: props3,
  emits: ["click", "change", "scroll", "disabled", "rendered", "update:active"],
  setup(props10, {
    emit,
    slots
  }) {
    var tabHeight;
    var lockScroll;
    var stickyFixed;
    var root2 = ref();
    var navRef = ref();
    var wrapRef = ref();
    var windowSize = useWindowSize();
    var scroller = useScrollParent(root2);
    var [titleRefs, setTitleRefs] = useRefs();
    var {
      children,
      linkChildren
    } = useChildren(TABS_KEY);
    var state = reactive({
      inited: false,
      position: "",
      lineStyle: {},
      currentIndex: -1
    });
    var scrollable = computed(() => children.length > props10.swipeThreshold || !props10.ellipsis);
    var navStyle = computed(() => ({
      borderColor: props10.color,
      background: props10.background
    }));
    var getTabName = (tab, index) => {
      var _tab$name;
      return (_tab$name = tab.name) != null ? _tab$name : index;
    };
    var currentName = computed(() => {
      var activeTab = children[state.currentIndex];
      if (activeTab) {
        return getTabName(activeTab, state.currentIndex);
      }
    });
    var offsetTopPx = computed(() => unitToPx(props10.offsetTop));
    var scrollOffset = computed(() => {
      if (props10.sticky) {
        return offsetTopPx.value + tabHeight;
      }
      return 0;
    });
    var scrollIntoView = (immediate) => {
      var nav = navRef.value;
      var titles = titleRefs.value;
      if (!scrollable.value || !nav || !titles || !titles[state.currentIndex]) {
        return;
      }
      var title = titles[state.currentIndex].$el;
      var to = title.offsetLeft - (nav.offsetWidth - title.offsetWidth) / 2;
      scrollLeftTo(nav, to, immediate ? 0 : +props10.duration);
    };
    var setLine = () => {
      var shouldAnimate = state.inited;
      nextTick(() => {
        var titles = titleRefs.value;
        if (!titles || !titles[state.currentIndex] || props10.type !== "line" || isHidden(root2.value)) {
          return;
        }
        var title = titles[state.currentIndex].$el;
        var {
          lineWidth,
          lineHeight
        } = props10;
        var left2 = title.offsetLeft + title.offsetWidth / 2;
        var lineStyle = {
          width: addUnit(lineWidth),
          backgroundColor: props10.color,
          transform: "translateX(" + left2 + "px) translateX(-50%)"
        };
        if (shouldAnimate) {
          lineStyle.transitionDuration = props10.duration + "s";
        }
        if (isDef(lineHeight)) {
          var height = addUnit(lineHeight);
          lineStyle.height = height;
          lineStyle.borderRadius = height;
        }
        state.lineStyle = lineStyle;
      });
    };
    var findAvailableTab = (index) => {
      var diff = index < state.currentIndex ? -1 : 1;
      while (index >= 0 && index < children.length) {
        if (!children[index].disabled) {
          return index;
        }
        index += diff;
      }
    };
    var setCurrentIndex = (currentIndex) => {
      var newIndex = findAvailableTab(currentIndex);
      if (!isDef(newIndex)) {
        return;
      }
      var newTab = children[newIndex];
      var newName = getTabName(newTab, newIndex);
      var shouldEmitChange = state.currentIndex !== null;
      state.currentIndex = newIndex;
      if (newName !== props10.active) {
        emit("update:active", newName);
        if (shouldEmitChange) {
          emit("change", newName, newTab.title);
        }
      }
    };
    var setCurrentIndexByName = (name96) => {
      var matched = children.find((tab, index2) => getTabName(tab, index2) === name96);
      var index = matched ? children.indexOf(matched) : 0;
      setCurrentIndex(index);
    };
    var scrollToCurrentContent = (immediate = false) => {
      if (props10.scrollspy) {
        var target = children[state.currentIndex].$el;
        if (target && scroller.value) {
          var to = getElementTop(target, scroller.value) - scrollOffset.value;
          lockScroll = true;
          scrollTopTo(scroller.value, to, immediate ? 0 : +props10.duration, () => {
            lockScroll = false;
          });
        }
      }
    };
    var onClick = (item, index) => {
      var {
        title,
        disabled
      } = children[index];
      var name96 = getTabName(children[index], index);
      if (disabled) {
        emit("disabled", name96, title);
      } else {
        callInterceptor({
          interceptor: props10.beforeChange,
          args: [name96],
          done: () => {
            setCurrentIndex(index);
            scrollToCurrentContent();
          }
        });
        emit("click", name96, title);
        route(item);
      }
    };
    var onStickyScroll = (params) => {
      stickyFixed = params.isFixed;
      emit("scroll", params);
    };
    var scrollTo = (name96) => {
      nextTick(() => {
        setCurrentIndexByName(name96);
        scrollToCurrentContent(true);
      });
    };
    var getCurrentIndexOnScroll = () => {
      for (var index = 0; index < children.length; index++) {
        var top2 = getVisibleTop(children[index].$el);
        if (top2 > scrollOffset.value) {
          return index === 0 ? 0 : index - 1;
        }
      }
      return children.length - 1;
    };
    var onScroll = () => {
      if (props10.scrollspy && !lockScroll) {
        var index = getCurrentIndexOnScroll();
        setCurrentIndex(index);
      }
    };
    var renderNav = () => children.map((item, index) => createVNode(TabsTitle_default, {
      "ref": setTitleRefs(index),
      "dot": item.dot,
      "type": props10.type,
      "badge": item.badge,
      "title": item.title,
      "color": props10.color,
      "style": item.titleStyle,
      "class": item.titleClass,
      "isActive": index === state.currentIndex,
      "disabled": item.disabled,
      "scrollable": scrollable.value,
      "renderTitle": item.$slots.title,
      "activeColor": props10.titleActiveColor,
      "inactiveColor": props10.titleInactiveColor,
      "onClick": () => {
        onClick(item, index);
      }
    }, null));
    var renderHeader = () => {
      var _slots$navLeft, _slots$navRight;
      var {
        type,
        border
      } = props10;
      return createVNode("div", {
        "ref": wrapRef,
        "class": [bem34("wrap", {
          scrollable: scrollable.value
        }), {
          [BORDER_TOP_BOTTOM]: type === "line" && border
        }]
      }, [createVNode("div", {
        "ref": navRef,
        "role": "tablist",
        "class": bem34("nav", [type, {
          complete: scrollable.value
        }]),
        "style": navStyle.value
      }, [(_slots$navLeft = slots["nav-left"]) == null ? void 0 : _slots$navLeft.call(slots), renderNav(), type === "line" && createVNode("div", {
        "class": bem34("line"),
        "style": state.lineStyle
      }, null), (_slots$navRight = slots["nav-right"]) == null ? void 0 : _slots$navRight.call(slots)])]);
    };
    watch([() => props10.color, windowSize.width], setLine);
    watch(() => props10.active, (value) => {
      if (value !== currentName.value) {
        setCurrentIndexByName(value);
      }
    });
    watch(() => children.length, () => {
      if (state.inited) {
        setCurrentIndexByName(props10.active);
        setLine();
        nextTick(() => {
          scrollIntoView(true);
        });
      }
    });
    watch(() => state.currentIndex, () => {
      scrollIntoView();
      setLine();
      if (stickyFixed && !props10.scrollspy) {
        setRootScrollTop(Math.ceil(getElementTop(root2.value) - offsetTopPx.value));
      }
    });
    var init = () => {
      setCurrentIndexByName(props10.active);
      nextTick(() => {
        state.inited = true;
        tabHeight = getVisibleHeight(wrapRef.value);
        scrollIntoView(true);
      });
    };
    var onRendered = (name96, title) => emit("rendered", name96, title);
    useExpose({
      resize: setLine,
      scrollTo
    });
    onActivated(setLine);
    onPopupReopen(setLine);
    onMountedOrActivated(init);
    useEventListener("scroll", onScroll, {
      target: scroller
    });
    linkChildren({
      props: props10,
      setLine,
      onRendered,
      currentName,
      scrollIntoView
    });
    return () => {
      var _slots$navBottom, _slots$navBottom2;
      return createVNode("div", {
        "ref": root2,
        "class": bem34([props10.type])
      }, [props10.sticky ? createVNode(Sticky, {
        "container": root2.value,
        "offsetTop": offsetTopPx.value,
        "onScroll": onStickyScroll
      }, {
        default: () => [renderHeader(), (_slots$navBottom = slots["nav-bottom"]) == null ? void 0 : _slots$navBottom.call(slots)]
      }) : [renderHeader(), (_slots$navBottom2 = slots["nav-bottom"]) == null ? void 0 : _slots$navBottom2.call(slots)], createVNode(TabsContent_default, {
        "count": children.length,
        "inited": state.inited,
        "animated": props10.animated,
        "duration": props10.duration,
        "swipeable": props10.swipeable,
        "lazyRender": props10.lazyRender,
        "currentIndex": state.currentIndex,
        "onChange": setCurrentIndex
      }, {
        default: () => [slots.default == null ? void 0 : slots.default()]
      })]);
    };
  }
});

// node_modules/vant/es/composables/use-tab-status.js
init_vue_runtime_esm_bundler();
var TAB_STATUS_KEY = Symbol();
function useTabStatus() {
  return inject(TAB_STATUS_KEY, null);
}

// node_modules/vant/es/swipe-item/SwipeItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name38, bem35] = createNamespace("swipe-item");
var SwipeItem_default = defineComponent({
  name: name38,
  setup(props10, {
    slots
  }) {
    var rendered;
    var state = reactive({
      offset: 0,
      inited: false,
      mounted: false
    });
    var {
      parent,
      index
    } = useParent(SWIPE_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <SwipeItem> must be a child component of <Swipe>.");
      }
      return;
    }
    var style2 = computed(() => {
      var style3 = {};
      var {
        vertical
      } = parent.props;
      if (parent.size.value) {
        style3[vertical ? "height" : "width"] = parent.size.value + "px";
      }
      if (state.offset) {
        style3.transform = "translate" + (vertical ? "Y" : "X") + "(" + state.offset + "px)";
      }
      return style3;
    });
    var shouldRender = computed(() => {
      var {
        loop,
        lazyRender
      } = parent.props;
      if (!lazyRender || rendered) {
        return true;
      }
      if (!state.mounted) {
        return false;
      }
      var active = parent.activeIndicator.value;
      var maxActive = parent.count.value - 1;
      var prevActive = active === 0 && loop ? maxActive : active - 1;
      var nextActive = active === maxActive && loop ? 0 : active + 1;
      rendered = index.value === active || index.value === prevActive || index.value === nextActive;
      return rendered;
    });
    var setOffset = (offset2) => {
      state.offset = offset2;
    };
    onMounted(() => {
      nextTick(() => {
        state.mounted = true;
      });
    });
    useExpose({
      setOffset
    });
    return () => createVNode("div", {
      "class": bem35(),
      "style": style2.value
    }, [shouldRender.value ? slots.default == null ? void 0 : slots.default() : null]);
  }
});

// node_modules/vant/es/swipe-item/index.js
var SwipeItem = withInstall(SwipeItem_default);

// node_modules/vant/es/tab/Tab.js
var [name39, bem36] = createNamespace("tab");
var Tab_default = defineComponent({
  name: name39,
  props: extend({}, routeProps, {
    dot: Boolean,
    name: [Number, String],
    badge: [Number, String],
    title: String,
    disabled: Boolean,
    titleClass: unknownProp,
    titleStyle: [String, Object]
  }),
  setup(props10, {
    slots
  }) {
    var inited = ref(false);
    var {
      parent,
      index
    } = useParent(TABS_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <Tab> must be a child component of <Tabs>.");
      }
      return;
    }
    var getName = () => {
      var _props$name;
      return (_props$name = props10.name) != null ? _props$name : index.value;
    };
    var init = () => {
      inited.value = true;
      if (parent.props.lazyRender) {
        nextTick(() => {
          parent.onRendered(getName(), props10.title);
        });
      }
    };
    var active = computed(() => {
      var isActive = getName() === parent.currentName.value;
      if (isActive && !inited.value) {
        init();
      }
      return isActive;
    });
    watch(() => props10.title, () => {
      parent.setLine();
      parent.scrollIntoView();
    });
    provide(TAB_STATUS_KEY, active);
    return () => {
      var {
        animated,
        swipeable,
        scrollspy,
        lazyRender
      } = parent.props;
      if (!slots.default && !animated) {
        return;
      }
      var show = scrollspy || active.value;
      if (animated || swipeable) {
        return createVNode(SwipeItem, {
          "role": "tabpanel",
          "aria-hidden": !active.value,
          "class": bem36("pane-wrapper", {
            inactive: !active.value
          })
        }, {
          default: () => [createVNode("div", {
            "class": bem36("pane")
          }, [slots.default == null ? void 0 : slots.default()])]
        });
      }
      var shouldRender = inited.value || scrollspy || !lazyRender;
      var Content = shouldRender ? slots.default == null ? void 0 : slots.default() : null;
      return withDirectives(createVNode("div", {
        "role": "tabpanel",
        "class": bem36("pane")
      }, [Content]), [[vShow, show]]);
    };
  }
});

// node_modules/vant/es/tab/index.js
var Tab = withInstall(Tab_default);

// node_modules/vant/es/tabs/index.js
var Tabs = withInstall(Tabs_default);

// node_modules/vant/es/cascader/Cascader.js
var [name40, bem37, t7] = createNamespace("cascader");
var Cascader_default = defineComponent({
  name: name40,
  props: {
    title: String,
    closeable: truthProp,
    swipeable: truthProp,
    modelValue: [Number, String],
    fieldNames: Object,
    placeholder: String,
    activeColor: String,
    options: {
      type: Array,
      default: () => []
    },
    closeIcon: {
      type: String,
      default: "cross"
    }
  },
  emits: ["close", "change", "finish", "update:modelValue", "click-tab"],
  setup(props10, {
    slots,
    emit
  }) {
    var state = reactive({
      tabs: [],
      activeTab: 0
    });
    var {
      text: textKey,
      value: valueKey,
      children: childrenKey
    } = extend({
      text: "text",
      value: "value",
      children: "children"
    }, props10.fieldNames);
    var getSelectedOptionsByValue = (options, value) => {
      for (var i = 0; i < options.length; i++) {
        var option = options[i];
        if (option[valueKey] === value) {
          return [option];
        }
        if (option[childrenKey]) {
          var selectedOptions = getSelectedOptionsByValue(option[childrenKey], value);
          if (selectedOptions) {
            return [option, ...selectedOptions];
          }
        }
      }
    };
    var updateTabs = () => {
      if (props10.modelValue || props10.modelValue === 0) {
        var selectedOptions = getSelectedOptionsByValue(props10.options, props10.modelValue);
        if (selectedOptions) {
          var optionsCursor = props10.options;
          state.tabs = selectedOptions.map((option) => {
            var tab = {
              options: optionsCursor,
              selectedOption: option
            };
            var next = optionsCursor.find((item) => item[valueKey] === option[valueKey]);
            if (next) {
              optionsCursor = next[childrenKey];
            }
            return tab;
          });
          if (optionsCursor) {
            state.tabs.push({
              options: optionsCursor,
              selectedOption: null
            });
          }
          nextTick(() => {
            state.activeTab = state.tabs.length - 1;
          });
          return;
        }
      }
      state.tabs = [{
        options: props10.options,
        selectedOption: null
      }];
    };
    var onSelect = (option, tabIndex) => {
      if (option.disabled) {
        return;
      }
      state.tabs[tabIndex].selectedOption = option;
      if (state.tabs.length > tabIndex + 1) {
        state.tabs = state.tabs.slice(0, tabIndex + 1);
      }
      if (option[childrenKey]) {
        var nextTab = {
          options: option[childrenKey],
          selectedOption: null
        };
        if (state.tabs[tabIndex + 1]) {
          state.tabs[tabIndex + 1] = nextTab;
        } else {
          state.tabs.push(nextTab);
        }
        nextTick(() => {
          state.activeTab++;
        });
      }
      var selectedOptions = state.tabs.map((tab) => tab.selectedOption).filter(Boolean);
      var eventParams = {
        value: option[valueKey],
        tabIndex,
        selectedOptions
      };
      emit("update:modelValue", option[valueKey]);
      emit("change", eventParams);
      if (!option[childrenKey]) {
        emit("finish", eventParams);
      }
    };
    var onClose = () => emit("close");
    var onClickTab = (tabIndex, title) => {
      emit("click-tab", tabIndex, title);
    };
    var renderHeader = () => createVNode("div", {
      "class": bem37("header")
    }, [createVNode("h2", {
      "class": bem37("title")
    }, [slots.title ? slots.title() : props10.title]), props10.closeable ? createVNode(Icon, {
      "name": props10.closeIcon,
      "class": bem37("close-icon"),
      "onClick": onClose
    }, null) : null]);
    var renderOptions = (options, selectedOption, tabIndex) => {
      var renderOption = (option) => {
        var isSelected = selectedOption && option[valueKey] === selectedOption[valueKey];
        var color = option.color || (isSelected ? props10.activeColor : void 0);
        return createVNode("li", {
          "class": [bem37("option", {
            selected: isSelected,
            disabled: option.disabled
          }), option.className],
          "style": {
            color
          },
          "onClick": () => onSelect(option, tabIndex)
        }, [createVNode("span", null, [option[textKey]]), isSelected ? createVNode(Icon, {
          "name": "success",
          "class": bem37("selected-icon")
        }, null) : null]);
      };
      return createVNode("ul", {
        "class": bem37("options")
      }, [options.map(renderOption)]);
    };
    var renderTab = (tab, tabIndex) => {
      var {
        options,
        selectedOption
      } = tab;
      var title = selectedOption ? selectedOption[textKey] : props10.placeholder || t7("select");
      return createVNode(Tab, {
        "title": title,
        "titleClass": bem37("tab", {
          unselected: !selectedOption
        })
      }, {
        default: () => [renderOptions(options, selectedOption, tabIndex)]
      });
    };
    var renderTabs = () => createVNode(Tabs, {
      "active": state.activeTab,
      "onUpdate:active": ($event) => state.activeTab = $event,
      "animated": true,
      "class": bem37("tabs"),
      "color": props10.activeColor,
      "swipeThreshold": 0,
      "swipeable": props10.swipeable,
      "onClick": onClickTab
    }, {
      default: () => [state.tabs.map(renderTab)]
    });
    updateTabs();
    watch(() => props10.options, updateTabs, {
      deep: true
    });
    watch(() => props10.modelValue, (value) => {
      if (value || value === 0) {
        var values = state.tabs.map((tab) => {
          var _tab$selectedOption;
          return (_tab$selectedOption = tab.selectedOption) == null ? void 0 : _tab$selectedOption[valueKey];
        });
        if (values.includes(value)) {
          return;
        }
      }
      updateTabs();
    });
    return () => createVNode("div", {
      "class": bem37()
    }, [renderHeader(), renderTabs()]);
  }
});

// node_modules/vant/es/cascader/index.js
var Cascader = withInstall(Cascader_default);

// node_modules/vant/es/cell-group/CellGroup.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name41, bem38] = createNamespace("cell-group");
var CellGroup_default = defineComponent({
  name: name41,
  inheritAttrs: false,
  props: {
    title: String,
    inset: Boolean,
    border: truthProp
  },
  setup(props10, {
    slots,
    attrs
  }) {
    var renderGroup = () => createVNode("div", mergeProps({
      "class": [bem38({
        inset: props10.inset
      }), {
        [BORDER_TOP_BOTTOM]: props10.border && !props10.inset
      }]
    }, attrs), [slots.default == null ? void 0 : slots.default()]);
    var renderTitle = () => createVNode("div", {
      "class": bem38("title", {
        inset: props10.inset
      })
    }, [slots.title ? slots.title() : props10.title]);
    return () => {
      if (props10.title || slots.title) {
        return createVNode(Fragment, null, [renderTitle(), renderGroup()]);
      }
      return renderGroup();
    };
  }
});

// node_modules/vant/es/cell-group/index.js
var CellGroup = withInstall(CellGroup_default);

// node_modules/vant/es/checkbox/Checkbox.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/checkbox-group/CheckboxGroup.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name42, bem39] = createNamespace("checkbox-group");
var CHECKBOX_GROUP_KEY = Symbol(name42);
var props4 = {
  max: [Number, String],
  disabled: Boolean,
  direction: String,
  iconSize: [Number, String],
  checkedColor: String,
  modelValue: {
    type: Array,
    default: () => []
  }
};
var CheckboxGroup_default = defineComponent({
  name: name42,
  props: props4,
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      children,
      linkChildren
    } = useChildren(CHECKBOX_GROUP_KEY);
    var updateValue = (value) => emit("update:modelValue", value);
    var toggleAll = (options = {}) => {
      if (typeof options === "boolean") {
        options = {
          checked: options
        };
      }
      var {
        checked,
        skipDisabled
      } = options;
      var checkedChildren = children.filter((item) => {
        if (!item.props.bindGroup) {
          return false;
        }
        if (item.props.disabled && skipDisabled) {
          return item.checked.value;
        }
        return checked != null ? checked : !item.checked.value;
      });
      var names = checkedChildren.map((item) => item.name);
      updateValue(names);
    };
    watch(() => props10.modelValue, (value) => emit("change", value));
    useExpose({
      toggleAll
    });
    useLinkField(() => props10.modelValue);
    linkChildren({
      props: props10,
      updateValue
    });
    return () => createVNode("div", {
      "class": bem39([props10.direction])
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/checkbox/Checkbox.js
var [name43, bem40] = createNamespace("checkbox");
var Checkbox_default = defineComponent({
  name: name43,
  props: extend({}, checkerProps, {
    bindGroup: truthProp
  }),
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      parent
    } = useParent(CHECKBOX_GROUP_KEY);
    var setParentValue = (checked2) => {
      var {
        name: name96
      } = props10;
      var {
        max,
        modelValue
      } = parent.props;
      var value = modelValue.slice();
      if (checked2) {
        var overlimit = max && value.length >= max;
        if (!overlimit && !value.includes(name96)) {
          value.push(name96);
          if (props10.bindGroup) {
            parent.updateValue(value);
          }
        }
      } else {
        var index = value.indexOf(name96);
        if (index !== -1) {
          value.splice(index, 1);
          if (props10.bindGroup) {
            parent.updateValue(value);
          }
        }
      }
    };
    var checked = computed(() => {
      if (parent && props10.bindGroup) {
        return parent.props.modelValue.indexOf(props10.name) !== -1;
      }
      return !!props10.modelValue;
    });
    var toggle = (newValue = !checked.value) => {
      if (parent && props10.bindGroup) {
        setParentValue(newValue);
      } else {
        emit("update:modelValue", newValue);
      }
    };
    watch(() => props10.modelValue, (value) => emit("change", value));
    useExpose({
      toggle,
      props: props10,
      checked
    });
    useLinkField(() => props10.modelValue);
    return () => createVNode(Checker_default, mergeProps({
      "bem": bem40,
      "role": "checkbox",
      "parent": parent,
      "checked": checked.value,
      "onToggle": toggle
    }, props10), pick(slots, ["default", "icon"]));
  }
});

// node_modules/vant/es/checkbox/index.js
var Checkbox = withInstall(Checkbox_default);

// node_modules/vant/es/checkbox-group/index.js
var CheckboxGroup = withInstall(CheckboxGroup_default);

// node_modules/vant/es/circle/Circle.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name44, bem41] = createNamespace("circle");
var uid = 0;
function format(rate) {
  return Math.min(Math.max(+rate, 0), 100);
}
function getPath(clockwise, viewBoxSize) {
  var sweepFlag = clockwise ? 1 : 0;
  return "M " + viewBoxSize / 2 + " " + viewBoxSize / 2 + " m 0, -500 a 500, 500 0 1, " + sweepFlag + " 0, 1000 a 500, 500 0 1, " + sweepFlag + " 0, -1000";
}
var Circle_default = defineComponent({
  name: name44,
  props: {
    text: String,
    size: [Number, String],
    color: [String, Object],
    clockwise: truthProp,
    layerColor: String,
    strokeLinecap: String,
    currentRate: {
      type: Number,
      default: 0
    },
    speed: {
      type: [Number, String],
      default: 0
    },
    fill: {
      type: String,
      default: "none"
    },
    rate: {
      type: [Number, String],
      default: 100
    },
    strokeWidth: {
      type: [Number, String],
      default: 40
    }
  },
  emits: ["update:currentRate"],
  setup(props10, {
    emit,
    slots
  }) {
    var id = "van-circle-" + uid++;
    var viewBoxSize = computed(() => +props10.strokeWidth + 1e3);
    var path = computed(() => getPath(props10.clockwise, viewBoxSize.value));
    watch(() => props10.rate, (rate) => {
      var rafId2;
      var startTime = Date.now();
      var startRate = props10.currentRate;
      var endRate = format(rate);
      var duration = Math.abs((startRate - endRate) * 1e3 / +props10.speed);
      var animate = () => {
        var now = Date.now();
        var progress = Math.min((now - startTime) / duration, 1);
        var rate2 = progress * (endRate - startRate) + startRate;
        emit("update:currentRate", format(parseFloat(rate2.toFixed(1))));
        if (endRate > startRate ? rate2 < endRate : rate2 > endRate) {
          rafId2 = raf(animate);
        }
      };
      if (props10.speed) {
        if (rafId2) {
          cancelRaf(rafId2);
        }
        rafId2 = raf(animate);
      } else {
        emit("update:currentRate", endRate);
      }
    }, {
      immediate: true
    });
    var renderHover = () => {
      var PERIMETER = 3140;
      var {
        strokeWidth,
        currentRate,
        strokeLinecap
      } = props10;
      var offset2 = PERIMETER * currentRate / 100;
      var color = isObject(props10.color) ? "url(#" + id + ")" : props10.color;
      var style2 = {
        stroke: color,
        strokeWidth: +strokeWidth + 1 + "px",
        strokeLinecap,
        strokeDasharray: offset2 + "px " + PERIMETER + "px"
      };
      return createVNode("path", {
        "d": path.value,
        "style": style2,
        "class": bem41("hover"),
        "stroke": color
      }, null);
    };
    var renderLayer = () => {
      var style2 = {
        fill: props10.fill,
        stroke: props10.layerColor,
        strokeWidth: props10.strokeWidth + "px"
      };
      return createVNode("path", {
        "class": bem41("layer"),
        "style": style2,
        "d": path.value
      }, null);
    };
    var renderGradient = () => {
      var {
        color
      } = props10;
      if (!isObject(color)) {
        return;
      }
      var Stops = Object.keys(color).sort((a, b) => parseFloat(a) - parseFloat(b)).map((key, index) => createVNode("stop", {
        "key": index,
        "offset": key,
        "stop-color": color[key]
      }, null));
      return createVNode("defs", null, [createVNode("linearGradient", {
        "id": id,
        "x1": "100%",
        "y1": "0%",
        "x2": "0%",
        "y2": "0%"
      }, [Stops])]);
    };
    var renderText = () => {
      if (slots.default) {
        return slots.default();
      }
      if (props10.text) {
        return createVNode("div", {
          "class": bem41("text")
        }, [props10.text]);
      }
    };
    return () => createVNode("div", {
      "class": bem41(),
      "style": getSizeStyle(props10.size)
    }, [createVNode("svg", {
      "viewBox": "0 0 " + viewBoxSize.value + " " + viewBoxSize.value
    }, [renderGradient(), renderLayer(), renderHover()]), renderText()]);
  }
});

// node_modules/vant/es/circle/index.js
var Circle = withInstall(Circle_default);

// node_modules/vant/es/col/Col.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/row/Row.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name45, bem42] = createNamespace("row");
var ROW_KEY = Symbol(name45);
var Row_default = defineComponent({
  name: name45,
  props: {
    wrap: truthProp,
    align: String,
    justify: String,
    tag: {
      type: String,
      default: "div"
    },
    gutter: {
      type: [Number, String],
      default: 0
    }
  },
  setup(props10, {
    slots
  }) {
    var {
      children,
      linkChildren
    } = useChildren(ROW_KEY);
    var groups = computed(() => {
      var groups2 = [[]];
      var totalSpan = 0;
      children.forEach((child, index) => {
        totalSpan += Number(child.span);
        if (totalSpan > 24) {
          groups2.push([index]);
          totalSpan -= 24;
        } else {
          groups2[groups2.length - 1].push(index);
        }
      });
      return groups2;
    });
    var spaces = computed(() => {
      var gutter = Number(props10.gutter);
      var spaces2 = [];
      if (!gutter) {
        return spaces2;
      }
      groups.value.forEach((group) => {
        var averagePadding = gutter * (group.length - 1) / group.length;
        group.forEach((item, index) => {
          if (index === 0) {
            spaces2.push({
              right: averagePadding
            });
          } else {
            var left2 = gutter - spaces2[item - 1].right;
            var right2 = averagePadding - left2;
            spaces2.push({
              left: left2,
              right: right2
            });
          }
        });
      });
      return spaces2;
    });
    linkChildren({
      spaces
    });
    return () => {
      var {
        tag,
        wrap,
        align,
        justify
      } = props10;
      return createVNode(tag, {
        "class": bem42({
          ["align-" + align]: align,
          ["justify-" + justify]: justify,
          nowrap: !wrap
        })
      }, {
        default: () => [slots.default == null ? void 0 : slots.default()]
      });
    };
  }
});

// node_modules/vant/es/col/Col.js
var [name46, bem43] = createNamespace("col");
var Col_default = defineComponent({
  name: name46,
  props: {
    offset: [Number, String],
    tag: {
      type: String,
      default: "div"
    },
    span: {
      type: [Number, String],
      default: 0
    }
  },
  setup(props10, {
    slots
  }) {
    var {
      parent,
      index
    } = useParent(ROW_KEY);
    var style2 = computed(() => {
      if (!parent) {
        return;
      }
      var {
        spaces
      } = parent;
      if (spaces && spaces.value && spaces.value[index.value]) {
        var {
          left: left2,
          right: right2
        } = spaces.value[index.value];
        return {
          paddingLeft: left2 ? left2 + "px" : null,
          paddingRight: right2 ? right2 + "px" : null
        };
      }
    });
    return () => {
      var {
        tag,
        span,
        offset: offset2
      } = props10;
      return createVNode(tag, {
        "style": style2.value,
        "class": bem43({
          [span]: span,
          ["offset-" + offset2]: offset2
        })
      }, {
        default: () => [slots.default == null ? void 0 : slots.default()]
      });
    };
  }
});

// node_modules/vant/es/col/index.js
var Col = withInstall(Col_default);

// node_modules/vant/es/collapse/Collapse.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name47, bem44] = createNamespace("collapse");
var COLLAPSE_KEY = Symbol(name47);
function validateModelValue(modelValue, accordion) {
  if (accordion && Array.isArray(modelValue)) {
    console.error('[Vant] Collapse: "v-model" should not be Array in accordion mode');
    return false;
  }
  if (!accordion && !Array.isArray(modelValue)) {
    console.error('[Vant] Collapse: "v-model" should be Array in non-accordion mode');
    return false;
  }
  return true;
}
var Collapse_default = defineComponent({
  name: name47,
  props: {
    border: truthProp,
    accordion: Boolean,
    modelValue: {
      type: [String, Number, Array],
      default: ""
    }
  },
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      linkChildren
    } = useChildren(COLLAPSE_KEY);
    var updateName = (name96) => {
      emit("change", name96);
      emit("update:modelValue", name96);
    };
    var toggle = (name96, expanded) => {
      var {
        accordion,
        modelValue
      } = props10;
      if (accordion) {
        updateName(name96 === modelValue ? "" : name96);
      } else if (expanded) {
        updateName(modelValue.concat(name96));
      } else {
        updateName(modelValue.filter((activeName) => activeName !== name96));
      }
    };
    var isExpanded = (name96) => {
      var {
        accordion,
        modelValue
      } = props10;
      if (!validateModelValue(modelValue, accordion)) {
        return false;
      }
      return accordion ? modelValue === name96 : modelValue.includes(name96);
    };
    linkChildren({
      toggle,
      isExpanded
    });
    return () => createVNode("div", {
      "class": [bem44(), {
        [BORDER_TOP_BOTTOM]: props10.border
      }]
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/collapse/index.js
var Collapse = withInstall(Collapse_default);

// node_modules/vant/es/collapse-item/CollapseItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name48, bem45] = createNamespace("collapse-item");
var CELL_SLOTS = ["icon", "title", "value", "label", "right-icon"];
var CollapseItem_default = defineComponent({
  name: name48,
  props: extend({}, cellProps, {
    name: [Number, String],
    isLink: truthProp,
    disabled: Boolean,
    readonly: Boolean
  }),
  setup(props10, {
    slots
  }) {
    var wrapperRef = ref();
    var contentRef = ref();
    var {
      parent,
      index
    } = useParent(COLLAPSE_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <CollapseItem> must be a child component of <Collapse>.");
      }
      return;
    }
    var name96 = computed(() => {
      var _props$name;
      return (_props$name = props10.name) != null ? _props$name : index.value;
    });
    var expanded = computed(() => parent.isExpanded(name96.value));
    var show = ref(expanded.value);
    var lazyRender = useLazyRender(show);
    var onTransitionEnd = () => {
      if (!expanded.value) {
        show.value = false;
      } else {
        wrapperRef.value.style.height = "";
      }
    };
    watch(expanded, (value, oldValue) => {
      if (oldValue === null) {
        return;
      }
      if (value) {
        show.value = true;
      }
      var tick = value ? nextTick : raf;
      tick(() => {
        if (!contentRef.value || !wrapperRef.value) {
          return;
        }
        var {
          offsetHeight
        } = contentRef.value;
        if (offsetHeight) {
          var contentHeight = offsetHeight + "px";
          wrapperRef.value.style.height = value ? "0" : contentHeight;
          doubleRaf(() => {
            wrapperRef.value.style.height = value ? contentHeight : "0";
          });
        } else {
          onTransitionEnd();
        }
      });
    });
    var toggle = (newValue = !expanded.value) => {
      parent.toggle(name96.value, newValue);
    };
    var onClickTitle = () => {
      if (!props10.disabled && !props10.readonly) {
        toggle();
      }
    };
    var renderTitle = () => {
      var {
        border,
        disabled,
        readonly
      } = props10;
      var attrs = pick(props10, Object.keys(cellProps));
      if (readonly) {
        attrs.isLink = false;
      }
      if (disabled || readonly) {
        attrs.clickable = false;
      }
      return createVNode(Cell, mergeProps({
        "role": "button",
        "class": bem45("title", {
          disabled,
          expanded: expanded.value,
          borderless: !border
        }),
        "aria-expanded": String(expanded.value),
        "onClick": onClickTitle
      }, attrs), pick(slots, CELL_SLOTS));
    };
    var renderContent = lazyRender(() => withDirectives(createVNode("div", {
      "ref": wrapperRef,
      "class": bem45("wrapper"),
      "onTransitionend": onTransitionEnd
    }, [createVNode("div", {
      "ref": contentRef,
      "class": bem45("content")
    }, [slots.default == null ? void 0 : slots.default()])]), [[vShow, show.value]]));
    useExpose({
      toggle
    });
    return () => createVNode("div", {
      "class": [bem45({
        border: index.value && props10.border
      })]
    }, [renderTitle(), renderContent()]);
  }
});

// node_modules/vant/es/collapse-item/index.js
var CollapseItem = withInstall(CollapseItem_default);

// node_modules/vant/es/config-provider/index.js
var ConfigProvider = withInstall(ConfigProvider_default);

// node_modules/vant/es/contact-card/ContactCard.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name49, bem46, t8] = createNamespace("contact-card");
var ContactCard_default = defineComponent({
  name: name49,
  props: {
    tel: String,
    name: String,
    addText: String,
    editable: truthProp,
    type: {
      type: String,
      default: "add"
    }
  },
  emits: ["click"],
  setup(props10, {
    emit
  }) {
    var onClick = (event) => {
      if (props10.editable) {
        emit("click", event);
      }
    };
    var renderContent = () => {
      if (props10.type === "add") {
        return props10.addText || t8("addText");
      }
      return [createVNode("div", null, [t8("name") + "\uFF1A" + props10.name]), createVNode("div", null, [t8("tel") + "\uFF1A" + props10.tel])];
    };
    return () => createVNode(Cell, {
      "center": true,
      "icon": props10.type === "edit" ? "contact" : "add-square",
      "class": bem46([props10.type]),
      "border": false,
      "isLink": props10.editable,
      "valueClass": bem46("value"),
      "onClick": onClick
    }, {
      value: renderContent
    });
  }
});

// node_modules/vant/es/contact-card/index.js
var ContactCard = withInstall(ContactCard_default);

// node_modules/vant/es/contact-edit/ContactEdit.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/form/Form.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name50, bem47] = createNamespace("form");
var Form_default = defineComponent({
  name: name50,
  props: {
    colon: Boolean,
    disabled: Boolean,
    readonly: Boolean,
    showError: Boolean,
    labelWidth: [Number, String],
    labelAlign: String,
    inputAlign: String,
    scrollToError: Boolean,
    validateFirst: Boolean,
    submitOnEnter: truthProp,
    showErrorMessage: truthProp,
    errorMessageAlign: String,
    validateTrigger: {
      type: String,
      default: "onBlur"
    }
  },
  emits: ["submit", "failed"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      children,
      linkChildren
    } = useChildren(FORM_KEY);
    var getFieldsByNames = (names) => {
      if (names) {
        return children.filter((field) => names.includes(field.name));
      }
      return children;
    };
    var validateSeq = (names) => new Promise((resolve, reject) => {
      var errors = [];
      var fields = getFieldsByNames(names);
      fields.reduce((promise, field) => promise.then(() => {
        if (!errors.length) {
          return field.validate().then((error) => {
            if (error) {
              errors.push(error);
            }
          });
        }
      }), Promise.resolve()).then(() => {
        if (errors.length) {
          reject(errors);
        } else {
          resolve();
        }
      });
    });
    var validateAll = (names) => new Promise((resolve, reject) => {
      var fields = getFieldsByNames(names);
      Promise.all(fields.map((item) => item.validate())).then((errors) => {
        errors = errors.filter(Boolean);
        if (errors.length) {
          reject(errors);
        } else {
          resolve();
        }
      });
    });
    var validateField = (name96) => {
      var matched = children.find((item) => item.name === name96);
      if (matched) {
        return new Promise((resolve, reject) => {
          matched.validate().then((error) => {
            if (error) {
              reject(error);
            } else {
              resolve();
            }
          });
        });
      }
      return Promise.reject();
    };
    var validate = (name96) => {
      if (typeof name96 === "string") {
        return validateField(name96);
      }
      return props10.validateFirst ? validateSeq(name96) : validateAll(name96);
    };
    var resetValidation = (name96) => {
      if (typeof name96 === "string") {
        name96 = [name96];
      }
      var fields = getFieldsByNames(name96);
      fields.forEach((item) => {
        item.resetValidation();
      });
    };
    var scrollToField = (name96, options) => {
      children.some((item) => {
        if (item.name === name96) {
          item.$el.scrollIntoView(options);
          return true;
        }
        return false;
      });
    };
    var getValues = () => children.reduce((form, field) => {
      form[field.name] = field.formValue.value;
      return form;
    }, {});
    var submit = () => {
      var values = getValues();
      validate().then(() => emit("submit", values)).catch((errors) => {
        emit("failed", {
          values,
          errors
        });
        if (props10.scrollToError && errors[0].name) {
          scrollToField(errors[0].name);
        }
      });
    };
    var onSubmit = (event) => {
      event.preventDefault();
      submit();
    };
    linkChildren({
      props: props10
    });
    useExpose({
      submit,
      validate,
      scrollToField,
      resetValidation
    });
    return () => createVNode("form", {
      "class": bem47(),
      "onSubmit": onSubmit
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/form/index.js
var Form = withInstall(Form_default);

// node_modules/vant/es/contact-edit/ContactEdit.js
var [name51, bem48, t9] = createNamespace("contact-edit");
var DEFAULT_CONTACT = {
  tel: "",
  name: ""
};
var ContactEdit_default = defineComponent({
  name: name51,
  props: {
    isEdit: Boolean,
    isSaving: Boolean,
    isDeleting: Boolean,
    showSetDefault: Boolean,
    setDefaultLabel: String,
    contactInfo: {
      type: Object,
      default: () => extend({}, DEFAULT_CONTACT)
    },
    telValidator: {
      type: Function,
      default: isMobile
    }
  },
  emits: ["save", "delete", "change-default"],
  setup(props10, {
    emit
  }) {
    var contact = reactive(extend({}, DEFAULT_CONTACT, props10.contactInfo));
    var onSave = () => {
      if (!props10.isSaving) {
        emit("save", contact);
      }
    };
    var onDelete = () => {
      Dialog.confirm({
        title: t9("confirmDelete")
      }).then(() => emit("delete", contact));
    };
    var renderButtons = () => createVNode("div", {
      "class": bem48("buttons")
    }, [createVNode(Button, {
      "block": true,
      "round": true,
      "type": "danger",
      "text": t9("save"),
      "loading": props10.isSaving,
      "nativeType": "submit"
    }, null), props10.isEdit && createVNode(Button, {
      "block": true,
      "round": true,
      "text": t9("delete"),
      "loading": props10.isDeleting,
      "onClick": onDelete
    }, null)]);
    var renderSwitch = () => createVNode(Switch, {
      "modelValue": contact.isDefault,
      "onUpdate:modelValue": ($event) => contact.isDefault = $event,
      "size": 24,
      "onChange": (checked) => emit("change-default", checked)
    }, null);
    var renderSetDefault = () => {
      if (props10.showSetDefault) {
        return createVNode(Cell, {
          "title": props10.setDefaultLabel,
          "class": bem48("switch-cell"),
          "border": false
        }, {
          "right-icon": renderSwitch
        });
      }
    };
    watch(() => props10.contactInfo, (value) => extend(contact, DEFAULT_CONTACT, value));
    return () => createVNode(Form, {
      "class": bem48(),
      "onSubmit": onSave
    }, {
      default: () => [createVNode("div", {
        "class": bem48("fields")
      }, [createVNode(Field, {
        "modelValue": contact.name,
        "onUpdate:modelValue": ($event) => contact.name = $event,
        "clearable": true,
        "label": t9("name"),
        "rules": [{
          required: true,
          message: t9("nameInvalid")
        }],
        "maxlength": "30",
        "placeholder": t9("nameEmpty")
      }, null), createVNode(Field, {
        "modelValue": contact.tel,
        "onUpdate:modelValue": ($event) => contact.tel = $event,
        "clearable": true,
        "type": "tel",
        "label": t9("tel"),
        "rules": [{
          validator: props10.telValidator,
          message: t9("telInvalid")
        }],
        "placeholder": t9("telEmpty")
      }, null)]), renderSetDefault(), renderButtons()]
    });
  }
});

// node_modules/vant/es/contact-edit/index.js
var ContactEdit = withInstall(ContactEdit_default);

// node_modules/vant/es/contact-list/ContactList.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name52, bem49, t10] = createNamespace("contact-list");
var ContactList_default = defineComponent({
  name: name52,
  props: {
    list: Array,
    addText: String,
    modelValue: unknownProp,
    defaultTagText: String
  },
  emits: ["add", "edit", "select", "update:modelValue"],
  setup(props10, {
    emit
  }) {
    var renderItem = (item, index) => {
      var onClick = () => {
        emit("update:modelValue", item.id);
        emit("select", item, index);
      };
      var renderRightIcon = () => createVNode(Radio, {
        "class": bem49("radio"),
        "name": item.id,
        "iconSize": 16
      }, null);
      var renderEditIcon = () => createVNode(Icon, {
        "name": "edit",
        "class": bem49("edit"),
        "onClick": (event) => {
          event.stopPropagation();
          emit("edit", item, index);
        }
      }, null);
      var renderContent = () => {
        var nodes = [item.name + "\uFF0C" + item.tel];
        if (item.isDefault && props10.defaultTagText) {
          nodes.push(createVNode(Tag, {
            "type": "danger",
            "round": true,
            "class": bem49("item-tag")
          }, {
            default: () => [props10.defaultTagText]
          }));
        }
        return nodes;
      };
      return createVNode(Cell, {
        "key": item.id,
        "isLink": true,
        "center": true,
        "class": bem49("item"),
        "valueClass": bem49("item-value"),
        "onClick": onClick
      }, {
        icon: renderEditIcon,
        value: renderContent,
        "right-icon": renderRightIcon
      });
    };
    return () => createVNode("div", {
      "class": bem49()
    }, [createVNode(RadioGroup, {
      "modelValue": props10.modelValue,
      "class": bem49("group")
    }, {
      default: () => [props10.list && props10.list.map(renderItem)]
    }), createVNode("div", {
      "class": bem49("bottom")
    }, [createVNode(Button, {
      "round": true,
      "block": true,
      "type": "danger",
      "class": bem49("add"),
      "text": props10.addText || t10("addText"),
      "onClick": () => emit("add")
    }, null)])]);
  }
});

// node_modules/vant/es/contact-list/index.js
var ContactList = withInstall(ContactList_default);

// node_modules/vant/es/count-down/CountDown.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/count-down/utils.js
function parseFormat(format3, currentTime) {
  var {
    days
  } = currentTime;
  var {
    hours,
    minutes,
    seconds,
    milliseconds
  } = currentTime;
  if (format3.includes("DD")) {
    format3 = format3.replace("DD", padZero(days));
  } else {
    hours += days * 24;
  }
  if (format3.includes("HH")) {
    format3 = format3.replace("HH", padZero(hours));
  } else {
    minutes += hours * 60;
  }
  if (format3.includes("mm")) {
    format3 = format3.replace("mm", padZero(minutes));
  } else {
    seconds += minutes * 60;
  }
  if (format3.includes("ss")) {
    format3 = format3.replace("ss", padZero(seconds));
  } else {
    milliseconds += seconds * 1e3;
  }
  if (format3.includes("S")) {
    var ms = padZero(milliseconds, 3);
    if (format3.includes("SSS")) {
      format3 = format3.replace("SSS", ms);
    } else if (format3.includes("SS")) {
      format3 = format3.replace("SS", ms.slice(0, 2));
    } else {
      format3 = format3.replace("S", ms.charAt(0));
    }
  }
  return format3;
}

// node_modules/vant/es/count-down/CountDown.js
var [name53, bem50] = createNamespace("count-down");
var CountDown_default = defineComponent({
  name: name53,
  props: {
    autoStart: truthProp,
    millisecond: Boolean,
    time: {
      type: [Number, String],
      default: 0
    },
    format: {
      type: String,
      default: "HH:mm:ss"
    }
  },
  emits: ["change", "finish"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      start: start2,
      pause,
      reset,
      current
    } = useCountDown({
      time: +props10.time,
      millisecond: props10.millisecond,
      onChange: (current2) => emit("change", current2),
      onFinish: () => emit("finish")
    });
    var timeText = computed(() => parseFormat(props10.format, current.value));
    var resetTime = () => {
      reset(+props10.time);
      if (props10.autoStart) {
        start2();
      }
    };
    watch(() => props10.time, resetTime, {
      immediate: true
    });
    useExpose({
      start: start2,
      pause,
      reset: resetTime
    });
    return () => createVNode("div", {
      "class": bem50()
    }, [slots.default ? slots.default(current.value) : timeText.value]);
  }
});

// node_modules/vant/es/count-down/index.js
var CountDown = withInstall(CountDown_default);

// node_modules/vant/es/coupon/Coupon.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name54, bem51, t11] = createNamespace("coupon");
function getDate(timeStamp) {
  var date = new Date(timeStamp * 1e3);
  return date.getFullYear() + "." + padZero(date.getMonth() + 1) + "." + padZero(date.getDate());
}
function formatDiscount(discount) {
  return (discount / 10).toFixed(discount % 10 === 0 ? 0 : 1);
}
function formatAmount(amount) {
  return (amount / 100).toFixed(amount % 100 === 0 ? 0 : amount % 10 === 0 ? 1 : 2);
}
var Coupon_default = defineComponent({
  name: name54,
  props: {
    chosen: Boolean,
    disabled: Boolean,
    coupon: {
      type: Object,
      required: true
    },
    currency: {
      type: String,
      default: "\xA5"
    }
  },
  setup(props10) {
    var validPeriod = computed(() => {
      var {
        startAt,
        endAt
      } = props10.coupon;
      return getDate(startAt) + " - " + getDate(endAt);
    });
    var faceAmount = computed(() => {
      var {
        coupon,
        currency
      } = props10;
      if (coupon.valueDesc) {
        return [coupon.valueDesc, createVNode("span", null, [coupon.unitDesc || ""])];
      }
      if (coupon.denominations) {
        var denominations = formatAmount(coupon.denominations);
        return [createVNode("span", null, [currency]), " " + denominations];
      }
      if (coupon.discount) {
        return t11("discount", formatDiscount(coupon.discount));
      }
      return "";
    });
    var conditionMessage = computed(() => {
      var condition = formatAmount(props10.coupon.originCondition || 0);
      return condition === "0" ? t11("unlimited") : t11("condition", condition);
    });
    return () => {
      var {
        chosen,
        coupon,
        disabled
      } = props10;
      var description = disabled && coupon.reason || coupon.description;
      return createVNode("div", {
        "class": bem51({
          disabled
        })
      }, [createVNode("div", {
        "class": bem51("content")
      }, [createVNode("div", {
        "class": bem51("head")
      }, [createVNode("h2", {
        "class": bem51("amount")
      }, [faceAmount.value]), createVNode("p", {
        "class": bem51("condition")
      }, [coupon.condition || conditionMessage.value])]), createVNode("div", {
        "class": bem51("body")
      }, [createVNode("p", {
        "class": bem51("name")
      }, [coupon.name]), createVNode("p", {
        "class": bem51("valid")
      }, [validPeriod.value]), !disabled && createVNode(Checkbox, {
        "class": bem51("corner"),
        "modelValue": chosen
      }, null)])]), description && createVNode("p", {
        "class": bem51("description")
      }, [description])]);
    };
  }
});

// node_modules/vant/es/coupon/index.js
var Coupon = withInstall(Coupon_default);

// node_modules/vant/es/coupon-cell/CouponCell.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name55, bem52, t12] = createNamespace("coupon-cell");
function formatValue(coupons, chosenCoupon, currency) {
  var coupon = coupons[+chosenCoupon];
  if (coupon) {
    var value = 0;
    if (isDef(coupon.value)) {
      ({
        value
      } = coupon);
    } else if (isDef(coupon.denominations)) {
      value = coupon.denominations;
    }
    return "-" + currency + " " + (value / 100).toFixed(2);
  }
  return coupons.length === 0 ? t12("tips") : t12("count", coupons.length);
}
var CouponCell_default = defineComponent({
  name: name55,
  props: {
    title: String,
    border: truthProp,
    editable: truthProp,
    coupons: {
      type: Array,
      default: () => []
    },
    currency: {
      type: String,
      default: "\xA5"
    },
    chosenCoupon: {
      type: [Number, String],
      default: -1
    }
  },
  setup(props10) {
    return () => {
      var selected = props10.coupons[+props10.chosenCoupon];
      var value = formatValue(props10.coupons, props10.chosenCoupon, props10.currency);
      return createVNode(Cell, {
        "class": bem52(),
        "value": value,
        "title": props10.title || t12("title"),
        "border": props10.border,
        "isLink": props10.editable,
        "valueClass": bem52("value", {
          selected
        })
      }, null);
    };
  }
});

// node_modules/vant/es/coupon-cell/index.js
var CouponCell = withInstall(CouponCell_default);

// node_modules/vant/es/coupon-list/CouponList.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name56, bem53, t13] = createNamespace("coupon-list");
var EMPTY_IMAGE = "https://img.yzcdn.cn/vant/coupon-empty.png";
var CouponList_default = defineComponent({
  name: name56,
  props: {
    showCount: truthProp,
    enabledTitle: String,
    disabledTitle: String,
    showExchangeBar: truthProp,
    showCloseButton: truthProp,
    closeButtonText: String,
    inputPlaceholder: String,
    exchangeButtonText: String,
    exchangeButtonLoading: Boolean,
    exchangeButtonDisabled: Boolean,
    code: {
      type: String,
      default: ""
    },
    exchangeMinLength: {
      type: Number,
      default: 1
    },
    chosenCoupon: {
      type: Number,
      default: -1
    },
    coupons: {
      type: Array,
      default: () => []
    },
    disabledCoupons: {
      type: Array,
      default: () => []
    },
    displayedCouponIndex: {
      type: Number,
      default: -1
    },
    currency: {
      type: String,
      default: "\xA5"
    },
    emptyImage: {
      type: String,
      default: EMPTY_IMAGE
    }
  },
  emits: ["change", "exchange", "update:code"],
  setup(props10, {
    emit,
    slots
  }) {
    var [couponRefs, setCouponRefs] = useRefs();
    var state = reactive({
      tab: 0,
      code: props10.code
    });
    var {
      height: windowHeight
    } = useWindowSize();
    var buttonDisabled = computed(() => !props10.exchangeButtonLoading && (props10.exchangeButtonDisabled || !state.code || state.code.length < props10.exchangeMinLength));
    var listStyle = computed(() => ({
      height: windowHeight.value - (props10.showExchangeBar ? 140 : 94) + "px"
    }));
    var onExchange = () => {
      emit("exchange", state.code);
      if (!props10.code) {
        state.code = "";
      }
    };
    var scrollToCoupon = (index) => {
      nextTick(() => {
        if (couponRefs.value[index]) {
          couponRefs.value[index].scrollIntoView();
        }
      });
    };
    var renderEmpty = () => createVNode("div", {
      "class": bem53("empty")
    }, [createVNode("img", {
      "src": props10.emptyImage
    }, null), createVNode("p", null, [t13("empty")])]);
    var renderExchangeBar = () => {
      if (props10.showExchangeBar) {
        return createVNode("div", {
          "class": bem53("exchange-bar")
        }, [createVNode(Field, {
          "modelValue": state.code,
          "onUpdate:modelValue": ($event) => state.code = $event,
          "clearable": true,
          "border": false,
          "class": bem53("field"),
          "placeholder": props10.inputPlaceholder || t13("placeholder"),
          "maxlength": "20"
        }, null), createVNode(Button, {
          "plain": true,
          "type": "danger",
          "class": bem53("exchange"),
          "text": props10.exchangeButtonText || t13("exchange"),
          "loading": props10.exchangeButtonLoading,
          "disabled": buttonDisabled.value,
          "onClick": onExchange
        }, null)]);
      }
    };
    var renderCouponTab = () => {
      var _slots$listFooter;
      var {
        coupons
      } = props10;
      var count = props10.showCount ? " (" + coupons.length + ")" : "";
      var title = (props10.enabledTitle || t13("enable")) + count;
      return createVNode(Tab, {
        "title": title
      }, {
        default: () => [createVNode("div", {
          "class": bem53("list", {
            "with-bottom": props10.showCloseButton
          }),
          "style": listStyle.value
        }, [coupons.map((coupon, index) => createVNode(Coupon, {
          "key": coupon.id,
          "ref": setCouponRefs(index),
          "coupon": coupon,
          "chosen": index === props10.chosenCoupon,
          "currency": props10.currency,
          "onClick": () => emit("change", index)
        }, null)), !coupons.length && renderEmpty(), (_slots$listFooter = slots["list-footer"]) == null ? void 0 : _slots$listFooter.call(slots)])]
      });
    };
    var renderDisabledTab = () => {
      var _slots$disabledList;
      var {
        disabledCoupons
      } = props10;
      var count = props10.showCount ? " (" + disabledCoupons.length + ")" : "";
      var title = (props10.disabledTitle || t13("disabled")) + count;
      return createVNode(Tab, {
        "title": title
      }, {
        default: () => [createVNode("div", {
          "class": bem53("list", {
            "with-bottom": props10.showCloseButton
          }),
          "style": listStyle.value
        }, [disabledCoupons.map((coupon) => createVNode(Coupon, {
          "disabled": true,
          "key": coupon.id,
          "coupon": coupon,
          "currency": props10.currency
        }, null)), !disabledCoupons.length && renderEmpty(), (_slots$disabledList = slots["disabled-list-footer"]) == null ? void 0 : _slots$disabledList.call(slots)])]
      });
    };
    watch(() => props10.code, (value) => {
      state.code = value;
    });
    watch(() => state.code, (value) => emit("update:code", value));
    watch(() => props10.displayedCouponIndex, scrollToCoupon);
    onMounted(() => {
      scrollToCoupon(props10.displayedCouponIndex);
    });
    return () => createVNode("div", {
      "class": bem53()
    }, [renderExchangeBar(), createVNode(Tabs, {
      "modelValue": state.tab,
      "onUpdate:modelValue": ($event) => state.tab = $event,
      "class": bem53("tab"),
      "border": false
    }, {
      default: () => [renderCouponTab(), renderDisabledTab()]
    }), createVNode("div", {
      "class": bem53("bottom")
    }, [withDirectives(createVNode(Button, {
      "round": true,
      "block": true,
      "type": "danger",
      "class": bem53("close"),
      "text": props10.closeButtonText || t13("close"),
      "onClick": () => emit("change", -1)
    }, null), [[vShow, props10.showCloseButton]])])]);
  }
});

// node_modules/vant/es/coupon-list/index.js
var CouponList = withInstall(CouponList_default);

// node_modules/vant/es/datetime-picker/DatetimePicker.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/datetime-picker/TimePicker.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name57] = createNamespace("time-picker");
var TimePicker_default = defineComponent({
  name: name57,
  props: extend({}, sharedProps, {
    modelValue: String,
    minHour: {
      type: [Number, String],
      default: 0
    },
    maxHour: {
      type: [Number, String],
      default: 23
    },
    minMinute: {
      type: [Number, String],
      default: 0
    },
    maxMinute: {
      type: [Number, String],
      default: 59
    }
  }),
  emits: ["confirm", "cancel", "change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var formatValue2 = (value) => {
      var {
        minHour,
        maxHour,
        maxMinute,
        minMinute
      } = props10;
      if (!value) {
        value = padZero(minHour) + ":" + padZero(minMinute);
      }
      var [hour, minute] = value.split(":");
      hour = padZero(clamp(+hour, +minHour, +maxHour));
      minute = padZero(clamp(+minute, +minMinute, +maxMinute));
      return hour + ":" + minute;
    };
    var picker = ref();
    var currentDate = ref(formatValue2(props10.modelValue));
    var ranges = computed(() => [{
      type: "hour",
      range: [+props10.minHour, +props10.maxHour]
    }, {
      type: "minute",
      range: [+props10.minMinute, +props10.maxMinute]
    }]);
    var originColumns = computed(() => ranges.value.map(({
      type,
      range: rangeArr
    }) => {
      var values = times(rangeArr[1] - rangeArr[0] + 1, (index) => padZero(rangeArr[0] + index));
      if (props10.filter) {
        values = props10.filter(type, values);
      }
      return {
        type,
        values
      };
    }));
    var columns = computed(() => originColumns.value.map((column) => ({
      values: column.values.map((value) => props10.formatter(column.type, value))
    })));
    var updateColumnValue = () => {
      var pair = currentDate.value.split(":");
      var values = [props10.formatter("hour", pair[0]), props10.formatter("minute", pair[1])];
      nextTick(() => {
        picker.value.setValues(values);
      });
    };
    var updateInnerValue = () => {
      var [hourIndex, minuteIndex] = picker.value.getIndexes();
      var [hourColumn, minuteColumn] = originColumns.value;
      var hour = hourColumn.values[hourIndex] || hourColumn.values[0];
      var minute = minuteColumn.values[minuteIndex] || minuteColumn.values[0];
      currentDate.value = formatValue2(hour + ":" + minute);
      updateColumnValue();
    };
    var onConfirm = () => emit("confirm", currentDate.value);
    var onCancel = () => emit("cancel");
    var onChange = () => {
      updateInnerValue();
      nextTick(() => {
        nextTick(() => emit("change", currentDate.value));
      });
    };
    onMounted(() => {
      updateColumnValue();
      nextTick(updateInnerValue);
    });
    watch(columns, updateColumnValue);
    watch(() => [props10.filter, props10.maxHour, props10.minMinute, props10.maxMinute], updateInnerValue);
    watch(() => props10.minHour, () => {
      nextTick(updateInnerValue);
    });
    watch(currentDate, (value) => emit("update:modelValue", value));
    watch(() => props10.modelValue, (value) => {
      value = formatValue2(value);
      if (value !== currentDate.value) {
        currentDate.value = value;
        updateColumnValue();
      }
    });
    useExpose({
      getPicker: () => picker.value
    });
    return () => createVNode(Picker, mergeProps({
      "ref": picker,
      "columns": columns.value,
      "onChange": onChange,
      "onCancel": onCancel,
      "onConfirm": onConfirm
    }, pick(props10, pickerKeys)), slots);
  }
});

// node_modules/vant/es/datetime-picker/DatePicker.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var currentYear = new Date().getFullYear();
var [name58] = createNamespace("date-picker");
var DatePicker_default = defineComponent({
  name: name58,
  props: extend({}, sharedProps, {
    modelValue: Date,
    type: {
      type: String,
      default: "datetime"
    },
    minDate: {
      type: Date,
      default: () => new Date(currentYear - 10, 0, 1),
      validator: isDate
    },
    maxDate: {
      type: Date,
      default: () => new Date(currentYear + 10, 11, 31),
      validator: isDate
    }
  }),
  emits: ["confirm", "cancel", "change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var formatValue2 = (value) => {
      if (isDate(value)) {
        var timestamp = clamp(value.getTime(), props10.minDate.getTime(), props10.maxDate.getTime());
        return new Date(timestamp);
      }
      return void 0;
    };
    var picker = ref();
    var currentDate = ref(formatValue2(props10.modelValue));
    var getBoundary = (type, value) => {
      var boundary = props10[type + "Date"];
      var year = boundary.getFullYear();
      var month = 1;
      var date = 1;
      var hour = 0;
      var minute = 0;
      if (type === "max") {
        month = 12;
        date = getMonthEndDay(value.getFullYear(), value.getMonth() + 1);
        hour = 23;
        minute = 59;
      }
      if (value.getFullYear() === year) {
        month = boundary.getMonth() + 1;
        if (value.getMonth() + 1 === month) {
          date = boundary.getDate();
          if (value.getDate() === date) {
            hour = boundary.getHours();
            if (value.getHours() === hour) {
              minute = boundary.getMinutes();
            }
          }
        }
      }
      return {
        [type + "Year"]: year,
        [type + "Month"]: month,
        [type + "Date"]: date,
        [type + "Hour"]: hour,
        [type + "Minute"]: minute
      };
    };
    var ranges = computed(() => {
      var {
        maxYear,
        maxDate,
        maxMonth,
        maxHour,
        maxMinute
      } = getBoundary("max", currentDate.value || props10.minDate);
      var {
        minYear,
        minDate,
        minMonth,
        minHour,
        minMinute
      } = getBoundary("min", currentDate.value || props10.minDate);
      var result = [{
        type: "year",
        range: [minYear, maxYear]
      }, {
        type: "month",
        range: [minMonth, maxMonth]
      }, {
        type: "day",
        range: [minDate, maxDate]
      }, {
        type: "hour",
        range: [minHour, maxHour]
      }, {
        type: "minute",
        range: [minMinute, maxMinute]
      }];
      switch (props10.type) {
        case "date":
          result = result.slice(0, 3);
          break;
        case "year-month":
          result = result.slice(0, 2);
          break;
        case "month-day":
          result = result.slice(1, 3);
          break;
        case "datehour":
          result = result.slice(0, 4);
          break;
      }
      if (props10.columnsOrder) {
        var columnsOrder = props10.columnsOrder.concat(result.map((column) => column.type));
        result.sort((a, b) => columnsOrder.indexOf(a.type) - columnsOrder.indexOf(b.type));
      }
      return result;
    });
    var originColumns = computed(() => ranges.value.map(({
      type,
      range: rangeArr
    }) => {
      var values = times(rangeArr[1] - rangeArr[0] + 1, (index) => padZero(rangeArr[0] + index));
      if (props10.filter) {
        values = props10.filter(type, values);
      }
      return {
        type,
        values
      };
    }));
    var columns = computed(() => originColumns.value.map((column) => ({
      values: column.values.map((value) => props10.formatter(column.type, value))
    })));
    var updateColumnValue = () => {
      var value = currentDate.value || props10.minDate;
      var {
        formatter
      } = props10;
      var values = originColumns.value.map((column) => {
        switch (column.type) {
          case "year":
            return formatter("year", "" + value.getFullYear());
          case "month":
            return formatter("month", padZero(value.getMonth() + 1));
          case "day":
            return formatter("day", padZero(value.getDate()));
          case "hour":
            return formatter("hour", padZero(value.getHours()));
          case "minute":
            return formatter("minute", padZero(value.getMinutes()));
          default:
            return null;
        }
      });
      nextTick(() => {
        picker.value.setValues(values);
      });
    };
    var updateInnerValue = () => {
      var {
        type
      } = props10;
      var indexes = picker.value.getIndexes();
      var getValue = (type2) => {
        var index = 0;
        originColumns.value.forEach((column, columnIndex) => {
          if (type2 === column.type) {
            index = columnIndex;
          }
        });
        var {
          values
        } = originColumns.value[index];
        return getTrueValue(values[indexes[index]]);
      };
      var year;
      var month;
      var day;
      if (type === "month-day") {
        year = (currentDate.value || props10.minDate).getFullYear();
        month = getValue("month");
        day = getValue("day");
      } else {
        year = getValue("year");
        month = getValue("month");
        day = type === "year-month" ? 1 : getValue("day");
      }
      var maxDay = getMonthEndDay(year, month);
      day = day > maxDay ? maxDay : day;
      var hour = 0;
      var minute = 0;
      if (type === "datehour") {
        hour = getValue("hour");
      }
      if (type === "datetime") {
        hour = getValue("hour");
        minute = getValue("minute");
      }
      var value = new Date(year, month - 1, day, hour, minute);
      currentDate.value = formatValue2(value);
    };
    var onConfirm = () => {
      emit("update:modelValue", currentDate.value);
      emit("confirm", currentDate.value);
    };
    var onCancel = () => emit("cancel");
    var onChange = () => {
      updateInnerValue();
      nextTick(() => {
        nextTick(() => emit("change", currentDate.value));
      });
    };
    onMounted(() => {
      updateColumnValue();
      nextTick(updateInnerValue);
    });
    watch(columns, updateColumnValue);
    watch(currentDate, (value, oldValue) => emit("update:modelValue", oldValue ? value : null));
    watch(() => [props10.filter, props10.maxDate], updateInnerValue);
    watch(() => props10.minDate, () => {
      nextTick(updateInnerValue);
    });
    watch(() => props10.modelValue, (value) => {
      var _currentDate$value;
      value = formatValue2(value);
      if (value && value.valueOf() !== ((_currentDate$value = currentDate.value) == null ? void 0 : _currentDate$value.valueOf())) {
        currentDate.value = value;
      }
    });
    useExpose({
      getPicker: () => picker.value
    });
    return () => createVNode(Picker, mergeProps({
      "ref": picker,
      "columns": columns.value,
      "onChange": onChange,
      "onCancel": onCancel,
      "onConfirm": onConfirm
    }, pick(props10, pickerKeys)), slots);
  }
});

// node_modules/vant/es/datetime-picker/DatetimePicker.js
var [name59, bem54] = createNamespace("datetime-picker");
var timePickerProps = Object.keys(TimePicker_default.props);
var datePickerProps = Object.keys(DatePicker_default.props);
var DatetimePicker_default = defineComponent({
  name: name59,
  props: extend({}, TimePicker_default.props, DatePicker_default.props, {
    modelValue: [String, Date]
  }),
  setup(props10, {
    attrs,
    slots
  }) {
    var root2 = ref();
    useExpose({
      getPicker: () => {
        var _root$value;
        return (_root$value = root2.value) == null ? void 0 : _root$value.getPicker();
      }
    });
    return () => {
      var isTimePicker = props10.type === "time";
      var Component = isTimePicker ? TimePicker_default : DatePicker_default;
      var inheritProps = pick(props10, isTimePicker ? timePickerProps : datePickerProps);
      return createVNode(Component, mergeProps({
        "ref": root2,
        "class": bem54()
      }, inheritProps, attrs), slots);
    };
  }
});

// node_modules/vant/es/datetime-picker/index.js
var DatetimePicker = withInstall(DatetimePicker_default);

// node_modules/vant/es/divider/Divider.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name60, bem55] = createNamespace("divider");
var Divider_default = defineComponent({
  name: name60,
  props: {
    dashed: Boolean,
    hairline: truthProp,
    contentPosition: {
      type: String,
      default: "center"
    }
  },
  setup(props10, {
    slots
  }) {
    return () => createVNode("div", {
      "role": "separator",
      "class": bem55({
        dashed: props10.dashed,
        hairline: props10.hairline,
        ["content-" + props10.contentPosition]: !!slots.default
      })
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/divider/index.js
var Divider = withInstall(Divider_default);

// node_modules/vant/es/dropdown-item/DropdownItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/dropdown-menu/DropdownMenu.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name61, bem56] = createNamespace("dropdown-menu");
var DROPDOWN_KEY = Symbol(name61);
var props5 = {
  overlay: truthProp,
  zIndex: [Number, String],
  activeColor: String,
  closeOnClickOutside: truthProp,
  closeOnClickOverlay: truthProp,
  duration: {
    type: [Number, String],
    default: 0.2
  },
  direction: {
    type: String,
    default: "down"
  }
};
var DropdownMenu_default = defineComponent({
  name: name61,
  props: props5,
  setup(props10, {
    slots
  }) {
    var root2 = ref();
    var barRef = ref();
    var offset2 = ref(0);
    var {
      children,
      linkChildren
    } = useChildren(DROPDOWN_KEY);
    var scrollParent2 = useScrollParent(root2);
    var opened = computed(() => children.some((item) => item.state.showWrapper));
    var barStyle = computed(() => {
      if (opened.value && isDef(props10.zIndex)) {
        return {
          zIndex: +props10.zIndex + 1
        };
      }
    });
    var onClickAway = () => {
      if (props10.closeOnClickOutside) {
        children.forEach((item) => {
          item.toggle(false);
        });
      }
    };
    var updateOffset = () => {
      if (barRef.value) {
        var rect = useRect(barRef);
        if (props10.direction === "down") {
          offset2.value = rect.bottom;
        } else {
          offset2.value = window.innerHeight - rect.top;
        }
      }
    };
    var onScroll = () => {
      if (opened.value) {
        updateOffset();
      }
    };
    var toggleItem = (active) => {
      children.forEach((item, index) => {
        if (index === active) {
          updateOffset();
          item.toggle();
        } else if (item.state.showPopup) {
          item.toggle(false, {
            immediate: true
          });
        }
      });
    };
    var renderTitle = (item, index) => {
      var {
        showPopup
      } = item.state;
      var {
        disabled,
        titleClass
      } = item;
      return createVNode("div", {
        "role": "button",
        "tabindex": disabled ? -1 : 0,
        "class": bem56("item", {
          disabled
        }),
        "onClick": () => {
          if (!disabled) {
            toggleItem(index);
          }
        }
      }, [createVNode("span", {
        "class": [bem56("title", {
          down: showPopup === (props10.direction === "down"),
          active: showPopup
        }), titleClass],
        "style": {
          color: showPopup ? props10.activeColor : ""
        }
      }, [createVNode("div", {
        "class": "van-ellipsis"
      }, [item.renderTitle()])])]);
    };
    linkChildren({
      props: props10,
      offset: offset2
    });
    useClickAway(root2, onClickAway);
    useEventListener("scroll", onScroll, {
      target: scrollParent2
    });
    return () => createVNode("div", {
      "ref": root2,
      "class": bem56()
    }, [createVNode("div", {
      "ref": barRef,
      "style": barStyle.value,
      "class": bem56("bar", {
        opened: opened.value
      })
    }, [children.map(renderTitle)]), slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/dropdown-item/DropdownItem.js
var [name62, bem57] = createNamespace("dropdown-item");
var DropdownItem_default = defineComponent({
  name: name62,
  props: {
    title: String,
    disabled: Boolean,
    teleport: [String, Object],
    lazyRender: truthProp,
    modelValue: unknownProp,
    titleClass: unknownProp,
    options: {
      type: Array,
      default: () => []
    }
  },
  emits: ["open", "opened", "close", "closed", "change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var state = reactive({
      showPopup: false,
      transition: true,
      showWrapper: false
    });
    var {
      parent
    } = useParent(DROPDOWN_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <DropdownItem> must be a child component of <DropdownMenu>.");
      }
      return;
    }
    var getEmitter = (name96) => () => emit(name96);
    var onOpen = getEmitter("open");
    var onClose = getEmitter("close");
    var onOpened = getEmitter("opened");
    var onClosed = () => {
      state.showWrapper = false;
      emit("closed");
    };
    var onClickWrapper = (event) => {
      if (props10.teleport) {
        event.stopPropagation();
      }
    };
    var toggle = (show = !state.showPopup, options = {}) => {
      if (show === state.showPopup) {
        return;
      }
      state.showPopup = show;
      state.transition = !options.immediate;
      if (show) {
        state.showWrapper = true;
      }
    };
    var renderTitle = () => {
      if (slots.title) {
        return slots.title();
      }
      if (props10.title) {
        return props10.title;
      }
      var match = props10.options.find((option) => option.value === props10.modelValue);
      return match ? match.text : "";
    };
    var renderOption = (option) => {
      var {
        activeColor
      } = parent.props;
      var active = option.value === props10.modelValue;
      var onClick = () => {
        state.showPopup = false;
        if (option.value !== props10.modelValue) {
          emit("update:modelValue", option.value);
          emit("change", option.value);
        }
      };
      var renderIcon = () => {
        if (active) {
          return createVNode(Icon, {
            "class": bem57("icon"),
            "color": activeColor,
            "name": "success"
          }, null);
        }
      };
      return createVNode(Cell, {
        "clickable": true,
        "key": option.value,
        "icon": option.icon,
        "title": option.text,
        "class": bem57("option", {
          active
        }),
        "style": {
          color: active ? activeColor : ""
        },
        "onClick": onClick
      }, {
        value: renderIcon
      });
    };
    var renderContent = () => {
      var {
        offset: offset2
      } = parent;
      var {
        zIndex,
        overlay,
        duration,
        direction,
        closeOnClickOverlay
      } = parent.props;
      var style2 = getZIndexStyle(zIndex);
      if (direction === "down") {
        style2.top = offset2.value + "px";
      } else {
        style2.bottom = offset2.value + "px";
      }
      return withDirectives(createVNode("div", {
        "style": style2,
        "class": bem57([direction]),
        "onClick": onClickWrapper
      }, [createVNode(Popup, {
        "show": state.showPopup,
        "onUpdate:show": ($event) => state.showPopup = $event,
        "class": bem57("content"),
        "overlay": overlay,
        "position": direction === "down" ? "top" : "bottom",
        "duration": state.transition ? duration : 0,
        "lazyRender": props10.lazyRender,
        "overlayStyle": {
          position: "absolute"
        },
        "closeOnClickOverlay": closeOnClickOverlay,
        "onOpen": onOpen,
        "onClose": onClose,
        "onOpened": onOpened,
        "onClosed": onClosed
      }, {
        default: () => [props10.options.map(renderOption), slots.default == null ? void 0 : slots.default()]
      })]), [[vShow, state.showWrapper]]);
    };
    useExpose({
      state,
      toggle,
      renderTitle
    });
    return () => {
      if (props10.teleport) {
        return createVNode(Teleport, {
          "to": props10.teleport
        }, {
          default: () => [renderContent()]
        });
      }
      return renderContent();
    };
  }
});

// node_modules/vant/es/dropdown-item/index.js
var DropdownItem = withInstall(DropdownItem_default);

// node_modules/vant/es/dropdown-menu/index.js
var DropdownMenu = withInstall(DropdownMenu_default);

// node_modules/vant/es/empty/Empty.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/empty/Network.js
init_vue_runtime_esm_bundler();
var prefix = "van-empty-network-";
var renderStop = (color, offset2, opacity) => createVNode("stop", {
  "stop-color": color,
  "offset": offset2 + "%",
  "stop-opacity": opacity
}, null);
var Network = createVNode("svg", {
  "viewBox": "0 0 160 160"
}, [createVNode("defs", null, [createVNode("linearGradient", {
  "id": prefix + "1",
  "x1": "64.022%",
  "y1": "100%",
  "x2": "64.022%",
  "y2": "0%"
}, [renderStop("#FFF", 0, 0.5), renderStop("#F2F3F5", 100)]), createVNode("linearGradient", {
  "id": prefix + "2",
  "x1": "50%",
  "y1": "0%",
  "x2": "50%",
  "y2": "84.459%"
}, [renderStop("#EBEDF0", 0), renderStop("#DCDEE0", 100, 0)]), createVNode("linearGradient", {
  "id": prefix + "3",
  "x1": "100%",
  "y1": "0%",
  "x2": "100%",
  "y2": "100%"
}, [renderStop("#EAEDF0", 0), renderStop("#DCDEE0", 100)]), createVNode("linearGradient", {
  "id": prefix + "4",
  "x1": "100%",
  "y1": "100%",
  "x2": "100%",
  "y2": "0%"
}, [renderStop("#EAEDF0", 0), renderStop("#DCDEE0", 100)]), createVNode("linearGradient", {
  "id": prefix + "5",
  "x1": "0%",
  "y1": "43.982%",
  "x2": "100%",
  "y2": "54.703%"
}, [renderStop("#EAEDF0", 0), renderStop("#DCDEE0", 100)]), createVNode("linearGradient", {
  "id": prefix + "6",
  "x1": "94.535%",
  "y1": "43.837%",
  "x2": "5.465%",
  "y2": "54.948%"
}, [renderStop("#EAEDF0", 0), renderStop("#DCDEE0", 100)]), createVNode("radialGradient", {
  "id": prefix + "7",
  "cx": "50%",
  "cy": "0%",
  "fx": "50%",
  "fy": "0%",
  "r": "100%",
  "gradientTransform": "matrix(0 1 -.54835 0 .5 -.5)"
}, [renderStop("#EBEDF0", 0), renderStop("#FFF", 100, 0)])]), createVNode("g", {
  "fill": "none",
  "fill-rule": "evenodd"
}, [createVNode("g", {
  "opacity": ".8"
}, [createVNode("path", {
  "d": "M0 124V46h20v20h14v58H0z",
  "fill": "url(#" + prefix + "1)",
  "transform": "matrix(-1 0 0 1 36 7)"
}, null), createVNode("path", {
  "d": "M121 8h22.231v14H152v77.37h-31V8z",
  "fill": "url(#" + prefix + "1)",
  "transform": "translate(2 7)"
}, null)]), createVNode("path", {
  "fill": "url(#" + prefix + "7)",
  "d": "M0 139h160v21H0z"
}, null), createVNode("path", {
  "d": "M37 18a7 7 0 013 13.326v26.742c0 1.23-.997 2.227-2.227 2.227h-1.546A2.227 2.227 0 0134 58.068V31.326A7 7 0 0137 18z",
  "fill": "url(#" + prefix + "2)",
  "fill-rule": "nonzero",
  "transform": "translate(43 36)"
}, null), createVNode("g", {
  "opacity": ".6",
  "stroke-linecap": "round",
  "stroke-width": "7"
}, [createVNode("path", {
  "d": "M20.875 11.136a18.868 18.868 0 00-5.284 13.121c0 5.094 2.012 9.718 5.284 13.12",
  "stroke": "url(#" + prefix + "3)",
  "transform": "translate(43 36)"
}, null), createVNode("path", {
  "d": "M9.849 0C3.756 6.225 0 14.747 0 24.146c0 9.398 3.756 17.92 9.849 24.145",
  "stroke": "url(#" + prefix + "3)",
  "transform": "translate(43 36)"
}, null), createVNode("path", {
  "d": "M57.625 11.136a18.868 18.868 0 00-5.284 13.121c0 5.094 2.012 9.718 5.284 13.12",
  "stroke": "url(#" + prefix + "4)",
  "transform": "rotate(-180 76.483 42.257)"
}, null), createVNode("path", {
  "d": "M73.216 0c-6.093 6.225-9.849 14.747-9.849 24.146 0 9.398 3.756 17.92 9.849 24.145",
  "stroke": "url(#" + prefix + "4)",
  "transform": "rotate(-180 89.791 42.146)"
}, null)]), createVNode("g", {
  "transform": "translate(31 105)",
  "fill-rule": "nonzero"
}, [createVNode("rect", {
  "fill": "url(#" + prefix + "5)",
  "width": "98",
  "height": "34",
  "rx": "2"
}, null), createVNode("rect", {
  "fill": "#FFF",
  "x": "9",
  "y": "8",
  "width": "80",
  "height": "18",
  "rx": "1.114"
}, null), createVNode("rect", {
  "fill": "url(#" + prefix + "6)",
  "x": "15",
  "y": "12",
  "width": "18",
  "height": "6",
  "rx": "1.114"
}, null)])])]);

// node_modules/vant/es/empty/Empty.js
var [name63, bem58] = createNamespace("empty");
var PRESET_IMAGES = ["error", "search", "default"];
var Empty_default = defineComponent({
  name: name63,
  props: {
    imageSize: [Number, String],
    description: String,
    image: {
      type: String,
      default: "default"
    }
  },
  setup(props10, {
    slots
  }) {
    var renderImage = () => {
      if (slots.image) {
        return slots.image();
      }
      var {
        image
      } = props10;
      if (image === "network") {
        return Network;
      }
      if (PRESET_IMAGES.includes(image)) {
        image = "https://img.yzcdn.cn/vant/empty-image-" + image + ".png";
      }
      return createVNode("img", {
        "src": image
      }, null);
    };
    var renderDescription = () => {
      var description = slots.description ? slots.description() : props10.description;
      if (description) {
        return createVNode("p", {
          "class": bem58("description")
        }, [description]);
      }
    };
    var renderBottom = () => {
      if (slots.default) {
        return createVNode("div", {
          "class": bem58("bottom")
        }, [slots.default()]);
      }
    };
    return () => createVNode("div", {
      "class": bem58()
    }, [createVNode("div", {
      "class": bem58("image"),
      "style": getSizeStyle(props10.imageSize)
    }, [renderImage()]), renderDescription(), renderBottom()]);
  }
});

// node_modules/vant/es/empty/index.js
var Empty = withInstall(Empty_default);

// node_modules/vant/es/grid/Grid.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name64, bem59] = createNamespace("grid");
var GRID_KEY = Symbol(name64);
var props6 = {
  square: Boolean,
  center: truthProp,
  border: truthProp,
  gutter: [Number, String],
  reverse: Boolean,
  iconSize: [Number, String],
  direction: String,
  clickable: Boolean,
  columnNum: {
    type: [Number, String],
    default: 4
  }
};
var Grid_default = defineComponent({
  name: name64,
  props: props6,
  setup(props10, {
    slots
  }) {
    var {
      linkChildren
    } = useChildren(GRID_KEY);
    linkChildren({
      props: props10
    });
    return () => createVNode("div", {
      "style": {
        paddingLeft: addUnit(props10.gutter)
      },
      "class": [bem59(), {
        [BORDER_TOP]: props10.border && !props10.gutter
      }]
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/grid/index.js
var Grid = withInstall(Grid_default);

// node_modules/vant/es/grid-item/GridItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name65, bem60] = createNamespace("grid-item");
var GridItem_default = defineComponent({
  name: name65,
  props: extend({}, routeProps, {
    dot: Boolean,
    text: String,
    icon: String,
    badge: [Number, String],
    iconPrefix: String,
    iconColor: String
  }),
  setup(props10, {
    slots
  }) {
    var {
      parent,
      index
    } = useParent(GRID_KEY);
    var route2 = useRoute();
    if (!parent) {
      if (true) {
        console.error("[Vant] <GridItem> must be a child component of <Grid>.");
      }
      return;
    }
    var rootStyle = computed(() => {
      var {
        square,
        gutter,
        columnNum
      } = parent.props;
      var percent = 100 / +columnNum + "%";
      var style2 = {
        flexBasis: percent
      };
      if (square) {
        style2.paddingTop = percent;
      } else if (gutter) {
        var gutterValue = addUnit(gutter);
        style2.paddingRight = gutterValue;
        if (index.value >= columnNum) {
          style2.marginTop = gutterValue;
        }
      }
      return style2;
    });
    var contentStyle = computed(() => {
      var {
        square,
        gutter
      } = parent.props;
      if (square && gutter) {
        var gutterValue = addUnit(gutter);
        return {
          right: gutterValue,
          bottom: gutterValue,
          height: "auto"
        };
      }
    });
    var renderIcon = () => {
      if (slots.icon) {
        return createVNode(Badge, {
          "dot": props10.dot,
          "content": props10.badge
        }, {
          default: () => [slots.icon()]
        });
      }
      if (props10.icon) {
        return createVNode(Icon, {
          "dot": props10.dot,
          "name": props10.icon,
          "size": parent.props.iconSize,
          "badge": props10.badge,
          "class": bem60("icon"),
          "classPrefix": props10.iconPrefix,
          "color": props10.iconColor
        }, null);
      }
    };
    var renderText = () => {
      if (slots.text) {
        return slots.text();
      }
      if (props10.text) {
        return createVNode("span", {
          "class": bem60("text")
        }, [props10.text]);
      }
    };
    var renderContent = () => {
      if (slots.default) {
        return slots.default();
      }
      return [renderIcon(), renderText()];
    };
    return () => {
      var {
        center,
        border,
        square,
        gutter,
        reverse,
        direction,
        clickable
      } = parent.props;
      var classes = [bem60("content", [direction, {
        center,
        square,
        reverse,
        clickable,
        surround: border && gutter
      }]), {
        [BORDER]: border
      }];
      return createVNode("div", {
        "class": [bem60({
          square
        })],
        "style": rootStyle.value
      }, [createVNode("div", {
        "role": clickable ? "button" : void 0,
        "class": classes,
        "style": contentStyle.value,
        "tabindex": clickable ? 0 : void 0,
        "onClick": route2
      }, [renderContent()])]);
    };
  }
});

// node_modules/vant/es/grid-item/index.js
var GridItem = withInstall(GridItem_default);

// node_modules/vant/es/image-preview/function-call.js
init_vue_runtime_esm_bundler();

// node_modules/vant/es/image-preview/ImagePreview.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/image-preview/ImagePreviewItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
function getDistance(touches) {
  return Math.sqrt(Math.pow(touches[0].clientX - touches[1].clientX, 2) + Math.pow(touches[0].clientY - touches[1].clientY, 2));
}
var bem61 = createNamespace("image-preview")[1];
var ImagePreviewItem_default = defineComponent({
  props: {
    src: String,
    show: Boolean,
    active: Number,
    minZoom: {
      type: [Number, String],
      required: true
    },
    maxZoom: {
      type: [Number, String],
      required: true
    },
    rootWidth: {
      type: Number,
      required: true
    },
    rootHeight: {
      type: Number,
      required: true
    }
  },
  emits: ["scale", "close"],
  setup(props10, {
    emit
  }) {
    var state = reactive({
      scale: 1,
      moveX: 0,
      moveY: 0,
      moving: false,
      zooming: false,
      imageRatio: 0,
      displayWidth: 0,
      displayHeight: 0
    });
    var touch = useTouch();
    var vertical = computed(() => {
      var {
        rootWidth,
        rootHeight
      } = props10;
      var rootRatio = rootHeight / rootWidth;
      return state.imageRatio > rootRatio;
    });
    var imageStyle = computed(() => {
      var {
        scale,
        moveX,
        moveY,
        moving,
        zooming
      } = state;
      var style2 = {
        transitionDuration: zooming || moving ? "0s" : ".3s"
      };
      if (scale !== 1) {
        var offsetX = moveX / scale;
        var offsetY = moveY / scale;
        style2.transform = "scale(" + scale + ", " + scale + ") translate(" + offsetX + "px, " + offsetY + "px)";
      }
      return style2;
    });
    var maxMoveX = computed(() => {
      if (state.imageRatio) {
        var {
          rootWidth,
          rootHeight
        } = props10;
        var displayWidth = vertical.value ? rootHeight / state.imageRatio : rootWidth;
        return Math.max(0, (state.scale * displayWidth - rootWidth) / 2);
      }
      return 0;
    });
    var maxMoveY = computed(() => {
      if (state.imageRatio) {
        var {
          rootWidth,
          rootHeight
        } = props10;
        var displayHeight = vertical.value ? rootHeight : rootWidth * state.imageRatio;
        return Math.max(0, (state.scale * displayHeight - rootHeight) / 2);
      }
      return 0;
    });
    var setScale = (scale) => {
      scale = clamp(scale, +props10.minZoom, +props10.maxZoom);
      if (scale !== state.scale) {
        state.scale = scale;
        emit("scale", {
          scale,
          index: props10.active
        });
      }
    };
    var resetScale = () => {
      setScale(1);
      state.moveX = 0;
      state.moveY = 0;
    };
    var toggleScale = () => {
      var scale = state.scale > 1 ? 1 : 2;
      setScale(scale);
      state.moveX = 0;
      state.moveY = 0;
    };
    var startMoveX;
    var startMoveY;
    var startScale;
    var startDistance;
    var doubleTapTimer;
    var touchStartTime;
    var onTouchStart = (event) => {
      var {
        touches
      } = event;
      var {
        offsetX
      } = touch;
      touch.start(event);
      startMoveX = state.moveX;
      startMoveY = state.moveY;
      touchStartTime = Date.now();
      state.moving = touches.length === 1 && state.scale !== 1;
      state.zooming = touches.length === 2 && !offsetX.value;
      if (state.zooming) {
        startScale = state.scale;
        startDistance = getDistance(event.touches);
      }
    };
    var onTouchMove = (event) => {
      var {
        touches
      } = event;
      touch.move(event);
      if (state.moving || state.zooming) {
        preventDefault(event, true);
      }
      if (state.moving) {
        var {
          deltaX,
          deltaY
        } = touch;
        var moveX = deltaX.value + startMoveX;
        var moveY = deltaY.value + startMoveY;
        state.moveX = clamp(moveX, -maxMoveX.value, maxMoveX.value);
        state.moveY = clamp(moveY, -maxMoveY.value, maxMoveY.value);
      }
      if (state.zooming && touches.length === 2) {
        var distance = getDistance(touches);
        var scale = startScale * distance / startDistance;
        setScale(scale);
      }
    };
    var checkTap = () => {
      var {
        offsetX,
        offsetY
      } = touch;
      var deltaTime = Date.now() - touchStartTime;
      var TAP_TIME = 250;
      var TAP_OFFSET = 10;
      if (offsetX.value < TAP_OFFSET && offsetY.value < TAP_OFFSET && deltaTime < TAP_TIME) {
        if (doubleTapTimer) {
          clearTimeout(doubleTapTimer);
          doubleTapTimer = null;
          toggleScale();
        } else {
          doubleTapTimer = setTimeout(() => {
            emit("close");
            doubleTapTimer = null;
          }, TAP_TIME);
        }
      }
    };
    var onTouchEnd = (event) => {
      var stopPropagation2 = false;
      if (state.moving || state.zooming) {
        stopPropagation2 = true;
        if (state.moving && startMoveX === state.moveX && startMoveY === state.moveY) {
          stopPropagation2 = false;
        }
        if (!event.touches.length) {
          if (state.zooming) {
            state.moveX = clamp(state.moveX, -maxMoveX.value, maxMoveX.value);
            state.moveY = clamp(state.moveY, -maxMoveY.value, maxMoveY.value);
            state.zooming = false;
          }
          state.moving = false;
          startMoveX = 0;
          startMoveY = 0;
          startScale = 1;
          if (state.scale < 1) {
            resetScale();
          }
        }
      }
      preventDefault(event, stopPropagation2);
      checkTap();
      touch.reset();
    };
    var onLoad = (event) => {
      var {
        naturalWidth,
        naturalHeight
      } = event.target;
      state.imageRatio = naturalHeight / naturalWidth;
    };
    watch(() => props10.active, resetScale);
    watch(() => props10.show, (value) => {
      if (!value) {
        resetScale();
      }
    });
    return () => {
      var imageSlots = {
        loading: () => createVNode(Loading, {
          "type": "spinner"
        }, null)
      };
      return createVNode(SwipeItem, {
        "class": bem61("swipe-item"),
        "onTouchstart": onTouchStart,
        "onTouchmove": onTouchMove,
        "onTouchend": onTouchEnd,
        "onTouchcancel": onTouchEnd
      }, {
        default: () => [createVNode(Image2, {
          "src": props10.src,
          "fit": "contain",
          "class": bem61("image", {
            vertical: vertical.value
          }),
          "style": imageStyle.value,
          "onLoad": onLoad
        }, imageSlots)]
      });
    };
  }
});

// node_modules/vant/es/image-preview/ImagePreview.js
var [name66, bem62] = createNamespace("image-preview");
var ImagePreview_default = defineComponent({
  name: name66,
  props: {
    show: Boolean,
    loop: truthProp,
    overlay: truthProp,
    closeable: Boolean,
    showIndex: truthProp,
    className: unknownProp,
    transition: String,
    beforeClose: Function,
    overlayStyle: Object,
    showIndicators: Boolean,
    closeOnPopstate: truthProp,
    images: {
      type: Array,
      default: () => []
    },
    minZoom: {
      type: [Number, String],
      default: 1 / 3
    },
    maxZoom: {
      type: [Number, String],
      default: 3
    },
    swipeDuration: {
      type: [Number, String],
      default: 300
    },
    startPosition: {
      type: [Number, String],
      default: 0
    },
    closeIcon: {
      type: String,
      default: "clear"
    },
    closeIconPosition: {
      type: String,
      default: "top-right"
    }
  },
  emits: ["scale", "close", "closed", "change", "update:show"],
  setup(props10, {
    emit,
    slots
  }) {
    var swipeRef = ref();
    var windowSize = useWindowSize();
    var state = reactive({
      active: 0,
      rootWidth: 0,
      rootHeight: 0
    });
    var resize = () => {
      if (swipeRef.value) {
        var rect = swipeRef.value.$el.getBoundingClientRect();
        state.rootWidth = rect.width;
        state.rootHeight = rect.height;
        swipeRef.value.resize();
      }
    };
    var emitScale = (args) => emit("scale", args);
    var updateShow = (show) => emit("update:show", show);
    var emitClose = () => {
      callInterceptor({
        interceptor: props10.beforeClose,
        args: [state.active],
        done: () => updateShow(false)
      });
    };
    var setActive = (active) => {
      if (active !== state.active) {
        state.active = active;
        emit("change", active);
      }
    };
    var renderIndex = () => {
      if (props10.showIndex) {
        return createVNode("div", {
          "class": bem62("index")
        }, [slots.index ? slots.index({
          index: state.active
        }) : state.active + 1 + " / " + props10.images.length]);
      }
    };
    var renderCover = () => {
      if (slots.cover) {
        return createVNode("div", {
          "class": bem62("cover")
        }, [slots.cover()]);
      }
    };
    var renderImages = () => createVNode(Swipe, {
      "ref": swipeRef,
      "lazyRender": true,
      "loop": props10.loop,
      "class": bem62("swipe"),
      "duration": props10.swipeDuration,
      "initialSwipe": props10.startPosition,
      "showIndicators": props10.showIndicators,
      "indicatorColor": "white",
      "onChange": setActive
    }, {
      default: () => [props10.images.map((image) => createVNode(ImagePreviewItem_default, {
        "src": image,
        "show": props10.show,
        "active": state.active,
        "maxZoom": props10.maxZoom,
        "minZoom": props10.minZoom,
        "rootWidth": state.rootWidth,
        "rootHeight": state.rootHeight,
        "onScale": emitScale,
        "onClose": emitClose
      }, null))]
    });
    var renderClose = () => {
      if (props10.closeable) {
        return createVNode(Icon, {
          "role": "button",
          "name": props10.closeIcon,
          "class": bem62("close-icon", props10.closeIconPosition),
          "onClick": emitClose
        }, null);
      }
    };
    var onClosed = () => emit("closed");
    var swipeTo = (index, options) => {
      var _swipeRef$value;
      return (_swipeRef$value = swipeRef.value) == null ? void 0 : _swipeRef$value.swipeTo(index, options);
    };
    useExpose({
      swipeTo
    });
    onMounted(resize);
    watch([windowSize.width, windowSize.height], resize);
    watch(() => props10.startPosition, (value) => setActive(+value));
    watch(() => props10.show, (value) => {
      var {
        images,
        startPosition
      } = props10;
      if (value) {
        setActive(+startPosition);
        nextTick(() => {
          resize();
          swipeTo(+startPosition, {
            immediate: true
          });
        });
      } else {
        emit("close", {
          index: state.active,
          url: images[state.active]
        });
      }
    });
    return () => createVNode(Popup, mergeProps({
      "class": [bem62(), props10.className],
      "overlayClass": bem62("overlay"),
      "onClosed": onClosed
    }, pick(props10, ["show", "transition", "overlayStyle", "closeOnPopstate"]), {
      "onUpdate:show": updateShow
    }), {
      default: () => [renderClose(), renderImages(), renderIndex(), renderCover()]
    });
  }
});

// node_modules/vant/es/image-preview/function-call.js
var instance2;
var defaultConfig = {
  loop: true,
  images: [],
  maxZoom: 3,
  minZoom: 1 / 3,
  onScale: void 0,
  onClose: void 0,
  onChange: void 0,
  teleport: "body",
  className: "",
  showIndex: true,
  closeable: false,
  closeIcon: "clear",
  transition: void 0,
  beforeClose: void 0,
  overlayStyle: void 0,
  startPosition: 0,
  swipeDuration: 300,
  showIndicators: false,
  closeOnPopstate: true,
  closeIconPosition: "top-right"
};
function initInstance2() {
  ({
    instance: instance2
  } = mountComponent({
    setup() {
      var {
        state,
        toggle
      } = usePopupState();
      var onClosed = () => {
        state.images = [];
      };
      return () => createVNode(ImagePreview_default, mergeProps(state, {
        onClosed,
        "onUpdate:show": toggle
      }), null);
    }
  }));
}
var ImagePreview = (images, startPosition = 0) => {
  if (!inBrowser) {
    return;
  }
  if (!instance2) {
    initInstance2();
  }
  var options = Array.isArray(images) ? {
    images,
    startPosition
  } : images;
  instance2.open(extend({}, defaultConfig, options));
  return instance2;
};
ImagePreview.Component = withInstall(ImagePreview_default);
ImagePreview.install = (app) => {
  app.use(withInstall(ImagePreview_default));
};

// node_modules/vant/es/index-anchor/IndexAnchor.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/index-bar/IndexBar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
function genAlphabet() {
  var charCodeOfA = "A".charCodeAt(0);
  var indexList = Array(26).fill("").map((_2, i) => String.fromCharCode(charCodeOfA + i));
  return indexList;
}
var [name67, bem63] = createNamespace("index-bar");
var INDEX_BAR_KEY = Symbol(name67);
var props7 = {
  sticky: truthProp,
  zIndex: [Number, String],
  teleport: [String, Object],
  highlightColor: String,
  stickyOffsetTop: {
    type: Number,
    default: 0
  },
  indexList: {
    type: Array,
    default: genAlphabet
  }
};
var IndexBar_default = defineComponent({
  name: name67,
  props: props7,
  emits: ["select", "change"],
  setup(props10, {
    emit,
    slots
  }) {
    var root2 = ref();
    var activeAnchor = ref("");
    var touch = useTouch();
    var scrollParent2 = useScrollParent(root2);
    var {
      children,
      linkChildren
    } = useChildren(INDEX_BAR_KEY);
    linkChildren({
      props: props10
    });
    var sidebarStyle = computed(() => {
      if (isDef(props10.zIndex)) {
        return {
          zIndex: +props10.zIndex + 1
        };
      }
    });
    var highlightStyle = computed(() => {
      if (props10.highlightColor) {
        return {
          color: props10.highlightColor
        };
      }
    });
    var getScrollerRect = () => {
      if ("getBoundingClientRect" in scrollParent2.value) {
        return useRect(scrollParent2);
      }
      return {
        top: 0,
        left: 0
      };
    };
    var getActiveAnchor = (scrollTop, rects) => {
      for (var i = children.length - 1; i >= 0; i--) {
        var prevHeight = i > 0 ? rects[i - 1].height : 0;
        var reachTop = props10.sticky ? prevHeight + props10.stickyOffsetTop : 0;
        if (scrollTop + reachTop >= rects[i].top) {
          return i;
        }
      }
      return -1;
    };
    var onScroll = () => {
      if (isHidden(root2)) {
        return;
      }
      var {
        sticky,
        indexList
      } = props10;
      var scrollTop = getScrollTop(scrollParent2.value);
      var scrollParentRect = getScrollerRect();
      var rects = children.map((item) => item.getRect(scrollParent2.value, scrollParentRect));
      var active = getActiveAnchor(scrollTop, rects);
      activeAnchor.value = indexList[active];
      if (sticky) {
        children.forEach((item, index) => {
          var {
            state,
            $el
          } = item;
          if (index === active || index === active - 1) {
            var rect = $el.getBoundingClientRect();
            state.left = rect.left;
            state.width = rect.width;
          } else {
            state.left = null;
            state.width = null;
          }
          if (index === active) {
            state.active = true;
            state.top = Math.max(props10.stickyOffsetTop, rects[index].top - scrollTop) + scrollParentRect.top;
          } else if (index === active - 1) {
            var activeItemTop = rects[active].top - scrollTop;
            state.active = activeItemTop > 0;
            state.top = activeItemTop + scrollParentRect.top - rects[index].height;
          } else {
            state.active = false;
          }
        });
      }
    };
    var init = () => nextTick(onScroll);
    useEventListener("scroll", onScroll, {
      target: scrollParent2
    });
    onMounted(init);
    watch(() => props10.indexList, init);
    watch(activeAnchor, (value) => {
      if (value) {
        emit("change", value);
      }
    });
    var renderIndexes = () => props10.indexList.map((index) => {
      var active = index === activeAnchor.value;
      return createVNode("span", {
        "class": bem63("index", {
          active
        }),
        "style": active ? highlightStyle.value : void 0,
        "data-index": index
      }, [index]);
    });
    var scrollTo = (index) => {
      if (!index) {
        return;
      }
      var match = children.find((item) => String(item.index) === index);
      if (match) {
        match.$el.scrollIntoView();
        if (props10.sticky && props10.stickyOffsetTop) {
          setRootScrollTop(getRootScrollTop() - props10.stickyOffsetTop);
        }
        emit("select", match.index);
      }
    };
    var scrollToElement = (element) => {
      var {
        index
      } = element.dataset;
      if (index) {
        scrollTo(index);
      }
    };
    var onClickSidebar = (event) => {
      scrollToElement(event.target);
    };
    var touchActiveIndex;
    var onTouchMove = (event) => {
      touch.move(event);
      if (touch.isVertical()) {
        preventDefault(event);
        var {
          clientX,
          clientY
        } = event.touches[0];
        var target = document.elementFromPoint(clientX, clientY);
        if (target) {
          var {
            index
          } = target.dataset;
          if (index && touchActiveIndex !== index) {
            touchActiveIndex = index;
            scrollToElement(target);
          }
        }
      }
    };
    var renderSidebar = () => createVNode("div", {
      "class": bem63("sidebar"),
      "style": sidebarStyle.value,
      "onClick": onClickSidebar,
      "onTouchstart": touch.start,
      "onTouchmove": onTouchMove
    }, [renderIndexes()]);
    useExpose({
      scrollTo
    });
    return () => createVNode("div", {
      "ref": root2,
      "class": bem63()
    }, [props10.teleport ? createVNode(Teleport, {
      "to": props10.teleport
    }, {
      default: () => [renderSidebar()]
    }) : renderSidebar(), slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/index-anchor/IndexAnchor.js
var [name68, bem64] = createNamespace("index-anchor");
var IndexAnchor_default = defineComponent({
  name: name68,
  props: {
    index: [Number, String]
  },
  setup(props10, {
    slots
  }) {
    var state = reactive({
      top: 0,
      left: null,
      rect: {
        top: 0,
        height: 0
      },
      width: null,
      active: false
    });
    var root2 = ref();
    var {
      parent
    } = useParent(INDEX_BAR_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <IndexAnchor> must be a child component of <IndexBar>.");
      }
      return;
    }
    var isSticky = () => state.active && parent.props.sticky;
    var anchorStyle = computed(() => {
      var {
        zIndex,
        highlightColor
      } = parent.props;
      if (isSticky()) {
        return extend(getZIndexStyle(zIndex), {
          left: state.left ? state.left + "px" : void 0,
          width: state.width ? state.width + "px" : void 0,
          transform: state.top ? "translate3d(0, " + state.top + "px, 0)" : void 0,
          color: highlightColor
        });
      }
    });
    var getRect = (scrollParent2, scrollParentRect) => {
      var rootRect = useRect(root2);
      state.rect.height = rootRect.height;
      if (scrollParent2 === window || scrollParent2 === document.body) {
        state.rect.top = rootRect.top + getRootScrollTop();
      } else {
        state.rect.top = rootRect.top + getScrollTop(scrollParent2) - scrollParentRect.top;
      }
      return state.rect;
    };
    useExpose({
      state,
      getRect
    });
    return () => {
      var sticky = isSticky();
      return createVNode("div", {
        "ref": root2,
        "style": {
          height: sticky ? state.rect.height + "px" : void 0
        }
      }, [createVNode("div", {
        "style": anchorStyle.value,
        "class": [bem64({
          sticky
        }), {
          [BORDER_BOTTOM]: sticky
        }]
      }, [slots.default ? slots.default() : props10.index])]);
    };
  }
});

// node_modules/vant/es/index-anchor/index.js
var IndexAnchor = withInstall(IndexAnchor_default);

// node_modules/vant/es/index-bar/index.js
var IndexBar = withInstall(IndexBar_default);

// node_modules/@vant/lazyload/dist/esm/lazy.js
init_vue_runtime_esm_bundler();

// node_modules/@vant/lazyload/dist/esm/util.js
var inBrowser3 = typeof window !== "undefined" && window !== null;
function checkIntersectionObserver() {
  if (inBrowser3 && "IntersectionObserver" in window && "IntersectionObserverEntry" in window && "intersectionRatio" in window.IntersectionObserverEntry.prototype) {
    if (!("isIntersecting" in window.IntersectionObserverEntry.prototype)) {
      Object.defineProperty(window.IntersectionObserverEntry.prototype, "isIntersecting", {
        get() {
          return this.intersectionRatio > 0;
        }
      });
    }
    return true;
  }
  return false;
}
var hasIntersectionObserver = checkIntersectionObserver();
var modeType = {
  event: "event",
  observer: "observer"
};
var CustomEvent = function() {
  if (!inBrowser3)
    return;
  if (typeof window.CustomEvent === "function")
    return window.CustomEvent;
  function CustomEvent2(event, params) {
    params = params || {
      bubbles: false,
      cancelable: false,
      detail: void 0
    };
    var evt = document.createEvent("CustomEvent");
    evt.initCustomEvent(event, params.bubbles, params.cancelable, params.detail);
    return evt;
  }
  CustomEvent2.prototype = window.Event.prototype;
  return CustomEvent2;
}();
function remove(arr, item) {
  if (!arr.length)
    return;
  var index = arr.indexOf(item);
  if (index > -1)
    return arr.splice(index, 1);
}
function getBestSelectionFromSrcset(el, scale) {
  if (el.tagName !== "IMG" || !el.getAttribute("data-srcset"))
    return;
  var options = el.getAttribute("data-srcset");
  var container = el.parentNode;
  var containerWidth = container.offsetWidth * scale;
  var spaceIndex;
  var tmpSrc;
  var tmpWidth;
  options = options.trim().split(",");
  var result = options.map((item) => {
    item = item.trim();
    spaceIndex = item.lastIndexOf(" ");
    if (spaceIndex === -1) {
      tmpSrc = item;
      tmpWidth = 999998;
    } else {
      tmpSrc = item.substr(0, spaceIndex);
      tmpWidth = parseInt(item.substr(spaceIndex + 1, item.length - spaceIndex - 2), 10);
    }
    return [tmpWidth, tmpSrc];
  });
  result.sort(function(a, b) {
    if (a[0] < b[0]) {
      return 1;
    }
    if (a[0] > b[0]) {
      return -1;
    }
    if (a[0] === b[0]) {
      if (b[1].indexOf(".webp", b[1].length - 5) !== -1) {
        return 1;
      }
      if (a[1].indexOf(".webp", a[1].length - 5) !== -1) {
        return -1;
      }
    }
    return 0;
  });
  var bestSelectedSrc = "";
  var tmpOption;
  for (var i = 0; i < result.length; i++) {
    tmpOption = result[i];
    bestSelectedSrc = tmpOption[1];
    var next = result[i + 1];
    if (next && next[0] < containerWidth) {
      bestSelectedSrc = tmpOption[1];
      break;
    } else if (!next) {
      bestSelectedSrc = tmpOption[1];
      break;
    }
  }
  return bestSelectedSrc;
}
function find(arr, fn2) {
  var item;
  for (var i = 0, len = arr.length; i < len; i++) {
    if (fn2(arr[i])) {
      item = arr[i];
      break;
    }
  }
  return item;
}
var getDPR = (scale = 1) => inBrowser3 ? window.devicePixelRatio || scale : scale;
function supportWebp() {
  if (!inBrowser3)
    return false;
  var support = true;
  try {
    var elem = document.createElement("canvas");
    if (elem.getContext && elem.getContext("2d")) {
      support = elem.toDataURL("image/webp").indexOf("data:image/webp") === 0;
    }
  } catch (err) {
    support = false;
  }
  return support;
}
function throttle(action, delay) {
  var timeout = null;
  var lastRun = 0;
  return function(...args) {
    if (timeout) {
      return;
    }
    var elapsed = Date.now() - lastRun;
    var runCallback = () => {
      lastRun = Date.now();
      timeout = false;
      action.apply(this, args);
    };
    if (elapsed >= delay) {
      runCallback();
    } else {
      timeout = setTimeout(runCallback, delay);
    }
  };
}
function testSupportsPassive() {
  if (!inBrowser3)
    return;
  var support = false;
  try {
    var opts = Object.defineProperty({}, "passive", {
      get() {
        support = true;
      }
    });
    window.addEventListener("test", null, opts);
  } catch (e) {
  }
  return support;
}
var supportsPassive2 = testSupportsPassive();
var _ = {
  on(el, type, func, capture = false) {
    if (supportsPassive2) {
      el.addEventListener(type, func, {
        capture,
        passive: true
      });
    } else {
      el.addEventListener(type, func, capture);
    }
  },
  off(el, type, func, capture = false) {
    el.removeEventListener(type, func, capture);
  }
};
var loadImageAsync = (item, resolve, reject) => {
  var image = new Image();
  if (!item || !item.src) {
    var err = new Error("image src is required");
    return reject(err);
  }
  image.src = item.src;
  if (item.cors) {
    image.crossOrigin = item.cors;
  }
  image.onload = function() {
    resolve({
      naturalHeight: image.naturalHeight,
      naturalWidth: image.naturalWidth,
      src: image.src
    });
  };
  image.onerror = function(e) {
    reject(e);
  };
};
var style = (el, prop) => {
  return typeof getComputedStyle !== "undefined" ? getComputedStyle(el, null).getPropertyValue(prop) : el.style[prop];
};
var overflow = (el) => {
  return style(el, "overflow") + style(el, "overflow-y") + style(el, "overflow-x");
};
var scrollParent = (el) => {
  if (!inBrowser3)
    return;
  if (!(el instanceof HTMLElement)) {
    return window;
  }
  var parent = el;
  while (parent) {
    if (parent === document.body || parent === document.documentElement) {
      break;
    }
    if (!parent.parentNode) {
      break;
    }
    if (/(scroll|auto)/.test(overflow(parent))) {
      return parent;
    }
    parent = parent.parentNode;
  }
  return window;
};
function isObject2(obj) {
  return obj !== null && typeof obj === "object";
}
function noop2() {
}
var ImageCache = class {
  constructor({
    max
  }) {
    this.options = {
      max: max || 100
    };
    this._caches = [];
  }
  has(key) {
    return this._caches.indexOf(key) > -1;
  }
  add(key) {
    if (this.has(key))
      return;
    this._caches.push(key);
    if (this._caches.length > this.options.max) {
      this.free();
    }
  }
  free() {
    this._caches.shift();
  }
};

// node_modules/@vant/lazyload/dist/esm/listener.js
var ReactiveListener = class {
  constructor({
    el,
    src,
    error,
    loading,
    bindType,
    $parent,
    options,
    cors,
    elRenderer,
    imageCache
  }) {
    this.el = el;
    this.src = src;
    this.error = error;
    this.loading = loading;
    this.bindType = bindType;
    this.attempt = 0;
    this.cors = cors;
    this.naturalHeight = 0;
    this.naturalWidth = 0;
    this.options = options;
    this.rect = null;
    this.$parent = $parent;
    this.elRenderer = elRenderer;
    this._imageCache = imageCache;
    this.performanceData = {
      init: Date.now(),
      loadStart: 0,
      loadEnd: 0
    };
    this.filter();
    this.initState();
    this.render("loading", false);
  }
  initState() {
    if ("dataset" in this.el) {
      this.el.dataset.src = this.src;
    } else {
      this.el.setAttribute("data-src", this.src);
    }
    this.state = {
      loading: false,
      error: false,
      loaded: false,
      rendered: false
    };
  }
  record(event) {
    this.performanceData[event] = Date.now();
  }
  update({
    src,
    loading,
    error
  }) {
    var oldSrc = this.src;
    this.src = src;
    this.loading = loading;
    this.error = error;
    this.filter();
    if (oldSrc !== this.src) {
      this.attempt = 0;
      this.initState();
    }
  }
  getRect() {
    this.rect = this.el.getBoundingClientRect();
  }
  checkInView() {
    this.getRect();
    return this.rect.top < window.innerHeight * this.options.preLoad && this.rect.bottom > this.options.preLoadTop && this.rect.left < window.innerWidth * this.options.preLoad && this.rect.right > 0;
  }
  filter() {
    Object.keys(this.options.filter).forEach((key) => {
      this.options.filter[key](this, this.options);
    });
  }
  renderLoading(cb) {
    this.state.loading = true;
    loadImageAsync({
      src: this.loading,
      cors: this.cors
    }, () => {
      this.render("loading", false);
      this.state.loading = false;
      cb();
    }, () => {
      cb();
      this.state.loading = false;
      if (!this.options.silent)
        console.warn("VueLazyload log: load failed with loading image(" + this.loading + ")");
    });
  }
  load(onFinish = noop2) {
    if (this.attempt > this.options.attempt - 1 && this.state.error) {
      if (!this.options.silent)
        console.log("VueLazyload log: " + this.src + " tried too more than " + this.options.attempt + " times");
      onFinish();
      return;
    }
    if (this.state.rendered && this.state.loaded)
      return;
    if (this._imageCache.has(this.src)) {
      this.state.loaded = true;
      this.render("loaded", true);
      this.state.rendered = true;
      return onFinish();
    }
    this.renderLoading(() => {
      var _this$options$adapter, _this$options$adapter2;
      this.attempt++;
      (_this$options$adapter = (_this$options$adapter2 = this.options.adapter).beforeLoad) == null ? void 0 : _this$options$adapter.call(_this$options$adapter2, this, this.options);
      this.record("loadStart");
      loadImageAsync({
        src: this.src,
        cors: this.cors
      }, (data) => {
        this.naturalHeight = data.naturalHeight;
        this.naturalWidth = data.naturalWidth;
        this.state.loaded = true;
        this.state.error = false;
        this.record("loadEnd");
        this.render("loaded", false);
        this.state.rendered = true;
        this._imageCache.add(this.src);
        onFinish();
      }, (err) => {
        !this.options.silent && console.error(err);
        this.state.error = true;
        this.state.loaded = false;
        this.render("error", false);
      });
    });
  }
  render(state, cache) {
    this.elRenderer(this, state, cache);
  }
  performance() {
    var state = "loading";
    var time = 0;
    if (this.state.loaded) {
      state = "loaded";
      time = (this.performanceData.loadEnd - this.performanceData.loadStart) / 1e3;
    }
    if (this.state.error)
      state = "error";
    return {
      src: this.src,
      state,
      time
    };
  }
  $destroy() {
    this.el = null;
    this.src = null;
    this.error = null;
    this.loading = null;
    this.bindType = null;
    this.attempt = 0;
  }
};

// node_modules/@vant/lazyload/dist/esm/lazy.js
var DEFAULT_URL = "data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7";
var DEFAULT_EVENTS = ["scroll", "wheel", "mousewheel", "resize", "animationend", "transitionend", "touchmove"];
var DEFAULT_OBSERVER_OPTIONS = {
  rootMargin: "0px",
  threshold: 0
};
function lazy_default() {
  return class Lazy {
    constructor({
      preLoad,
      error,
      throttleWait,
      preLoadTop,
      dispatchEvent,
      loading,
      attempt,
      silent = true,
      scale,
      listenEvents,
      filter,
      adapter,
      observer,
      observerOptions
    }) {
      this.mode = modeType.event;
      this.ListenerQueue = [];
      this.TargetIndex = 0;
      this.TargetQueue = [];
      this.options = {
        silent,
        dispatchEvent: !!dispatchEvent,
        throttleWait: throttleWait || 200,
        preLoad: preLoad || 1.3,
        preLoadTop: preLoadTop || 0,
        error: error || DEFAULT_URL,
        loading: loading || DEFAULT_URL,
        attempt: attempt || 3,
        scale: scale || getDPR(scale),
        ListenEvents: listenEvents || DEFAULT_EVENTS,
        hasbind: false,
        supportWebp: supportWebp(),
        filter: filter || {},
        adapter: adapter || {},
        observer: !!observer,
        observerOptions: observerOptions || DEFAULT_OBSERVER_OPTIONS
      };
      this._initEvent();
      this._imageCache = new ImageCache({
        max: 200
      });
      this.lazyLoadHandler = throttle(this._lazyLoadHandler.bind(this), this.options.throttleWait);
      this.setMode(this.options.observer ? modeType.observer : modeType.event);
    }
    config(options = {}) {
      Object.assign(this.options, options);
    }
    performance() {
      return this.ListenerQueue.map((item) => item.performance());
    }
    addLazyBox(vm) {
      this.ListenerQueue.push(vm);
      if (inBrowser3) {
        this._addListenerTarget(window);
        this._observer && this._observer.observe(vm.el);
        if (vm.$el && vm.$el.parentNode) {
          this._addListenerTarget(vm.$el.parentNode);
        }
      }
    }
    add(el, binding, vnode) {
      if (this.ListenerQueue.some((item) => item.el === el)) {
        this.update(el, binding);
        return nextTick(this.lazyLoadHandler);
      }
      var value = this._valueFormatter(binding.value);
      var {
        src
      } = value;
      nextTick(() => {
        src = getBestSelectionFromSrcset(el, this.options.scale) || src;
        this._observer && this._observer.observe(el);
        var container = Object.keys(binding.modifiers)[0];
        var $parent;
        if (container) {
          $parent = vnode.context.$refs[container];
          $parent = $parent ? $parent.$el || $parent : document.getElementById(container);
        }
        if (!$parent) {
          $parent = scrollParent(el);
        }
        var newListener = new ReactiveListener({
          bindType: binding.arg,
          $parent,
          el,
          src,
          loading: value.loading,
          error: value.error,
          cors: value.cors,
          elRenderer: this._elRenderer.bind(this),
          options: this.options,
          imageCache: this._imageCache
        });
        this.ListenerQueue.push(newListener);
        if (inBrowser3) {
          this._addListenerTarget(window);
          this._addListenerTarget($parent);
        }
        this.lazyLoadHandler();
        nextTick(() => this.lazyLoadHandler());
      });
    }
    update(el, binding, vnode) {
      var value = this._valueFormatter(binding.value);
      var {
        src
      } = value;
      src = getBestSelectionFromSrcset(el, this.options.scale) || src;
      var exist = find(this.ListenerQueue, (item) => item.el === el);
      if (!exist) {
        this.add(el, binding, vnode);
      } else {
        exist.update({
          src,
          error: value.error,
          loading: value.loading
        });
      }
      if (this._observer) {
        this._observer.unobserve(el);
        this._observer.observe(el);
      }
      this.lazyLoadHandler();
      nextTick(() => this.lazyLoadHandler());
    }
    remove(el) {
      if (!el)
        return;
      this._observer && this._observer.unobserve(el);
      var existItem = find(this.ListenerQueue, (item) => item.el === el);
      if (existItem) {
        this._removeListenerTarget(existItem.$parent);
        this._removeListenerTarget(window);
        remove(this.ListenerQueue, existItem);
        existItem.$destroy();
      }
    }
    removeComponent(vm) {
      if (!vm)
        return;
      remove(this.ListenerQueue, vm);
      this._observer && this._observer.unobserve(vm.el);
      if (vm.$parent && vm.$el.parentNode) {
        this._removeListenerTarget(vm.$el.parentNode);
      }
      this._removeListenerTarget(window);
    }
    setMode(mode) {
      if (!hasIntersectionObserver && mode === modeType.observer) {
        mode = modeType.event;
      }
      this.mode = mode;
      if (mode === modeType.event) {
        if (this._observer) {
          this.ListenerQueue.forEach((listener) => {
            this._observer.unobserve(listener.el);
          });
          this._observer = null;
        }
        this.TargetQueue.forEach((target) => {
          this._initListen(target.el, true);
        });
      } else {
        this.TargetQueue.forEach((target) => {
          this._initListen(target.el, false);
        });
        this._initIntersectionObserver();
      }
    }
    _addListenerTarget(el) {
      if (!el)
        return;
      var target = find(this.TargetQueue, (target2) => target2.el === el);
      if (!target) {
        target = {
          el,
          id: ++this.TargetIndex,
          childrenCount: 1,
          listened: true
        };
        this.mode === modeType.event && this._initListen(target.el, true);
        this.TargetQueue.push(target);
      } else {
        target.childrenCount++;
      }
      return this.TargetIndex;
    }
    _removeListenerTarget(el) {
      this.TargetQueue.forEach((target, index) => {
        if (target.el === el) {
          target.childrenCount--;
          if (!target.childrenCount) {
            this._initListen(target.el, false);
            this.TargetQueue.splice(index, 1);
            target = null;
          }
        }
      });
    }
    _initListen(el, start2) {
      this.options.ListenEvents.forEach((evt) => _[start2 ? "on" : "off"](el, evt, this.lazyLoadHandler));
    }
    _initEvent() {
      this.Event = {
        listeners: {
          loading: [],
          loaded: [],
          error: []
        }
      };
      this.$on = (event, func) => {
        if (!this.Event.listeners[event])
          this.Event.listeners[event] = [];
        this.Event.listeners[event].push(func);
      };
      this.$once = (event, func) => {
        var on = (...args) => {
          this.$off(event, on);
          func.apply(this, args);
        };
        this.$on(event, on);
      };
      this.$off = (event, func) => {
        if (!func) {
          if (!this.Event.listeners[event])
            return;
          this.Event.listeners[event].length = 0;
          return;
        }
        remove(this.Event.listeners[event], func);
      };
      this.$emit = (event, context, inCache) => {
        if (!this.Event.listeners[event])
          return;
        this.Event.listeners[event].forEach((func) => func(context, inCache));
      };
    }
    _lazyLoadHandler() {
      var freeList = [];
      this.ListenerQueue.forEach((listener) => {
        if (!listener.el || !listener.el.parentNode) {
          freeList.push(listener);
        }
        var catIn = listener.checkInView();
        if (!catIn)
          return;
        listener.load();
      });
      freeList.forEach((item) => {
        remove(this.ListenerQueue, item);
        item.$destroy();
      });
    }
    _initIntersectionObserver() {
      if (!hasIntersectionObserver) {
        return;
      }
      this._observer = new IntersectionObserver(this._observerHandler.bind(this), this.options.observerOptions);
      if (this.ListenerQueue.length) {
        this.ListenerQueue.forEach((listener) => {
          this._observer.observe(listener.el);
        });
      }
    }
    _observerHandler(entries) {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          this.ListenerQueue.forEach((listener) => {
            if (listener.el === entry.target) {
              if (listener.state.loaded)
                return this._observer.unobserve(listener.el);
              listener.load();
            }
          });
        }
      });
    }
    _elRenderer(listener, state, cache) {
      if (!listener.el)
        return;
      var {
        el,
        bindType
      } = listener;
      var src;
      switch (state) {
        case "loading":
          src = listener.loading;
          break;
        case "error":
          src = listener.error;
          break;
        default:
          ({
            src
          } = listener);
          break;
      }
      if (bindType) {
        el.style[bindType] = 'url("' + src + '")';
      } else if (el.getAttribute("src") !== src) {
        el.setAttribute("src", src);
      }
      el.setAttribute("lazy", state);
      this.$emit(state, listener, cache);
      this.options.adapter[state] && this.options.adapter[state](listener, this.options);
      if (this.options.dispatchEvent) {
        var event = new CustomEvent(state, {
          detail: listener
        });
        el.dispatchEvent(event);
      }
    }
    _valueFormatter(value) {
      var src = value;
      var {
        loading,
        error
      } = this.options;
      if (isObject2(value)) {
        if (!value.src && !this.options.silent)
          console.error("Vue Lazyload warning: miss src with " + value);
        ({
          src
        } = value);
        loading = value.loading || this.options.loading;
        error = value.error || this.options.error;
      }
      return {
        src,
        loading,
        error
      };
    }
  };
}

// node_modules/@vant/lazyload/dist/esm/lazy-component.js
init_vue_runtime_esm_bundler();
var lazy_component_default = (lazy) => {
  return {
    props: {
      tag: {
        type: String,
        default: "div"
      }
    },
    emits: ["show"],
    render() {
      return h(this.tag, this.show && this.$slots.default ? this.$slots.default() : null);
    },
    data() {
      return {
        el: null,
        state: {
          loaded: false
        },
        rect: {},
        show: false
      };
    },
    mounted() {
      this.el = this.$el;
      lazy.addLazyBox(this);
      lazy.lazyLoadHandler();
    },
    beforeUnmount() {
      lazy.removeComponent(this);
    },
    methods: {
      getRect() {
        this.rect = this.$el.getBoundingClientRect();
      },
      checkInView() {
        this.getRect();
        return inBrowser3 && this.rect.top < window.innerHeight * lazy.options.preLoad && this.rect.bottom > 0 && this.rect.left < window.innerWidth * lazy.options.preLoad && this.rect.right > 0;
      },
      load() {
        this.show = true;
        this.state.loaded = true;
        this.$emit("show", this);
      },
      destroy() {
        return this.$destroy;
      }
    }
  };
};

// node_modules/@vant/lazyload/dist/esm/lazy-container.js
var defaultOptions2 = {
  selector: "img"
};
var LazyContainer = class {
  constructor({
    el,
    binding,
    vnode,
    lazy
  }) {
    this.el = null;
    this.vnode = vnode;
    this.binding = binding;
    this.options = {};
    this.lazy = lazy;
    this._queue = [];
    this.update({
      el,
      binding
    });
  }
  update({
    el,
    binding
  }) {
    this.el = el;
    this.options = Object.assign({}, defaultOptions2, binding.value);
    var imgs = this.getImgs();
    imgs.forEach((el2) => {
      this.lazy.add(el2, Object.assign({}, this.binding, {
        value: {
          src: "dataset" in el2 ? el2.dataset.src : el2.getAttribute("data-src"),
          error: ("dataset" in el2 ? el2.dataset.error : el2.getAttribute("data-error")) || this.options.error,
          loading: ("dataset" in el2 ? el2.dataset.loading : el2.getAttribute("data-loading")) || this.options.loading
        }
      }), this.vnode);
    });
  }
  getImgs() {
    return Array.from(this.el.querySelectorAll(this.options.selector));
  }
  clear() {
    var imgs = this.getImgs();
    imgs.forEach((el) => this.lazy.remove(el));
    this.vnode = null;
    this.binding = null;
    this.lazy = null;
  }
};
var LazyContainerManager = class {
  constructor({
    lazy
  }) {
    this.lazy = lazy;
    this._queue = [];
  }
  bind(el, binding, vnode) {
    var container = new LazyContainer({
      el,
      binding,
      vnode,
      lazy: this.lazy
    });
    this._queue.push(container);
  }
  update(el, binding, vnode) {
    var container = find(this._queue, (item) => item.el === el);
    if (!container)
      return;
    container.update({
      el,
      binding,
      vnode
    });
  }
  unbind(el) {
    var container = find(this._queue, (item) => item.el === el);
    if (!container)
      return;
    container.clear();
    remove(this._queue, container);
  }
};

// node_modules/@vant/lazyload/dist/esm/lazy-image.js
var lazy_image_default = (lazyManager) => {
  return {
    props: {
      src: [String, Object],
      tag: {
        type: String,
        default: "img"
      }
    },
    render(h2) {
      return h2(this.tag, {
        attrs: {
          src: this.renderSrc
        }
      }, this.$slots.default);
    },
    data() {
      return {
        el: null,
        options: {
          src: "",
          error: "",
          loading: "",
          attempt: lazyManager.options.attempt
        },
        state: {
          loaded: false,
          error: false,
          attempt: 0
        },
        rect: {},
        renderSrc: ""
      };
    },
    watch: {
      src() {
        this.init();
        lazyManager.addLazyBox(this);
        lazyManager.lazyLoadHandler();
      }
    },
    created() {
      this.init();
      this.renderSrc = this.options.loading;
    },
    mounted() {
      this.el = this.$el;
      lazyManager.addLazyBox(this);
      lazyManager.lazyLoadHandler();
    },
    beforeUnmount() {
      lazyManager.removeComponent(this);
    },
    methods: {
      init() {
        var {
          src,
          loading,
          error
        } = lazyManager._valueFormatter(this.src);
        this.state.loaded = false;
        this.options.src = src;
        this.options.error = error;
        this.options.loading = loading;
        this.renderSrc = this.options.loading;
      },
      getRect() {
        this.rect = this.$el.getBoundingClientRect();
      },
      checkInView() {
        this.getRect();
        return inBrowser3 && this.rect.top < window.innerHeight * lazyManager.options.preLoad && this.rect.bottom > 0 && this.rect.left < window.innerWidth * lazyManager.options.preLoad && this.rect.right > 0;
      },
      load(onFinish = noop2) {
        if (this.state.attempt > this.options.attempt - 1 && this.state.error) {
          if (!lazyManager.options.silent)
            console.log("VueLazyload log: " + this.options.src + " tried too more than " + this.options.attempt + " times");
          onFinish();
          return;
        }
        var {
          src
        } = this.options;
        loadImageAsync({
          src
        }, ({
          src: src2
        }) => {
          this.renderSrc = src2;
          this.state.loaded = true;
        }, () => {
          this.state.attempt++;
          this.renderSrc = this.options.error;
          this.state.error = true;
        });
      }
    }
  };
};

// node_modules/@vant/lazyload/dist/esm/index.js
var Lazyload = {
  install(app, options = {}) {
    var LazyClass = lazy_default();
    var lazy = new LazyClass(options);
    var lazyContainer = new LazyContainerManager({
      lazy
    });
    app.config.globalProperties.$Lazyload = lazy;
    if (options.lazyComponent) {
      app.component("LazyComponent", lazy_component_default(lazy));
    }
    if (options.lazyImage) {
      app.component("LazyImage", lazy_image_default(lazy));
    }
    app.directive("lazy", {
      beforeMount: lazy.add.bind(lazy),
      updated: lazy.update.bind(lazy),
      unmounted: lazy.remove.bind(lazy)
    });
    app.directive("lazy-container", {
      beforeMount: lazyContainer.bind.bind(lazyContainer),
      updated: lazyContainer.update.bind(lazyContainer),
      unmounted: lazyContainer.unbind.bind(lazyContainer)
    });
  }
};

// node_modules/vant/es/list/List.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name69, bem65, t14] = createNamespace("list");
var List_default = defineComponent({
  name: name69,
  props: {
    error: Boolean,
    loading: Boolean,
    finished: Boolean,
    errorText: String,
    loadingText: String,
    finishedText: String,
    immediateCheck: truthProp,
    offset: {
      type: [Number, String],
      default: 300
    },
    direction: {
      type: String,
      default: "down"
    }
  },
  emits: ["load", "update:error", "update:loading"],
  setup(props10, {
    emit,
    slots
  }) {
    var loading = ref(false);
    var root2 = ref();
    var placeholder = ref();
    var tabStatus = useTabStatus();
    var scrollParent2 = useScrollParent(root2);
    var check = () => {
      nextTick(() => {
        if (loading.value || props10.finished || props10.error || (tabStatus == null ? void 0 : tabStatus.value) === false) {
          return;
        }
        var {
          offset: offset2,
          direction
        } = props10;
        var scrollParentRect = useRect(scrollParent2);
        if (!scrollParentRect.height || isHidden(root2)) {
          return;
        }
        var isReachEdge = false;
        var placeholderRect = useRect(placeholder);
        if (direction === "up") {
          isReachEdge = scrollParentRect.top - placeholderRect.top <= offset2;
        } else {
          isReachEdge = placeholderRect.bottom - scrollParentRect.bottom <= offset2;
        }
        if (isReachEdge) {
          loading.value = true;
          emit("update:loading", true);
          emit("load");
        }
      });
    };
    var renderFinishedText = () => {
      if (props10.finished) {
        var text = slots.finished ? slots.finished() : props10.finishedText;
        if (text) {
          return createVNode("div", {
            "class": bem65("finished-text")
          }, [text]);
        }
      }
    };
    var clickErrorText = () => {
      emit("update:error", false);
      check();
    };
    var renderErrorText = () => {
      if (props10.error) {
        var text = slots.error ? slots.error() : props10.errorText;
        if (text) {
          return createVNode("div", {
            "class": bem65("error-text"),
            "onClick": clickErrorText
          }, [text]);
        }
      }
    };
    var renderLoading = () => {
      if (loading.value && !props10.finished) {
        return createVNode("div", {
          "class": bem65("loading")
        }, [slots.loading ? slots.loading() : createVNode(Loading, {
          "class": bem65("loading-icon")
        }, {
          default: () => [props10.loadingText || t14("loading")]
        })]);
      }
    };
    watch([() => props10.loading, () => props10.finished, () => props10.error], check);
    onUpdated(() => {
      loading.value = props10.loading;
    });
    onMounted(() => {
      if (props10.immediateCheck) {
        check();
      }
    });
    useExpose({
      check
    });
    useEventListener("scroll", check, {
      target: scrollParent2
    });
    return () => {
      var Content = slots.default == null ? void 0 : slots.default();
      var Placeholder = createVNode("div", {
        "ref": placeholder,
        "class": bem65("placeholder")
      }, null);
      return createVNode("div", {
        "ref": root2,
        "role": "feed",
        "class": bem65(),
        "aria-busy": loading.value
      }, [props10.direction === "down" ? Content : Placeholder, renderLoading(), renderFinishedText(), renderErrorText(), props10.direction === "up" ? Content : Placeholder]);
    };
  }
});

// node_modules/vant/es/list/index.js
var List = withInstall(List_default);

// node_modules/vant/es/nav-bar/NavBar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/composables/use-placeholder.js
init_vue_runtime_esm_bundler();
function usePlaceholder(contentRef, bem92) {
  var height = useHeight(contentRef);
  return (renderContent) => createVNode("div", {
    "class": bem92("placeholder"),
    "style": {
      height: height.value ? height.value + "px" : void 0
    }
  }, [renderContent()]);
}

// node_modules/vant/es/nav-bar/NavBar.js
var [name70, bem66] = createNamespace("nav-bar");
var NavBar_default = defineComponent({
  name: name70,
  props: {
    title: String,
    fixed: Boolean,
    zIndex: [Number, String],
    border: truthProp,
    leftText: String,
    rightText: String,
    leftArrow: Boolean,
    placeholder: Boolean,
    safeAreaInsetTop: Boolean
  },
  emits: ["click-left", "click-right"],
  setup(props10, {
    emit,
    slots
  }) {
    var navBarRef = ref();
    var renderPlaceholder = usePlaceholder(navBarRef, bem66);
    var onClickLeft = (event) => emit("click-left", event);
    var onClickRight = (event) => emit("click-right", event);
    var renderLeft = () => {
      if (slots.left) {
        return slots.left();
      }
      return [props10.leftArrow && createVNode(Icon, {
        "class": bem66("arrow"),
        "name": "arrow-left"
      }, null), props10.leftText && createVNode("span", {
        "class": bem66("text")
      }, [props10.leftText])];
    };
    var renderRight = () => {
      if (slots.right) {
        return slots.right();
      }
      return createVNode("span", {
        "class": bem66("text")
      }, [props10.rightText]);
    };
    var renderNavBar = () => {
      var {
        title,
        fixed,
        border,
        zIndex
      } = props10;
      var style2 = getZIndexStyle(zIndex);
      var hasLeft = props10.leftArrow || props10.leftText || slots.left;
      var hasRight = props10.rightText || slots.right;
      return createVNode("div", {
        "ref": navBarRef,
        "style": style2,
        "class": [bem66({
          fixed,
          "safe-area-inset-top": props10.safeAreaInsetTop
        }), {
          [BORDER_BOTTOM]: border
        }]
      }, [createVNode("div", {
        "class": bem66("content")
      }, [hasLeft && createVNode("div", {
        "class": bem66("left"),
        "onClick": onClickLeft
      }, [renderLeft()]), createVNode("div", {
        "class": [bem66("title"), "van-ellipsis"]
      }, [slots.title ? slots.title() : title]), hasRight && createVNode("div", {
        "class": bem66("right"),
        "onClick": onClickRight
      }, [renderRight()])])]);
    };
    return () => {
      if (props10.fixed && props10.placeholder) {
        return renderPlaceholder(renderNavBar);
      }
      return renderNavBar();
    };
  }
});

// node_modules/vant/es/nav-bar/index.js
var NavBar = withInstall(NavBar_default);

// node_modules/vant/es/notice-bar/NoticeBar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name71, bem67] = createNamespace("notice-bar");
var NoticeBar_default = defineComponent({
  name: name71,
  props: {
    text: String,
    mode: String,
    color: String,
    leftIcon: String,
    wrapable: Boolean,
    background: String,
    scrollable: {
      type: Boolean,
      default: null
    },
    delay: {
      type: [Number, String],
      default: 1
    },
    speed: {
      type: [Number, String],
      default: 60
    }
  },
  emits: ["close", "replay"],
  setup(props10, {
    emit,
    slots
  }) {
    var wrapWidth = 0;
    var contentWidth = 0;
    var startTimer;
    var wrapRef = ref();
    var contentRef = ref();
    var state = reactive({
      show: true,
      offset: 0,
      duration: 0
    });
    var renderLeftIcon = () => {
      if (slots["left-icon"]) {
        return slots["left-icon"]();
      }
      if (props10.leftIcon) {
        return createVNode(Icon, {
          "class": bem67("left-icon"),
          "name": props10.leftIcon
        }, null);
      }
    };
    var getRightIconName = () => {
      if (props10.mode === "closeable") {
        return "cross";
      }
      if (props10.mode === "link") {
        return "arrow";
      }
    };
    var onClickRightIcon = (event) => {
      if (props10.mode === "closeable") {
        state.show = false;
        emit("close", event);
      }
    };
    var renderRightIcon = () => {
      if (slots["right-icon"]) {
        return slots["right-icon"]();
      }
      var name96 = getRightIconName();
      if (name96) {
        return createVNode(Icon, {
          "name": name96,
          "class": bem67("right-icon"),
          "onClick": onClickRightIcon
        }, null);
      }
    };
    var onTransitionEnd = () => {
      state.offset = wrapWidth;
      state.duration = 0;
      raf(() => {
        doubleRaf(() => {
          state.offset = -contentWidth;
          state.duration = (contentWidth + wrapWidth) / +props10.speed;
          emit("replay");
        });
      });
    };
    var renderMarquee = () => {
      var ellipsis = props10.scrollable === false && !props10.wrapable;
      var style2 = {
        transform: state.offset ? "translateX(" + state.offset + "px)" : "",
        transitionDuration: state.duration + "s"
      };
      return createVNode("div", {
        "ref": wrapRef,
        "role": "marquee",
        "class": bem67("wrap")
      }, [createVNode("div", {
        "ref": contentRef,
        "style": style2,
        "class": [bem67("content"), {
          "van-ellipsis": ellipsis
        }],
        "onTransitionend": onTransitionEnd
      }, [slots.default ? slots.default() : props10.text])]);
    };
    var reset = () => {
      var {
        delay,
        speed,
        scrollable
      } = props10;
      var ms = isDef(delay) ? +delay * 1e3 : 0;
      wrapWidth = 0;
      contentWidth = 0;
      state.offset = 0;
      state.duration = 0;
      clearTimeout(startTimer);
      startTimer = setTimeout(() => {
        if (!wrapRef.value || !contentRef.value || scrollable === false) {
          return;
        }
        var wrapRefWidth = useRect(wrapRef).width;
        var contentRefWidth = useRect(contentRef).width;
        if (scrollable || contentRefWidth > wrapRefWidth) {
          doubleRaf(() => {
            wrapWidth = wrapRefWidth;
            contentWidth = contentRefWidth;
            state.offset = -contentWidth;
            state.duration = contentWidth / +speed;
          });
        }
      }, ms);
    };
    onPopupReopen(reset);
    onMountedOrActivated(reset);
    useEventListener("pageshow", reset);
    useExpose({
      reset
    });
    watch(() => [props10.text, props10.scrollable], reset);
    return () => {
      var {
        color,
        wrapable,
        background
      } = props10;
      return withDirectives(createVNode("div", {
        "role": "alert",
        "class": bem67({
          wrapable
        }),
        "style": {
          color,
          background
        }
      }, [renderLeftIcon(), renderMarquee(), renderRightIcon()]), [[vShow, state.show]]);
    };
  }
});

// node_modules/vant/es/notice-bar/index.js
var NoticeBar = withInstall(NoticeBar_default);

// node_modules/vant/es/notify/function-call.js
init_vue_runtime_esm_bundler();

// node_modules/vant/es/notify/Notify.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name72, bem68] = createNamespace("notify");
var Notify_default = defineComponent({
  name: name72,
  props: extend({}, popupSharedProps, {
    color: String,
    message: [Number, String],
    className: unknownProp,
    background: String,
    lockScroll: Boolean,
    type: {
      type: String,
      default: "danger"
    }
  }),
  setup(props10, {
    slots
  }) {
    return () => {
      var style2 = {
        color: props10.color,
        background: props10.background
      };
      return createVNode(Popup, {
        "show": props10.show,
        "class": [bem68([props10.type]), props10.className],
        "style": style2,
        "overlay": false,
        "position": "top",
        "duration": 0.2,
        "lockScroll": props10.lockScroll
      }, {
        default: () => [slots.default ? slots.default() : props10.message]
      });
    };
  }
});

// node_modules/vant/es/notify/function-call.js
var timer;
var instance3;
function parseOptions2(message) {
  return isObject(message) ? message : {
    message
  };
}
function initInstance3() {
  ({
    instance: instance3
  } = mountComponent({
    setup() {
      var {
        state,
        toggle
      } = usePopupState();
      return () => createVNode(Notify_default, mergeProps(state, {
        "onUpdate:show": toggle
      }), null);
    }
  }));
}
function Notify(options) {
  if (!inBrowser) {
    return;
  }
  if (!instance3) {
    initInstance3();
  }
  options = extend(Notify.currentOptions, parseOptions2(options));
  instance3.open(options);
  clearTimeout(timer);
  if (options.duration > 0) {
    timer = window.setTimeout(Notify.clear, options.duration);
  }
  return instance3;
}
function defaultOptions3() {
  return {
    type: "danger",
    color: void 0,
    message: "",
    onClose: void 0,
    onClick: void 0,
    onOpened: void 0,
    duration: 3e3,
    className: "",
    lockScroll: false,
    background: void 0
  };
}
Notify.clear = () => {
  if (instance3) {
    instance3.toggle(false);
  }
};
Notify.currentOptions = defaultOptions3();
Notify.setDefaultOptions = (options) => {
  extend(Notify.currentOptions, options);
};
Notify.resetDefaultOptions = () => {
  Notify.currentOptions = defaultOptions3();
};
Notify.install = (app) => {
  app.use(withInstall(Notify_default));
  app.config.globalProperties.$notify = Notify;
};
Notify.Component = withInstall(Notify_default);

// node_modules/vant/es/number-keyboard/NumberKeyboard.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/number-keyboard/NumberKeyboardKey.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name73, bem69] = createNamespace("key");
var CollapseIcon = createVNode("svg", {
  "class": bem69("collapse-icon"),
  "viewBox": "0 0 30 24"
}, [createVNode("path", {
  "d": "M25.877 12.843h-1.502c-.188 0-.188 0-.188.19v1.512c0 .188 0 .188.188.188h1.5c.187 0 .187 0 .187-.188v-1.511c0-.19 0-.191-.185-.191zM17.999 10.2c0 .188 0 .188.188.188h1.687c.188 0 .188 0 .188-.188V8.688c0-.187.004-.187-.186-.19h-1.69c-.187 0-.187 0-.187.19V10.2zm2.25-3.967h1.5c.188 0 .188 0 .188-.188v-1.7c0-.19 0-.19-.188-.19h-1.5c-.189 0-.189 0-.189.19v1.7c0 .188 0 .188.19.188zm2.063 4.157h3.563c.187 0 .187 0 .187-.189V4.346c0-.19.004-.19-.185-.19h-1.69c-.187 0-.187 0-.187.188v4.155h-1.688c-.187 0-.187 0-.187.189v1.514c0 .19 0 .19.187.19zM14.812 24l2.812-3.4H12l2.813 3.4zm-9-11.157H4.31c-.188 0-.188 0-.188.19v1.512c0 .188 0 .188.188.188h1.502c.187 0 .187 0 .187-.188v-1.511c0-.19.01-.191-.189-.191zm15.937 0H8.25c-.188 0-.188 0-.188.19v1.512c0 .188 0 .188.188.188h13.5c.188 0 .188 0 .188-.188v-1.511c0-.19 0-.191-.188-.191zm-11.438-2.454h1.5c.188 0 .188 0 .188-.188V8.688c0-.187 0-.187-.188-.189h-1.5c-.187 0-.187 0-.187.189V10.2c0 .188 0 .188.187.188zM27.94 0c.563 0 .917.21 1.313.567.518.466.748.757.748 1.51v14.92c0 .567-.188 1.134-.562 1.512-.376.378-.938.566-1.313.566H2.063c-.563 0-.938-.188-1.313-.566-.562-.378-.75-.945-.75-1.511V2.078C0 1.51.188.944.562.567.938.189 1.5 0 1.875 0zm-.062 2H2v14.92h25.877V2zM5.81 4.157c.19 0 .19 0 .19.189v1.762c-.003.126-.024.126-.188.126H4.249c-.126-.003-.126-.023-.126-.188v-1.7c-.187-.19 0-.19.188-.19zm10.5 2.077h1.503c.187 0 .187 0 .187-.188v-1.7c0-.19 0-.19-.187-.19h-1.502c-.188 0-.188.001-.188.19v1.7c0 .188 0 .188.188.188zM7.875 8.5c.187 0 .187.002.187.189V10.2c0 .188 0 .188-.187.188H4.249c-.126-.002-.126-.023-.126-.188V8.625c.003-.126.024-.126.188-.126zm7.875 0c.19.002.19.002.19.189v1.575c-.003.126-.024.126-.19.126h-1.563c-.126-.002-.126-.023-.126-.188V8.625c.002-.126.023-.126.189-.126zm-6-4.342c.187 0 .187 0 .187.189v1.7c0 .188 0 .188-.187.188H8.187c-.126-.003-.126-.023-.126-.188V4.283c.003-.126.024-.126.188-.126zm3.94 0c.185 0 .372 0 .372.189v1.762c-.002.126-.023.126-.187.126h-1.75C12 6.231 12 6.211 12 6.046v-1.7c0-.19.187-.19.187-.19z",
  "fill": "currentColor"
}, null)]);
var DeleteIcon = createVNode("svg", {
  "class": bem69("delete-icon"),
  "viewBox": "0 0 32 22"
}, [createVNode("path", {
  "d": "M28.016 0A3.991 3.991 0 0132 3.987v14.026c0 2.2-1.787 3.987-3.98 3.987H10.382c-.509 0-.996-.206-1.374-.585L.89 13.09C.33 12.62 0 11.84 0 11.006c0-.86.325-1.62.887-2.08L9.01.585A1.936 1.936 0 0110.383 0zm0 1.947H10.368L2.24 10.28c-.224.226-.312.432-.312.73 0 .287.094.51.312.729l8.128 8.333h17.648a2.041 2.041 0 002.037-2.04V3.987c0-1.127-.915-2.04-2.037-2.04zM23.028 6a.96.96 0 01.678.292.95.95 0 01-.003 1.377l-3.342 3.348 3.326 3.333c.189.188.292.43.292.679 0 .248-.103.49-.292.679a.96.96 0 01-.678.292.959.959 0 01-.677-.292L18.99 12.36l-3.343 3.345a.96.96 0 01-.677.292.96.96 0 01-.678-.292.962.962 0 01-.292-.68c0-.248.104-.49.292-.679l3.342-3.348-3.342-3.348A.963.963 0 0114 6.971c0-.248.104-.49.292-.679A.96.96 0 0114.97 6a.96.96 0 01.677.292l3.358 3.348 3.345-3.348A.96.96 0 0123.028 6z",
  "fill": "currentColor"
}, null)]);
var NumberKeyboardKey_default = defineComponent({
  name: name73,
  props: {
    type: String,
    text: [Number, String],
    color: String,
    wider: Boolean,
    large: Boolean,
    loading: Boolean
  },
  emits: ["press"],
  setup(props10, {
    emit,
    slots
  }) {
    var active = ref(false);
    var touch = useTouch();
    var onTouchStart = (event) => {
      touch.start(event);
      active.value = true;
    };
    var onTouchMove = (event) => {
      touch.move(event);
      if (touch.direction.value) {
        active.value = false;
      }
    };
    var onTouchEnd = (event) => {
      if (active.value) {
        if (!slots.default) {
          event.preventDefault();
        }
        active.value = false;
        emit("press", props10.text, props10.type);
      }
    };
    var renderContent = () => {
      if (props10.loading) {
        return createVNode(Loading, {
          "class": bem69("loading-icon")
        }, null);
      }
      var text = slots.default ? slots.default() : props10.text;
      switch (props10.type) {
        case "delete":
          return text || DeleteIcon;
        case "extra":
          return text || CollapseIcon;
        default:
          return text;
      }
    };
    return () => createVNode("div", {
      "class": bem69("wrapper", {
        wider: props10.wider
      }),
      "onTouchstart": onTouchStart,
      "onTouchmove": onTouchMove,
      "onTouchend": onTouchEnd,
      "onTouchcancel": onTouchEnd
    }, [createVNode("div", {
      "role": "button",
      "tabindex": 0,
      "class": bem69([props10.color, {
        large: props10.large,
        active: active.value,
        delete: props10.type === "delete"
      }])
    }, [renderContent()])]);
  }
});

// node_modules/vant/es/number-keyboard/NumberKeyboard.js
var [name74, bem70] = createNamespace("number-keyboard");
var NumberKeyboard_default = defineComponent({
  name: name74,
  props: {
    show: Boolean,
    title: String,
    zIndex: [Number, String],
    teleport: [String, Object],
    transition: truthProp,
    blurOnClose: truthProp,
    showDeleteKey: truthProp,
    randomKeyOrder: Boolean,
    closeButtonText: String,
    deleteButtonText: String,
    closeButtonLoading: Boolean,
    hideOnClickOutside: truthProp,
    safeAreaInsetBottom: truthProp,
    theme: {
      type: String,
      default: "default"
    },
    modelValue: {
      type: String,
      default: ""
    },
    extraKey: {
      type: [String, Array],
      default: ""
    },
    maxlength: {
      type: [Number, String],
      default: Number.MAX_VALUE
    }
  },
  emits: ["show", "hide", "blur", "input", "close", "delete", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var root2 = ref();
    var genBasicKeys = () => {
      var keys2 = Array(9).fill("").map((_2, i) => ({
        text: i + 1
      }));
      if (props10.randomKeyOrder) {
        keys2.sort(() => Math.random() > 0.5 ? 1 : -1);
      }
      return keys2;
    };
    var genDefaultKeys = () => [...genBasicKeys(), {
      text: props10.extraKey,
      type: "extra"
    }, {
      text: 0
    }, {
      text: props10.showDeleteKey ? props10.deleteButtonText : "",
      type: props10.showDeleteKey ? "delete" : ""
    }];
    var genCustomKeys = () => {
      var keys2 = genBasicKeys();
      var {
        extraKey
      } = props10;
      var extraKeys = Array.isArray(extraKey) ? extraKey : [extraKey];
      if (extraKeys.length === 1) {
        keys2.push({
          text: 0,
          wider: true
        }, {
          text: extraKeys[0],
          type: "extra"
        });
      } else if (extraKeys.length === 2) {
        keys2.push({
          text: extraKeys[0],
          type: "extra"
        }, {
          text: 0
        }, {
          text: extraKeys[1],
          type: "extra"
        });
      }
      return keys2;
    };
    var keys = computed(() => props10.theme === "custom" ? genCustomKeys() : genDefaultKeys());
    var onBlur = () => {
      if (props10.show) {
        emit("blur");
      }
    };
    var onClose = () => {
      emit("close");
      if (props10.blurOnClose) {
        onBlur();
      }
    };
    var onAnimationEnd = () => emit(props10.show ? "show" : "hide");
    var onPress = (text, type) => {
      if (text === "") {
        if (type === "extra") {
          onBlur();
        }
        return;
      }
      var value = props10.modelValue;
      if (type === "delete") {
        emit("delete");
        emit("update:modelValue", value.slice(0, value.length - 1));
      } else if (type === "close") {
        onClose();
      } else if (value.length < props10.maxlength) {
        emit("input", text);
        emit("update:modelValue", value + text);
      }
    };
    var renderTitle = () => {
      var {
        title,
        theme,
        closeButtonText
      } = props10;
      var leftSlot = slots["title-left"];
      var showClose = closeButtonText && theme === "default";
      var showTitle = title || showClose || leftSlot;
      if (!showTitle) {
        return;
      }
      return createVNode("div", {
        "class": bem70("header")
      }, [leftSlot && createVNode("span", {
        "class": bem70("title-left")
      }, [leftSlot()]), title && createVNode("h2", {
        "class": bem70("title")
      }, [title]), showClose && createVNode("button", {
        "type": "button",
        "class": bem70("close"),
        "onClick": onClose
      }, [closeButtonText])]);
    };
    var renderKeys = () => {
      return keys.value.map((key) => {
        var keySlots = {};
        if (key.type === "delete") {
          keySlots.default = slots.delete;
        }
        if (key.type === "extra") {
          keySlots.default = slots["extra-key"];
        }
        return createVNode(NumberKeyboardKey_default, {
          "key": key.text,
          "text": key.text,
          "type": key.type,
          "wider": key.wider,
          "color": key.color,
          "onPress": onPress
        }, keySlots);
      });
    };
    var renderSidebar = () => {
      if (props10.theme === "custom") {
        return createVNode("div", {
          "class": bem70("sidebar")
        }, [props10.showDeleteKey && createVNode(NumberKeyboardKey_default, {
          "large": true,
          "text": props10.deleteButtonText,
          "type": "delete",
          "onPress": onPress
        }, {
          delete: slots.delete
        }), createVNode(NumberKeyboardKey_default, {
          "large": true,
          "text": props10.closeButtonText,
          "type": "close",
          "color": "blue",
          "loading": props10.closeButtonLoading,
          "onPress": onPress
        }, null)]);
      }
    };
    watch(() => props10.show, (value) => {
      if (!props10.transition) {
        emit(value ? "show" : "hide");
      }
    });
    if (props10.hideOnClickOutside) {
      useClickAway(root2, onClose, {
        eventName: "touchstart"
      });
    }
    return () => {
      var Title = renderTitle();
      var Content = createVNode(Transition, {
        "name": props10.transition ? "van-slide-up" : ""
      }, {
        default: () => [withDirectives(createVNode("div", {
          "ref": root2,
          "style": getZIndexStyle(props10.zIndex),
          "class": bem70({
            unfit: !props10.safeAreaInsetBottom,
            "with-title": !!Title
          }),
          "onTouchstart": stopPropagation,
          "onAnimationend": onAnimationEnd,
          "onWebkitAnimationEnd": onAnimationEnd
        }, [Title, createVNode("div", {
          "class": bem70("body")
        }, [createVNode("div", {
          "class": bem70("keys")
        }, [renderKeys()]), renderSidebar()])]), [[vShow, props10.show]])]
      });
      if (props10.teleport) {
        return createVNode(Teleport, {
          "to": props10.teleport
        }, {
          default: () => [Content]
        });
      }
      return Content;
    };
  }
});

// node_modules/vant/es/number-keyboard/index.js
var NumberKeyboard = withInstall(NumberKeyboard_default);

// node_modules/vant/es/pagination/Pagination.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name75, bem71, t15] = createNamespace("pagination");
function makePage(number, text, active) {
  return {
    number,
    text,
    active
  };
}
var Pagination_default = defineComponent({
  name: name75,
  props: {
    prevText: String,
    nextText: String,
    forceEllipses: Boolean,
    mode: {
      type: String,
      default: "multi"
    },
    modelValue: {
      type: Number,
      default: 0
    },
    pageCount: {
      type: [Number, String],
      default: 0
    },
    totalItems: {
      type: [Number, String],
      default: 0
    },
    itemsPerPage: {
      type: [Number, String],
      default: 10
    },
    showPageSize: {
      type: [Number, String],
      default: 5
    }
  },
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var count = computed(() => {
      var {
        pageCount,
        totalItems,
        itemsPerPage
      } = props10;
      var count2 = +pageCount || Math.ceil(+totalItems / +itemsPerPage);
      return Math.max(1, count2);
    });
    var pages = computed(() => {
      var items = [];
      var pageCount = count.value;
      var showPageSize = +props10.showPageSize;
      var {
        modelValue,
        forceEllipses
      } = props10;
      if (props10.mode !== "multi") {
        return items;
      }
      var startPage = 1;
      var endPage = pageCount;
      var isMaxSized = showPageSize < pageCount;
      if (isMaxSized) {
        startPage = Math.max(modelValue - Math.floor(showPageSize / 2), 1);
        endPage = startPage + showPageSize - 1;
        if (endPage > pageCount) {
          endPage = pageCount;
          startPage = endPage - showPageSize + 1;
        }
      }
      for (var number = startPage; number <= endPage; number++) {
        var page = makePage(number, number, number === modelValue);
        items.push(page);
      }
      if (isMaxSized && showPageSize > 0 && forceEllipses) {
        if (startPage > 1) {
          var prevPages = makePage(startPage - 1, "...");
          items.unshift(prevPages);
        }
        if (endPage < pageCount) {
          var nextPages = makePage(endPage + 1, "...");
          items.push(nextPages);
        }
      }
      return items;
    });
    var select = (page, emitChange) => {
      page = Math.min(count.value, Math.max(1, page));
      if (props10.modelValue !== page) {
        emit("update:modelValue", page);
        if (emitChange) {
          emit("change", page);
        }
      }
    };
    watch(() => props10.modelValue, (value) => {
      select(value);
    }, {
      immediate: true
    });
    var renderDesc = () => {
      if (props10.mode !== "multi") {
        return createVNode("li", {
          "class": bem71("page-desc")
        }, [slots.pageDesc ? slots.pageDesc() : props10.modelValue + "/" + count.value]);
      }
    };
    return () => {
      var value = props10.modelValue;
      var simple = props10.mode !== "multi";
      var onSelect = (value2) => () => select(value2, true);
      return createVNode("ul", {
        "class": bem71({
          simple
        })
      }, [createVNode("li", {
        "class": [bem71("item", {
          disabled: value === 1
        }), bem71("prev"), BORDER],
        "onClick": onSelect(value - 1)
      }, [slots["prev-text"] ? slots["prev-text"]() : props10.prevText || t15("prev")]), pages.value.map((page) => createVNode("li", {
        "class": [bem71("item", {
          active: page.active
        }), bem71("page"), BORDER],
        "onClick": onSelect(page.number)
      }, [slots.page ? slots.page(page) : page.text])), renderDesc(), createVNode("li", {
        "class": [bem71("item", {
          disabled: value === count.value
        }), bem71("next"), BORDER],
        "onClick": onSelect(value + 1)
      }, [slots["next-text"] ? slots["next-text"]() : props10.nextText || t15("next")])]);
    };
  }
});

// node_modules/vant/es/pagination/index.js
var Pagination = withInstall(Pagination_default);

// node_modules/vant/es/password-input/PasswordInput.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name76, bem72] = createNamespace("password-input");
var PasswordInput_default = defineComponent({
  name: name76,
  props: {
    info: String,
    mask: truthProp,
    gutter: [Number, String],
    focused: Boolean,
    errorInfo: String,
    value: {
      type: String,
      default: ""
    },
    length: {
      type: [Number, String],
      default: 6
    }
  },
  emits: ["focus"],
  setup(props10, {
    emit
  }) {
    var onTouchStart = (event) => {
      event.stopPropagation();
      emit("focus", event);
    };
    var renderPoints = () => {
      var Points = [];
      var {
        mask,
        value,
        length,
        gutter,
        focused
      } = props10;
      for (var i = 0; i < length; i++) {
        var char = value[i];
        var showBorder = i !== 0 && !gutter;
        var showCursor = focused && i === value.length;
        var style2 = void 0;
        if (i !== 0 && gutter) {
          style2 = {
            marginLeft: addUnit(gutter)
          };
        }
        Points.push(createVNode("li", {
          "class": [{
            [BORDER_LEFT]: showBorder
          }, bem72("item", {
            focus: showCursor
          })],
          "style": style2
        }, [mask ? createVNode("i", {
          "style": {
            visibility: char ? "visible" : "hidden"
          }
        }, null) : char, showCursor && createVNode("div", {
          "class": bem72("cursor")
        }, null)]));
      }
      return Points;
    };
    return () => {
      var info = props10.errorInfo || props10.info;
      return createVNode("div", {
        "class": bem72()
      }, [createVNode("ul", {
        "class": [bem72("security"), {
          [BORDER_SURROUND]: !props10.gutter
        }],
        "onTouchstart": onTouchStart
      }, [renderPoints()]), info && createVNode("div", {
        "class": bem72(props10.errorInfo ? "error-info" : "info")
      }, [info])]);
    };
  }
});

// node_modules/vant/es/password-input/index.js
var PasswordInput = withInstall(PasswordInput_default);

// node_modules/vant/es/popover/Popover.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/@vant/popperjs/dist/esm/index.js
function getBoundingClientRect(element) {
  var rect = element.getBoundingClientRect();
  return {
    width: rect.width,
    height: rect.height,
    top: rect.top,
    right: rect.right,
    bottom: rect.bottom,
    left: rect.left,
    x: rect.left,
    y: rect.top
  };
}
function getWindow(node) {
  if (node == null) {
    return window;
  }
  if (node.toString() !== "[object Window]") {
    var ownerDocument = node.ownerDocument;
    return ownerDocument ? ownerDocument.defaultView || window : window;
  }
  return node;
}
function getWindowScroll(node) {
  var win = getWindow(node);
  var scrollLeft = win.pageXOffset;
  var scrollTop = win.pageYOffset;
  return {
    scrollLeft,
    scrollTop
  };
}
function isElement2(node) {
  var OwnElement = getWindow(node).Element;
  return node instanceof OwnElement || node instanceof Element;
}
function isHTMLElement(node) {
  var OwnElement = getWindow(node).HTMLElement;
  return node instanceof OwnElement || node instanceof HTMLElement;
}
function isShadowRoot(node) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow(node).ShadowRoot;
  return node instanceof OwnElement || node instanceof ShadowRoot;
}
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}
function getNodeScroll(node) {
  if (node === getWindow(node) || !isHTMLElement(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}
function getNodeName(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}
function getDocumentElement(element) {
  return ((isElement2(element) ? element.ownerDocument : element.document) || window.document).documentElement;
}
function getWindowScrollBarX(element) {
  return getBoundingClientRect(getDocumentElement(element)).left + getWindowScroll(element).scrollLeft;
}
function getComputedStyle2(element) {
  return getWindow(element).getComputedStyle(element);
}
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle2(element), overflow2 = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow2 + overflowY + overflowX);
}
function getCompositeRect(elementOrVirtualElement, offsetParent, isFixed) {
  if (isFixed === void 0) {
    isFixed = false;
  }
  var documentElement = getDocumentElement(offsetParent);
  var rect = getBoundingClientRect(elementOrVirtualElement);
  var isOffsetParentAnElement = isHTMLElement(offsetParent);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed) {
    if (getNodeName(offsetParent) !== "body" || isScrollParent(documentElement)) {
      scroll = getNodeScroll(offsetParent);
    }
    if (isHTMLElement(offsetParent)) {
      offsets = getBoundingClientRect(offsetParent);
      offsets.x += offsetParent.clientLeft;
      offsets.y += offsetParent.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect(element);
  var width = element.offsetWidth;
  var height = element.offsetHeight;
  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }
  if (Math.abs(clientRect.height - height) <= 1) {
    height = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width,
    height
  };
}
function getParentNode(element) {
  if (getNodeName(element) === "html") {
    return element;
  }
  return element.assignedSlot || element.parentNode || (isShadowRoot(element) ? element.host : null) || getDocumentElement(element);
}
function getScrollParent2(node) {
  if (["html", "body", "#document"].indexOf(getNodeName(node)) >= 0) {
    return node.ownerDocument.body;
  }
  if (isHTMLElement(node) && isScrollParent(node)) {
    return node;
  }
  return getScrollParent2(getParentNode(node));
}
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent2 = getScrollParent2(element);
  var isBody = scrollParent2 === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = getWindow(scrollParent2);
  var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent2) ? scrollParent2 : []) : scrollParent2;
  var updatedList = list.concat(target);
  return isBody ? updatedList : updatedList.concat(listScrollParents(getParentNode(target)));
}
function isTableElement(element) {
  return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
}
function getTrueOffsetParent(element) {
  if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock(element) {
  var isFirefox = navigator.userAgent.toLowerCase().indexOf("firefox") !== -1;
  var isIE = navigator.userAgent.indexOf("Trident") !== -1;
  if (isIE && isHTMLElement(element)) {
    var elementCss = getComputedStyle2(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode(element);
  while (isHTMLElement(currentNode) && ["html", "body"].indexOf(getNodeName(currentNode)) < 0) {
    var css = getComputedStyle2(currentNode);
    if (css.transform !== "none" || css.perspective !== "none" || css.contain === "paint" || ["transform", "perspective"].indexOf(css.willChange) !== -1 || isFirefox && css.willChange === "filter" || isFirefox && css.filter && css.filter !== "none") {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent(element) {
  var window2 = getWindow(element);
  var offsetParent = getTrueOffsetParent(element);
  while (offsetParent && isTableElement(offsetParent) && getComputedStyle2(offsetParent).position === "static") {
    offsetParent = getTrueOffsetParent(offsetParent);
  }
  if (offsetParent && (getNodeName(offsetParent) === "html" || getNodeName(offsetParent) === "body" && getComputedStyle2(offsetParent).position === "static")) {
    return window2;
  }
  return offsetParent || getContainingBlock(element) || window2;
}
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var placements = [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main, afterMain, beforeWrite, write, afterWrite];
function order(modifiers) {
  var map = new Map();
  var visited = new Set();
  var result = [];
  modifiers.forEach(function(modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function(dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function(modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function(acc, phase) {
    return acc.concat(orderedModifiers.filter(function(modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}
function debounce(fn2) {
  var pending;
  return function() {
    if (!pending) {
      pending = new Promise(function(resolve) {
        Promise.resolve().then(function() {
          pending = void 0;
          resolve(fn2());
        });
      });
    }
    return pending;
  };
}
function format2(str) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }
  return [].concat(args).reduce(function(p, c) {
    return p.replace(/%s/, c);
  }, str);
}
var INVALID_MODIFIER_ERROR = 'Popper: modifier "%s" provided an invalid %s property, expected %s but got %s';
var MISSING_DEPENDENCY_ERROR = 'Popper: modifier "%s" requires "%s", but "%s" modifier is not available';
var VALID_PROPERTIES = ["name", "enabled", "phase", "fn", "effect", "requires", "options"];
function validateModifiers(modifiers) {
  modifiers.forEach(function(modifier) {
    Object.keys(modifier).forEach(function(key) {
      switch (key) {
        case "name":
          if (typeof modifier.name !== "string") {
            console.error(format2(INVALID_MODIFIER_ERROR, String(modifier.name), '"name"', '"string"', '"' + String(modifier.name) + '"'));
          }
          break;
        case "enabled":
          if (typeof modifier.enabled !== "boolean") {
            console.error(format2(INVALID_MODIFIER_ERROR, modifier.name, '"enabled"', '"boolean"', '"' + String(modifier.enabled) + '"'));
          }
        case "phase":
          if (modifierPhases.indexOf(modifier.phase) < 0) {
            console.error(format2(INVALID_MODIFIER_ERROR, modifier.name, '"phase"', "either " + modifierPhases.join(", "), '"' + String(modifier.phase) + '"'));
          }
          break;
        case "fn":
          if (typeof modifier.fn !== "function") {
            console.error(format2(INVALID_MODIFIER_ERROR, modifier.name, '"fn"', '"function"', '"' + String(modifier.fn) + '"'));
          }
          break;
        case "effect":
          if (typeof modifier.effect !== "function") {
            console.error(format2(INVALID_MODIFIER_ERROR, modifier.name, '"effect"', '"function"', '"' + String(modifier.fn) + '"'));
          }
          break;
        case "requires":
          if (!Array.isArray(modifier.requires)) {
            console.error(format2(INVALID_MODIFIER_ERROR, modifier.name, '"requires"', '"array"', '"' + String(modifier.requires) + '"'));
          }
          break;
        case "requiresIfExists":
          if (!Array.isArray(modifier.requiresIfExists)) {
            console.error(format2(INVALID_MODIFIER_ERROR, modifier.name, '"requiresIfExists"', '"array"', '"' + String(modifier.requiresIfExists) + '"'));
          }
          break;
        case "options":
        case "data":
          break;
        default:
          console.error('PopperJS: an invalid property has been provided to the "' + modifier.name + '" modifier, valid properties are ' + VALID_PROPERTIES.map(function(s) {
            return '"' + s + '"';
          }).join(", ") + '; but "' + key + '" was provided.');
      }
      modifier.requires && modifier.requires.forEach(function(requirement) {
        if (modifiers.find(function(mod) {
          return mod.name === requirement;
        }) == null) {
          console.error(format2(MISSING_DEPENDENCY_ERROR, String(modifier.name), requirement, requirement));
        }
      });
    });
  });
}
function uniqueBy(arr, fn2) {
  var identifiers = new Set();
  return arr.filter(function(item) {
    var identifier = fn2(item);
    if (!identifiers.has(identifier)) {
      identifiers.add(identifier);
      return true;
    }
  });
}
function getBasePlacement(placement) {
  return placement.split("-")[0];
}
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function(merged2, current) {
    var existing = merged2[current.name];
    merged2[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged2;
  }, {});
  return Object.keys(merged).map(function(key) {
    return merged[key];
  });
}
var round = Math.round;
function getVariation(placement) {
  return placement.split("-")[1];
}
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}
function computeOffsets(_ref) {
  var reference = _ref.reference, element = _ref.element, placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference.x + reference.width / 2 - element.width / 2;
  var commonY = reference.y + reference.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference.y - element.height
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference.y + reference.height
      };
      break;
    case right:
      offsets = {
        x: reference.x + reference.width,
        y: commonY
      };
      break;
    case left:
      offsets = {
        x: reference.x - element.width,
        y: commonY
      };
      break;
    default:
      offsets = {
        x: reference.x,
        y: reference.y
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] = offsets[mainAxis] - (reference[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] = offsets[mainAxis] + (reference[len] / 2 - element[len] / 2);
        break;
    }
  }
  return offsets;
}
var INVALID_ELEMENT_ERROR = "Popper: Invalid reference or popper argument provided. They must be either a DOM element or virtual element.";
var INFINITE_LOOP_ERROR = "Popper: An infinite loop in the modifiers cycle has been detected! The cycle has been interrupted to prevent a browser crash.";
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return !args.some(function(element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers2 = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions4 = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function createPopper2(reference, popper, options) {
    if (options === void 0) {
      options = defaultOptions4;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions4),
      modifiersData: {},
      elements: {
        reference,
        popper
      },
      attributes: {},
      styles: {}
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance4 = {
      state,
      setOptions: function setOptions(options2) {
        cleanupModifierEffects();
        state.options = Object.assign({}, defaultOptions4, state.options, options2);
        state.scrollParents = {
          reference: isElement2(reference) ? listScrollParents(reference) : reference.contextElement ? listScrollParents(reference.contextElement) : [],
          popper: listScrollParents(popper)
        };
        var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers2, state.options.modifiers)));
        state.orderedModifiers = orderedModifiers.filter(function(m) {
          return m.enabled;
        });
        if (true) {
          var modifiers = uniqueBy([].concat(orderedModifiers, state.options.modifiers), function(_ref) {
            var name96 = _ref.name;
            return name96;
          });
          validateModifiers(modifiers);
          if (getBasePlacement(state.options.placement) === auto) {
            var flipModifier = state.orderedModifiers.find(function(_ref2) {
              var name96 = _ref2.name;
              return name96 === "flip";
            });
            if (!flipModifier) {
              console.error(['Popper: "auto" placements require the "flip" modifier be', "present and enabled to work."].join(" "));
            }
          }
          var _getComputedStyle = getComputedStyle2(popper), marginTop = _getComputedStyle.marginTop, marginRight = _getComputedStyle.marginRight, marginBottom = _getComputedStyle.marginBottom, marginLeft = _getComputedStyle.marginLeft;
          if ([marginTop, marginRight, marginBottom, marginLeft].some(function(margin) {
            return parseFloat(margin);
          })) {
            console.warn(['Popper: CSS "margin" styles cannot be used to apply padding', "between the popper and its reference element or boundary.", "To replicate margin, use the `offset` modifier, as well as", "the `padding` option in the `preventOverflow` and `flip`", "modifiers."].join(" "));
          }
        }
        runModifierEffects();
        return instance4.update();
      },
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements, reference2 = _state$elements.reference, popper2 = _state$elements.popper;
        if (!areValidElements(reference2, popper2)) {
          if (true) {
            console.error(INVALID_ELEMENT_ERROR);
          }
          return;
        }
        state.rects = {
          reference: getCompositeRect(reference2, getOffsetParent(popper2), state.options.strategy === "fixed"),
          popper: getLayoutRect(popper2)
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function(modifier) {
          return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
        });
        var __debug_loops__ = 0;
        for (var index = 0; index < state.orderedModifiers.length; index++) {
          if (true) {
            __debug_loops__ += 1;
            if (__debug_loops__ > 100) {
              console.error(INFINITE_LOOP_ERROR);
              break;
            }
          }
          if (state.reset === true) {
            state.reset = false;
            index = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index], fn2 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name96 = _state$orderedModifie.name;
          if (typeof fn2 === "function") {
            state = fn2({
              state,
              options: _options,
              name: name96,
              instance: instance4
            }) || state;
          }
        }
      },
      update: debounce(function() {
        return new Promise(function(resolve) {
          instance4.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      }
    };
    if (!areValidElements(reference, popper)) {
      if (true) {
        console.error(INVALID_ELEMENT_ERROR);
      }
      return instance4;
    }
    instance4.setOptions(options).then(function(state2) {
      if (!isDestroyed && options.onFirstUpdate) {
        options.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function(_ref3) {
        var name96 = _ref3.name, _ref3$options = _ref3.options, options2 = _ref3$options === void 0 ? {} : _ref3$options, effect2 = _ref3.effect;
        if (typeof effect2 === "function") {
          var cleanupFn = effect2({
            state,
            name: name96,
            instance: instance4,
            options: options2
          });
          var noopFn = function noopFn2() {
          };
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function(fn2) {
        return fn2();
      });
      effectCleanupFns = [];
    }
    return instance4;
  };
}
var passive = {
  passive: true
};
function effect(_ref) {
  var state = _ref.state, instance4 = _ref.instance, options = _ref.options;
  var _options$scroll = options.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options.resize, resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
  if (scroll) {
    scrollParents.forEach(function(scrollParent2) {
      scrollParent2.addEventListener("scroll", instance4.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance4.update, passive);
  }
  return function() {
    if (scroll) {
      scrollParents.forEach(function(scrollParent2) {
        scrollParent2.removeEventListener("scroll", instance4.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance4.update, passive);
    }
  };
}
var eventListeners = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {
  },
  effect,
  data: {}
};
function popperOffsets(_ref) {
  var state = _ref.state, name96 = _ref.name;
  state.modifiersData[name96] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    strategy: "absolute",
    placement: state.placement
  });
}
var popperOffsets$1 = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {}
};
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function roundOffsetsByDPR(_ref) {
  var x = _ref.x, y = _ref.y;
  var win = window;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round(round(x * dpr) / dpr) || 0,
    y: round(round(y * dpr) / dpr) || 0
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, offsets = _ref2.offsets, position = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets;
  var _ref3 = roundOffsets === true ? roundOffsetsByDPR(offsets) : typeof roundOffsets === "function" ? roundOffsets(offsets) : offsets, _ref3$x = _ref3.x, x = _ref3$x === void 0 ? 0 : _ref3$x, _ref3$y = _ref3.y, y = _ref3$y === void 0 ? 0 : _ref3$y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent = getOffsetParent(popper);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent === getWindow(popper)) {
      offsetParent = getDocumentElement(popper);
      if (getComputedStyle2(offsetParent).position !== "static") {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent = offsetParent;
    if (placement === top) {
      sideY = bottom;
      y -= offsetParent[heightProp] - popperRect.height;
      y *= gpuAcceleration ? 1 : -1;
    }
    if (placement === left) {
      sideX = right;
      x -= offsetParent[widthProp] - popperRect.width;
      x *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign({
    position
  }, adaptive && unsetSides);
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) < 2 ? "translate(" + x + "px, " + y + "px)" : "translate3d(" + x + "px, " + y + "px, 0)", _Object$assign));
  }
  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y + "px" : "", _Object$assign2[sideX] = hasX ? x + "px" : "", _Object$assign2.transform = "", _Object$assign2));
}
function computeStyles(_ref4) {
  var state = _ref4.state, options = _ref4.options;
  var _options$gpuAccelerat = options.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  if (true) {
    var transitionProperty = getComputedStyle2(state.elements.popper).transitionProperty || "";
    if (adaptive && ["transform", "top", "right", "bottom", "left"].some(function(property) {
      return transitionProperty.indexOf(property) >= 0;
    })) {
      console.warn(["Popper: Detected CSS transitions on at least one of the following", 'CSS properties: "transform", "top", "right", "bottom", "left".', "\n\n", 'Disable the "computeStyles" modifier\'s `adaptive` option to allow', "for smooth transitions, or remove these properties from the CSS", "transition declaration on the popper element if only transitioning", "opacity or background-color for example.", "\n\n", "We recommend using the popper element as a wrapper around an inner", "element that can have any CSS property transitioned for animations."].join(" "));
    }
  }
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive,
      roundOffsets
    })));
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: "absolute",
      adaptive: false,
      roundOffsets
    })));
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement
  });
}
var computeStyles$1 = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {}
};
function applyStyles(_ref) {
  var state = _ref.state;
  Object.keys(state.elements).forEach(function(name96) {
    var style2 = state.styles[name96] || {};
    var attributes = state.attributes[name96] || {};
    var element = state.elements[name96];
    if (!isHTMLElement(element) || !getNodeName(element)) {
      return;
    }
    Object.assign(element.style, style2);
    Object.keys(attributes).forEach(function(name97) {
      var value = attributes[name97];
      if (value === false) {
        element.removeAttribute(name97);
      } else {
        element.setAttribute(name97, value === true ? "" : value);
      }
    });
  });
}
function effect$1(_ref2) {
  var state = _ref2.state;
  var initialStyles = {
    popper: {
      position: state.options.strategy,
      left: "0",
      top: "0",
      margin: "0"
    },
    arrow: {
      position: "absolute"
    },
    reference: {}
  };
  Object.assign(state.elements.popper.style, initialStyles.popper);
  state.styles = initialStyles;
  if (state.elements.arrow) {
    Object.assign(state.elements.arrow.style, initialStyles.arrow);
  }
  return function() {
    Object.keys(state.elements).forEach(function(name96) {
      var element = state.elements[name96];
      var attributes = state.attributes[name96] || {};
      var styleProperties = Object.keys(state.styles.hasOwnProperty(name96) ? state.styles[name96] : initialStyles[name96]);
      var style2 = styleProperties.reduce(function(style3, property) {
        style3[property] = "";
        return style3;
      }, {});
      if (!isHTMLElement(element) || !getNodeName(element)) {
        return;
      }
      Object.assign(element.style, style2);
      Object.keys(attributes).forEach(function(attribute) {
        element.removeAttribute(attribute);
      });
    });
  };
}
var applyStyles$1 = {
  name: "applyStyles",
  enabled: true,
  phase: "write",
  fn: applyStyles,
  effect: effect$1,
  requires: ["computeStyles"]
};
var defaultModifiers = [eventListeners, popperOffsets$1, computeStyles$1, applyStyles$1];
var createPopper = popperGenerator({
  defaultModifiers
});
function distanceAndSkiddingToXY(placement, rects, offset2) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref = typeof offset2 === "function" ? offset2(Object.assign({}, rects, {
    placement
  })) : offset2, skidding = _ref[0], distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}
function offset(_ref2) {
  var state = _ref2.state, options = _ref2.options, name96 = _ref2.name;
  var _options$offset = options.offset, offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements.reduce(function(acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement], x = _data$state$placement.x, y = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x;
    state.modifiersData.popperOffsets.y += y;
  }
  state.modifiersData[name96] = data;
}
var offset$1 = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset
};

// node_modules/vant/es/popover/Popover.js
var [name77, bem73] = createNamespace("popover");
var popupProps = ["show", "overlay", "duration", "teleport", "overlayStyle", "overlayClass", "closeOnClickOverlay"];
var Popover_default = defineComponent({
  name: name77,
  props: {
    show: Boolean,
    overlay: Boolean,
    duration: [Number, String],
    iconPrefix: String,
    overlayClass: unknownProp,
    overlayStyle: Object,
    closeOnClickAction: truthProp,
    closeOnClickOverlay: truthProp,
    closeOnClickOutside: truthProp,
    offset: {
      type: Array,
      default: () => [0, 8]
    },
    theme: {
      type: String,
      default: "light"
    },
    trigger: {
      type: String,
      default: "click"
    },
    actions: {
      type: Array,
      default: () => []
    },
    placement: {
      type: String,
      default: "bottom"
    },
    teleport: {
      type: [String, Object],
      default: "body"
    }
  },
  emits: ["select", "touchstart", "update:show"],
  setup(props10, {
    emit,
    slots,
    attrs
  }) {
    var popper;
    var wrapperRef = ref();
    var popoverRef = ref();
    var createPopperInstance = () => {
      return createPopper(wrapperRef.value, popoverRef.value.popupRef.value, {
        placement: props10.placement,
        modifiers: [{
          name: "computeStyles",
          options: {
            adaptive: false,
            gpuAcceleration: false
          }
        }, extend({}, offset$1, {
          options: {
            offset: props10.offset
          }
        })]
      });
    };
    var updateLocation = () => {
      nextTick(() => {
        if (!props10.show) {
          return;
        }
        if (!popper) {
          popper = createPopperInstance();
        } else {
          popper.setOptions({
            placement: props10.placement
          });
        }
      });
    };
    var updateShow = (value) => emit("update:show", value);
    var onClickWrapper = () => {
      if (props10.trigger === "click") {
        updateShow(!props10.show);
      }
    };
    var onTouchstart = (event) => {
      event.stopPropagation();
      emit("touchstart", event);
    };
    var onClickAction = (action, index) => {
      if (action.disabled) {
        return;
      }
      emit("select", action, index);
      if (props10.closeOnClickAction) {
        updateShow(false);
      }
    };
    var onClickAway = () => {
      if (props10.closeOnClickOutside && (!props10.overlay || props10.closeOnClickOverlay)) {
        updateShow(false);
      }
    };
    var renderAction = (action, index) => {
      var {
        icon,
        text,
        color,
        disabled,
        className
      } = action;
      return createVNode("div", {
        "role": "menuitem",
        "class": [bem73("action", {
          disabled,
          "with-icon": icon
        }), className],
        "style": {
          color
        },
        "onClick": () => onClickAction(action, index)
      }, [icon && createVNode(Icon, {
        "name": icon,
        "classPrefix": props10.iconPrefix,
        "class": bem73("action-icon")
      }, null), createVNode("div", {
        "class": [bem73("action-text"), BORDER_BOTTOM]
      }, [text])]);
    };
    onMounted(updateLocation);
    onBeforeUnmount(() => {
      if (popper) {
        popper.destroy();
        popper = null;
      }
    });
    watch(() => [props10.show, props10.placement], updateLocation);
    useClickAway(wrapperRef, onClickAway, {
      eventName: "touchstart"
    });
    return () => createVNode(Fragment, null, [createVNode("span", {
      "ref": wrapperRef,
      "class": bem73("wrapper"),
      "onClick": onClickWrapper
    }, [slots.reference == null ? void 0 : slots.reference()]), createVNode(Popup, mergeProps({
      "ref": popoverRef,
      "class": bem73([props10.theme]),
      "position": "",
      "transition": "van-popover-zoom",
      "lockScroll": false,
      "onTouchstart": onTouchstart
    }, attrs, pick(props10, popupProps), {
      "onUpdate:show": updateShow
    }), {
      default: () => [createVNode("div", {
        "class": bem73("arrow")
      }, null), createVNode("div", {
        "role": "menu",
        "class": bem73("content")
      }, [slots.default ? slots.default() : props10.actions.map(renderAction)])]
    })]);
  }
});

// node_modules/vant/es/popover/index.js
var Popover = withInstall(Popover_default);

// node_modules/vant/es/progress/Progress.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name78, bem74] = createNamespace("progress");
var Progress_default = defineComponent({
  name: name78,
  props: {
    color: String,
    inactive: Boolean,
    pivotText: String,
    textColor: String,
    showPivot: truthProp,
    pivotColor: String,
    trackColor: String,
    strokeWidth: [Number, String],
    percentage: {
      type: [Number, String],
      required: true,
      validator: (value) => value >= 0 && value <= 100
    }
  },
  setup(props10) {
    var root2 = ref();
    var pivotRef = ref();
    var state = reactive({
      rootWidth: 0,
      pivotWidth: 0
    });
    var background = computed(() => props10.inactive ? "#cacaca" : props10.color);
    var resize = () => {
      nextTick(() => {
        state.rootWidth = root2.value ? root2.value.offsetWidth : 0;
        state.pivotWidth = pivotRef.value ? pivotRef.value.offsetWidth : 0;
      });
    };
    var renderPivot = () => {
      var {
        rootWidth,
        pivotWidth
      } = state;
      var {
        textColor,
        pivotText,
        pivotColor,
        percentage
      } = props10;
      var text = pivotText != null ? pivotText : percentage + "%";
      var show = props10.showPivot && text;
      if (show) {
        var left2 = (rootWidth - pivotWidth) * +percentage / 100;
        var style2 = {
          color: textColor,
          left: left2 + "px",
          background: pivotColor || background.value
        };
        return createVNode("span", {
          "ref": pivotRef,
          "style": style2,
          "class": bem74("pivot")
        }, [text]);
      }
    };
    watch(() => [props10.showPivot, props10.pivotText], resize);
    onMounted(resize);
    useExpose({
      resize
    });
    return () => {
      var {
        trackColor,
        percentage,
        strokeWidth
      } = props10;
      var rootStyle = {
        background: trackColor,
        height: addUnit(strokeWidth)
      };
      var portionStyle = {
        background: background.value,
        width: state.rootWidth * +percentage / 100 + "px"
      };
      return createVNode("div", {
        "ref": root2,
        "class": bem74(),
        "style": rootStyle
      }, [createVNode("span", {
        "class": bem74("portion"),
        "style": portionStyle
      }, [renderPivot()])]);
    };
  }
});

// node_modules/vant/es/progress/index.js
var Progress = withInstall(Progress_default);

// node_modules/vant/es/pull-refresh/PullRefresh.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name79, bem75, t16] = createNamespace("pull-refresh");
var DEFAULT_HEAD_HEIGHT = 50;
var TEXT_STATUS = ["pulling", "loosing", "success"];
var PullRefresh_default = defineComponent({
  name: name79,
  props: {
    disabled: Boolean,
    successText: String,
    pullingText: String,
    loosingText: String,
    loadingText: String,
    pullDistance: [Number, String],
    modelValue: {
      type: Boolean,
      default: false
    },
    successDuration: {
      type: [Number, String],
      default: 500
    },
    animationDuration: {
      type: [Number, String],
      default: 300
    },
    headHeight: {
      type: [Number, String],
      default: DEFAULT_HEAD_HEIGHT
    }
  },
  emits: ["refresh", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var reachTop;
    var root2 = ref();
    var scrollParent2 = useScrollParent(root2);
    var state = reactive({
      status: "normal",
      distance: 0,
      duration: 0
    });
    var touch = useTouch();
    var getHeadStyle = () => {
      if (props10.headHeight !== DEFAULT_HEAD_HEIGHT) {
        return {
          height: props10.headHeight + "px"
        };
      }
    };
    var isTouchable = () => state.status !== "loading" && state.status !== "success" && !props10.disabled;
    var ease = (distance) => {
      var pullDistance = +(props10.pullDistance || props10.headHeight);
      if (distance > pullDistance) {
        if (distance < pullDistance * 2) {
          distance = pullDistance + (distance - pullDistance) / 2;
        } else {
          distance = pullDistance * 1.5 + (distance - pullDistance * 2) / 4;
        }
      }
      return Math.round(distance);
    };
    var setStatus = (distance, isLoading) => {
      var pullDistance = +(props10.pullDistance || props10.headHeight);
      state.distance = distance;
      if (isLoading) {
        state.status = "loading";
      } else if (distance === 0) {
        state.status = "normal";
      } else if (distance < pullDistance) {
        state.status = "pulling";
      } else {
        state.status = "loosing";
      }
    };
    var getStatusText = () => {
      var {
        status
      } = state;
      if (status === "normal") {
        return "";
      }
      return props10[status + "Text"] || t16(status);
    };
    var renderStatus = () => {
      var {
        status,
        distance
      } = state;
      if (slots[status]) {
        return slots[status]({
          distance
        });
      }
      var nodes = [];
      if (TEXT_STATUS.includes(status)) {
        nodes.push(createVNode("div", {
          "class": bem75("text")
        }, [getStatusText()]));
      }
      if (status === "loading") {
        nodes.push(createVNode(Loading, {
          "class": bem75("loading")
        }, {
          default: () => [getStatusText()]
        }));
      }
      return nodes;
    };
    var showSuccessTip = () => {
      state.status = "success";
      setTimeout(() => {
        setStatus(0);
      }, +props10.successDuration);
    };
    var checkPosition = (event) => {
      reachTop = getScrollTop(scrollParent2.value) === 0;
      if (reachTop) {
        state.duration = 0;
        touch.start(event);
      }
    };
    var onTouchStart = (event) => {
      if (isTouchable()) {
        checkPosition(event);
      }
    };
    var onTouchMove = (event) => {
      if (isTouchable()) {
        if (!reachTop) {
          checkPosition(event);
        }
        var {
          deltaY
        } = touch;
        touch.move(event);
        if (reachTop && deltaY.value >= 0 && touch.isVertical()) {
          preventDefault(event);
          setStatus(ease(deltaY.value));
        }
      }
    };
    var onTouchEnd = () => {
      if (reachTop && touch.deltaY.value && isTouchable()) {
        state.duration = +props10.animationDuration;
        if (state.status === "loosing") {
          setStatus(+props10.headHeight, true);
          emit("update:modelValue", true);
          nextTick(() => emit("refresh"));
        } else {
          setStatus(0);
        }
      }
    };
    watch(() => props10.modelValue, (value) => {
      state.duration = +props10.animationDuration;
      if (value) {
        setStatus(+props10.headHeight, true);
      } else if (slots.success || props10.successText) {
        showSuccessTip();
      } else {
        setStatus(0, false);
      }
    });
    return () => {
      var trackStyle = {
        transitionDuration: state.duration + "ms",
        transform: state.distance ? "translate3d(0," + state.distance + "px, 0)" : ""
      };
      return createVNode("div", {
        "ref": root2,
        "class": bem75()
      }, [createVNode("div", {
        "class": bem75("track"),
        "style": trackStyle,
        "onTouchstart": onTouchStart,
        "onTouchmove": onTouchMove,
        "onTouchend": onTouchEnd,
        "onTouchcancel": onTouchEnd
      }, [createVNode("div", {
        "class": bem75("head"),
        "style": getHeadStyle()
      }, [renderStatus()]), slots.default == null ? void 0 : slots.default()])]);
    };
  }
});

// node_modules/vant/es/pull-refresh/index.js
var PullRefresh = withInstall(PullRefresh_default);

// node_modules/vant/es/rate/Rate.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name80, bem76] = createNamespace("rate");
function getRateStatus(value, index, allowHalf, readonly) {
  if (value >= index) {
    return {
      status: "full",
      value: 1
    };
  }
  if (value + 0.5 >= index && allowHalf && !readonly) {
    return {
      status: "half",
      value: 0.5
    };
  }
  if (value + 1 >= index && allowHalf && readonly) {
    var cardinal = Math.pow(10, 10);
    return {
      status: "half",
      value: Math.round((value - index + 1) * cardinal) / cardinal
    };
  }
  return {
    status: "void",
    value: 0
  };
}
var Rate_default = defineComponent({
  name: name80,
  props: {
    size: [Number, String],
    color: String,
    gutter: [Number, String],
    readonly: Boolean,
    disabled: Boolean,
    allowHalf: Boolean,
    voidColor: String,
    touchable: truthProp,
    iconPrefix: String,
    disabledColor: String,
    modelValue: {
      type: Number,
      default: 0
    },
    icon: {
      type: String,
      default: "star"
    },
    voidIcon: {
      type: String,
      default: "star-o"
    },
    count: {
      type: [Number, String],
      default: 5
    }
  },
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit
  }) {
    var touch = useTouch();
    var [itemRefs, setItemRefs] = useRefs();
    var untouchable = () => props10.readonly || props10.disabled || !props10.touchable;
    var list = computed(() => Array(props10.count).fill("").map((_2, i) => getRateStatus(props10.modelValue, i + 1, props10.allowHalf, props10.readonly)));
    var ranges;
    var updateRanges = () => {
      var rects = itemRefs.value.map((item) => item.getBoundingClientRect());
      ranges = [];
      rects.forEach((rect, index) => {
        if (props10.allowHalf) {
          ranges.push({
            score: index + 0.5,
            left: rect.left
          }, {
            score: index + 1,
            left: rect.left + rect.width / 2
          });
        } else {
          ranges.push({
            score: index + 1,
            left: rect.left
          });
        }
      });
    };
    var getScoreByPosition = (x) => {
      for (var i = ranges.length - 1; i > 0; i--) {
        if (x > ranges[i].left) {
          return ranges[i].score;
        }
      }
      return props10.allowHalf ? 0.5 : 1;
    };
    var select = (index) => {
      if (!props10.disabled && !props10.readonly && index !== props10.modelValue) {
        emit("update:modelValue", index);
        emit("change", index);
      }
    };
    var onTouchStart = (event) => {
      if (untouchable()) {
        return;
      }
      touch.start(event);
      updateRanges();
    };
    var onTouchMove = (event) => {
      if (untouchable()) {
        return;
      }
      touch.move(event);
      if (touch.isHorizontal()) {
        var {
          clientX
        } = event.touches[0];
        preventDefault(event);
        select(getScoreByPosition(clientX));
      }
    };
    var renderStar = (item, index) => {
      var {
        icon,
        size,
        color,
        count,
        gutter,
        voidIcon,
        disabled,
        voidColor,
        allowHalf,
        iconPrefix,
        disabledColor
      } = props10;
      var score = index + 1;
      var isFull = item.status === "full";
      var isVoid = item.status === "void";
      var renderHalf = allowHalf && item.value > 0 && item.value < 1;
      var style2;
      if (gutter && score !== +count) {
        style2 = {
          paddingRight: addUnit(gutter)
        };
      }
      var onClickItem = (event) => {
        updateRanges();
        select(allowHalf ? getScoreByPosition(event.clientX) : score);
      };
      return createVNode("div", {
        "key": index,
        "ref": setItemRefs(index),
        "role": "radio",
        "style": style2,
        "class": bem76("item"),
        "tabindex": 0,
        "aria-setsize": +count,
        "aria-posinset": score,
        "aria-checked": !isVoid,
        "onClick": onClickItem
      }, [createVNode(Icon, {
        "size": size,
        "name": isFull ? icon : voidIcon,
        "class": bem76("icon", {
          disabled,
          full: isFull
        }),
        "color": disabled ? disabledColor : isFull ? color : voidColor,
        "classPrefix": iconPrefix
      }, null), renderHalf && createVNode(Icon, {
        "size": size,
        "style": {
          width: item.value + "em"
        },
        "name": isVoid ? voidIcon : icon,
        "class": bem76("icon", ["half", {
          disabled,
          full: !isVoid
        }]),
        "color": disabled ? disabledColor : isVoid ? voidColor : color,
        "classPrefix": iconPrefix
      }, null)]);
    };
    useLinkField(() => props10.modelValue);
    return () => createVNode("div", {
      "role": "radiogroup",
      "class": bem76({
        readonly: props10.readonly,
        disabled: props10.disabled
      }),
      "tabindex": 0,
      "onTouchstart": onTouchStart,
      "onTouchmove": onTouchMove
    }, [list.value.map(renderStar)]);
  }
});

// node_modules/vant/es/rate/index.js
var Rate = withInstall(Rate_default);

// node_modules/vant/es/row/index.js
var Row = withInstall(Row_default);

// node_modules/vant/es/search/Search.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name81, bem77, t17] = createNamespace("search");
var Search_default = defineComponent({
  name: name81,
  props: extend({}, fieldProps, {
    label: String,
    clearable: truthProp,
    actionText: String,
    background: String,
    showAction: Boolean,
    shape: {
      type: String,
      default: "square"
    },
    leftIcon: {
      type: String,
      default: "search"
    }
  }),
  emits: ["search", "cancel", "update:modelValue"],
  setup(props10, {
    emit,
    slots,
    attrs
  }) {
    var filedRef = ref();
    var onCancel = () => {
      if (!slots.action) {
        emit("update:modelValue", "");
        emit("cancel");
      }
    };
    var onKeypress = (event) => {
      var ENTER_CODE = 13;
      if (event.keyCode === ENTER_CODE) {
        preventDefault(event);
        emit("search", props10.modelValue);
      }
    };
    var renderLabel = () => {
      if (slots.label || props10.label) {
        return createVNode("div", {
          "class": bem77("label")
        }, [slots.label ? slots.label() : props10.label]);
      }
    };
    var renderAction = () => {
      if (props10.showAction) {
        var text = props10.actionText || t17("cancel");
        return createVNode("div", {
          "class": bem77("action"),
          "role": "button",
          "tabindex": 0,
          "onClick": onCancel
        }, [slots.action ? slots.action() : text]);
      }
    };
    var blur = () => {
      var _filedRef$value;
      return (_filedRef$value = filedRef.value) == null ? void 0 : _filedRef$value.blur();
    };
    var focus = () => {
      var _filedRef$value2;
      return (_filedRef$value2 = filedRef.value) == null ? void 0 : _filedRef$value2.focus();
    };
    var fieldPropNames = Object.keys(fieldProps);
    var renderField = () => {
      var fieldAttrs = extend({}, attrs, pick(props10, fieldPropNames));
      var onInput = (value) => emit("update:modelValue", value);
      return createVNode(Field, mergeProps({
        "ref": filedRef,
        "type": "search",
        "border": false,
        "onKeypress": onKeypress
      }, fieldAttrs, {
        "onUpdate:modelValue": onInput
      }), pick(slots, ["left-icon", "right-icon"]));
    };
    useExpose({
      focus,
      blur
    });
    return () => createVNode("div", {
      "class": bem77({
        "show-action": props10.showAction
      }),
      "style": {
        background: props10.background
      }
    }, [slots.left == null ? void 0 : slots.left(), createVNode("div", {
      "class": bem77("content", props10.shape)
    }, [renderLabel(), renderField()]), renderAction()]);
  }
});

// node_modules/vant/es/search/index.js
var Search = withInstall(Search_default);

// node_modules/vant/es/share-sheet/ShareSheet.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var PRESET_ICONS = ["qq", "link", "weibo", "wechat", "poster", "qrcode", "weapp-qrcode", "wechat-moments"];
var popupKeys2 = [...popupSharedPropKeys, "closeOnPopstate", "safeAreaInsetBottom"];
function getIconURL(icon) {
  if (PRESET_ICONS.includes(icon)) {
    return "https://img.yzcdn.cn/vant/share-sheet-" + icon + ".png";
  }
  return icon;
}
var [name82, bem78, t18] = createNamespace("share-sheet");
var ShareSheet_default = defineComponent({
  name: name82,
  props: extend({}, popupSharedProps, {
    title: String,
    cancelText: String,
    description: String,
    closeOnPopstate: truthProp,
    safeAreaInsetBottom: truthProp,
    options: {
      type: Array,
      default: () => []
    }
  }),
  emits: ["cancel", "select", "update:show"],
  setup(props10, {
    emit,
    slots
  }) {
    var updateShow = (value) => emit("update:show", value);
    var onCancel = () => {
      updateShow(false);
      emit("cancel");
    };
    var onSelect = (option, index) => emit("select", option, index);
    var renderHeader = () => {
      var title = slots.title ? slots.title() : props10.title;
      var description = slots.description ? slots.description() : props10.description;
      if (title || description) {
        return createVNode("div", {
          "class": bem78("header")
        }, [title && createVNode("h2", {
          "class": bem78("title")
        }, [title]), description && createVNode("span", {
          "class": bem78("description")
        }, [description])]);
      }
    };
    var renderOption = (option, index) => {
      var {
        name: name96,
        icon,
        className,
        description
      } = option;
      return createVNode("div", {
        "role": "button",
        "tabindex": 0,
        "class": [bem78("option"), className],
        "onClick": () => onSelect(option, index)
      }, [createVNode("img", {
        "src": getIconURL(icon),
        "class": bem78("icon")
      }, null), name96 && createVNode("span", {
        "class": bem78("name")
      }, [name96]), description && createVNode("span", {
        "class": bem78("option-description")
      }, [description])]);
    };
    var renderOptions = (options, border) => createVNode("div", {
      "class": bem78("options", {
        border
      })
    }, [options.map(renderOption)]);
    var renderRows = () => {
      var {
        options
      } = props10;
      if (Array.isArray(options[0])) {
        return options.map((item, index) => renderOptions(item, index !== 0));
      }
      return renderOptions(options);
    };
    var renderCancelButton = () => {
      var _props$cancelText;
      var cancelText = (_props$cancelText = props10.cancelText) != null ? _props$cancelText : t18("cancel");
      if (slots.cancel || cancelText) {
        return createVNode("button", {
          "type": "button",
          "class": bem78("cancel"),
          "onClick": onCancel
        }, [slots.cancel ? slots.cancel() : cancelText]);
      }
    };
    return () => createVNode(Popup, mergeProps({
      "round": true,
      "class": bem78(),
      "position": "bottom"
    }, pick(props10, popupKeys2), {
      "onUpdate:show": updateShow
    }), {
      default: () => [renderHeader(), renderRows(), renderCancelButton()]
    });
  }
});

// node_modules/vant/es/share-sheet/index.js
var ShareSheet = withInstall(ShareSheet_default);

// node_modules/vant/es/sidebar/Sidebar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name83, bem79] = createNamespace("sidebar");
var SIDEBAR_KEY = Symbol(name83);
var Sidebar_default = defineComponent({
  name: name83,
  props: {
    modelValue: {
      type: [Number, String],
      default: 0
    }
  },
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      linkChildren
    } = useChildren(SIDEBAR_KEY);
    var getActive = () => +props10.modelValue;
    var setActive = (value) => {
      if (value !== getActive()) {
        emit("update:modelValue", value);
        emit("change", value);
      }
    };
    linkChildren({
      getActive,
      setActive
    });
    return () => createVNode("div", {
      "class": bem79()
    }, [slots.default == null ? void 0 : slots.default()]);
  }
});

// node_modules/vant/es/sidebar/index.js
var Sidebar = withInstall(Sidebar_default);

// node_modules/vant/es/sidebar-item/SidebarItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name84, bem80] = createNamespace("sidebar-item");
var SidebarItem_default = defineComponent({
  name: name84,
  props: extend({}, routeProps, {
    dot: Boolean,
    title: String,
    badge: [Number, String],
    disabled: Boolean
  }),
  emits: ["click"],
  setup(props10, {
    emit,
    slots
  }) {
    var route2 = useRoute();
    var {
      parent,
      index
    } = useParent(SIDEBAR_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <SidebarItem> must be a child component of <Sidebar>.");
      }
      return;
    }
    var onClick = () => {
      if (props10.disabled) {
        return;
      }
      emit("click", index.value);
      parent.setActive(index.value);
      route2();
    };
    return () => {
      var {
        dot,
        badge,
        title,
        disabled
      } = props10;
      var selected = index.value === parent.getActive();
      return createVNode("a", {
        "class": bem80({
          select: selected,
          disabled
        }),
        "onClick": onClick
      }, [createVNode(Badge, {
        "dot": dot,
        "content": badge,
        "class": bem80("text")
      }, {
        default: () => [slots.title ? slots.title() : title]
      })]);
    };
  }
});

// node_modules/vant/es/sidebar-item/index.js
var SidebarItem = withInstall(SidebarItem_default);

// node_modules/vant/es/skeleton/Skeleton.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name85, bem81] = createNamespace("skeleton");
var DEFAULT_ROW_WIDTH = "100%";
var DEFAULT_LAST_ROW_WIDTH = "60%";
var Skeleton_default = defineComponent({
  name: name85,
  props: {
    title: Boolean,
    round: Boolean,
    avatar: Boolean,
    loading: truthProp,
    animate: truthProp,
    avatarSize: [Number, String],
    titleWidth: [Number, String],
    row: {
      type: [Number, String],
      default: 0
    },
    avatarShape: {
      type: String,
      default: "round"
    },
    rowWidth: {
      type: [Number, String, Array],
      default: DEFAULT_ROW_WIDTH
    }
  },
  setup(props10, {
    slots
  }) {
    var renderAvatar = () => {
      if (props10.avatar) {
        return createVNode("div", {
          "class": bem81("avatar", props10.avatarShape),
          "style": getSizeStyle(props10.avatarSize)
        }, null);
      }
    };
    var renderTitle = () => {
      if (props10.title) {
        return createVNode("h3", {
          "class": bem81("title"),
          "style": {
            width: addUnit(props10.titleWidth)
          }
        }, null);
      }
    };
    var getRowWidth = (index) => {
      var {
        rowWidth
      } = props10;
      if (rowWidth === DEFAULT_ROW_WIDTH && index === +props10.row - 1) {
        return DEFAULT_LAST_ROW_WIDTH;
      }
      if (Array.isArray(rowWidth)) {
        return rowWidth[index];
      }
      return rowWidth;
    };
    var renderRows = () => Array(props10.row).fill("").map((_2, i) => createVNode("div", {
      "class": bem81("row"),
      "style": {
        width: addUnit(getRowWidth(i))
      }
    }, null));
    return () => {
      if (!props10.loading) {
        return slots.default == null ? void 0 : slots.default();
      }
      return createVNode("div", {
        "class": bem81({
          animate: props10.animate,
          round: props10.round
        })
      }, [renderAvatar(), createVNode("div", {
        "class": bem81("content")
      }, [renderTitle(), renderRows()])]);
    };
  }
});

// node_modules/vant/es/skeleton/index.js
var Skeleton = withInstall(Skeleton_default);

// node_modules/vant/es/slider/Slider.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name86, bem82] = createNamespace("slider");
var Slider_default = defineComponent({
  name: name86,
  props: {
    range: Boolean,
    disabled: Boolean,
    readonly: Boolean,
    vertical: Boolean,
    barHeight: [Number, String],
    buttonSize: [Number, String],
    activeColor: String,
    inactiveColor: String,
    min: {
      type: [Number, String],
      default: 0
    },
    max: {
      type: [Number, String],
      default: 100
    },
    step: {
      type: [Number, String],
      default: 1
    },
    modelValue: {
      type: [Number, Array],
      default: 0
    }
  },
  emits: ["change", "drag-end", "drag-start", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var buttonIndex;
    var startValue;
    var currentValue;
    var root2 = ref();
    var dragStatus = ref();
    var touch = useTouch();
    var scope = computed(() => Number(props10.max) - Number(props10.min));
    var wrapperStyle = computed(() => {
      var crossAxis = props10.vertical ? "width" : "height";
      return {
        background: props10.inactiveColor,
        [crossAxis]: addUnit(props10.barHeight)
      };
    });
    var isRange = (val) => props10.range && Array.isArray(val);
    var calcMainAxis = () => {
      var {
        modelValue,
        min
      } = props10;
      if (isRange(modelValue)) {
        return (modelValue[1] - modelValue[0]) * 100 / scope.value + "%";
      }
      return (modelValue - Number(min)) * 100 / scope.value + "%";
    };
    var calcOffset = () => {
      var {
        modelValue,
        min
      } = props10;
      if (isRange(modelValue)) {
        return (modelValue[0] - Number(min)) * 100 / scope.value + "%";
      }
      return "0%";
    };
    var barStyle = computed(() => {
      var mainAxis = props10.vertical ? "height" : "width";
      return {
        [mainAxis]: calcMainAxis(),
        left: props10.vertical ? void 0 : calcOffset(),
        top: props10.vertical ? calcOffset() : void 0,
        background: props10.activeColor,
        transition: dragStatus.value ? "none" : void 0
      };
    });
    var format3 = (value) => {
      var min = +props10.min;
      var max = +props10.max;
      var step = +props10.step;
      value = clamp(value, min, max);
      var diff = Math.round((value - min) / step) * step;
      return addNumber(min, diff);
    };
    var isSameValue = (newValue, oldValue) => JSON.stringify(newValue) === JSON.stringify(oldValue);
    var handleOverlap = (value) => {
      if (value[0] > value[1]) {
        return value.slice(0).reverse();
      }
      return value;
    };
    var updateValue = (value, end2) => {
      if (isRange(value)) {
        value = handleOverlap(value).map(format3);
      } else {
        value = format3(value);
      }
      if (!isSameValue(value, props10.modelValue)) {
        emit("update:modelValue", value);
      }
      if (end2 && !isSameValue(value, startValue)) {
        emit("change", value);
      }
    };
    var onClick = (event) => {
      event.stopPropagation();
      if (props10.disabled || props10.readonly) {
        return;
      }
      var {
        min,
        vertical,
        modelValue
      } = props10;
      var rect = useRect(root2);
      var delta = vertical ? event.clientY - rect.top : event.clientX - rect.left;
      var total = vertical ? rect.height : rect.width;
      var value = Number(min) + delta / total * scope.value;
      if (isRange(modelValue)) {
        var [left2, right2] = modelValue;
        var middle = (left2 + right2) / 2;
        if (value <= middle) {
          updateValue([value, right2], true);
        } else {
          updateValue([left2, value], true);
        }
      } else {
        updateValue(value, true);
      }
    };
    var onTouchStart = (event) => {
      if (props10.disabled || props10.readonly) {
        return;
      }
      touch.start(event);
      currentValue = props10.modelValue;
      if (isRange(currentValue)) {
        startValue = currentValue.map(format3);
      } else {
        startValue = format3(currentValue);
      }
      dragStatus.value = "start";
    };
    var onTouchMove = (event) => {
      if (props10.disabled || props10.readonly) {
        return;
      }
      if (dragStatus.value === "start") {
        emit("drag-start", event);
      }
      preventDefault(event, true);
      touch.move(event);
      dragStatus.value = "draging";
      var rect = useRect(root2);
      var delta = props10.vertical ? touch.deltaY.value : touch.deltaX.value;
      var total = props10.vertical ? rect.height : rect.width;
      var diff = delta / total * scope.value;
      if (isRange(startValue)) {
        currentValue[buttonIndex] = startValue[buttonIndex] + diff;
      } else {
        currentValue = startValue + diff;
      }
      updateValue(currentValue);
    };
    var onTouchEnd = (event) => {
      if (props10.disabled || props10.readonly) {
        return;
      }
      if (dragStatus.value === "draging") {
        updateValue(currentValue, true);
        emit("drag-end", event);
      }
      dragStatus.value = "";
    };
    var getButtonClassName = (index) => {
      if (typeof index === "number") {
        var position = ["left", "right"];
        return bem82("button-wrapper-" + position[index]);
      }
      return bem82("button-wrapper");
    };
    var renderButtonContent = (value, index) => {
      if (typeof index === "number") {
        var slot = slots[index === 0 ? "left-button" : "right-button"];
        if (slot) {
          return slot({
            value
          });
        }
      }
      if (slots.button) {
        return slots.button({
          value
        });
      }
      return createVNode("div", {
        "class": bem82("button"),
        "style": getSizeStyle(props10.buttonSize)
      }, null);
    };
    var renderButton = (index) => {
      var currentValue2 = typeof index === "number" ? props10.modelValue[index] : props10.modelValue;
      return createVNode("div", {
        "role": "slider",
        "class": getButtonClassName(index),
        "tabindex": props10.disabled || props10.readonly ? -1 : 0,
        "aria-valuemin": +props10.min,
        "aria-valuenow": currentValue2,
        "aria-valuemax": +props10.max,
        "aria-orientation": props10.vertical ? "vertical" : "horizontal",
        "onTouchstart": (event) => {
          if (typeof index === "number") {
            buttonIndex = index;
          }
          onTouchStart(event);
        },
        "onTouchmove": onTouchMove,
        "onTouchend": onTouchEnd,
        "onTouchcancel": onTouchEnd,
        "onClick": stopPropagation
      }, [renderButtonContent(currentValue2, index)]);
    };
    updateValue(props10.modelValue);
    useLinkField(() => props10.modelValue);
    return () => createVNode("div", {
      "ref": root2,
      "style": wrapperStyle.value,
      "class": bem82({
        vertical: props10.vertical,
        disabled: props10.disabled
      }),
      "onClick": onClick
    }, [createVNode("div", {
      "class": bem82("bar"),
      "style": barStyle.value
    }, [props10.range ? [renderButton(0), renderButton(1)] : renderButton()])]);
  }
});

// node_modules/vant/es/slider/index.js
var Slider = withInstall(Slider_default);

// node_modules/vant/es/step/Step.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/steps/Steps.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name87, bem83] = createNamespace("steps");
var STEPS_KEY = Symbol(name87);
var props8 = {
  iconPrefix: String,
  finishIcon: String,
  activeColor: String,
  inactiveIcon: String,
  inactiveColor: String,
  active: {
    type: [Number, String],
    default: 0
  },
  direction: {
    type: String,
    default: "horizontal"
  },
  activeIcon: {
    type: String,
    default: "checked"
  }
};
var Steps_default = defineComponent({
  name: name87,
  props: props8,
  emits: ["click-step"],
  setup(props10, {
    emit,
    slots
  }) {
    var {
      linkChildren
    } = useChildren(STEPS_KEY);
    var onClickStep = (index) => emit("click-step", index);
    linkChildren({
      props: props10,
      onClickStep
    });
    return () => createVNode("div", {
      "class": bem83([props10.direction])
    }, [createVNode("div", {
      "class": bem83("items")
    }, [slots.default == null ? void 0 : slots.default()])]);
  }
});

// node_modules/vant/es/step/Step.js
var [name88, bem84] = createNamespace("step");
var Step_default = defineComponent({
  name: name88,
  setup(props10, {
    slots
  }) {
    var {
      parent,
      index
    } = useParent(STEPS_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <Step> must be a child component of <Steps>.");
      }
      return;
    }
    var parentProps = parent.props;
    var getStatus = () => {
      var active = +parentProps.active;
      if (index.value < active) {
        return "finish";
      }
      return index.value === active ? "process" : "waiting";
    };
    var isActive = () => getStatus() === "process";
    var lineStyle = computed(() => ({
      background: getStatus() === "finish" ? parentProps.activeColor : parentProps.inactiveColor
    }));
    var titleStyle = computed(() => {
      if (isActive()) {
        return {
          color: parentProps.activeColor
        };
      }
      if (!getStatus()) {
        return {
          color: parentProps.inactiveColor
        };
      }
    });
    var onClickStep = () => parent.onClickStep(index.value);
    var renderCircle = () => {
      var {
        iconPrefix,
        finishIcon,
        activeIcon,
        activeColor,
        inactiveIcon
      } = parentProps;
      if (isActive()) {
        if (slots["active-icon"]) {
          return slots["active-icon"]();
        }
        return createVNode(Icon, {
          "class": bem84("icon", "active"),
          "name": activeIcon,
          "color": activeColor,
          "classPrefix": iconPrefix
        }, null);
      }
      if (getStatus() === "finish" && (finishIcon || slots["finish-icon"])) {
        if (slots["finish-icon"]) {
          return slots["finish-icon"]();
        }
        return createVNode(Icon, {
          "class": bem84("icon", "finish"),
          "name": finishIcon,
          "color": activeColor,
          "classPrefix": iconPrefix
        }, null);
      }
      if (slots["inactive-icon"]) {
        return slots["inactive-icon"]();
      }
      if (inactiveIcon) {
        return createVNode(Icon, {
          "class": bem84("icon"),
          "name": inactiveIcon,
          "classPrefix": iconPrefix
        }, null);
      }
      return createVNode("i", {
        "class": bem84("circle"),
        "style": lineStyle.value
      }, null);
    };
    return () => {
      var status = getStatus();
      return createVNode("div", {
        "class": [BORDER, bem84([parentProps.direction, {
          [status]: status
        }])]
      }, [createVNode("div", {
        "class": bem84("title", {
          active: isActive()
        }),
        "style": titleStyle.value,
        "onClick": onClickStep
      }, [slots.default == null ? void 0 : slots.default()]), createVNode("div", {
        "class": bem84("circle-container"),
        "onClick": onClickStep
      }, [renderCircle()]), createVNode("div", {
        "class": bem84("line"),
        "style": lineStyle.value
      }, null)]);
    };
  }
});

// node_modules/vant/es/step/index.js
var Step = withInstall(Step_default);

// node_modules/vant/es/stepper/Stepper.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name89, bem85] = createNamespace("stepper");
var LONG_PRESS_INTERVAL = 200;
var LONG_PRESS_START_TIME = 600;
function equal(value1, value2) {
  return String(value1) === String(value2);
}
var Stepper_default = defineComponent({
  name: name89,
  props: {
    theme: String,
    integer: Boolean,
    disabled: Boolean,
    showPlus: truthProp,
    showMinus: truthProp,
    showInput: truthProp,
    longPress: truthProp,
    allowEmpty: Boolean,
    modelValue: [Number, String],
    inputWidth: [Number, String],
    buttonSize: [Number, String],
    placeholder: String,
    disablePlus: Boolean,
    disableMinus: Boolean,
    disableInput: Boolean,
    beforeChange: Function,
    decimalLength: [Number, String],
    name: {
      type: [Number, String],
      default: ""
    },
    min: {
      type: [Number, String],
      default: 1
    },
    max: {
      type: [Number, String],
      default: Infinity
    },
    step: {
      type: [Number, String],
      default: 1
    },
    defaultValue: {
      type: [Number, String],
      default: 1
    }
  },
  emits: ["plus", "blur", "minus", "focus", "change", "overlimit", "update:modelValue"],
  setup(props10, {
    emit
  }) {
    var format3 = (value) => {
      var {
        min,
        max,
        allowEmpty,
        decimalLength
      } = props10;
      if (allowEmpty && value === "") {
        return value;
      }
      value = formatNumber(String(value), !props10.integer);
      value = value === "" ? 0 : +value;
      value = Number.isNaN(value) ? +min : value;
      value = Math.max(Math.min(+max, value), +min);
      if (isDef(decimalLength)) {
        value = value.toFixed(+decimalLength);
      }
      return value;
    };
    var getInitialValue = () => {
      var _props$modelValue;
      var defaultValue = (_props$modelValue = props10.modelValue) != null ? _props$modelValue : props10.defaultValue;
      var value = format3(defaultValue);
      if (!equal(value, props10.modelValue)) {
        emit("update:modelValue", value);
      }
      return value;
    };
    var actionType;
    var inputRef = ref();
    var current = ref(getInitialValue());
    var minusDisabled = computed(() => props10.disabled || props10.disableMinus || current.value <= +props10.min);
    var plusDisabled = computed(() => props10.disabled || props10.disablePlus || current.value >= +props10.max);
    var inputStyle = computed(() => ({
      width: addUnit(props10.inputWidth),
      height: addUnit(props10.buttonSize)
    }));
    var buttonStyle = computed(() => getSizeStyle(props10.buttonSize));
    var check = () => {
      var value = format3(current.value);
      if (!equal(value, current.value)) {
        current.value = value;
      }
    };
    var setValue = (value) => {
      if (props10.beforeChange) {
        callInterceptor({
          args: [value],
          interceptor: props10.beforeChange,
          done() {
            current.value = value;
          }
        });
      } else {
        current.value = value;
      }
    };
    var onChange = () => {
      if (actionType === "plus" && plusDisabled.value || actionType === "minus" && minusDisabled.value) {
        emit("overlimit", actionType);
        return;
      }
      var diff = actionType === "minus" ? -props10.step : +props10.step;
      var value = format3(addNumber(+current.value, diff));
      setValue(value);
      emit(actionType);
    };
    var onInput = (event) => {
      var input = event.target;
      var {
        value
      } = input;
      var {
        decimalLength
      } = props10;
      var formatted = formatNumber(String(value), !props10.integer);
      if (isDef(decimalLength) && formatted.includes(".")) {
        var pair = formatted.split(".");
        formatted = pair[0] + "." + pair[1].slice(0, +decimalLength);
      }
      if (props10.beforeChange) {
        input.value = String(current.value);
      } else if (!equal(value, formatted)) {
        input.value = formatted;
      }
      var isNumeric2 = formatted === String(+formatted);
      setValue(isNumeric2 ? +formatted : formatted);
    };
    var onFocus = (event) => {
      if (props10.disableInput) {
        var _inputRef$value;
        (_inputRef$value = inputRef.value) == null ? void 0 : _inputRef$value.blur();
      } else {
        emit("focus", event);
      }
    };
    var onBlur = (event) => {
      var input = event.target;
      var value = format3(input.value);
      input.value = String(value);
      current.value = value;
      nextTick(() => {
        emit("blur", event);
        resetScroll();
      });
    };
    var isLongPress;
    var longPressTimer;
    var longPressStep = () => {
      longPressTimer = setTimeout(() => {
        onChange();
        longPressStep();
      }, LONG_PRESS_INTERVAL);
    };
    var onTouchStart = () => {
      if (props10.longPress) {
        isLongPress = false;
        clearTimeout(longPressTimer);
        longPressTimer = setTimeout(() => {
          isLongPress = true;
          onChange();
          longPressStep();
        }, LONG_PRESS_START_TIME);
      }
    };
    var onTouchEnd = (event) => {
      if (props10.longPress) {
        clearTimeout(longPressTimer);
        if (isLongPress) {
          preventDefault(event);
        }
      }
    };
    var onMousedown = (event) => {
      if (props10.disableInput) {
        event.preventDefault();
      }
    };
    var createListeners = (type) => ({
      onClick: (event) => {
        event.preventDefault();
        actionType = type;
        onChange();
      },
      onTouchstart: () => {
        actionType = type;
        onTouchStart();
      },
      onTouchend: onTouchEnd,
      onTouchcancel: onTouchEnd
    });
    watch([() => props10.max, () => props10.min, () => props10.integer, () => props10.decimalLength], check);
    watch(() => props10.modelValue, (value) => {
      if (!equal(value, current.value)) {
        current.value = format3(value);
      }
    });
    watch(current, (value) => {
      emit("update:modelValue", value);
      emit("change", value, {
        name: props10.name
      });
    });
    useLinkField(() => props10.modelValue);
    return () => createVNode("div", {
      "class": bem85([props10.theme])
    }, [withDirectives(createVNode("button", mergeProps({
      "type": "button",
      "style": buttonStyle.value,
      "class": bem85("minus", {
        disabled: minusDisabled.value
      })
    }, createListeners("minus")), null), [[vShow, props10.showMinus]]), withDirectives(createVNode("input", {
      "ref": inputRef,
      "type": props10.integer ? "tel" : "text",
      "role": "spinbutton",
      "class": bem85("input"),
      "value": current.value,
      "style": inputStyle.value,
      "disabled": props10.disabled,
      "readonly": props10.disableInput,
      "inputmode": props10.integer ? "numeric" : "decimal",
      "placeholder": props10.placeholder,
      "aria-valuemax": +props10.max,
      "aria-valuemin": +props10.min,
      "aria-valuenow": +current.value,
      "onBlur": onBlur,
      "onInput": onInput,
      "onFocus": onFocus,
      "onMousedown": onMousedown
    }, null), [[vShow, props10.showInput]]), withDirectives(createVNode("button", mergeProps({
      "type": "button",
      "style": buttonStyle.value,
      "class": bem85("plus", {
        disabled: plusDisabled.value
      })
    }, createListeners("plus")), null), [[vShow, props10.showPlus]])]);
  }
});

// node_modules/vant/es/stepper/index.js
var Stepper = withInstall(Stepper_default);

// node_modules/vant/es/steps/index.js
var Steps = withInstall(Steps_default);

// node_modules/vant/es/submit-bar/SubmitBar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name90, bem86, t19] = createNamespace("submit-bar");
var SubmitBar_default = defineComponent({
  name: name90,
  props: {
    tip: String,
    label: String,
    price: Number,
    tipIcon: String,
    loading: Boolean,
    disabled: Boolean,
    textAlign: String,
    buttonText: String,
    buttonColor: String,
    suffixLabel: String,
    safeAreaInsetBottom: truthProp,
    decimalLength: {
      type: [Number, String],
      default: 2
    },
    currency: {
      type: String,
      default: "\xA5"
    },
    buttonType: {
      type: String,
      default: "danger"
    }
  },
  emits: ["submit"],
  setup(props10, {
    emit,
    slots
  }) {
    var renderText = () => {
      var {
        price,
        label,
        currency,
        textAlign,
        suffixLabel,
        decimalLength
      } = props10;
      if (typeof price === "number") {
        var pricePair = (price / 100).toFixed(+decimalLength).split(".");
        var decimal = decimalLength ? "." + pricePair[1] : "";
        return createVNode("div", {
          "class": bem86("text"),
          "style": {
            textAlign
          }
        }, [createVNode("span", null, [label || t19("label")]), createVNode("span", {
          "class": bem86("price")
        }, [currency, createVNode("span", {
          "class": bem86("price-integer")
        }, [pricePair[0]]), decimal]), suffixLabel && createVNode("span", {
          "class": bem86("suffix-label")
        }, [suffixLabel])]);
      }
    };
    var renderTip = () => {
      var {
        tip,
        tipIcon
      } = props10;
      if (slots.tip || tip) {
        return createVNode("div", {
          "class": bem86("tip")
        }, [tipIcon && createVNode(Icon, {
          "class": bem86("tip-icon"),
          "name": tipIcon
        }, null), tip && createVNode("span", {
          "class": bem86("tip-text")
        }, [tip]), slots.tip == null ? void 0 : slots.tip()]);
      }
    };
    var onClickButton = () => emit("submit");
    var renderButton = () => {
      if (slots.button) {
        return slots.button();
      }
      return createVNode(Button, {
        "round": true,
        "type": props10.buttonType,
        "text": props10.buttonText,
        "class": bem86("button", props10.buttonType),
        "color": props10.buttonColor,
        "loading": props10.loading,
        "disabled": props10.disabled,
        "onClick": onClickButton
      }, null);
    };
    return () => createVNode("div", {
      "class": bem86({
        unfit: !props10.safeAreaInsetBottom
      })
    }, [slots.top == null ? void 0 : slots.top(), renderTip(), createVNode("div", {
      "class": bem86("bar")
    }, [slots.default == null ? void 0 : slots.default(), renderText(), renderButton()])]);
  }
});

// node_modules/vant/es/submit-bar/index.js
var SubmitBar = withInstall(SubmitBar_default);

// node_modules/vant/es/swipe-cell/SwipeCell.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name91, bem87] = createNamespace("swipe-cell");
var SwipeCell_default = defineComponent({
  name: name91,
  props: {
    disabled: Boolean,
    leftWidth: [Number, String],
    rightWidth: [Number, String],
    beforeClose: Function,
    stopPropagation: Boolean,
    name: {
      type: [Number, String],
      default: ""
    }
  },
  emits: ["open", "close", "click"],
  setup(props10, {
    emit,
    slots
  }) {
    var opened;
    var lockClick2;
    var startOffset;
    var root2 = ref();
    var leftRef = ref();
    var rightRef = ref();
    var state = reactive({
      offset: 0,
      dragging: false
    });
    var touch = useTouch();
    var getWidthByRef = (ref2) => ref2.value ? useRect(ref2).width : 0;
    var leftWidth = computed(() => isDef(props10.leftWidth) ? +props10.leftWidth : getWidthByRef(leftRef));
    var rightWidth = computed(() => isDef(props10.rightWidth) ? +props10.rightWidth : getWidthByRef(rightRef));
    var open = (side) => {
      opened = true;
      state.offset = side === "left" ? leftWidth.value : -rightWidth.value;
      emit("open", {
        name: props10.name,
        position: side
      });
    };
    var close = (position) => {
      state.offset = 0;
      if (opened) {
        opened = false;
        emit("close", {
          name: props10.name,
          position
        });
      }
    };
    var toggle = (side) => {
      var offset2 = Math.abs(state.offset);
      var THRESHOLD = 0.15;
      var threshold = opened ? 1 - THRESHOLD : THRESHOLD;
      var width = side === "left" ? leftWidth.value : rightWidth.value;
      if (width && offset2 > width * threshold) {
        open(side);
      } else {
        close(side);
      }
    };
    var onTouchStart = (event) => {
      if (!props10.disabled) {
        startOffset = state.offset;
        touch.start(event);
      }
    };
    var onTouchMove = (event) => {
      if (props10.disabled) {
        return;
      }
      var {
        deltaX
      } = touch;
      touch.move(event);
      if (touch.isHorizontal()) {
        lockClick2 = true;
        state.dragging = true;
        var isEdge = !opened || deltaX.value * startOffset < 0;
        if (isEdge) {
          preventDefault(event, props10.stopPropagation);
        }
        state.offset = clamp(deltaX.value + startOffset, -rightWidth.value, leftWidth.value);
      }
    };
    var onTouchEnd = () => {
      if (state.dragging) {
        state.dragging = false;
        toggle(state.offset > 0 ? "left" : "right");
        setTimeout(() => {
          lockClick2 = false;
        }, 0);
      }
    };
    var onClick = (position = "outside") => {
      emit("click", position);
      if (opened && !lockClick2) {
        callInterceptor({
          interceptor: props10.beforeClose,
          args: [{
            name: props10.name,
            position
          }],
          done: () => close(position)
        });
      }
    };
    var getClickHandler = (position, stop) => (event) => {
      if (stop) {
        event.stopPropagation();
      }
      onClick(position);
    };
    var renderSideContent = (side, ref2) => {
      var contentSlot = slots[side];
      if (contentSlot) {
        return createVNode("div", {
          "ref": ref2,
          "class": bem87(side),
          "onClick": getClickHandler(side, true)
        }, [contentSlot()]);
      }
    };
    useExpose({
      open,
      close
    });
    useClickAway(root2, () => onClick("outside"), {
      eventName: "touchstart"
    });
    return () => {
      var wrapperStyle = {
        transform: "translate3d(" + state.offset + "px, 0, 0)",
        transitionDuration: state.dragging ? "0s" : ".6s"
      };
      return createVNode("div", {
        "ref": root2,
        "class": bem87(),
        "onClick": getClickHandler("cell"),
        "onTouchstart": onTouchStart,
        "onTouchmove": onTouchMove,
        "onTouchend": onTouchEnd,
        "onTouchcancel": onTouchEnd
      }, [createVNode("div", {
        "class": bem87("wrapper"),
        "style": wrapperStyle
      }, [renderSideContent("left", leftRef), slots.default == null ? void 0 : slots.default(), renderSideContent("right", rightRef)])]);
    };
  }
});

// node_modules/vant/es/swipe-cell/index.js
var SwipeCell = withInstall(SwipeCell_default);

// node_modules/vant/es/tabbar/Tabbar.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name92, bem88] = createNamespace("tabbar");
var TABBAR_KEY = Symbol(name92);
var props9 = {
  route: Boolean,
  fixed: truthProp,
  border: truthProp,
  zIndex: [Number, String],
  placeholder: Boolean,
  activeColor: String,
  beforeChange: Function,
  inactiveColor: String,
  modelValue: {
    type: [Number, String],
    default: 0
  },
  safeAreaInsetBottom: {
    type: Boolean,
    default: null
  }
};
var Tabbar_default = defineComponent({
  name: name92,
  props: props9,
  emits: ["change", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var root2 = ref();
    var {
      linkChildren
    } = useChildren(TABBAR_KEY);
    var renderPlaceholder = usePlaceholder(root2, bem88);
    var isUnfit = () => {
      var _props$safeAreaInsetB;
      return !((_props$safeAreaInsetB = props10.safeAreaInsetBottom) != null ? _props$safeAreaInsetB : props10.fixed);
    };
    var renderTabbar = () => {
      var {
        fixed,
        zIndex,
        border
      } = props10;
      return createVNode("div", {
        "ref": root2,
        "style": getZIndexStyle(zIndex),
        "class": [bem88({
          unfit: isUnfit(),
          fixed
        }), {
          [BORDER_TOP_BOTTOM]: border
        }]
      }, [slots.default == null ? void 0 : slots.default()]);
    };
    var setActive = (active) => {
      if (active !== props10.modelValue) {
        callInterceptor({
          interceptor: props10.beforeChange,
          args: [active],
          done() {
            emit("update:modelValue", active);
            emit("change", active);
          }
        });
      }
    };
    linkChildren({
      props: props10,
      setActive
    });
    return () => {
      if (props10.fixed && props10.placeholder) {
        return renderPlaceholder(renderTabbar);
      }
      return renderTabbar();
    };
  }
});

// node_modules/vant/es/tabbar/index.js
var Tabbar = withInstall(Tabbar_default);

// node_modules/vant/es/tabbar-item/TabbarItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name93, bem89] = createNamespace("tabbar-item");
var TabbarItem_default = defineComponent({
  name: name93,
  props: extend({}, routeProps, {
    dot: Boolean,
    icon: String,
    name: [Number, String],
    badge: [Number, String],
    iconPrefix: String
  }),
  emits: ["click"],
  setup(props10, {
    emit,
    slots
  }) {
    var route2 = useRoute();
    var vm = getCurrentInstance().proxy;
    var {
      parent,
      index
    } = useParent(TABBAR_KEY);
    if (!parent) {
      if (true) {
        console.error("[Vant] <TabbarItem> must be a child component of <Tabbar>.");
      }
      return;
    }
    var active = computed(() => {
      var {
        route: route3,
        modelValue
      } = parent.props;
      if (route3 && "$route" in vm) {
        var {
          $route
        } = vm;
        var {
          to
        } = props10;
        var config = isObject(to) ? to : {
          path: to
        };
        var pathMatched = "path" in config && config.path === $route.path;
        var nameMatched = "name" in config && config.name === $route.name;
        return pathMatched || nameMatched;
      }
      return (props10.name || index.value) === modelValue;
    });
    var onClick = (event) => {
      var _props$name;
      parent.setActive((_props$name = props10.name) != null ? _props$name : index.value);
      emit("click", event);
      route2();
    };
    var renderIcon = () => {
      if (slots.icon) {
        return slots.icon({
          active: active.value
        });
      }
      if (props10.icon) {
        return createVNode(Icon, {
          "name": props10.icon,
          "classPrefix": props10.iconPrefix
        }, null);
      }
    };
    return () => {
      var {
        dot,
        badge
      } = props10;
      var {
        activeColor,
        inactiveColor
      } = parent.props;
      var color = active.value ? activeColor : inactiveColor;
      return createVNode("div", {
        "class": bem89({
          active: active.value
        }),
        "style": {
          color
        },
        "onClick": onClick
      }, [createVNode(Badge, {
        "dot": dot,
        "content": badge,
        "class": bem89("icon")
      }, {
        default: () => [renderIcon()]
      }), createVNode("div", {
        "class": bem89("text")
      }, [slots.default == null ? void 0 : slots.default({
        active: active.value
      })])]);
    };
  }
});

// node_modules/vant/es/tabbar-item/index.js
var TabbarItem = withInstall(TabbarItem_default);

// node_modules/vant/es/tree-select/TreeSelect.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var [name94, bem90] = createNamespace("tree-select");
var TreeSelect_default = defineComponent({
  name: name94,
  props: {
    max: {
      type: [Number, String],
      default: Infinity
    },
    items: {
      type: Array,
      default: () => []
    },
    height: {
      type: [Number, String],
      default: 300
    },
    activeId: {
      type: [Number, String, Array],
      default: 0
    },
    selectedIcon: {
      type: String,
      default: "success"
    },
    mainActiveIndex: {
      type: [Number, String],
      default: 0
    }
  },
  emits: ["click-nav", "click-item", "update:activeId", "update:mainActiveIndex"],
  setup(props10, {
    emit,
    slots
  }) {
    var isActiveItem = (id) => {
      return Array.isArray(props10.activeId) ? props10.activeId.includes(id) : props10.activeId === id;
    };
    var renderSubItem = (item) => {
      var onClick = () => {
        if (item.disabled) {
          return;
        }
        var activeId;
        if (Array.isArray(props10.activeId)) {
          activeId = props10.activeId.slice();
          var index = activeId.indexOf(item.id);
          if (index !== -1) {
            activeId.splice(index, 1);
          } else if (activeId.length < props10.max) {
            activeId.push(item.id);
          }
        } else {
          activeId = item.id;
        }
        emit("update:activeId", activeId);
        emit("click-item", item);
      };
      return createVNode("div", {
        "key": item.id,
        "class": ["van-ellipsis", bem90("item", {
          active: isActiveItem(item.id),
          disabled: item.disabled
        })],
        "onClick": onClick
      }, [item.text, isActiveItem(item.id) && createVNode(Icon, {
        "name": props10.selectedIcon,
        "class": bem90("selected")
      }, null)]);
    };
    var onSidebarChange = (index) => {
      emit("update:mainActiveIndex", index);
      emit("click-nav", index);
    };
    var renderSidebar = () => {
      var Items = props10.items.map((item) => createVNode(SidebarItem, {
        "dot": item.dot,
        "title": item.text,
        "badge": item.badge,
        "class": [bem90("nav-item"), item.className],
        "disabled": item.disabled
      }, null));
      return createVNode(Sidebar, {
        "class": bem90("nav"),
        "modelValue": props10.mainActiveIndex,
        "onChange": onSidebarChange
      }, {
        default: () => [Items]
      });
    };
    var renderContent = () => {
      if (slots.content) {
        return slots.content();
      }
      var selected = props10.items[+props10.mainActiveIndex] || {};
      if (selected.children) {
        return selected.children.map(renderSubItem);
      }
    };
    return () => createVNode("div", {
      "class": bem90(),
      "style": {
        height: addUnit(props10.height)
      }
    }, [renderSidebar(), createVNode("div", {
      "class": bem90("content")
    }, [renderContent()])]);
  }
});

// node_modules/vant/es/tree-select/index.js
var TreeSelect = withInstall(TreeSelect_default);

// node_modules/vant/es/uploader/Uploader.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();

// node_modules/vant/es/uploader/utils.js
var [name95, bem91] = createNamespace("uploader");
function toArray(item) {
  if (Array.isArray(item)) {
    return item;
  }
  return [item];
}
function readFileContent(file, resultType) {
  return new Promise((resolve) => {
    if (resultType === "file") {
      resolve();
      return;
    }
    var reader = new FileReader();
    reader.onload = (event) => {
      resolve(event.target.result);
    };
    if (resultType === "dataUrl") {
      reader.readAsDataURL(file);
    } else if (resultType === "text") {
      reader.readAsText(file);
    }
  });
}
function isOversize(items, maxSize) {
  return toArray(items).some((item) => {
    if (item.file) {
      if (isFunction(maxSize)) {
        return maxSize(item.file);
      }
      return item.file.size > maxSize;
    }
    return false;
  });
}
function filterFiles(items, maxSize) {
  var valid = [];
  var invalid = [];
  items.forEach((item) => {
    if (isOversize(item, maxSize)) {
      invalid.push(item);
    } else {
      valid.push(item);
    }
  });
  return {
    valid,
    invalid
  };
}
var IMAGE_REGEXP = /\.(jpeg|jpg|gif|png|svg|webp|jfif|bmp|dpg)/i;
function isImageUrl(url) {
  return IMAGE_REGEXP.test(url);
}
function isImageFile(item) {
  if (item.isImage) {
    return true;
  }
  if (item.file && item.file.type) {
    return item.file.type.indexOf("image") === 0;
  }
  if (item.url) {
    return isImageUrl(item.url);
  }
  if (typeof item.content === "string") {
    return item.content.indexOf("data:image") === 0;
  }
  return false;
}

// node_modules/vant/es/uploader/UploaderPreviewItem.js
init_vue_runtime_esm_bundler();
init_vue_runtime_esm_bundler();
var UploaderPreviewItem_default = defineComponent({
  props: {
    name: [Number, String],
    index: Number,
    imageFit: String,
    lazyLoad: Boolean,
    deletable: Boolean,
    previewSize: [Number, String],
    beforeDelete: Function,
    item: {
      type: Object,
      required: true
    }
  },
  emits: ["delete", "preview"],
  setup(props10, {
    emit,
    slots
  }) {
    var renderMask = () => {
      var {
        status,
        message
      } = props10.item;
      if (status === "uploading" || status === "failed") {
        var MaskIcon = status === "failed" ? createVNode(Icon, {
          "name": "close",
          "class": bem91("mask-icon")
        }, null) : createVNode(Loading, {
          "class": bem91("loading")
        }, null);
        var showMessage = isDef(message) && message !== "";
        return createVNode("div", {
          "class": bem91("mask")
        }, [MaskIcon, showMessage && createVNode("div", {
          "class": bem91("mask-message")
        }, [message])]);
      }
    };
    var onDelete = (event) => {
      var {
        name: name96,
        item,
        index,
        beforeDelete
      } = props10;
      event.stopPropagation();
      callInterceptor({
        interceptor: beforeDelete,
        args: [item, {
          name: name96,
          index
        }],
        done: () => emit("delete")
      });
    };
    var onPreview = () => emit("preview");
    var renderDeleteIcon = () => {
      if (props10.deletable && props10.item.status !== "uploading") {
        return createVNode("div", {
          "class": bem91("preview-delete"),
          "onClick": onDelete
        }, [createVNode(Icon, {
          "name": "cross",
          "class": bem91("preview-delete-icon")
        }, null)]);
      }
    };
    var renderCover = () => {
      if (slots["preview-cover"]) {
        var {
          index,
          item
        } = props10;
        return createVNode("div", {
          "class": bem91("preview-cover")
        }, [slots["preview-cover"](extend({
          index
        }, item))]);
      }
    };
    var renderPreview = () => {
      var {
        item
      } = props10;
      if (isImageFile(item)) {
        return createVNode(Image2, {
          "fit": props10.imageFit,
          "src": item.content || item.url,
          "class": bem91("preview-image"),
          "width": props10.previewSize,
          "height": props10.previewSize,
          "lazyLoad": props10.lazyLoad,
          "onClick": onPreview
        }, {
          default: () => [renderCover()]
        });
      }
      return createVNode("div", {
        "class": bem91("file"),
        "style": getSizeStyle(props10.previewSize)
      }, [createVNode(Icon, {
        "class": bem91("file-icon"),
        "name": "description"
      }, null), createVNode("div", {
        "class": [bem91("file-name"), "van-ellipsis"]
      }, [item.file ? item.file.name : item.url]), renderCover()]);
    };
    return () => createVNode("div", {
      "class": bem91("preview")
    }, [renderPreview(), renderMask(), renderDeleteIcon()]);
  }
});

// node_modules/vant/es/uploader/Uploader.js
var Uploader_default = defineComponent({
  name: name95,
  props: {
    capture: String,
    multiple: Boolean,
    disabled: Boolean,
    lazyLoad: Boolean,
    uploadText: String,
    deletable: truthProp,
    afterRead: Function,
    showUpload: truthProp,
    beforeRead: Function,
    beforeDelete: Function,
    previewSize: [Number, String],
    previewImage: truthProp,
    previewOptions: Object,
    previewFullImage: truthProp,
    name: {
      type: [Number, String],
      default: ""
    },
    accept: {
      type: String,
      default: "image/*"
    },
    modelValue: {
      type: Array,
      default: () => []
    },
    maxSize: {
      type: [Number, String, Function],
      default: Number.MAX_VALUE
    },
    maxCount: {
      type: [Number, String],
      default: Number.MAX_VALUE
    },
    imageFit: {
      type: String,
      default: "cover"
    },
    resultType: {
      type: String,
      default: "dataUrl"
    },
    uploadIcon: {
      type: String,
      default: "photograph"
    }
  },
  emits: ["delete", "oversize", "close-preview", "click-preview", "update:modelValue"],
  setup(props10, {
    emit,
    slots
  }) {
    var inputRef = ref();
    var getDetail = (index = props10.modelValue.length) => ({
      name: props10.name,
      index
    });
    var resetInput = () => {
      if (inputRef.value) {
        inputRef.value.value = "";
      }
    };
    var onAfterRead = (items) => {
      resetInput();
      if (isOversize(items, props10.maxSize)) {
        if (Array.isArray(items)) {
          var result = filterFiles(items, props10.maxSize);
          items = result.valid;
          emit("oversize", result.invalid, getDetail());
          if (!items.length) {
            return;
          }
        } else {
          emit("oversize", items, getDetail());
          return;
        }
      }
      items = reactive(items);
      emit("update:modelValue", [...props10.modelValue, ...toArray(items)]);
      if (props10.afterRead) {
        props10.afterRead(items, getDetail());
      }
    };
    var readFile = (files) => {
      var {
        maxCount,
        modelValue,
        resultType
      } = props10;
      if (Array.isArray(files)) {
        var remainCount = +maxCount - modelValue.length;
        if (files.length > remainCount) {
          files = files.slice(0, remainCount);
        }
        Promise.all(files.map((file) => readFileContent(file, resultType))).then((contents) => {
          var fileList = files.map((file, index) => {
            var result = {
              file,
              status: "",
              message: ""
            };
            if (contents[index]) {
              result.content = contents[index];
            }
            return result;
          });
          onAfterRead(fileList);
        });
      } else {
        readFileContent(files, resultType).then((content) => {
          var result = {
            file: files,
            status: "",
            message: ""
          };
          if (content) {
            result.content = content;
          }
          onAfterRead(result);
        });
      }
    };
    var onChange = (event) => {
      var {
        files
      } = event.target;
      if (props10.disabled || !files || !files.length) {
        return;
      }
      var file = files.length === 1 ? files[0] : [].slice.call(files);
      if (props10.beforeRead) {
        var response = props10.beforeRead(file, getDetail());
        if (!response) {
          resetInput();
          return;
        }
        if (isPromise(response)) {
          response.then((data) => {
            if (data) {
              readFile(data);
            } else {
              readFile(file);
            }
          }).catch(resetInput);
          return;
        }
      }
      readFile(file);
    };
    var imagePreview;
    var onClosePreview = () => emit("close-preview");
    var previewImage = (item) => {
      if (props10.previewFullImage) {
        var imageFiles = props10.modelValue.filter(isImageFile);
        var images = imageFiles.map((item2) => item2.content || item2.url).filter(Boolean);
        imagePreview = ImagePreview(extend({
          images,
          startPosition: imageFiles.indexOf(item),
          onClose: onClosePreview
        }, props10.previewOptions));
      }
    };
    var closeImagePreview = () => {
      if (imagePreview) {
        imagePreview.close();
      }
    };
    var deleteFile = (item, index) => {
      var fileList = props10.modelValue.slice(0);
      fileList.splice(index, 1);
      emit("update:modelValue", fileList);
      emit("delete", item, getDetail(index));
    };
    var renderPreviewItem = (item, index) => {
      var needPickData = ["imageFit", "deletable", "previewSize", "beforeDelete"];
      var previewData = extend(pick(props10, needPickData), pick(item, needPickData, true));
      return createVNode(UploaderPreviewItem_default, mergeProps({
        "item": item,
        "index": index,
        "onClick": () => emit("click-preview", item, getDetail(index)),
        "onDelete": () => deleteFile(item, index),
        "onPreview": () => previewImage(item)
      }, pick(props10, ["name", "lazyLoad"]), previewData), {
        "preview-cover": slots["preview-cover"]
      });
    };
    var renderPreviewList = () => {
      if (props10.previewImage) {
        return props10.modelValue.map(renderPreviewItem);
      }
    };
    var renderUpload = () => {
      if (props10.modelValue.length >= props10.maxCount || !props10.showUpload) {
        return;
      }
      var Input = createVNode("input", {
        "ref": inputRef,
        "type": "file",
        "class": bem91("input"),
        "accept": props10.accept,
        "capture": props10.capture,
        "multiple": props10.multiple,
        "disabled": props10.disabled,
        "onChange": onChange
      }, null);
      if (slots.default) {
        return createVNode("div", {
          "class": bem91("input-wrapper")
        }, [slots.default(), Input]);
      }
      return createVNode("div", {
        "class": bem91("upload"),
        "style": getSizeStyle(props10.previewSize)
      }, [createVNode(Icon, {
        "name": props10.uploadIcon,
        "class": bem91("upload-icon")
      }, null), props10.uploadText && createVNode("span", {
        "class": bem91("upload-text")
      }, [props10.uploadText]), Input]);
    };
    var chooseFile = () => {
      if (inputRef.value && !props10.disabled) {
        inputRef.value.click();
      }
    };
    useExpose({
      chooseFile,
      closeImagePreview
    });
    useLinkField(() => props10.modelValue);
    return () => createVNode("div", {
      "class": bem91()
    }, [createVNode("div", {
      "class": bem91("wrapper", {
        disabled: props10.disabled
      })
    }, [renderPreviewList(), renderUpload()])]);
  }
});

// node_modules/vant/es/uploader/index.js
var Uploader = withInstall(Uploader_default);

// node_modules/vant/es/index.js
var version = "3.1.3";
function install(app) {
  var components = [ActionBar, ActionBarButton, ActionBarIcon, ActionSheet, AddressEdit, AddressList, Area, Badge, Button, Calendar, Card, Cascader, Cell, CellGroup, Checkbox, CheckboxGroup, Circle, Col, Collapse, CollapseItem, ConfigProvider, ContactCard, ContactEdit, ContactList, CountDown, Coupon, CouponCell, CouponList, DatetimePicker, Dialog, Divider, DropdownItem, DropdownMenu, Empty, Field, Form, Grid, GridItem, Icon, Image2, ImagePreview, IndexAnchor, IndexBar, List, Loading, Locale, NavBar, NoticeBar, Notify, NumberKeyboard, Overlay, Pagination, PasswordInput, Picker, Popover, Popup, Progress, PullRefresh, Radio, RadioGroup, Rate, Row, Search, ShareSheet, Sidebar, SidebarItem, Skeleton, Slider, Step, Stepper, Steps, Sticky, SubmitBar, Swipe, SwipeCell, SwipeItem, Switch, Tab, Tabbar, TabbarItem, Tabs, Tag, Toast, TreeSelect, Uploader];
  components.forEach((item) => {
    if (item.install) {
      app.use(item);
    } else if (item.name) {
      app.component(item.name, item);
    }
  });
}
var es_default = {
  install,
  version
};

// dep:vant
var vant_default = es_default;
export {
  ActionBar,
  ActionBarButton,
  ActionBarIcon,
  ActionSheet,
  AddressEdit,
  AddressList,
  Area,
  Badge,
  Button,
  Calendar,
  Card,
  Cascader,
  Cell,
  CellGroup,
  Checkbox,
  CheckboxGroup,
  Circle,
  Col,
  Collapse,
  CollapseItem,
  ConfigProvider,
  ContactCard,
  ContactEdit,
  ContactList,
  CountDown,
  Coupon,
  CouponCell,
  CouponList,
  DatetimePicker,
  Dialog,
  Divider,
  DropdownItem,
  DropdownMenu,
  Empty,
  Field,
  Form,
  Grid,
  GridItem,
  Icon,
  Image2 as Image,
  ImagePreview,
  IndexAnchor,
  IndexBar,
  Lazyload,
  List,
  Loading,
  Locale,
  NavBar,
  NoticeBar,
  Notify,
  NumberKeyboard,
  Overlay,
  Pagination,
  PasswordInput,
  Picker,
  Popover,
  Popup,
  Progress,
  PullRefresh,
  Radio,
  RadioGroup,
  Rate,
  Row,
  Search,
  ShareSheet,
  Sidebar,
  SidebarItem,
  Skeleton,
  Slider,
  Step,
  Stepper,
  Steps,
  Sticky,
  SubmitBar,
  Swipe,
  SwipeCell,
  SwipeItem,
  Switch,
  Tab,
  Tabbar,
  TabbarItem,
  Tabs,
  Tag,
  Toast,
  TreeSelect,
  Uploader,
  vant_default as default,
  install,
  version
};
//# sourceMappingURL=vant.js.map
